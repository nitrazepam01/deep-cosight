# MATLAB Backend

MATLAB is optional. Put `matlab` on `PATH` and install Control System Toolbox to
enable the fixed JSON protocol adapter. The adapter does not use MATLAB Engine for
Python and does not accept arbitrary MATLAB code.

MATLAB is not a core acceptance dependency. Missing MATLAB is reported as
`BACKEND_UNAVAILABLE`.

