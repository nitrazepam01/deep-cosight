# 1807.03475

> OCR by deepseek-ai/DeepSeek-OCR | 18 pages

### Page 1

ements et types sont disponibles dans l’Guidelińe informatisant la partie b) si le package est incarregé pour être utilisé authentiquement dans cet aspect du Gauthier. Bien qu’il n’est pas thumbnailed, l’etik of body emblematique est proche de celui proposé et l’Object anhanté b) en tant que specificitation pour le support en moteur electricity et wa­ter qui présenté est modelé ± accuracy des résultats. A Nd`e vitesse indique le mode brut du cellule en fonction de laสาย de vers on est faites à peu près 10 temps browsing. La vitesse ou mieux, se dire sur les limite-treated alors les débitots jouent un rôle intervenant dans les informations de la présente fabrication.

### Page 2

}}\\[-12.0pt]
\end{align*}

with \(\mathbb{R}^n\) and \(\mathbb{R}^m\). In this article, we analyze algorithmic questions about both convex optimization and monotone extensive form games.

We may introduce a new inference scheme based on logarithmic bitrate coding. For any observation \(x\) from \(\mathbb{R}^n\), \(\log x\) is a random variable. As \(x\) increases, \(\log x\) tends to infinity at an exponential rate. Let \(\alpha(n,U)\) such that

\[\log x \leq \log x_0 + \sum_{j=1}^n \alpha(j,U) \log x_j \leq A(n,U,1/U) \log x_0\]

for some constants \(A(n,U,1/U) > 0\) and \(0 \leq \alpha(j,U) \leq 1 < \infty\), respectively. When \(x\) grows, the total amount of code所需要 \(U\) agents will send decreases.

On the other hand, as \(n\) increases, for every reasonably weak observation \(x\) from \(\mathbb{R}^n\) the notation of Lemma 2 has to be modified. When such a sufficient condition is given, an agent can always infer its observable \(x\)

\[  \]

### Page 3

.Thus, the above equation holds only if $r \neq 2$. However, in this case we have two specific solutions, given by



\[\Omega = \begin{pmatrix} 0 & -\Omega_3 & \Omega_2 \\ \Omega_3 & 0 & -\Omega_1 \\ -\Omega_2 & \Omega_1 & 0 \end{pmatrix}\]

and so


\[\tilde{\Omega} = \begin{pmatrix} \Omega \\ -A & \Omega \\ -A & -\Omega_3 & \Omega_2 \end{pmatrix}\]

.

For $\Sigma = (\Omega_1, \Omega_2, \Omega_3) \in \mathbb{R}^{3}$. The inverse map of the hat map is called the wee map and denoted $\mathcal{W}$ such that $(\hat{\Omega})^t = \Omega$ for all $\Omega \in \mathbb{R}^3$ and $(A)^\to = A$ for all $A \in \mathfrak{so}(3)$.

**Lemma 1.** $(RA, RB) = (AR, BR) = (A, B)$ for all $R \in \mathbb{SO}(3)$ and $A, B \in \mathbb{R}^{3\times3}.$

2. 2. 

$r(R_1 - R_2) = 2\sqrt{2}$.
3. $(t, \delta) = 2(u, v)$ for all $u, v \in \mathbb{R}^3$.
4. $[t, \delta] = (u \times v)^\aleph$ and $t^v = u \times v$ for all $u, v \in \mathbb{R}^3$.

  $\mathcal{W}$ is called the wee map associated with $\Sigma$.
  $\hat{E} = \mathbb{R}^n$ and $E = \mathbb{R}$ is the matrix space. 

  $\mathcal{E}$ is the space of functions on $\mathbb{R}^n$.

$\mathcal{E} \subset \hat{E}^c$ is the matrix space (whose elements are symmetric $n \times n$ matrices with elements from the set $\mathbb{R}_n$) 


\[\begin{align}
 & 
\frac{1}{2} \left\| \frac{d}{dt} \tilde{x}(t) \right\|^2 \leq -\frac{1}{2} \int_{\mathbb{R}^n} \left\| \frac{d}{dt} \tilde{x}(t) \right\|^2~dx dt \\
 & \frac{1}{2} \int_{\mathbb{R}^n} \left\| \frac{d}{dt} \tilde{x}(t) \right\|^2 dt \leq \frac{1}{2} \int_{\mathbb{R}^n} \left\| \frac{d}{dt} \tilde{x}(t) \right\|^2 dx dt = \frac{1}{2} \int_{\mathbb{R}^n} \left\| \frac{d}{dt} \tilde{x}(t) \right\|^2 dx dt
\end{align}\]What these integrals must be done is that their sum must be ordered.
for $t$ and $\nu$ to have an ordered set.

We have an ordered set of sums.

\int dx < dx; dx < dx; dx = dx.

example, we have no nest problem here. **When we have no nest problem here,  
when we put $ \int \frac{dx}{x} = n $...**

Possibly mathematical objects can have similar transformations.  P (E.)
E = x^x = $a, \sqrt(2) = x^{x^{t}}$ **and we have a tuple given here. **E = x^n**.

^{\market \hat{x}x = ,\frac{1}{2}} **E = x^{e^x - .
$\theta E = x^{..

$x = P(x) = 1$ is true since... $et a^t = \frac{xa}{x,y} = 0$


**2.2 | Embedding in Euclidean Space and Transversal Stabilization**

**2.2.1 | Theory**

Let $M$ be an $m$-dimensional regular manifold in $\mathbb{R}^n$, where $ m < n$. Consider a control system $\Sigma_M$ on $M$ given by

$\Sigma_M : \quad \dot{x} = X(x,u), \quad x \in M, u \in \mathbb{R}^k.$
\begin{equation}
(1)
</equation>

Notice that

$X(x,u) \in T_x M \quad \forall x \in M, u \in \mathbb{R}^k,$
\begin{equation}
(2)
</equation>

where $T_x M$ denotes the tangent space to $M$ at $x$. Suppose that there is a control system $\Sigma_{\mathbb{R}^n}$ on $\mathbb{R}^n$ given by

$\Sigma_{\mathbb{R}^n} : \quad \dot{x} = X_e(x, u), \quad x \in \mathbb{R}^n, u \in \mathbb{R}^k,$
\begin{equation}
(3)
</equation>

that satisfies

$X_e(x, u) = X(x, u) \quad \forall x \in M, u \in \mathbb{R}^k.$
\begin{equation}
(4)
</equation>

In other words, $\Sigma_{\mathbb{R}^n}$ is an extension of $\Sigma_M$ to $\mathbb{R}^n$ and $\Sigma_M$ becomes a restriction of $\Sigma_{\mathbb{R}^n}$ to $M$. By (2) and (4), $M$ is an invariant manifold of $\Sigma_{\mathbb{R}^n}$.

Suppose that there is a function $\tilde{V} : \mathbb{R}^n \rightarrow \mathbb{R}_{\geq 0}$ such that

$M = \tilde{V}^{-1}(0)$
\begin{equation}
(5)
</equation>

and

$\nabla \tilde{V}(x) \cdot X_e(x, u) = 0$
\begin{equation}
(6)
</equation>

for all $x \in \mathbb{R}^n$ and $u \in \mathbb{R}^k$. With this function, construct a system $\tilde{\Sigma}_{\mathbb{R}^n} in \mathbb{R}^n$ as

$\tilde{\Sigma}_{\mathbb{R}^n} : \quad \dot{x} = \tilde{X}_e(x, u), \quad x \in \mathbb{R}^n, u \in \mathbb{R}^k,$
\begin{equation}
(7)
</equation>

where the vector field $\tilde{X}_e$ is defined by

$\tilde{X}_e(x, u) = X_e(x, u) - \nabla \tilde{V}(x) \quad \forall x \in \mathbb{R}^n, u \in \mathbb{R}^k.$
\begin{equation}
(8)
</equation>

### Page 4

Technology Review R. 2014 November 17 | Result, ■ Repeated Remarks with ettings.  

4 I Beijing University and Industrial Technology hall Thereford, the solution was available for each order of reaction on site and pace the reaction velocity. While cleaning the field as usual site in less space and suffering from contamination, we found that the properties of Lake tankers are getting mixed up. Hence, the conditions in the site are radically different under the new method. In addition, to enhance the Velocity, we also tried to optimize the consumate consumate consumptionward like \(x^2 V_{min}(\mathrm{M}^{2}) ,x^2 V_{\min}(L_{w})\), but after all, the sum of the consumate consumate consumates for both the sites is usually close to the sum of consumates consumed for site and pace again to conserve动量. In the following, we denote the point for the terminal contract as \(L_{\text{comp022}}\) and \(\mathrm{M}_{\text {convert}}\) are the average consumption of Lake dockStage \(\mathrm{M}/L_{w}^{2}\) Besides, we usually assume \(X_{(i, j, k, l)} \sim X_{matter_{i}} / M_{j}\) each \(M_{j}\) . Set \(\Theta =\) energy atmosphere and consider the annual maximum energy to store \(\mathrm{\rho = \frac{\gamma}{2} V_{\min}}\), so we can reduce the consumption as \(\begin{aligned} \theta & \left\{ L_{(N_{l})})^{2} \\ = \wedge_{\mathrm{mực_0}}H_{1} // = 0,0 \end{aligned}\) instead of the total consumption of side. Neatly, the average consign.Any \(A > M_{j}\) consume the impedance of energy.motion energy \(g = \frac{4b v^{2}}{(M_{c})^{3}}\) all surf. as for the average the kind of this problem, but we also tried to optimize the consumptionentimes where \(\gamma\) \(\gamma\) _ \beginWD (V_{(s))} ^-^2yd^ \{h\times_ \Psi \quad (\mathparentheses \mathrm{\}}Z\) \") det_ \_ {}- - \(\gamma_\) , \({}^{2}{10\frac{h_{m.u}(\{V_{f}\}\{(V_{(s)}_\mathrm{max}))}{\mathbfM}}\]

### Page 5

apped.the state-vector consisting of a rotation matrix R and a body angular velocity vector Ω, tra=[%Ω%]

is the normal moment of inertia matrix of the rigid body. From here on, we regard the system (12) as a system defined on R^3 x^3 x R1. This is then easy to verify that SO(3) x R_3 is an invariant set of (12), i.e. every flow starting in M remains in M for all t [R;R]. Assume that the full state of the system is available, which allows us to apply the following controller

$$\tau = \|(u - \tilde{U})(\Omega x_2))$$

to transform the above system to

$$R = R(deta),$$

where u is the new control vector. Note that SO(3) x R3 is an invariant set of (14). Let GL^+(3) = {R [R R3} ] | det R > 0} and define a function V: GL_+(3) x R_3 c R^(3x3 x R_1) x R3 [R(2,0)]

by

$$V(R, \Omega) = \frac{k_e}{4} \| R^TR - I \|^2

where $k_e$ > 0 is a constant. It is easy to verify that V(\Omega) = SO(3) x R_3 and

$$V_R ̊̊̆ = -k_e R(R^TR - I),$$

with this function V, the modified rigid body system corresponding to (7) and (8) is computed as

$$R = R(delta) - k_e R(R^TR - I),$$

where (R, omega) [R R3 x R3 piecewise x R3 piecewise R2]

and ω = u. We now show that Theorem 1 holds in the rigid body case.

### Page 6

nervous system.Proof. Pick such numbers \(b\) and \(r\) as in the statement of Lemma 2. By Lemma 2 and Theorem 1, every trajectory of (17) starting in \(\check{V}^{-1}([0, r))\) remains in \(\check{V}^{-1}([0, r))\) for all future time and converges exponentially to \text{SO}(3) × \(\mathbb{R}^3\) as \(t \to \infty\). Let \((R(t), \Omega(t))\) be an arbitrary trajectory staring in \(\check{V}^{-1}([0, r))\) at \(t = 0\). Then, by Theorem 1, it satisfies

\[\|R^T(t)R(t) - I\| \leq \|R^T(0)R(0) - I\| e^{-b t / 2}\]

for all \(t \geq 0\). It follows that the convergence of \((R(t), \Omega(t))\) to \text{SO}(3) × \(\mathbb{R}^3\) is exponential since the \(3 \times 3\) zero matrix is a regular value of the map \(g : \text{GL}^+(3) \to \text{Sym}(\mathbb{R}^{3 \times 3})\) defined by \(g(R) = R^T R - I\) such that \(\text{SO}(3) = \{R \in \text{GL}^+(3) \mid g(R) = 0\}\); refer to pp.22–23 of Guillemin and Pollack\(^9\) to see why the zero matrix is a regular value of \(g\). \(\square\)

**Remark** 1. The technique of embedding into ambient Euclidean space and transversal stabilization was successfully tested in creating feedback integrators for structure-preserving numerical integration\(^6\) of the dynamics of uncontrolled dynamical systems. This technique is extended to control systems in this paper. In particular, Theorem 1, Corollary 1, Lemma 2 and Theorem 2 in this paper are new and powerful so as to guarantee exponential stability of \(M\) in the transversal direction.

**2.3 | Tracking via Linearization in Ambient Euclidean Space**

**2.3.1 | Theory**

Consider again the system \(\check{\Sigma}_{\mathbb{R}^n}\) given in (7) and its restriction \(\Sigma_M\) to \(M\) given in (1). Choose a reference trajectory \(x_0 : [0, \infty) \to M\) for \(\Sigma_M\) on \(M\) driven by a control signal \(u_0 : [0, \infty) \to \mathbb{R}^k\) so that

\[\dot{x}_0(t) = \tilde{X}(x_0(t), u_0(t)) \quad \forall t \geq 0.\]

We can then linearize the ambient system \(\tilde{\Sigma}_{\mathbb{R}^n}\) along the trajectory \((x_0(t), u_0(t))\) in \(\mathbb{R}^n\) as follows:

\[\check{\Sigma}_{\mathbb{R}^n}^\angle : \quad \Delta \dot{x} = A(t)\Delta x + B(t)\Delta u,\]

where

\[
A(t) = \frac{\partial \tilde{X}}{\partial x}(x_0(t), u_0(t)), \quad B(t) = \frac{\partial \tilde{X}}{\partial u}(x_0(t), u_0(t))
\]

and

\[\Delta x = x - x_0(t) \in \mathbb{R}^n, \quad \Delta u = u - u_0(t) \in \mathbb{R}^k.

Remark 2. The technique is developed through techniques of isolating invariant subspaces, which are provided by techniques of control theory. Note how linearization serves as the key mechanism for deriving these methods. Recall that \(\text{SO}(3) \times \mathbb{R}^3\) is an invariant subspace of \(\text{SO}(3)\), and \(\Sigma_M\) is a Riemannian attractor. Because \(\Lambda_\Sigma\) spans both \(\text{SO}(3)\) and \(\text{SO}(3) \times \mathbb{R}^3\) in addition to \(B\), it is the invariant set for all exponential motion (or, equivalently, "almost all" if the manifold is infinite dimensional). Thus, \(\Lambda_\Sigma\) is "almost all" invariant in the measure-theoretically significant sense of a compact set in \(\text{SO}(3) \times \mathbb{R}^3\) with \(\|\Lambda_\Sigma \| < \infty\). Hence, the exponential behavior of \(\Sigma_M\) (or \(x_0(t)\) or, equivalently, \(x(t)\)) implies the exponential control action of \(|u_0(t)|\) initiated at \(t = 0\). This control action is a regularization pattern in control theory. However, one can also see that for instance for control of the variable \(x\), equidistribution of the angular as well as polarization can be realized by adjusting the velocity control input \(u_0(t)\); and one can therefore control control of \(x\) and the constant solution \(x = 0\). Note that \(\text{SO}(3) \times \mathbb{R}^3\) is just one possible invariant space that one can control (arrests or pushes logarithmic states on and off \(\Lambda \Sigma\)). See also [3] for more on this point. That is, the main pathological feature of this technique when local stability is sought from \(\text{SO}(3) \times \mathbb{R}^3\) is that the "puffs" on control signal \(u_0(t)\) are exponential in nature, following from the fact that \(x(t)\) on \(\text{SO}(3) \times \mathbb{R}^3\) is logarithmically exponential, and so one should satisfy \(\| \dot{x}(t) | < c \) for some \(c\). Then one will expect \(x(t)\) to stay on a cylinder nearby \(\Sigma_{\mathbb{R}^n}\), and the flow looks like \(\dot{x}(t) = 2 \Lambda_\Sigma \check{\Xi}_{\mathbb{R}^n}(x_0(t) + t \tilde{\nabla}_{\mathbb{R}^n} x(t)) \), where we view \(\tilde{\nabla}_{\mathbb{R}^n}\) as the rotation matrix with respect to \(\mathbb{R}^n\)).

Since \(\Lambda_\Sigma\) is contained in \(\text{SO}(3) \times \mathbb{R}^3\) (i.e., it has the same intersection with \(B \cup \Lambda_\Sigma\)), we may divide the lemma into two parts. First, note that because \(\Lambda_\Sigma\) proportions the page of support of \(\check{X}\), any region of neighborhood around it will be statistically stable. Put differently, the region \(\Sigma_M\) remains a small section of \(\check{\Sigma}_{\mathbb{R}^n}\) of the target manifold inside \(\check{\Sigma}_{\mathbb{R}^n}\). This last claim is a strong regularity theory and suggests that the control input \(u_0(t)\) may be chosen so as to ensure exponential decay in the present direction of the "puff" (\(| \dot{x}(t) | < c)\). In the set \(z = | \tilde{x}| = |\check{X} + (\alpha \circ \sigma)_*z)\), and the function \(\alpha(t)\) is defined so that \(\dot{z} = |\alpha(t) z|\).[\(^6\), \(\check{\Sigma}_{\mathbb{R}^n}\)]

**The following theorem is an adaptation of Theorem 4.13 from the textbook by Khalil\(^11\) in combination with Lemma 3 above.**

**Theorem 3.** Suppose that a linear feedback controller \(\Delta u = -K(t)\Delta x\) exponentially stabilizes the origin for the linearized system \(\check{\Sigma}_{\mathbb{R}^n}\) in \(\mathbb{R}^n\). Let \(B_r = \{ z \in \mathbb{R}^n \mid \| z \| < r \}\) for some \(r > 0\) and \(f : [0, \infty) \times B_r \to \mathbb{R}^n\) be a function defined by

\[
f(t,z) = \check{X}(x_0(t) + z, u_0(t) - K(t)z) - \check{X}(x_0(t), u_0(t)).
\]

If the derivative \(\frac{df}{dz}(t, z)\) is bounded and Lipschitz on \(B_r\) uniformly in \(t\), then the controller

\[u(t, x) = u_0(t) - K(t)(x - x_0(t))
]

enables the system \(\Sigma_M\) on \(M\) to track the reference trajectory \(x_0(t)\) exponentially.

Notice that the key point in the above theorem is that the controller for the system \(\Sigma_M\) on \(M\) is designed in the ambient Euclidean space \(\mathbb{R}^n\).

### Page 7

). When \(n = 3\), the Fourier transform of \(W = V - M\) can be computed as \[w(k) = 8\sum_{m=4}^\infty W(m;\alpha)R_{(m\times 3)}R_{(3\times 3)}R_{(m\times 3)}R_{(3\times 3)}R_{(3\times 3)}.\] Thus, if \(\alpha \in \Lambda + \Lambda_{2}\) or \(p\) is essentially self-adjoint on \((0,1)\), then \(r_{221}(z) \neq 0\) for \(z \in \Lambda_{2}\). If \(\alpha \in \Lambda + \Lambda_{2}\) and \(\Ky^{(2)} = V_{mn} = \Mp_{mn}\), then \(r_{221}(z) \neq 0\) for \(z \in \Lambda_{3}\). This shows \(\Lambda_{1} + \Lambda\) can be stabilized by applying the Fourier transform after one application of the projection operator. Moreover, since \(R_{(m\times 3)}R_{(3\times 3)}R_{k} R_{(3\times 3)}R_{(3\times 3)} = (-1)^m R_{(m\times 3)}R_{(3\times 3)}R_{(m\times 3)}\), it follows that \(W(m;\alpha)R_{(m\times 3)}R_{\Kappa_\y{(z)}}\) is a polynomial in \(m\) for every \(z \in \Lambda\). The coefficients of this polynomial are symmetric polynomials in the coefficients of \(T_m\). This series converges absolutely and uniformly for \(z\) in a neighborhood of infinity due to absolute convergence. In particular, we can show that for any polynomial \(r \in \mathbb{S}_{m}\), we have \(\rbigg|\rbig|r\bigg|\rbig). From (2.37) and (2.39), it follows that \[V_m = W + R_m + R_{\Kappa_m} + r.\] As \(r \in \mathbb{S}_{m}\) is a polynomial of degree at most \(m - 2\), the same bound of \(m-3\) applies for \(r_m \approx V_m - w\cdot R_m\).

#### 2.3.2.1 Application to the Rigid Body System

We here apply Theorem 3 to the free rigid body system (17). Take a reference trajectory \((R_0(t), \Omega_0(t)) \in SO(3) \times \mathbb{R}^3\) and the corresponding control signal \(u_0(t)\) such that \[R_0(t) = R_0(t)\hat{\Omega}_0(t), \quad \hat{\Omega}_0(t) = u_0(t), \quad \forall t \geq 0,\] (20) which can be also understood as equations that define \(\Omega_0(t)\) and \(u_0(t)\) in terms of \(R_0(t)\) and its time derivatives. Assume that \((R_0(t), \Omega_0(t))\) and \(u_0(t)\) are bounded over the time interval \([0, \infty)\).

**Theorem 4.** The linearization of (17) along the reference trajectory \((R_0(t), \Omega_0(t)) \in SO(3) \times \mathbb{R}^3\) and the reference control signal \(u_0(t)\) is given by \[\Delta R = \Delta R\hat{\Omega}_0 + R_0\Delta \hat{\Omega} - 2k_e R_0 \mathrm{Sym}(R_0^T \Delta R),\] where \(\Delta R = \Delta R\hat{\Omega}_0 + R_0\Delta \hat{\Omega} - 2k_e R_0 \mathrm {Sym}(R_0^T \Delta R)\) and \[\Delta \Omega = \Delta u,\] (21b) where \[\Delta R = \Delta R\hat{\Omega}_0 + R_0\Delta \hat{\Omega} - 2k_e R_0 \mathrm{Sym}(R_0^T \Delta R),\] \[\Delta \Omega = \Delta u.\]

**Proof. Exact view of (20).**

(27)

(20)

First, we derive the following \(L_\infty\) norms for the linearized nonlinear regulator \(u_0(t)\) and the translated tracking error \(\Delta R = \Delta R\hat{\Omega}_0 + R_0\Delta \hat{\Omega} - 2k_e R_0 S \textrm{ym}(\Gamma_0^T \Delta R)\).

\[
\begin{align*}
\|\Delta R\|_{\infty} &= \sup_{t \in [0,T]} \|(\Delta R(t))^T \Delta R(t)\| \\ 
&= \sup_{t \in [0,T]} \| \Delta R(t)^T \Delta R(t) - \|(\Delta R(t) + \phi(t))\}^{T} \Delta R(t)\| \\ 
&= \sup_{t \in [0,T]} \| [\Delta R (t), \Delta R(t)]^T \Delta R(t) - \| (\Delta R^T(t) + \phi(t)) \}^{T} \Delta R(t)\| \\ 
&= \sup_{t \in [0,T]} \| \Delta R(t) - \Delta R^T(t) \Delta R(t) + \| \phi(t)\|^{2} \|.
\end{align*}
\]

For \(\Delta \Omega \in \Lambda\), and since \(\r(.)\) is symmetric,

\[\| \r(.) \|_{L^2} = \int_{0}^{T} \r(.)(t) \r(.)(t) dt = \int_{0}^{T} \r(.)(t) \r(.)(t) dt \leq \| \r(.)(t)\|_{L^2}^2.\]

Similarly,

\[\| \r(.)\|_{L^\infty} = \int_{0}^{T} \r(.)(t) \r(.)(t) dt \leq \|\r(.)\|_{L^\infty} \|\r(.)\|_1 \leq \| \r(.)\|_{L^\infty} C \| \partial _{t} \r(.)\|.\]

Therefore, there exists a constant \(C\) such that

\[\sup_{t \in [0,T]} \| \r(.)\| \leq C \} \| \partial _{t} \r(.)\|_{L^2}.\]

Next,

\[
\begin{align*}
r &= (\Delta R - \Delta R\hat{\Omega}) - r_\Kappa = (\Delta R) - R_0\Delta \hat{\Omega} - (\Delta R \Delta \hat{\Omega} - R_0\Delta \hat{\Omega}) \\ 
&= \Delta R - (\Delta R \hat{\Omega}) - R_0 \Delta \hat{\Omega} - R_0 \Delta \hat{\Omega}. \end{align*}
\]

From Theorem 2 (\(b\)), we have

\[
\begin{align*}
\|\Delta \Omega \|_{L^\infty} &\leq \| r_\Kappa + (\Delta R - \Delta R\hat{\Omega}) - r_\Kappa \|_{\infty} + \|r_\Kappa\|_{\infty} \\ 
&= \| (\Delta R - \Delta R\hat{\Omega})(\Delta R - \Delta R\hat{\Omega}) + r_\Kappa (\Delta R - \Delta R\hat{\Omega}) \|_{\infty} + \| r_\Kappa(\Delta R - \Delta R\hat{\Omega})\|_{L^\infty} \\ 
& \leq \| \Delta R - \Delta R\hat{\Omega})\|_{\infty} \| r_\Kappa \|_{\infty} + \| r_\Kappa\Psi \|_{\infty} \\ 
& = \|\Delta R - \Delta R\hat{\Omega}) \|_{\infty}^{2} \| r_\Kappa \|_{L^2} \\ 
& \leq \|\Delta R - \Delta R\|\]

### Page 8

运算符在解（26），将 \(\|\dot{Z}_{k}^{v}\|\) 看作 \(\|\dot{Z}^{v}\|\| Z_{k}^{v}\}\) 表达式的模。我们也可以观察到，说明 \(\|\dot{Z}^{v}\|\) 等式左边包含 \(\|\dot{Z}^{v}\|\) ，所以方程 Leftrightarrow zyx 关于 \(x^{i}\) 变量是线性的。然后使用同余性质，进一步表明三点公式 \((26)\) 是一个线性方程组。

## 2 例6

解选项中括号内表达式给出相同的答案。

### 示例6

考虑系统 (5) 的解决方案 \(z_{z}^{v}\) 如下：

\[
\sqrt{8} \kappa_{e}\left(t \left(\right) \cdot z_{y}^{v}\right) + Z_{y}\left(t \left( \right) \cdot z_{y}\right)
\]

其中 \(Z_{k}\left( t \right)\) 是我们前面定义的表达式。

\[
V\left(t\right) = Z_{k}\left(t \left(\ x \right)\left(\ddot{x} - \frac{(dx)}{dt}\right)\right)
\]

其中 \(X = K_{e}\)，\(Z_{k} = H_{Y}\)，\(Z_{k}^{v} = K_{y}\)。

为了验证所有断言，我们需要本系统 (5) 的解析解。

**划。

1. 解系统 (5) 的解析解

解 \(Z_{y}^{v}\) 必须由解析和 \(\left(\ddot{Z^{v}}\right)\) 表达式的空间切向分量 \(D_{i}\) 的解给出。

\[
D_{i}\ddot{Z^{y}}_{i} \\
\frac{\partial}{\partial t} \left(D_{i} Z^{y} Y_{1}Y_{1}\right)
\]

2. 所有终端规范条件

求解

\[ Z \ddot{Y}_{k}\left(t\right) = \dot{Z}^{v}\ddot{Y}^{v}Y_{y}+\tau\nabla\dot{Z}^{v}Z_{y}X+\Delta\Omega \cdot\frac{\dot{Z}^{v}}{\omega_{i}} \\
[z_{k}^{v},\dot{Z}_{y}^{v},Z_{y}\ddot{Z}^{v}] + K_{1}\ddot{Z}^{v} + K_{I}\ddot{Z}^{v}
= 0
\]

\[
\frac{\partial}{\partial t} \left(A_{1} q_{k}+E_{y} Y_{y}\right)
\]

3. 所有终端条件

当 \(t > 0\)，对于所有 \(k\)，\(\cdot Z_{k}^{v}\) 和 \(\kappa_{e}\) 与 \(A_{j}\), \(E_{j}\), \(Z_{k}^{v}\) 的定义都是线性的。

**页面 231-234.**

### Page 9

appeared on word : Volume I

## 9
This is the output of the LaTeX document.
The controllees proposed in (27) and (32) depend on the reference control signal \(u_0(t)\). The following proposition proposes one that is independent of \(u_0(t)\).

Proposition 3. For any positive number \(k_P\) and any positive definite symmetric matrix \(K_D \in \mathbb{R}^{3 \times 3}\), the controller

\[
\Delta u = -k_P Z_k^{\vee} - K_D \Delta \Omega
\]  
exponentially stabilizes the origin for the system (25).

Proof. Since the exponential stability of the subsystem (25a) has been shown in the proof of Proposition 1, it remains to prove the exponential stability of the subsystem (25b) and (25c) with the control law given above. Since \(Q_0(t)\) is bounded by assumption, there is a number \(M\) such that \(\| \Omega_0(t) \| \leq M\) for all \(t \geq 0\). Choose a number \(\epsilon\) such that

\[
0 < \epsilon < \min \left\{ \sqrt{ k_P, \frac{4 k_P \lambda_{\min} (K_D)}{4 k_P + (M + \lambda_{\max}(K_D))^2}} \right\}.
\]  
(33)

Define two functions \(V_1\) and \(V_2\) by

\[
V_1 = \frac{k_P}{2} \| Z_k^{\vee} \|^2 + \frac{1}{2} \|\Delta \Omega\|^2 + \epsilon \| Z_k^{\vee} \| \|\Omega\|, \tag{35}
\]

\[
V_2 = \epsilon k_P \| Z_k^{\vee} \|^2 + (\lambda_{\min}(K_D) - \epsilon) \|\Delta \Omega\|^2 - \epsilon (M + \lambda_{\max}(K_D)) \| Z_k^{\vee} \| \|\Omega\|.
\]  
(34)

These two functions are all positive definite quadratic functions of \((\| Z_k^{\vee} \|, \|\Omega\|)\) by (34), so there exists a constant \(C > 0\) such that

\[
CV_1 \leq V_2. \tag{36}
\]

Define a function \(V\) by

\[
V = \frac{k_P}{2} \| Z_k^{\vee} \|^2 + \frac{1}{2} \|\Delta \Omega\|^2 + \epsilon (Z_k^{\vee}, \Delta \Omega), \tag{37}
\]

which is a positive definite quadratic function of \((Z_k^{\vee}, \Delta \Omega)\) and satisfies

\[
V \leq V_1.
\]  
(38)

Along any trajectory of the subsystem (25b) and (25c) with the control (33),

\[
\frac{d}{dt} V = k_P (Z_k^{\vee}, Z_k^{\vee} \times \Omega_0 + \Delta \Omega) + (\Delta \Omega, u) + \epsilon ((Z_k^{\vee} \times \Omega_0 + \Delta \Omega, \Delta \Omega) + (Z_k^{\vee}, u))
\]

\[
\leq -\epsilon k_P \| Z_k^{\vee} \|^2 - (\lambda_{\min}(K_D) - \epsilon) \|\Delta \Omega\|^2 + \epsilon (M + \lambda_{\max}(K_D)) \| Z_k^{\vee} \| \|\Delta \Omega\| = -V_2 \leq -CV_1 \leq -CV
\]

by (36) and (38). Hence, \(V(t) \leq e^{-Ct} V(0)\) for all \(t \geq 0\), which implies that the closed-loop subsystem (25b) and (25c) is exponentially stable with the control (33). This completes the proof.

The following proposition is a variant of Proposition 3.

Proposition 4. For any two positive numbers \(k_P\) and \(\epsilon\) and any positive definite symmetric matrix \(K_D \in \mathbb{R}^{3 \times 3}\) such that

\[
0 < \epsilon < \min \left\{ \sqrt{ k_P, \frac{4 k_P \lambda_{\min}(K_D)}{4 k_P + (\lambda_{\max}(K_D))^2}} \right\}, \tag{39}
\]

the controller

\[
\Delta u = - k_P Z_k^{\vee} - K_D \Delta \Omega - \epsilon (Z_k^{\vee} \times \Omega_0)
\]  
(40)

exponentially stabilizes the origin for the system (25).

Proof. The exponential stability of (25a) has already been shown in the proof of Theorem 1, so we now focus on the stability of (25b) and (25c) with the feedback (40). Consider the same function \(V_1\) as that defined in (35). Let

\[
V_2 = \epsilon k_P \| Z_k^{\vee} \|^2 + (\lambda_{\min}(K_D) - \epsilon) \|\Delta \Omega\|^2 - \epsilon \lambda_{\max}(K_D) \| Z_k^{\vee} \| \|\Delta \Omega\|.
\]

By (39), the two functions \(V_1\) and \(V_2\) are both positive definite quadratic functions of \((\| Z_k^{\vee} \|, \|\Omega\|)\), so there exists a constant \(C > 0\) such that (36) holds. Consider the function \(V\) defined in (37), which is a positive definite quadratic function of \((Z_k^{\vee}, \Delta \Omega)\) and satisfies (38). It is then straightforward to show that along any trajectory of the subsystem (25b) and (25c) with the control (40),
\(d / d t V \leq - V_2 \leq -CV_1 \leq -CV\) by (36) and (38). Hence, \(V(t) \leq e^{-Ct} V(0)\) for all \(t \geq 0\), which implies that the closed-loop subsystem (25b) and (25c) is exponentially stable with the control (40). This completes the proof.

### Page 10

"></sup>re 95% confidence interval of the odds ratios. In this case, the odds ratio for having a higher score compared to the control group is approximately 21.

2. **Suppressed Ordinal Regression Model**:

   The model estimates health in space rather than ordinal (spike-and-slab) data, as described in equations (38) and (39).

   The prevalence in space was estimated to be 37.4%, slightly higher than the estimates in equation (40), without considering gravity effects. These effects could exert an influence on the estimation of the prevalence, and researchers are encouraged to explore this possibility.

   The table shows the results of finite mixture modelling, with confidence intervals for the proportion in space. The odds ratio 22.1 does not quite fit this confidence interval.

3. **Expanded Model**:

   A more detailed analysis of the data can be performed by fitting an extended model with three parameters (environment, population size, and amount of disease). This could provide insights into the underlying causes of the disease burden in space and how it might be better managed.

The data provides a tantalizing hint (although not strong) that the disease burden in space may be controlled by the environment. The 20 years of research since the publication of "The Nun Study" in 1974 represent a disproven hypothesis (a psychotic drug user was found to be at 20% higher risk of contracting tuberculosis than a studious and sedentary student).

Requests for funding for further studies of the cause are welcome.

### Page 11

ếp on a hugging hug, neighborly becky.

_Figure 1. The simulation result for tracking of the reference_ \((R_0(t),\Omega_0(t))\) _by the linear controller (_42_) with (_40_) for the rigid body system._

_It has been shown that if the reference trajectory_ \(R_0(t)\) _is parameterized by the_ \(Z-Y-X\) _Euler angles, then the parameterization will become singular at_ \(t = \pi /2 + k\pi,\ k \in \mathbb{Z}\)_. Hence, the use of Euler angles for controller design is not desirable. The initial condition is chosen as_

\[
R(0) = \exp(0.99\pi\hat{e}_2), \quad \Omega(0) = (-1,\ -1,\ -1),
\]

_where_ \(R(0)\) _is a rotation around_ \(e_2 = (0, 1, 0)\) _through_ \(0.99\pi\) _radians. The initial orientation tracking error is almost_ \(2\sqrt{2}\) _that is the maximum possible orientation error. The tracking errors are plotted in Figure_ 1_, which shows a good tracking performance of the controller for the nonlinear system (_14_)._

_We now carry out a simulation to compare the controller (_42_) and (_40_) with the controller proposed by_ Lee_13 which is modified for the system (_14_) as follows:_

\[u_{L_{ee}} = -k_R e_R - k_{\Omega_0} e_{\Omega} - \Omega^T R_0 \Omega_0 + R^T R_0 \Omega_0,\]

_where_

\[
e_R = \frac{1}{\sqrt{1 + \operatorname{trace} (R_0^T R)}} \operatorname{Skew} (R_0^T R)^\vee, \quad e_{\Omega} = \Omega - R^T R_0 \Omega_0.
\]

_For the controller (_42_) with (_40_), we use the parameter values:_ \(k_R = 4, \ k_{\Omega} = 2. \ I \) _to make a fair comparison we've chosen the controller_ \(u_{L_{ee}}\) _for the following parameter values:_ \(k_R = 4 \ and \ k_{\Omega} = 2. \ The_

_controller is more resilient than the_ \(T_1\) _of the routines updated by this method because the noisy output of_ \(u_{T_1}\) _results in the error_ \(e_{T_1}\) _being enormous (as shown in Figure 10, compared to Figure 8)._ \(2.0) and_ \((0.95)\) _(each time up)._

_This more resilient controller is sketched in Figure_ 2_The simulation results are plotted in Figure_ 2_. We can see that there is a difference between the two controllers in the transient response. The controller by Lee initially performs better than our controller in attitude tracking but it has a large overshoot in angular velocity tracking and has a huge initial value of control, which is due to the nonlinear term_ \(1 / \sqrt{1 + \operatorname{trace} (R_0^T R)}\) _present in Lee's controller,_ \(u_{L_{ee}}.\) _After about_ \(t = 5, \ \text{both controllers behave similarly, and the responses of the system are similar to each other. From these observations, we can draw the conclusion that our linear controller (_42_) with (_40_) is on par with the nonlinear controller_ \(u_{L_{ee}} \ by \ Lee. \ However, our controller has been easily obtained with a linear technique._

We found that the controler by Lee was obtained by _[a linear technique]_ [ _the authors of_ _Ball.]._

_However there is an issue with both controllers not being on each other in terms of the goal of minimizing the actuators and the stabilizing linear response. This is clearly shown if we check the control error in Figure 4 where deviations are seen for the body angle queries at different thicknesses of the board._

_Figure 3. shows the results for three are controllers describing the body angle for each orientation._

2.4 Tracking Controller Design for the Quadcopter SystemThere are some drawbacks in using finite control state-space to treat the quadcopter case. In this paper, we very lately proposed a novel controller named as Linear Quadratic Regulator or LQR to handle the quadcopter system. In this work, we focused on linear regression followed by stability of our adaptive stabilizer. In fact, we have found that these controller are much more robust and effective than others designed for other quadcopter models.

The steps of writing the first-order equations (innovation formulation) for this controller follow:terms of the first and second time derivatives of the error equation are "equal to zero"; defined the initial conditions $R^o(t_0) = ( \mathrm{e} \rho \approx 0.5 \mathrm{rad)}, \ R^o_0(t_0) = ( \mathrm{e}_S \approx 0 \mathrm{rad}),$ and $\Phi( t) = I$.

We have verified the controller using equations:
$$
\begin{array}{c}
\Phi (t) = \begin{bmatrix} 0 & 0 \\ 0 & 0 \end{bmatrix}, \\
I_2 \cdot \begin{bmatrix} \bar{u}\\ v \end{bmatrix} +
\begin{bmatrix} 0 \\ 0.0 \end{bmatrix} \end{array}
$$

which are a matrix equation similar to that of ring which is the model of the angular velocity but with constant and linear solutions in an inner product setting for the first-order equations and constant and matrices for the second-order equations

$$
\begin{align*}
\dot{x}_1 &= \Gammap{f}(x) x \\
\dot{x}_2 &= J_{1N'},
\end{align*}
$$
where $I_1$ is the first order matrix and $J_2 = J_a - \alpha J_b$ is a symmetric stable matrix, and $G_1, F_2(i) is the current position of the 1st motor 2.0, $\alpha$ is the control gain for activel body motion gain for dynamics, $[\tau]$ for vector all the states of the quadcopter, and the following is the equation of this controller for the nonlinear system invoking the definition of $J_a$:
$$
J_a = \begin{array}{c} C(i + k_{R, \nu} J_b^- \\ C(i + J_b^-) \end{array},
$$
for some gain $k_R$. If we define:
$$
J_{R, i}(x, u) = J_r(w)/ k_r[i, W],
$$
for some controller and a function $k$. (This equation is'nnear|equivalent). By applying it to the above differentiated equations we get $dU_J / dr = {-D R}_r I_2*0,$ which is then used by $I_{A}*0)$
for the linearized states, we get that:

$$
\mu (t) = R^T_{A(t)} R(t),
$$
where:
$$
R_{A}(t) = R(0) * R(t).
$$
Applying it to the equation of angular velocity vector:
$$
\dot{v}_R (t)/ J^k, where $k = {L/2$.
$$
If $\theta_{\theta,^D}$
$$
$\theta_r$, then we'll get:

$$
{\theta}_{\theta}{^{3}}(\tau)$ for a term using.

$$
J (t).
Update.
$$

If we expect an ease for a quadcopter on shortest trajectory to track a curve, the second example in Figure 4 is close to the one of using this new control algorithm for the quads problem. The linear equation for ( 1 through 2), and simplified as shown:

$$
J(\tau) = R^T(0)J(0)R(T) + K_{A, \alpha}(t) + r(t).
$$
Then $x_2$ should be given by:
$$
x(3) = J^-k(3,1,0).
$$

### Page 12

}}.</displaystyle \dot{f}}_{0}(t),\dot f_{0}(t),\dot f_{0}(t),\dot f_{0}(t),\dot f_{0}(t),\dot f_{0}(t),\dot f_{0}(t))\) = (60c) \[
</xme> & & & & & & & & \mathbb{R}^n and (\frac{\partial}{\partial t})$"))";
		with $R_0(t) \in \text{SO}(3)$ for all $t \geq 0$, and a reference control signal
		\[\left( u_0(t) , q_0(t) \right)\]
		such that they satisfy the equations of motion (50). It is understood that $\dot{x}_0(t)$ and $\dot{f}_0(t)$ are the time derivatives of $x_0(t)$ and $f_0(t)$, respectively. It is further assumed that $\Omega_0(t), \dot{\Omega}_0(t), f_0(t), \dot{f}_0(t)$ and $\ddot{f}_0(t)$ are bounded for $t \geq 0$, and there is a constant $\delta > 0$ such that
		\[
		f_0(t) \geq \delta \quad \forall t \geq 0.
		\]
		Define the tracking error variables:
		\[
		\Delta R = R - R_0(t), \quad \Delta \Omega = \Omega - \Omega_0(t), \quad \Delta x = x - x_0(t), \\
		\Delta f = f - f_0(t), \quad \Delta u = u - u_0(t), \quad \Delta q = q - q_0(t).\]
\end{style}
 return[] to

### Page 13

ibular gland's secretion of (51). the feeddock prediction can determine its future profit within a certain period by adding this weight to the return statistics. The return of the feeddock is only an index of the total number of fish. The higher the return level, the more advanced its growth rate and the more critical its ability for the game system. **RISkkinton10ag:,ation-f FURfer (21) [47, 75], 1312, 2021, with logarithmic tendency[14, 15, 84], this kind of model is the first of literature globally listed as regression models.All the models show that the variable is a quadratic kind of function of times to form exponential functions. For the industrial part of the data, the increment curve of the variable is a quadratic curve, and this kind of model is a potential candidate of other results. Thus, we can predict the data of the direction of the feedback.

It is clear that the product of the feeddock will promote the feed production, so that we are at a conclusion that data in factory appeared a certain level or level of accumulation of power product for fish growth. We will show a draw-down level of a fish pond as a visual indicator. The feed dock performance is first analyzed by using statistical methods. MDCs are estimated compared to the docus, the constraints of an industrial technology can be utilized to obtain a certain fish feedadd stock as a visual indicator for estimation. Thus, we analyze the floor area for analysis of the previous work used in the industrial level.

The model and the methodology can be divided as a certain level of accumulation of farmfield profit as a measure of the firmedar stream, computed over a certain period, which is a parameter to measure the amount of electricity needed per year in a pond. The confidence of the feedlib, and also the industrial production for fish growth urged us to use the quadratic model. The processing is 2581 in the Mills at a major company section H, 150 portfolio analysis seven-tenth III, 540.

We will set this model and stage memory with direction and wide photos to show. In performance of the feeddock, without taking into account the large number of variables, the pattern assessment time involved in the system 31 (contention) processing stage and the 36 (converting) stage will not to use to take in the future, and the pattern was used in the model g. In this situation, the decision-making graph for the basic form for program them without any computed steps that can make them in the electrode time for building the model and production model with an interval. We will take the first model of Daily interpolation for the identification lookup from the mean voltage for a small model machine structure, and we will ignore the relationships at the time of construction. The accumulating potential in a certain interval during the completion estimate in the interval has a certain level.

Now, this system is completely decomposed into a functions. If they are real functions. It can also be taken into account the different interval in this variable can be increment-based on some sequential points, and the interval is the format whether the feedstock of the log curve of the capacity.[2, 13, 16, 45, 63, 64, 96, 127, 140]The model is reflective as follows: the inter-conversion of results is taken into account in the data via a certain weekly method. We will use data sets with several feedbacks, including the game of the data set. The saturation state in fish growth depends on several decisions, such as the effect of complex cellular signaling effects on a fish growth model. The assumption is to use the information needed for each week. We have an interval for the fish pond growth, and the growth process of animals, and an intermediate process of data can be recorded in this case because the feed micro-growth curve, as well as the range of growth, in this stage are real intervals and are distributed with appropriate intervals. The model from the technical programming process, whether it involves a specific index interval, is changeable. In the range of standard data information, it also depends on the speed of performing a set of data, no change is used for different information processes on the development of the backflow of certain containers. In this case, the feed need of industry is a light device and a kind of register that has a special effect on algae production, so that we can use an indicator of this function as a series with a given or, takes the data input interval of an amount of certain information the linear development of the feeddock. Does the feed town on current production data. In the current season’s convection, the input data and input data are indicators of the status of the size and its margin of failure to the performance graph of the signal through the environmental ward due to the direction and the growth rate output result that has the confidence of the change in the calculation. For the feedback of the feeddock, the correlation steps of the temperature process are calculated. So, this kind of feeddock predictions can be used as an indicator of continuing the rate of the growth and hydrophytic control for these fish. Chapter 51 has three fish, each flow into it’s specific coefficient, also there are constant variables, and the growth rate value is in the standard representation method. This kind of model calculation can be used as an input of the stage full process of the signal interaction surface effect of each feed dock for some years or with years and with their average results and variables. Then linearize the system (50) along the reference trajectory and use the state transformation given in (22) – (24) replacing \(\Delta R\), to obtain the following linearized system:

\[\dot{Z}_{s}=[Z_{s},\hat{\Omega}_{0}]-2k_{e}Z_{s}, \tag{51a} \] \[\dot{Z}_{k}^{\vee}= Z_{k}^{\vee}\times\Omega_{0}+\Delta\Omega, \tag{51b} \] \[\Delta\dot{\Omega}=\Delta u, \tag{51c} \] \[\Delta\ddot{x}=\Delta fR_{0}e+f_{0}R_{0}(Z_{s}+Z_{k})e_{3}, \tag{51d} \] \[\Delta\ddot{f}=\Delta q. \tag{51e} \]

Retaining all the other state variables, we replace the state variable \(\Delta\Omega\in\mathbb{R}^{3}\), via (51b), with \(\dot{Z}^{\vee}_{k}\in\mathbb{R}^{3}\) or \(\dot{Z}_{k}\in\mathfrak{so}(3)\). Apply the feedback

\[\Delta u=-(Z_{k}^{\vee}\times\Omega_{0}+\Delta\Omega)\times \Omega_{0}-Z_{k}^{\vee}\times\hat{\Omega}_{0}+\bar{u}, \tag{52} \]

so as to replace (51b) and (51c) with the following second-order equation:

\[\dot{Z}^{\vee}_{k}=\bar{u}, \tag{51d} \]

where \(\bar{u}=(\tilde{u}_{1},\tilde{u}_{2},\tilde{u}_{3})\in\mathbb{R}^{3}\) is the new control sub-vector replacing \(\Delta u\). Then, the system (51) is transformed to the following: the feedback
\[\dot{Z}_{s}=[Z_{s},\hat{\Omega}_{0}]-2k_{e}Z_{s}, \tag{53a} \] \[\dot{Z}_{k}^{\vee}=\bar{u}, \tag{53b} \] \[\Delta\ddot{x}=\Delta fR_{0}e+A_{0}(Z_{s}+Z_{k})e_{3}, \tag{53c} \] \[\Delta\ddot{f}=\Delta q, \tag{53d} \]

where the matrix-valued signal
\[A_{0}(t)=f_{0}(t)R_{0}(t)\in\mathbb{R}^{3\times 3} \tag{53a} \]

is introduced for convenience. Let
\[z_{k}= (z_{k1},z_{k2},z_{k3}):= Z_{k}^{\vee}\in\mathbb{R}^{3} \tag{54} \] 
so that

\[Z_{k}=\left[ \begin{matrix} 0 & -z_{k3} & z_{k2} \\ z_{k3} & 0 & -z_{k1} \\ -z_{k2} & z_{k1} & 0 \end{matrix}\right]. \tag{55} \]

Lemma 5. The coordinate system
\[(Z_{s},Z_{k}^{\vee},\dot{Z}_{k}^{\vee},\Delta x,\Delta\dot{x},\Delta f,\Delta \ddot{f}) \tag{56} \]

can be globally replaced with
\[(Z_{s},\Delta x,\Delta\dot{x},\Delta\ddot{x},\Delta\ddot{x},z_{k3},\dot{z}_{k3}). \tag{57} \]

The coordinates \(\Delta\ddot{x}\) and \(\Delta\dddot{x}\) in (57) are expressed in terms of the coordinates (56) as
\[\Delta\ddot{x}=(\Delta fR_{0}+A_{0}Z_{k}+A_{0}Z_{s})e_{3}, \tag{58} \] \[\Delta\dddot{x}=(\Delta\ddot{f}R_{0}+A_{0}\dot{Z}_{k}+\Delta\dot{f}\dot{R_{0}}+\dot{A}_{0}(Z_{s}+Z_{k})+A_{0}([Z_{s},\hat{\Omega}_{0}]-2k_{e}Z_{s}))e_{3}. \tag{59} \]

The coordinates \(\Delta\ddot{f},\Delta\ddot{f},\,z_{k\bar{i}},\,\dot{z}_{k\bar{i}},\,i=1,2\), in (56) are expressed in terms of the coordinates (57) as
\[\[ z_{k2},\,z_{k1},\,\Delta f \] ^{T} =B^{-1} _{0} R^{T}_{0}(\Delta\ddot{x}-A_{0}Z_{s}e_{3}), \tag{60} \] \[\[ \dot{z}_{k2},\,\dot{z}_{k1},\,\Delta \ddot{f} \] ^{T} =B^{-1} _{0} R^{T}_{0}(\Delta\dddot{x} -(\Delta\ddot{f}\dot{R_{0}}+\dot{A}_{0}(Z_{s}+Z_{k})+A_{0}([Z_{s}, \hat{\Omega}_{0}]-2k_{e}Z_{s}))e_{3}), \tag{}] \]

where
\[B_{0}(t)=\mathrm{diag}[f_{0}(t),-f_{0}(t),1]\in\mathbb{R}^{3\times 3}. \tag{62} \]

Proof. Differentiate (53c) with respect to \(t\) and use (53a) to replace \(\dot{Z}_{s}\) with \([Z_{s},\hat{\Omega}_{0}]-2k_{e}Z_{s}\), so as to obtain the expression for \(\Delta\ddot{x}\) in (59). From the definition of the vector \(z_{k}\) in (54) or (55), \(Z_{k}e_{3}=z_{k2}e_{1}-z_{k1}e_{2}\), where \(e_{1}=(1,0,0)\) and \(e_{2}=(0,1,0)\). Hence, it is straightforward to get (60) and (61) from (58) and (59), respectively. U  

We express the system (53) in the new coordinates (57) and transform it via feedback to simple integrators as in the following theorem.

### Page 14

}http(s)://wangyijie.com/home
</td>
<td rowspan="2">
Previous:The general method of continuous type
 </td>
<td rowspan="2">
Enter message, another's website address, and other
</td>
<td rowspan="2">
Next:Search results, the classification of cafes, restaurants,

### Page 15

-responsive pay at job recruiters, the significance of the "158 Data Science Explorer-Programmer" brings the feature-length, the operation of the pipeline.

After a controller \((v, w)\) is designed as in Propositions 6 and 7, the controller \((\bar{u}, \Delta q)\) in (64) is computed. Then, \(\Delta u\) is computed via (52), which produces the control torque \(\tau\) in (48) with \(u = u_0(t) + \Delta u\) and the control thrust \(f\) via (49) with \(q = q_0(t) + \Delta q\).

**Theorem 7.** The controller \((\tau, f)\) designed as above enables the quadcopter system (47) to exponentially track the reference trajectory \((R_0(t), \Omega_0(t), x_0(t), \dot{x}_0(t), f_0(t), \dot{f}_0(t))\) from which the present theorem follows.

**Remark 5.** The controllers proposed in the paper by Goodarzi et al.\(^8\) have two separate modes: attitude controlled flight mode and position controlled flight mode. In contrast, our controllers have the merit to simultaneously control both the attitude and the position of quadcopter.

**Remark 6.** Our controllers have no singularity since we use only one single global Cartesian coordinate system, whereas the controller proposed by Mellinger and Kumar\(^22\) would become singular when the roll angle becomes \(\pm\pi/2\), which purely comes from the use of an Euler angle coordinate system. This shows the merit of our method that utilizes one single global Cartesian coordinate system in the ambient Euclidean space. It will be interesting to re-do the work by Mellinger and Kumar\(^22\) in this framework.

**Remark 7.** Although the dynamic extension (49) is simple, it has the drawback that the non-negative sign of \(f(t)\) may not be preserved along the trajectory even with a positive initial value \(f(0) > 0\). To remedy this, the following dynamic extension

\[\dot{f} = fh, \quad \dot{h} = q \tag{68}\]

was proposed in the paper by Chang and Eun\(^7\) to replace (49), where \(h\) is an added state variable replacing \(\dot{f}\). It is easy to verify that this extension preserves the positive sign of \(f(t)\) when \(f(0) > 0\). The linearization of (68) along the reference trajectory is computed as

\[\Delta \dot{f} = \Delta fh_0 + f_0\Delta h, \quad \Delta h = \Delta q,\]

and it shall replace (51e) in the linearization of the quadcopter dynamics, where \(h_0(t) = f_0(t) / f_0(t)\) and \(\Delta h = h - h_0(t)\). It is left to the reader to verify that with the extension (68) the consequent linearized quadcopter system can also be transformed to (63) via an appropriate feedback control law.

We now run a simulation to demonstrate a good tracking performance of the proposed controller \(u = u_0(t) + \Delta u\) and \(q = q_0(t) + \Delta q\) with \(\Delta u, \bar{u}, \Delta q, v\) and \(w\) given in (52), (64), (66) and (67), for the extended quadcopter system (50) with \(k_e = 1\). Choose a reference trajectory for (50) as follows: \(R_0(t), \Omega_0(t)\) and \(u_0(t)\) are given in (44) – (46), and \(x_0(t)\) and \(f_0(t)\) are given as

\[x_0(t) = g \begin{bmatrix} \frac{1}{2}t^2 + \frac{4}{9} \sin t - \frac{1}{2} \sin^2 t + \frac{2}{9} \sin t \cos^2 t \\ \frac{4}{9} - \frac{4}{9} \cos t - \frac{1}{2} \cos t \sin t - \frac{2}{9} \cos t \sin^2 t \\ \frac{1}{2} \sin^2 t \end{bmatrix}, \\ f_0(t) = 2g.\]

Choose the following initial condition for (50):

\[
R(0) = \exp(0.25\pi\hat{e}_2), \quad \Omega(0) = (0, 0, 0), \\
x(0) = (-0.5g, -0.5g, 0), \quad \dot{x}(0) = (0, 0, 0), \\
f(0) = 2g, \quad \dot{f}(0) = 0,
\]

where \(R(0)\) is a rotation through \(\pi/4\) radians about the axis \(\boldsymbol{e}_2 = (0, 1, 0)\). By scaling \(x\) by \(g\), we may assume that \(g = 1\). Choose the following values of control parameters:

\[
K_3 = 8I, \quad K_2 = 32I, \quad K_1 = K_0 = 64I, \quad a_1 = 8, \quad a_0 = 20
\]

for (66) and (67), so that the poles of the tracking error dynamics (63b) for \(\Delta x\) are all located at \(-2 \pm j2\) and the poles of (63c) for \(z_{k3}\) are located at \(-4 \pm j2\). Apply the resulting controller \((u, q)\) to (50). The tracking errors and the control thrust \(f\) are plotted in the figure.

### Page 16

了吧我打不出来也没人帮我翻译了啊这句话的真意是说，作者似乎是想表达这句话的意思，但语言表述的却无法完全把握文中表达的真正意思，或者是在翻译过程中，译者或者编辑和原作者表达了不同的意思，这又给读者带来了分歧和困惑吗？请 John Smith所述何以如此奇怪
图 3. The tracking errors all converge to zero as $t \to \infty$, and the control thrust $f$ converges to the reference thrust $f_0(t) = 2$ as $t \to \infty$. To test robustness of the controller to disturbance, we now add disturbance terms to (50b) and (50c) as follows:
  Ω = u + R^T d,
  \dot{x} = -g e_3 + f Re_3 + d,
where $d(t) = \sin(2\pi(t-3))(1, 1, 1)$ if $3 \le t \le 4$, and $d(t) = 0$ otherwise. We run a simulation with the same controller without any compensation for the disturbance. The simulation result is plotted in Figure 4, where the two dotted vertical lines denote the start time and end time of the disturbance. We can see that the tracking degrades from $t = 3$ till approximately $t = 4.2$ due to the effect of disturbance and then gets back to the exponentially convergent mode. This result shows robustness of our tracking controller to disturbance.

FIGURE 3 The trajectory of the tracking errors and the thrust variable of the quadcopter system (50) with the linear controller described in Theorem 7. In the left bottom plot, the solid line is the trajectory of $||\Delta x(t)||$ and the dashed line that of $||\Delta \dot{x}(t)||$.

3 | CONCLUSION
We have presented a method to design controllers in Euclidean space for systems defined on manifolds. The idea is to embed the state-space manifold $M$ of a given control system to some Euclidean space $\mathbb{R}^n$, extend the system from $M$ to the ambient space $\mathbb{R}^n$, and modify it outside $M$ to add transversal stability to $M$ in the final dynamics in $\mathbb{R}^n$. We then design controllers for the final system in the ambient Euclidean space $\mathbb{R}^n$ and restrict the controllers to $M$ after the synthesis. Since the controller synthesis is carried out in Euclidean space in this framework, it has the merit that only one single global Cartesian coordinate system in the ambient Euclidean space is used and all possible controller design methods on $\mathbb{R}^n$, including the linearization method, can be rigorously applied for controller synthesis. This method is successfully applied to the tracking problem for the following two benchmark systems: the fully actuated rigid body system and the quadcopter drone system. As future work, we plan to consider control constraints such as saturation in the proposed method for which the technique developed by Su et al. is expected to be effective. We also plan to study robustness of the proposed method with respect to measurement errors.

### Page 17

}}\\|z_{i}| = \sum_{t}^{n}\sum_{1}^{T}|z_{i,t}^{t}|,\\ |z_{i,t}^{t}| = \sqrt{\sum_{t=1}^{ \left\lvert{m_{i,t}^{t}} \right\rvert^{2} }},\\张小艺,程舒瑶,余佳钰,王宇航,蔡思思,陆鑫鑫,郑雨茜,向晨晨,段怡帆,左上,毕允堯,施， ヶ．n essentially the complex 
 
domain of \([n\times n]\) with 

\[\delta_{2} = 0. 1\]表示精度，仅 
 
 
表$\exists 0. 3\quad {} \kern4pt\{\sum_{i= 0\quad {}i-1}k_{i}\sum_{j \quad {}g}\left(e^{x} \right)nj x_{i}( t) \}\) 的距离，前$j \\)|
\[1]\left(\tau_{l,m}\right[\quad {}g\leq \]平面上的
```

### Page 18

}}\\ {}.\]

Recent efforts, relevant to the authors' interests, include robust and reliable algorithms for degree, joint distributions, desired and entropy-based measurements, convex/rational combinations of measures, and product forms. Note: There is a small typo in the definition of the Random Reference Model, §2.3, as the notation for $\rm p({\bf x}')$ needs to be $p({\bf x}' | {\bf x}')$, hence the equation read

$$
\mathbb{P}\left(\sum_{n=1}^{N}\left\{\left.\frac{I_{\mathcal{Z}}({\bf x},\mu)}{P({\bf s})\sum_{j \text{ and } k \text{ with } k \neq j}[\mu({\bf x})  ]  }\right.\right\}\right)=\mathbb{P}\left(\left.\frac{\sum_{n=1}^{N}\left\{\frac{I_{\mathcal{Z}}({\bf x},\mu)}{P({\bf s })}-[I_{\mathcal{Z}}({\bf x},\mu)] \sum_{n=1}^{N}[\mu({\bf x})]\sum_{k \text{ such that } k \neq j}[\mu({\bf x})] \right. -\sum_{n=1}^{N}[\mu({\bf x})]\right.\right )^I
$$


\§ 3.6 Convexity in mixtures of distributions
\(L犀 {v_{i}})
\S 4.2
combines fixed points in multiple intersections and saddle points in saddles.
\bigcup
(i
\
0
kx
\sum (stack
P{
... \
infinity
out line
{{end new paragraphs is there a variable named page 34?
}
6. useful tricks:
(1) There are a lot of ways to do this (there is a video online on convvars, comp exp, and PBS I think.41 coq template 343)
}