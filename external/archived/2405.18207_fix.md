### Page 1

Figure 2 Experimental setup of the system developed for Glimpse-2. (...)

The experimental setup of the system developed for Glimpse-2.

Figure 3 Experimental results for Glimpse-2 system. ** Mean error when referring to the best system out of 3 experiments 2. Constraints and limits according to Section 4

Constraints and limits according to Section 4

Experimental limitations caused by the geometry of the camera.

Figure 1 Conceptual diagram of the camera geometry for the Glimpse system

1. Conceptual diagram of the camera geometry for the Glimpse system

The GL1920 camera has a nut-type lens housing with a focal length of 192 mm and an aperture diameter of 44 mm. An F1.8 lens has been used for the camera. The camera is equipped with a solar cell and an inertial measurement unit (IMU) for estimating the camera position and velocity. The camera was mounted on a tripod and covered with a polarizer filter. The camera was placed inside a rigid box to reduce its impact on the environment. The camera was illuminated with white light from a commercial light source positioned at a distance of 1.5 m from the camera. The dark chamber was also equipped with a filter to eliminate any external light. The IMU and the solar cell were connected to an external computer for data acquisition and the Glimpse app.2 The camera, the camera box, and the dark chamber were placed inside a greenhouse. The terrarium was a plastic slab filled with soil and was positioned in the middle of the greenhouse. The terrarium was 2 m long and 1 m wide. The soil was covered with a plastic sheet to maintain the humidity inside the terrarium. The plastic sheet was 1 m above the soil to prevent the loss of water from the terrarium. The terrarium was equipped with a humidifier to maintain a temperature of 27 °C and a humidity of 40%. The light inside the terrarium was 2800 lux. The dark chamber was a terrarium with a lid that allowed the entry of external light. The dark chamber was 3 m long and 2 m wide. The soil inside the dark chamber was covered with aluminum foil to 2 m above the soil. The external light was blocked from the terrarium. The terrarium was equipped with a filter to eliminate any external light. The filter was 1 m above the soil to prevent the loss of water from the terrarium. The terrarium was also placed in a shaded spot. The planted plants were not watered for 16 hours. Glimpse was powered by two 1.5 V alkaline batteries and a Rechargable USBC Lithium battery (capacity 2200 mAh, power 5 W).2,3 The camera was controlled via a GroveTab wireless module (GPIO-PKT v6.0.0.8) and an Android smartphone running an Android app (a so-called Glimpse Manager). The app stores up to 15 Glimpse shots a day. For the statespecific experiment, each shot is associated with three categories: Ephemeral, Cloudy, and Yearly. The day start time was initially set to midnight, but was changed to 10:00 PM. Seasonal considerations only significantly impacted Glimpse usage in happens during the months of June, July, and August, when the terrarium is being artificialized to more resemble its natural surroundings. In the course of the project, we suggest having the terrarium at once or divided into several independently capable terraria and connect them in series as a "Sonnenscheinbodern" (solarwise night observer).

Many projects focus on achieving a soft push from high-resolution data generated by traditional asset cameras or even through crowdsourcing platforms for far-field 3D images. Further, the development of 3D data capture has been updated with HD images. Also, large fields of view and autonomous image analysis have been achieved using photogrammetry for the high precision and rapid processing speed of the collected data. With larger datasets and higher resolution images, Blender provides a good foundation to proceed with automated estimation that can be adjusted for Zh-K360 online system or single long-duration sensor data acquisition.

The main factors on the state's development target can be listed as follows: to reduce the errors associated with the estimation of the texture, goals must shift from the simple smoothness to the complicated details in the shadow. The moments of the data application to the ecosystem are mostly focused on efficient approaches, such as Template Matching2 and the evaluation of scene physics through machine learning algorithms such as convolutional neural networks. In the next step, we could introduce the 2D signatures of the scenes of a deep learning that augmented reality systems could make complex situations formulated by data drawings (Harmany, & Nystrom, 2020; Petrov & Estern, 2020). The examples provided lie in last but not least still in the context of the required data transfer to a section of artificial vegetation through robots. In most cases, high information transfer technologies will be necessary for vast regions of the world and the growing need of deep reinforcement, especially in developing countries (Kerem, Torgova, Babak, & Nikolova, 2020; Sinha, Tufail, & Charak, 2020) The creation of experiences are for the support of the human changes when using artificialized soil. By contributing to the increase in the quality of life by creating open data at the interface between the data space and the user experience (Calmes, 2020) This is, however, arranged intentionally from the system developers perspective. The example provided relies in the extent of Sun实录 but not much focus on future consideration regarding consideration of the period of hyper-efficient soil development tasks.

Table 1: Exports: Data captioning, (singular above all cases), Radically different or new product. In last but not least interest associated with data transfer are in and by dynamic texture enhancement on existing data streams such as hyperspectral imagery or night vision cameras used for terrestrial surveillance. 1: Unknown (singular: Research) 2: Research coming (singular) 3: Rural areas 4: Continent 5: Africa 6: South America 7: Asia 8: Europe 9: North America 10: Australia 11: Oceana 12: World 13: unknown (singular by years 14: in 2010 15: There is no relevant and/or correct description.). 16: Annémed data appears to significantly influence agriculture. An analytical search of the scope of the system and its role and the market in terms of agriculture is again running.

\multirow{2}{*}{Base-powered system}

22: Experimental webs browser 24: APIs for application profiling and data visualisation 25: N/W domain assistant 26: Application or function 27: Spectrum analysis 28: Applications 29: Analysis 30: Geekettes 31: System for 32:璃 33: program analysis 34: NIC 35: High performance web-engine 36: It is 37: software集成 engine 38:Ęers cendo tools 39: Data analysis 40: Call centre 41: Data scientist 42: Partner 43: daily activities 44: risk assessment 45: environmental 46: sensory-ear 62: sound recognition 63: data linkage 64: Gate 74: deep learning 75: objects in the room 76: high **accuracy manual 77: indirect 78: automatic (empty) quotations 79: automatic reporting tool 80: software 81: automatic database update 82: automated pipeline code compilation 83: **software 84: consulting 85: system for addressing

### Page 2

d^{2}\) by considering all possible semistations.

This was a fundamental issue that is barely mentioned till now. Consecutive semistations are non-overlapping and can be treated as distinct paths. We have shown later in Section 3.1 that it is convenient in the construction of the generalized field ideal R(X) to let \(c=\frac{\partial u}{\partial j}\equiv d\).

Let \(N(d^{2})|_{z\in\Gamma}^{N(d^{2})}\) and \(M(d^{2})|_{z\in\Gamma}^{M(d^{2})}\).

Such a semi_ theorem turns out to be useful to identify the different regions of the \(N(M(d^{2}))\). Hence, we define the New Structure of R(X) to be a Speckle(NXr(d2)) with unit normal vector \(\begin{bmatrix}\alpha_{L}\\ \begin{matrix}\alpha_{R}\\ d\end{matrix}\end{bmatrix}\).

Consequently, the newborn space that is the zero extended Ideal of R(X) with \(\alpha_{L}=\alpha_{R}\) is equivalent to the AKMS state space with positive time \(t>0\), given by

\[ZX(d^{2})\in \operatorname{AKMS}^{+}(X):x(t)=g(X(t),t),\; t\geq 0,\]

where g(X(t),t) is generated by all dumbbell interactions. for each mound and peaks of the amplitudes.

In such a case, one shouldn't consider the kurtosis value of the distribution. One should introduce a potential K(x)(t), given by

\[K(x)(t)=5\left\|...

Fig. 1: Region of interest (\(\mathcal{D}_{z}\)) in the joint input-state space with a center point (\(\tilde{z}_{i}\)). The red circle defines the local region around the center point with some data points (blue dots) in it.

### Page 3

minimizes the variance of such a nonparametric estimate would encourage the data to be present close to every center point \(\tilde{z}_{i}\), resulting in a space-filling input design. Hence, one could aim to minimize the following cost:

\[C_\theta = \int_{\mathcal{D}_i} \frac{1}{d(z, \tilde{z})} d\tilde{z}.\]

Note that such a cost automatically promotes a space covering input as having extra data points in a given region of the considered domain has a rapidly diminishing return due to the inverse proportional relation (3). Hence, it is more valuable to drive the system to regions of the input-state space where no data points are available. However, this introduced heuristic cost also has multiple downsides:

- It requires the calculation of an integral over the considered domain \(\mathcal{D}_z\);
- Counting the number of points \(d(z, \tilde{z}_i)\) present in an interval around a center point \(\tilde{z}_i\) results in a nonsmooth cost;
- If no data points are present in the considered interval, an infinite cost value results due to a division by zero.

Each of these downsides is addressed in the following paragraphs.

**From integral to sum:** Instead of considering each point of the domain as a center point \(\tilde{z}_i\), a uniform grid of a total of \(n\) center points is selected, over which the average is taken. This reduces the integral to a finite sum:

\[C_\theta = \frac{1}{n} \sum_{i=1}^{n} \frac{1}{d(z, \tilde{z}_i)}.\]

Note that one could also consider a non-uniform grid distribution depending on the expected complexity of the state and output function over the considered domain (e.g. adding more grid points in regions of the domain where the functions rapidly change). However, such a non-uniform grid could emphasize regions of the considered domain more than other regions if this is not properly weighted in the cost function.

**From finite to infinite support membership functions:** As counting the number of points \(d_i\) present in an interval around a center point would result in a nonsmooth cost, we consider an exponential membership function instead:

\[d_i = d(z, \tilde{z}_i) = \sum_{k=1}^{N} e^{-\frac{1}{2}(\tilde{z}_i - z_k) \Sigma^{-1}(\tilde{z}_i - z_k) \top},\]

where \(\Sigma\) is a diagonal matrix:

\[\Sigma = \text{diag}(\sigma_1^2, \sigma_2^2, \dots, \sigma_{n_z}^2)\]

where \(n_z = n_x + n_u\) is the total size of the input-state space. Observe that if \(z_k = \tilde{z}_i\), then the exponential function \(e^{-\frac{1}{2}(\tilde{z}_i - z_k) \Sigma^{-1}(\tilde{z}_i - z_k) \top}\) equals 1 and that the contribution, or ‘added information’, of a sample point decreases exponentially when it is further away from the considered grid point. The variances \(\sigma_i^2\) govern how fast this decay takes place. Implicitly, such a membership function makes a smoothness assumption on the considered state and output function: i.e. it is assumed that points far away from the considered center point \(\tilde{z}_i\) still have some influence on the functions evaluated in this center point.

The values \(\sigma_i^2\) need to be set by the user. It is advised to choose it large enough such that the grid points have an overlapping region to ensure that data points significantly contribute to multiple grid points at a time as this improves the cost smoothness and optimization performance. The \(\sigma_i^2\) values should also not be chosen too large as that would result in a cost that is insensitive to changes in the input signal parameters.

**Adding an \(\epsilon\) contribution:** by adding a small, positive, contribution in the denominator of the cost function terms, the division by zero, or division by very small numbers, is avoided:

\[C_\theta = \frac{1}{n} \sum_{i=1}^{n} \frac{1}{\epsilon + d_i}.\]

This results in the following completed expression of the considered cost:

\[C_\theta = \frac{1}{n} \sum_{i=1}^{n} \frac{1}{\epsilon + \sum_{k=1}^{N} e^{-\frac{1}{2}(\tilde{z}_{i} - z_k) \Sigma^{-1}(\tilde{z}_{i} - z_k) \top}}.\]

### 3.3 Optimization

The proposed cost function (3.3) is generally nonlinear in the parameters due to a) the nonlinear dependence of the input signal on the parameters, b) the nonlinear dependence of the state on the (past) inputs, c) the nonlinear dependence of the cost on the joint input-state values. The resulting cost function is minimized using a gradient-based optimization approach. Hence, the obtained solutions only converge to the closest local optimum. The use of gradient-based optimizers requires the partial derivatives of the input w.r.t. the parameters, as well as the partial derivatives of the state and output function (1) w.r.t. the current state and input to exist, as was assumed in the earlier sections.

In practice, the cost function (3.3) is minimized using the Matlab function fmincon which also allows additional (parameter) constraints to be added during the optimization, e.g. to limit the maximum amplitude of a signal (when using a direct parametrization \(u_k = \theta_k\)), or to limit the energy at each frequency (when using a multisine parametrization).

Note that as the dimensionality of the region of interest increases, the computational efficiency decreases due to the rapidly growing number of grid points that is required to cover the considered space. In many practical cases, the nonlinearities are acting on certain states rather than on the full state-space, then the dimensionality of the region of interest for which we need space-covering input design can be reduced.

### 4. SIMULATION EXAMPLE

The effectiveness of the proposed approach is illustrated on a nonlinear 1 DOF system (Figure 2). The system consists of a horizontally moving mass fixed in a rail which is connected by a spring to the ceiling and is linearly damped. This spring connection results in a geometrical nonlinearity. The system is excited by an external force \(F\).

### Page 4

\[ \bar{x}_1 = x_2 \] \[ \ddot{x}_2 = \frac{1}{m} \left( F - sx_1 + \frac{slx_1}{\sqrt{x_1^2 + a^2}} - cx_2 \right), \quad (9) \]

where \( x \) denotes the horizontal position of the mass ( m=5.0 kg). The spring ( s=800.0 N/m ) has linear characteristics with stretched length a=0.25 m and tensionless length l=0.17 m. The damping in the system is given by a lineard amplifier (c=10.0 Ns/m). As we consider discrete-time systems in the proposed input design approach, we discretize the system using the forward Euler method:

\[ x_{k+1} = x_k + T_s \cdot f(x_k, u_k), \quad (10) \]

where the function \( f(x_k, u_k) \) is defined as:

\[ f(x_k, u_k) = \begin{bmatrix} \frac{1}{m} \left( F_k - sx_{k+1} + \frac{slx_{k+1}}{\sqrt{x_{k+1}^2 + a^2}} - cx_{k+1} \right) \end{bmatrix}_k. \quad \ldots \quad (11) \]

where \( x_k \) and \( u_k \) represent the state and input at time step \( k \) respectively and the sampling time \( T_s = \frac{1}{f_s} = 0.01 \) s.

\[ x_{k+1} = x_k + T_s \cdot f(x_k, u_k), \quad (10) \]

where the function \( f(x_k, u_k) \) is defined as:

\[ f(x_k, u_k) = \begin{bmatrix} \frac{1}{m} \left( F_k - sx_{k+1} + \frac{slx_{k+1}}{\sqrt{x_{k+1}^2 + a^2}} - cx_{k+1} \right) \end{bmatrix}_k. \quad \ldots \quad (11) \]

where \( x_k \) and \( u_k \) represent the state and input at time step \( k \) respectively and the sampling time \( T_s = \frac{1}{f_s} = 0.01 \) s.

### 4.2 Input parametrization

We considered a periodic multisine excitation with \( F = 184 \) excited frequencies between the range \( f_{\min} = 1 \) Hz and \( f_{\max} = 10 \) Hz to cover the resonance frequency of the system, a sample frequency \( f_s = 100 \) Hz, and \( N = 2048 \) points per period during this example:

\[ u_k = \sum_{l=l_{min}}^{l_{max}} A \cdot \sin(2\pi l \frac{f_0}{f_s} k + \varphi_l). \quad \ldots \quad (12) \]

where \( l_{\min} = 21 \) and \( l_{\max} = 204 \). The amplitude of the multisine is chosen such that it is zero-mean and has a standard deviation of 160 Newton, while we optimize the signal by changing the phases of each frequency component: \( \theta = [\varphi_1 \varphi_2 \ldots \varphi_F] \).

By fixing the amplitude of each frequency component in our signal we have essentially constrained the energy content that is present in our signal. Hence, we will optimize the phases of the multisine signal while keeping the amplitude spectrum of the signal constant. Note that this is a strong constraint, which would be problematic for many input design approaches, however, the results will show that such a constraint does not pose a problem to the proposed method. Of course, other input parametrizations, with different constraints, might result in a better space-covering experiment.

The steady-state response of the system to the periodic multisine excitation is considered during optimization. The phases are initialized randomly, and distributed uniformly over the interval [0, 2\( \pi \)].

### 4.3 Considered Domain and Cost Function

The domain of interest is selected as a rectangular box in the 3-dimensional input-state space. The dimension of axis \( u \) is between \([-400:400]\) and of \( x_2 \) is between \([-20:20] \) with a grid distance of 42.1053 and 2.1053. Our system varies less along axis \( x_1 \), therefore the domain is limited between \([-2;2]\) with a grid distance equal to 0.2105. To have sufficient overlap between the region of influence of neighboring grid points, the kernel width of the cost function in direction \( u \), \( x_1 \) and \( x_2 \) is \( \sigma^2 = [40^2, 0.2^2, 2^2] \).

### 4.4 Results

Figure 3 shows how the time-domain behavior of the signal changed by the optimization, while the amplitude spectrum remained fixed. The cost (8) of the optimized signal is 69 times lower than the cost of the initial random phase multisine (3.88 vs 287.15).

Figure 4 shows the side views of the considered 3-dimensional domain \( D_z \), a 3D depiction over the considered domain can be observed in Figure 5, and sliced cross-sections of the domain are shown in Figure 6. While the initial input is strongly clustered around the domain center, a clear space-filling effect can be observed after optimization, this is best visible in the \( x_1u \)-plane. Nevertheless, due to the nature of the nonlinear dynamical system and the selected input parametrization, most data points are still clustered close to the center. This is especially visible at the cross sections for the extreme input amplitudes: only few data points are scattered in these cross sections, though the space-filling optimized signal is visibly better spread.

### Page 5

Fig. 4. Side view of the space-filling effect of the input before (blue) and after (orange) optimization and space-filling effect of the Schroeder multisine (purple). The black contour lines outline the borders of the domain of interest.

### 4.5 Comparison to Schroeder multisine

Furthermore, we compared our optimal solutions to a Schroeder multisine. We constructed the new signal (Figure 7) from (12) with the same parameters introduced in Section 4.2 with the exception of the phases. Each phase was constructed according to Schroeder’s equation:

\[ \varphi_l = \frac{-l(l-1)\pi}{N_l}, \quad (13) \]

where \( N_l \) is the number of excited frequencies.

Fig. 5. Optimized points in the 3D space. The grid points are shown by the yellow dots. The optimized input-state samples are shown by the black crosses.

### 4.6 Monte-Carlo Simulation

In order to check how the quality varies with different random initializations, we conducted 20 Monte-Carlo simulations. In each simulation, we performed the optimization starting from a new random phase multisine while the amplitude constraint and optimization method remained unchanged. Figure 8 presents the outcome of the simulations, where the left figure (blue) represents the initial costs and the right figure represents the costs at the end of the 20 simulations. Even if the resulting signals are different, they are similar in terms of space-filling costs.

Fig. 6. Cross-section view of the space-filling input before (blue) and after (orange) optimization sliced along the \( u \) axis.

### Page 6