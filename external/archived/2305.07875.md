# 2305.07875

> OCR by deepseek-ai/DeepSeek-OCR | 8 pages

### Page 1

value), respectively.This research demonstrates the effectiveness of extendible control strategies in the context of uninterruptible supply chains. The paper introduces an extended real-time control system for controlling network flow rates and allocates buffers or empowers buffers depending on the channel instant during the flow rate setting process. The algorithm to create a buffer consists of receivers-standards and producers-warmstarters and are run by up to two separate nodes to give better efficiency to the swap resolution. This algorithm is as a generalization of the Extended Swapping Algorithm and has been shown to work accurately.

### Page 2

武伸取，如表，以能继eda改进的方法，实到春約日准。 Lost control

huanc-es to keep track of dropouts in the feedback loop. Leveraging the switched system performance results from [17] and prior work [18], we derive sufficient LMI conditions for \(\ell _{2}\)-performance analysis of WHRT control systems. This novel result can also be used for \(\ell _{2}\)-performance analysis of general graph-constrained switched systems. In contrast to previous works, this has not been done yet for an \(\ell _{2}\)-performance measure, but only for stability. Moreover, we utilize a classical system lifting method [19] to derive an LMI- based approach for synthesis of switched and non-switched state-feedback controllers with guaranteed \(\ell _{2}\)-performance, which additionally allows for a more efficient performance analysis. The lifting method is based on combining multiple past inputs and outputs and “discretizing” the system at time instants of successful control attempts, which directly enables controller synthesis.

  The work at hand is based on [17],[18] and motivated by networked and real-time control setups. It is also possible to view this from a graph-constrained switched systems perspective in a more general dissipativity-based framework. We present this approach in our work [20]. The results obtained in [20] can be used for WHRT control systems as well, arriving at conditions for robust quadratic performance which contain the results for nominal \(\ell _{2}\)-performance, presented in this paper, as special case.

  The remainder of the paper is organized as follows. In Section II, we define our setup and state preliminaries. We then present some first \(\ell _{2}\)-performance analysis results in Section III. Section IV defines the lifting method and gives an improved analysis results for the lifted system. In Section V, controller synthesis is presented and in Section VI we emphasize our findings by a numerical example. Section VII concludes the paper.

A. Notation

The set of real and natural numbers are denoted \(\mathbb {R}\) and \(\mathbb {N}\), respectively, and the notation \(A \succ 0\) means that \(A=A^{\top }\) is positive definite. Matrix blocks which can be inferred from symmetry are abbreviated with \(*.\) A (block) diagonal matrix with the blocks \(\lambda _{1},...,\lambda _{n}\) is denoted \(\mathrm {diag}(\lambda _{1},...,\lambda _{n})\).

II. SETUP

In this paper, we consider the linear discrete-time plant

\[x_{k+1}=Ax_{k}+Bu_{k}^{\mathrm {a}}+B^{w}w_{k}\]

\[z_{k}=Cx_{k}+Du_{k}^{\mathrm {a}}+D^{w}w_{k}\]

with initial state \(x_{0}\in \mathbb {R}^{n}\), actuator input \(u_{k}^{\mathrm {a}}\in \mathbb {R}^{m}\), performance input \(w_{k}\in \mathbb {R}^{q}\), performance output \(z_{k}\in \mathbb {R}^{p}\),

and real system matrices. At every time instant \(k\in \mathbb {N}_{0}:=\mathbb {N}\cup \{0\}\), the control input \(u_{k}^{c}\in \mathbb {R}^{m}\) is computed by means of a linear state-feedback controller as

\[u_{k}^{c}=Kx_{k},\]

which may be lost, e.g., due to transmission failure or a deadline miss. These losses are described by the binary loss sequence \(\mu :=(\mu _{k})_{k\in \mathbb {N}_{0}}\), where \(\mu _{k}=1\) indicates a successful control attempt and \(\mu _{k}=0\) a loss. Lost control inputsa are discarded and not sent or computed again. Without loss of generality, we assume for simplicity that the first control attempt is always successful, i.e., \(\mu _{0}=1\). The actuator input \(u_{k}^{\mathrm {a}}\) depends on the control input \(u_{k}^{c}\) and the loss sequence, but also on the chosen strategy in case of a loss. In literature, typically the zero strategy (input is set to zero if control attempt is not successful) and the hold strategy (last input is held until a new control input is received) are considered, and none of them is seen to be in general superior to the other [21]. In particular, the two strategies result in the actuator inputs

\[u_{k}^{\mathrm {a}}=\mu _{k}u_{k}^{c}\]

for zero strategy

\[u_{k}^{\mathrm {a}}=\mu _{k}u_{k}^{c}+(1-\mu _{k})u_{k-1}^{\mathrm {a}}\]

for hold strategy,

with some initial condition\(u_{-1}^{\mathrm {a}}\) for the hold strategy. An overview of the system setup is given in Fig. 1.

A. WHRT constraints

 We assume that the loss sequence \(\mu\) satisfies a WHRT constraint \(\lambda\). These type of constraints can be interpreted as a window specification: Within a moving time window, a minimum number of control attempts has to be successful. More precisely, the standard type of WHRT constraints is defined as follows.

\textit{Definition 1 ([\textit{5}]): a loss sequence \(\mu\) “meets any \(r\) in \(s\) control attempts” \((r\), \(s\in \mathbb {N},r\leq s)\), denoted \(\mu \vdash \binom {r}{s}=\lambda\), if in any window of \(s\) consecutive control attempts there are at least \(r\) of them successful (in any order). An example is provided in Figure 2. Other types of WHRT constraints focus on the number of successful consecutive control attempts, or on the number of unsuccessful control attempts.}

\textit{Definition 2 ([\textit{5}]): a loss sequence \(\mu\) “meets row \(r\) in \(s\) control attempts” \((r,\)\(s\in \mathbb {N},r\leq s)\), denoted \(\mu\vdash \binom {r}{s}=\lambda\), if in any window of \(s\) consecutive control attempts, at least \(r\) consecutive of them are successful.}

\textit{Definition 3 ([\textit{5}]): a loss sequence \(\mu\) “misses any \(r\) in \(s\) control attempts” \((r,\)\(s\in \mathbb {N},r\leq s)\), denoted \(\mu\vdash \binom {r}{s}=\lambda\), if in any window of \(s\) consecutive control attempts there are not more than\(r\) of them unsuccessful (in any order).}

\textit{Definition 4 ([\textit{5}]): a loss sequence \(\mu\) “misses row \(r\) in \(s\) control attempts” \((r,\)\(s\in \mathbb {N},r\leq s)\), denoted \(\mu\vdash \binom {r}{s}=\lambda\), if in any window of \(s\) consecutive control attempts, there are not more than \(r\) consecutive of them unsuccessful. Moreover, some WHRT constraints can be related to each other as weaker or harder [5], [22].}

### Page 3

groups.The main components of the WHT control system are the distortion (or channel) as a function of the input, represented by two scalars \( z_k \) and \( z_k^r \). The channel can either be taken directly from the objective function, represented by the objective function’s sum and target values, or alternatively, it can be compressed into a global slice and then sent from the WHT convolution block to the objective function’s input dimension.

The proposed control system divides each block \( \delta = \{C_i \} \) into three components: the target \( \gamma_i = \{g_i,t_i\} \), the local slice \( \gamma_i^{l} = \{c_i\} \), and the output slice \( \gamma_i^{o} = \{c_i^o\} \). The reconstructed slice algorithm uses two parameters: the ratio of the single valued components of the local slice \( r = \{c_i^r\} \) and the reconstruction threshold \( \tau= \{c_i^\tau\} \).

For the reconstruction of the scalar slices \( g_i, t_i \), the sign and INAPI \((i) - A)</i> − 1 <\infty ” <|displaystyle|)\<i>. >/], a truncated version of the absolute value function was utilized. This truncated version of:

\[ g_i \)= \large{−1, if n < 

choosing −r 

(

mathfrak>|displaystyle|) + τ/2) , \qquad 0\textless<|displaystyle|) < r < 
operator name>, i = 1,...,n.

For the reconstruction of the Global Information Layer (GRO) slice \( c_i^o \), we identified the existence of a nonsingleton intersection of the upper and lower similarity curves, |")

### Page 4

17 (cs, cmtot), metric (trans). Note: A 2-way table breakdown.泳池搭载显示轨道控制器硬件设计图\r';
\documentclass{article}
\usepackage{graphicx}
\begin{document}
Furthermore, we do not directly consider the full spectrum of nonlinear surface-electrode systems to be able to operate with a poorly controllable bandwidth. Therefore it should be noted that the overall system can operate with a bandwidth that is smaller than $1 \text{ GHz}$.\r'");
}
We have analyzed three types of monitoring devices (CSPWAIRQUEE-level, CSPWAIRQUEE-level, and too distant detectors) for monitoring activity of volunteers in the field. They had varying latency (which does not necessarily imply their precision).\r');
We analyzed and characterized the frequency channels and the bandwidths delineated by the filter, assuming its perfect roll-off in both directions to be the $56 \text{ kHz}$.\r';  
Now, considering the hardware requirements, including both the processing and board requirements, they had considerable complexity.\r';}\r';    
The board was developed by Analog Devices’ Flight Electronics Division and was designed and reconstructed in a similar manner to the one presented earlier. Its general architecture is shown in Figure $6 - 1$. A simple CPLD chip (SAECOL6) was used as a central unit. The digital system provides two fast analog channels, whereas one analog channel做了 another analog channel was used for tracking and expediting control operations, such as the phase and gain compensation in order to maintain the stability of the carrier signal. This digital signal processing chain also included one barrel filter and a dedicated algorithm to track the frequency of the carrier signal. $\circ \text{ This means that we need all grounds available for the channel}\text{ at the beginning of the Gaussian process}$. The main advantage of an analog filter in case of a light environment is that it can always track the frequency accurately. Other advantages of the saturation channel are that it has one grounding point, whereas a voltage must be applied to a modern digital filter to find its one ground.\r';    
Using the chosen Kalman filter in particular, we ignore the additional noise and, as a last resort, use a minimum bandwidth consisting of \text{two bands: a frequency band $\Omega_{\text{OS}}$} and a short-reach band $\Omega_{\text{LS}}$}\r';<br/>  
$\bar{G}_{\text{}1} \text{ employs a] in all channels of the denominator of (6).}$ $\Omega_{OS}=\Omega_{\text{OS}}+\Omega_{\text{LS}}/2$\r'; $\alpha_{\text{ROW}}$, $\bar{G}_{\text{ROW}}$ dependence on $\Omega_{WS}$)<br/>---- - 3 part differential H-WDR process -
\r':  
} $\Omega$= $\text{const}$.$f_{2}$ values of $f_1$, $f_{2}={w_{\text{stage + 2.2}}}/4$ where ${\Omega}$= $\text{const.}$)}
\begin{itemize}\end{itemize}
\r:= {r, $r$ edge, $.5$, ${\bar{r}_1, \bar{r}_2 = w_f}}$}
\r:= {$\lambda_1$, $\alpha_( {\bar{\Omega}_1}, {\tau_P,{\bar{r}} dr}}}$ (SDPA}\\  
(7)$\bar{\Omega}= \text{const}}$
\\
CBC low pass filter)
\r:  
} ${c=\text{Med(1,} \text{}]$}
\ $\text$(331, {1}, {\Rightarrow})$}
\ \begin{itemize}\end{itemize}\r' $c=w_f$ (SDPA)\\ 
{\alpha[]{5}}.{11}}{\alpha_ } \\  
$\vspace{0.6cm}$
\ $\lambda= 1.5$ (\text{}){1$\}^{1}$)}
\tendin\ $c=$ $\alpha$\epsilon_ }{\Omega \$\}\lw}\\ 
\r:}
%This is the hypothetical, increased rate of output of the system. Berenberg has found few studies that confirm/disprove the 
For used in the digital domain for convenient visualization. For exploration, I have built a project model for the model. I intend to consider transpose operations as data_}

RJK: 
%\textit{\textit../c al}\text{f section. Some of the expected models have limited functionality.

\section{ The Video}

{\vspace{99}\}**
\c{$0{1.3}} \c{\Omega} with the others.\r' $C2$ $\qdot$ $\omega_1=$
\vspace{4mm} $c1, 3, c0, \text{ times 0.2 cm}$ 
\c{
 $ 100 o0$}
\c{$(C_{12}, Ocre)2, Q1}) * K, {0, [2 0 1]$}
\c {$\Omega}$./ \qdot{$\alpha$}  \c{0}
\c{\Omega 0.404}{0}\_{}}\)
\c
$4 h_{x}$ with.  \r' $Jo$ $\epsilon = (``\_{
Max.（）

\rcalls{\vec{x}} C, {.5$
$\c N($\mathrm{2sub} -\:J2
\c {
$ \\

\(\text{sequence}\$
\vspace{10mm}
{({\O}.42+$c.o/2}$\c (0${$ \n= \{ \pm$)}
$\cflow$)}+ \ $\text{\_{0}9}$ where
$

\therefore.
. {\$
\yyyt\dotly\gradronehelless25_{\delta}
\eo}{\$ONONONONONON$~{} /
C_3
\c{\}
} \\ &&\c 
2\cdot.
\overline{$$\langle2/$\n$ \\
\c{\\2$} 
\qdot\[\begin{array{
r: 
}\c
\c  .:\
.{ EuD_
\c\RevN5x
\Environment{5$;
min.{}
}

\\dinstil

\c{ \sqrtz~& 

\{$\\
\c.
\c {$\$ofvetree

\c{¥4% }
\text{**
, 
all)

d}\}\.
@@c_

}

    //\dotop


From programmatically given data

Author: Erdogan Ikbal


\begin{agegroup*}
\c{$\boxed{\${\geq \text{min(10,11,12)}}}$}
\c $\text{(\cc{11,12}$}
\c $\text{W12) common$}
\cc{($ 01.3,$\{\\
\{$w1$\}\}

\((F3acc\(\text{2/)$ 
$\]


For many more track	
\dot{\}
\namespace
\Nhib=








19

\begin{table}[]
\centering
\begin{tabular}{|c|}
\hline
\(\left\{ \begin{array}{cc} \\ {S_j^{-1}} & {0} \\ {0} & {\gamma I} \end{array} \right\} \quad\left\{ \begin{array}{c} {A_j^{c1}} \\ {\gamma^{-1} C_j^{c1}} \end{array}\right. \\ 
{\gamma I^{1}} \\ {0} & \gamma I}\end{array}\right.\)\)} \\ 
\hline
\end{tabular}
\end{table}

### Page 5

trangue salam 13

raph size, the computational complexity of our approach can be decreased, leading to a more efficient analysis.

A. Main idea and lifting
The fundamental concept is to “discretize” the system at time instants of successful control attempts, similarly as in [9]. This approach was called alternative discretization and constitutes a special case of the lifting presented in this section, which is adapted from [19]. To formalize, define
\[
\tau := (\tau_k)_{k \in \mathbb{N}_0} \text{ as in [9] as the sequence of time indices of successful control attempt instants } k, \text{ and } \alpha := (\alpha_k)_{k \in \mathbb{N}_0}
\]
as the sequence counting the number of losses between two successful control attempts. The sequence \( \alpha \) plays an important role in the lifting and its elements take values in the finite set {0, ..., s-1}, since we assumed \( s \) to be bounded and hence there can only be a finite amount of consecutive losses. As an example, for \( \mu = (1 \, 0 \, 1 \, 1 \, 0 \, 0 \, 1 \ldots) \) the corresponding sequences for the lifting are \( \tau = (0 \, 2 \, 3 \, 6 \ldots) \) and \( \alpha = (1 \, 0 \, 2 \ldots) \).

The goal is to find an alternative representation of (4), but based on the successful control attempt time instants. It is therefore defined at the time instants \( \tau_k \) only. All variables which refer to the lifted system are marked with \( (\cdot), \text{ e.g., } \tilde{x} \) represents the lifted state of \( x \). In [9], this alternative representation is relatively straightforward, because autonomous systems with no performance in- and output are considered therein. In contrast, our setup features such an in- and output, which cannot be discarded at time instants between successful control attempts without changing the \( \ell_2 \)-performance of the system. For this reason, we lift the input and output as follows
\[
\tilde{w}_k = \begin{bmatrix} w^\top_{\tau_{k-1}} & \ldots & w^\top_{\tau_k-1} \end{bmatrix}^\top \\
\hat{z}_k = \begin{bmatrix} z^\top_{\tau_{k-1}} & \ldots & z^\top_{\tau_k-1} \end{bmatrix}^\top.
\tag{12}
\]
Note that at each time instant \( \tau_k \), the lifted in- and output \( \tilde{w}_k \) and \( \hat{z}_k \) consist of past in- and outputs. Since we are interested in an equivalent formulation of (4), the lifted and the non-lifted system states are required to coincide at these time instants, i.e., \( x_k = \tilde{x}_{\tau_k} \). The lifted CSS is then defined as
\[
\begin{aligned}
\tilde{x}_{k+1} &= \tilde{A}_{\alpha_k \tau_k}^{\text{cl}} \tilde{x}_k + \tilde{B}_{\alpha_k}^{\text{w}} \tilde{w}_k \\
\hat{z}_k &= \tilde{C}_{\alpha_k \tau_k}^{\text{cl}} \hat{x}_k + \hat{D}_{\alpha_k}^{\text{w}} \hat{w}_k
\end{aligned}
\tag{13}
\]
with the closed-loop matrices
\[
\begin{aligned}
\tilde{A}_{\alpha_k \tau_k}^{\text{cl}} &= \tilde{A}_{\alpha_k \tau_k} + \tilde{B}_{\alpha_k}^K \\
\hat{C}_{\alpha_k \tau_k}^{\text{cl}} &= \hat{C}_{\alpha_k \tau_k} + \hat{D}_{\alpha_k}^K.
\end{aligned}
\tag{14}
\]
Since the number of time instants between two successful control attempts is not constant, the lifted CSS has varying in- and output dimensions, depending on the current mode \( \alpha_k \). For WHRT control systems, we state the lifted system matrices explicitly in the next subsection.

B. Application to WHRT control systems
The modes of the lifted system (13) are not dependent on the loss sequence \( \mu \) anymore, because it is defined at time instants of successful control attempts only. Instead, the switching is now based on the sequence \( \alpha \), whose elements take values in {0, ..., s-1}. Therefore, we need to redefine the WHRT graph, similar as in [10].

Definition 9 (Lifted WHRT graph): For a WHRT constraint \( \lambda \), the lifted WHRT graph \( \tilde{G} \) is defined as in Definition 8, but redefining
(b) The labels \( l_p \) take values in the set {0, ..., s-1}, representing the number of losses between successful control attempts.

The lifted WHRT graph has \( \tilde{n}_v \) nodes and \( \tilde{n}_e \) edges. Further, the indicator function (7) is redefined to \( \tilde{n}_l(k) \) by replacing \( \mu_k \) by \( \alpha_k \) and \( n_v \) by \( \tilde{n}_v \). Similar to the non-lifted graph, the \( \alpha \)-sequence can be generated by moving along the edges of \( \tilde{G} \). It is always possible to generate a lifted WHRT graph for any given WHRT constraint. An algorithm for its automatic generation has been presented in [10] and an implementation is referenced therein, while an algorithm for generating a non-lifted WHRT graph is given in [22]. Therein, the authors furthermore present a method for combining different WHRT constraints into a joint graph. Further, a (non-lifted) WHRT graph can be generated from the lifted version as follows. Any edge with \( l_p \ge 1 \) represents \( l_p \) consecutive losses followed by one successful control attempt. Replacing these edges by a series of \( l_p \) edges with label 0 followed by one with label 1, including the necessary nodes in-between, leads to a (non-lifted) WHRT graph for a given WHRT constraint. An example WHRT graph and its corresponding lifted version can be found in Fig. 3, where the relation between both graphs can be observed.

We are now able to state the WHRT control system as a lifted CSS of the form (13) with (14). Performing the calculations for the WHRT control system (equations (1) - (3)), “discretized” at time instants of successful control attempts, one obtains using the sequence \( \alpha \)
\[
\begin{aligned}
& \tilde{B}_{\alpha_k} = A^{\alpha_k} B \\
& \tilde{D}_{\alpha_k} = \begin{cases}
\text{for } \alpha_k = 0: D \\
\text{for } \alpha_k \ge 1: \\
\left[ D^T (CA^0 B)^\top \cdots (CA^{\alpha_k-1} B)^\top \right]
\end{cases}
\end{aligned}
\tag{15}
\]

### Page 6

showing to a graph \(g_{2}\) based on the time frequency resource availability provided byModerate Intermediate Transmission Rate (IMT_R) to both network and user. Also, the improved Multi-User version is given by (II E).The updated Cognitive Radio (CR) score is updated at the beginning of the system and modified as shown in Section III-B.Ancient CR Score: Then, the old source score is used as the new score and is updated according to the following formula:\(\Delta C_{R}^{\alpha +1} = (1 - \frac{1}{n}) \Delta C_{R}^{\alpha} + \frac{1}{n}\Delta C_{R}^{\alpha}\) for \(n \geq 1\), where 0 < \(\alpha\) < 1. For \(\alpha\) = 0, the score reduces to the historic architecture score. Since the historical score is periodic for each chunk imposed on this system, it is possible to set the period to change, or set a value beyond that period to an idling state that is monitored. In the first part of the simulation, we show that the theoretical period is set to be 1 \(K\). In the second part, we show that the scheduled sliding window of 1 \(K\) and a newly introduced sliding window with rolling windows of 3 \(K\) improve the CR performance. In the third part, we show that the CR performance improves as \(C_{W}\) increases.This provides a global view that the MUD performance is not as bad as claims made previously. Indeed, only relying on basic CRsubscriptsuperscript, which is similar to the ex6 data, we can conclude that improving CR scores is one strategy for MUD.A primary challenge in MUD is the decoder performance, which is estimated with the maximum likelihood theory when the channel response is known a priori. This implicit condition implies that the decoder cannot optimize the transmit parameters \(\mathbf{B}^{\text{t}}\) so that the specularity index can be evaluated more accurately. What more, the written form of the following may be very complicated for general cases but can be observed with numerical examples in results presented in the paper.Thus, our work focuses on exploiting the新增 index \(\alpha\) by adding a delay term to the time cost of each node's channel response. This makes particular trade-offs between timing and resource allocation and results in excellent performance improvement.  

or the zero strategy and  

\[B_{\alpha_{\tilde{k}}}^{\tilde{\alpha}} = \sum_{i = 0}^{\alpha_{\tilde{k}}} - \sum_{C\Sigma_{0}}^{A^{i}B}^{A^{i}B} = \frac{1}{\sum_{i = 0}^{\alpha_{\tilde{k}}} - \frac{1}{\sum_{i = 0}^{2 - 1} - \frac{1}{2}}\frac{\tilde{\alpha_{\tilde{k}}}^{-1}}{\sum_{i = 0}^{2 - 1 i^{- i}} - \frac{1}{2}\frac{\tilde{\alpha_{\tilde{k}}}^{-1}}{\sum_{i = 0}^{2 - 1}\tilde{\psi}})}},\]  

<|ref|>text<|/ref|><|det|>[[82, 214, 273, 230]]<|/det|>
for the slot strategy, while  

\[\tilde{A}_{\alpha_{\tilde{k}}} = Av_{\tilde{k}}^{2}\]  

<|ref|>equation<|/ref|><|det|>[[88, 251, 918, 408]]<|/det|>
\[\begin{array}{r l}&{\tilde{c}_{\alpha_{\tilde{k}}}=[D^{\top}(C_{\alpha_{\tilde{k}}}B^{w}\cdots A^{B^{w}}A^{0}B^{w})]}\\ &{\errors(\tilde{\alpha_{\tilde{k}}})=[\frac{B}{\alpha_{\tilde{k}}}]\in{-1,1} \quad\text{Kin terms of every fixing)}--\mathrm{saoetconnectingto}\{\mathcal{R}^{+}(2\mathrm{in}]-\}d\Big,\]  

<|ref|>text<|/ref|><|det|>[[86, 416, 490, 494]]<|/det|>
are identical for both strategies. These matrices also appear for lifting general (non- switched) discrete- time systems, cf. [19, Chapter 8.2]. Note that compared to [10] and previous works, due to our lifting no auxiliary state \(u_{k - 1}^{\alpha}\) is required for the hold strategy, thus reducing the state- space dimension.  

<|ref|>text<|/ref|><|det|>[[86, 495, 490, 615]]<|/det|>
Note that we can handle one- or multi- step delays in the feedback channel [2] with our framework. For that, augment the system state \(x_{k}\) with the delayed inputs \(\left[u_{k - 1}^{\alpha},u_{k - 2}^{\alpha},\dots\right]\) and adapt the system matrices in (1) accordingly. The later synthesized controller will additionally to \(x_{k}\) depend on the delayed inputs, which have to be known to the controller, e.g., by acknowledgment mechanisms of the communication channel.  

<|ref|>sub_title<|/ref|><|det|>[[86, 629, 352, 646]]<|/det|>
### C. \(\ell_2\) - performance for the lifted system  

<|ref|>text<|/ref|><|det|>[[101, 652, 354, 668]]<|/det|>
Observe that by definition of \(\tilde{w}\) and \(\tilde{z}\)  

<|ref|>equation<|/ref|><|det|>[[98, 675, 470, 718]]<|/det|>
\[\sum_{k = 0}^{\infty}z_{k}^{\top}z_{k} = \sum_{\tilde{k} = 0}^{\infty}z_{\tilde{k}}^{\top}\tilde{z}_{\tilde{k}}\quad \mathrm{and}\quad \sum_{k = 0}^{\infty}w_{k}^{\top}w_{k} = \sum_{\tilde{k} = 0}^{\infty}\tilde{w}_{\tilde{k}}^{\top}\tilde{w}_{\tilde{k}}.\]  

Therefore, lifting the system loses no input/output information, the \(\ell_2\) gan stays the same, also because asymptotic stability of the lifted system (13) carries over to the non- lifted one (4) [9]. Further, \(\ell_2\) performance guarantees given for (13) carry over to (4). The following Lemma states the equivalence.  

<|ref|>text<|/ref|><|det|>[[85, 818, 489, 848]]<|/det|>
Lemma 1: The CSS (4) and its lifted counterpart (13) have the same \(\ell_2\) performance gain \(\gamma\)  

<|ref|>text<|/ref|><|det|>[[85, 849, 488, 879]]<|/det|>
We can now state a similar \(\ell_2\) performance analysis result for the lifted system (13) as for the non- lifted system (4).  

<|ref|>text<|/ref|><|det|>[[84, 880, 488, 925]]<|/det|>
Theorem 2 (Lifted system \(\ell_2\) - performance analysis): The lifted CSS (13), whose switching is captured by a graph \(\tilde{\mathcal{G}}\) according to Definition 9, has \(\ell_2\) performance with gain \(\gamma\)  

<|ref|>text<|/ref|><|det|>[[507, 65, 912, 97]]<|/det|>
if there exist symmetric matrices \(S_{i}\in \mathbb{R}^{n\times n}\) and matrices \(G_{i}\in \mathbb{R}^{n\times n}\) \(i = 1,\dots,\tilde{n}_{\nu}\) such that  

<|ref|>equation<|/ref|><|det|>[[531, 100, 910, 168]]<|/det|>
\[\left[ \begin{array}{cccc}G_{i} + G_{i}^{\top} & -S_{i} & * & *\\ 0 & \gamma I & * & *\\ \tilde{A}_{i}^{1}G_{i} & \tilde{B}_{i}^{w} & S_{j} & *\\ \tilde{C}_{i}^{1}G_{i} & \tilde{D}_{i}^{w} & 0 & \gamma I \end{array} \right]\succcurlyeq 0\quad \forall (i,j,l)\in \mathcal{E}. \quad (18)\]  

<|ref|>text<|/ref|><|det|>[[508, 173, 793, 189]]<|/det|>
A Lyapunov function is then given by  

<|ref|>equation<|/ref|><|det|>[[586, 192, 909, 233]]<|/det|>
\[V(\tilde{x}_{\tilde{k}}) = \tilde{x}_{k}^{\top}\left(\sum_{\tilde{i} = 1}^{\tilde{n}_{\nu}}\tilde{\eta}_{i}(\tilde{k})S_{i}^{-1}\right)\tilde{x}_{\tilde{k}}. \quad (19)\]  

<|ref|>text<|/ref|><|det|>[[507, 232, 911, 277]]<|/det|>
Proof: The proof follows the same steps as in Theorem 1. The dimensions of \(B_{w}^{w}\) , \(C_{w}\) and \(D_{w}^{w}\) in the proof of Theorem 1 have to be adapted accordingly. \(\blacksquare\)  

<|ref|>text<|/ref|><|det|>[[508, 277, 911, 308]]<|/det|>
Corollary 1: Under the assumptions of Theorem 2, the WHRT control system has \(\ell_2\) performance with gain \(\gamma\)  

<|ref|>text<|/ref|><|det|>[[507, 308, 911, 368]]<|/det|>
Proof: From Theorem 2 and Lemma 1 we obtain that (13) and (4) have \(\ell_2\) - performance with gain \(\gamma\) . The fact that the WHRT control system can be represented by (13) completes the proof. \(\blacksquare\)  

<|ref|>text<|/ref|><|det|>[[508, 368, 912, 396]]<|/det|>
Due to the lifting, the system matrices \(\tilde{B}_{w}^{w}\) , \(\tilde{C}_{w}^{1}\) , and \(\tilde{D}_{w}^{w}\) vary in dimension depending on the current mode \(\alpha_{\tilde{k}}\) .  

<|ref|>text<|/ref|><|det|>[[507, 397, 912, 536]]<|/det|>
Therefore, the size of the LMI (18) increases, but the number of decision variables does not, because the dimension of \(\tilde{A}_{\alpha_{\tilde{k}}}^{1}\) is not changing. The state dimensions of \(\tilde{x}\) and \(\overline{x}\) are equal as well. As mentioned in [9], the lifted graph is typically much smaller, cf. Fig. 3. Hence, the overall computational demand for the analysis of the lifted CSS with Theorem 2 is typically less than for the non- lifted CSS with Theorem 1, although the dimension of the LMIs increase. A comparison by means of an example is given in Section VI.  

<|ref|>sub_title<|/ref|><|det|>[[588, 543, 828, 558]]<|/det|>
## V. CONTROLLER SYNTHESIS  

<|ref|>text<|/ref|><|det|>[[507, 564, 911, 760]]<|/det|>
Based on the lifted css, we are able to state sufficient LMI conditions which allow to synthesize a state- feedback controller such that the WHRT control system is not only asymptotically stable [9], but additionally has \(\ell_2\) - performance with gain \(\gamma\) . The reason is that in (14) the controller enters linearly, because at each time instant \(\tilde{k}\) the control attempt is successful and thus \(K\) influences the dynamics at these time instants only. For all inter- attempt time instants the controller does not appear, because the control signal is lost. Hence, the controller can be isolated in the LMI (18), which enables controller synthesis. The following result aims at synthesizing a non- switched state- feedback controller.  

<|ref|>text<|/ref|><|det|>[[507, 761, 911, 852]]<|/det|>
Theorem 3 \((\ell_2\) - performance controller synthesis): There exists a state- feedback controller such that the lifted CSS (13), whose switching is captured by a graph \(\tilde{g}\) according to Definition 9, has \(\ell_2\) performance with gain \(\gamma\) if there exist symmetric matrices \(S_{i}\in \mathbb{R}^{n\times n}\) \(i=1,\dots,\hat{n}_{v}\) and matrices \(G\in \mathbb{R}^{n\times n}\) \(R\in \mathbb{R}^{m\times n}\) such that  

<|ref|>equation<|/ref|><|det|>[[518, 856, 911, 922]]<|/det|>
\[\left[ \begin{array}{cccc}G + G^{\top} - S_{i} & * & * & *\\ 0 & \gamma I & * & *\\ \tilde{A}_{i}G + \tilde{B}_{i}R & \tilde{B}_{i}^{w} & S_{j} & *\\ \tilde{C}_{i}G + \tilde{D}_{i}R & \tilde{D}_{i}^{w} & 0 & I \end{array} \right]\succcurlyeq 0 \quad \forall (i,j,l)\in \mathcal{E}. \quad (20)\]

### Page 7

}}}{{1.53 \text{ pc}}}, \end{align*} \}\begin{equation}nb\_links\_t\] \[drop\_t\] & $sour\_t$, $dest\_t$ \\ $r\_t$ & $lb\_t$, $ub\_t$, $sec\_t$ \\ $a\_t$ & $a\_t+1$, $a\_t+2$, $a\_t+3$, $nb\_links\_t$ \\\end{equation}$..] * Each simulation setup is run with different initial cell sizes... The individual...The initial conditions for the simulations are introduced. Next, the initial and final exogenous changes...We then indicate that these changes are indeed found in the...The presented methods are applied under a broad range of conditions, given...Finally, the results provide evidence that the...This can be used to study the cell growth, e.g., by...In addition, the methods also...$$ /* Parameters (parameterized by figure), grid, boundary,...$nominal\_cell$*/ */ /const variables */ $basic\_ess \$ m净利润,广告\$ r **_inst $m\$ str$$ Ssol I2; */ *** These define the structure and the temporal/environmental parameterization of...$$ // Zielmarkt_G EL $$*/ ** The Temporal _and_Environmental ... The simulations...The software allows for a variety of cell based...的花朵" is presented...The approach not only provides...Computing reduced...In absence of an automatic...Various estimation methods are applied...Finally, the simulations... 

 coffчение с поскармсrosse rt и ёстойззян гцпд efficiency to reduced...

The corresponding controller is then given by $K = R G^{-1}$ and a Lyapunov function is (19).

* **Proof:** Along the lines of [17]: $G$ is invertible by standard arguments and insert $R = K G$ in (20) and together with (14) the conditions of Theorem 2 are obtained.

Theorem 3 can be used in combination with Corollary 1 to synthesize a state-feedback controller for the WHRT control system with guaranteed $\ell_2$-performance, while in previous works it was only possible to synthesize a stabilizing controller [9].

As already annotated, Theorem 3 synthesizes a non-switched controller, which is independent of the past loss sequence. This induces some conservatism, but is a crucial property for NCS in which the network features no acknowledgement mechanism. A switched controller requires the knowledge of the past packet losses, which is challenging to achieve without the presence of acknowledgments. However, our results can directly be extended to switched controllers to be applied to networks that feature such a mechanism by revising the proof by substituting $G$ with $G_i$ and $R$ with $R_i$ in (20) with the controller $K_i = R_i G_i^{-1}$. Switched controllers can improve the feasibility of the control design. In this case, the control input (2) and the controller in (14) have to be adapted accordingly to account for the switching. The resulting controller is dependent on the starting node of the corresponding edge in the lifted WHRT graph, i.e., implicitly depends on the past loss sequence and may switch whenever a control attempt is successful. In [10], a possibility for designing switched controller policies in the absence of acknowledgment mechanisms is presented, with the tradeoff of requiring computational capabilities at the actuator and having to compute and send larger control input packets. This technique can be applied here similarly.

Note finally that if a certain performance gain $\gamma$ is guaranteed for a specific $\lambda$, it is also guaranteed for all WHRT control systems with a harder WHRT constraint.

## VI NUMERICAL EXAMPLE

We consider the numerical example from [8], [9], but extended by an performance in- and output, namely

\begin{equation}
x_{k+1} = \begin{bmatrix}
0 & 1 \\
1 & 1
\end{bmatrix} x_k + \begin{bmatrix}
1 \\
1
\end{bmatrix} u_k^a + \begin{bmatrix}
1 \\
1
\end{bmatrix} w_k
\end{equation} (21)

\[
\begin{align*} z_k &= \begin{bmatrix}
1 & 1
\end{bmatrix} x_k + u_k^a + w_k. \end{align*}
\]

The setup is as described in Section II, i.e., the control input losses are described by a WHRT constraint and we use the state-feedback controller (2). We use YALMIP (Version R20210331) [27] and MOSEK [28] to minimize $\gamma$ with the respective LMIs as constraints. Note that the obtained $\gamma$ is in general only an upper bound on the $\ell_2$-gain of the WHRT control system, since the presented conditions are only sufficient. In [8], the controller $K = \begin{bmatrix} -0.35 & -0.85 \end{bmatrix}$ for the WHRT constraint $\lambda = \begin{pmatrix}2 \\ 3 \end{pmatrix}$ and the zero strategy was proposed. For this controller, Theorems 1 and 2 yield $\gamma = 3.52$. Simulating this system with $w_k = 1$ for $k \ge T$ and $w_k = 0$ otherwise with sufficiently large, finite $T$ and the worst-case loss sequence $\mu = \begin{pmatrix} 1 & 0 & 1 & 1 & ..., \end{pmatrix}$, the simulated $\gamma$ obtained by computing (6) is $\gamma_{\text{sim}} = 3.38$. Note that it is in general difficult to find the worst-case input $w$ and loss sequence $\mu$ that lead to the highest $\gamma$, i.e., the $\ell_2$-gain. By synthesizing a non-switched controller with Theorem 3 we obtain the controller $K = \begin{bmatrix} -0.61 & -1.00 \end{bmatrix}$, which results in $\gamma = 2.505$. The respective simulated gain is $\gamma_{\text{sim}} = 2.10$. Therefore, we are able to significantly improve the $\ell_2$-performance of (21). If we synthesize a switched controller, the $\ell_2$-performance can be improved even a bit further to $\gamma = 2.488$. Note that the improvement of a switched controller over a non-switched one strongly depends on the chosen system and the WHRT constraint.

To compare the computational complexity, we consider the WHRT constraint $\begin{pmatrix} 4 \\ 10 \end{pmatrix}$. The corresponding non-lifted WHRT graph has 462 edges and 336 nodes and its lifted counterpart has 210 edges and 84 nodes. This leads to 462 LMIs with 2352 decision variables, or 210 LMIs with 588 decision variables. This significant reduction can also be seen in the computation time. For above example, the analysis with Theorem 1 took around $1.1$ s to compute on a standard computer, while for the lifted system it finished after roughly $0.3$ s. Thus, our numerical findings confirm the computational advantages when considering the lifted system instead of the non-lifted one.

## VII CONCLUSIONS

In this paper, we considered the $\ell_2$-performance of linear plants with unreliable feedback, i.e., the control signal can be lost, which is modeled by a WHRT constraint. We presented sufficient LMI conditions under which such systems have $\ell_2$-performance with a certain gain $\gamma$. Moreover, we proposed an approach for the synthesis of state-feedback controller that ensures that the WHRT control system has $\ell_2$-performance with gain $\gamma$. This was made possible by lifting the system, which in addition typically reduces the computational complexity of the respective LMI conditions for the $\ell_2$-performance analysis. The resulting controller can be a non-switched, loss sequence independent one for NCS without acknowledgment mechanisms, or designed as a switched controller, that switches depending on the past loss sequence. As a side result, we also obtained an $\ell_2$-performance analysis result for general switched systems with constrained switching that can be captured by a graph.

The presented results can be generalized to a dissipativity framework, allowing for robust quadratic performance of CSS and WHRT control systems, see [20].

To make the results more applicable to NCS and real-time control systems, a potential future line of research is the extension to nonlinear plants. Another interesting open problem is to consider output feedback controllers. Further research can also be conducted the direction of how an actuator with computational capabilities can improve the control performance.

## References

[1] J. P. Hespanha, P. Naghshtabrizi, and Y. Xu, "A survey of recent results in networked control systems," _Proc. of the IEEE_, vol. 95, no. 1, pp. 138-162, 2007.

### Page 8

}}}{={{}}}{}}}~{}]}\,{\}mathline}}}~{}]}\,{{}}}}}}}%~{}}-

}}( \errm{i}’’\errm{ M_{ acrelea}}{\elevenm{d}\cross}{ thatAn}(\le {\I}}}{we\e cub(E E{}\td ( \, E\M)e\ul Allevate)(no})thenAmbul ( \thro{G}_E\errm{ptarshimell}{\e \een{Ac}revea}c(}E{}tt{YSet\prime seChsueaSShcClli\,Arnschit, ite\rc \keu{et\emand-pack Haover:())*\ensely let we\e EB{}ofenCondist$ {ueletS蛋白教育of}, \][{{\elevenm}Prect_C _“}ntuh_{ }let, Sc{seech}}(一道的时候, the(on) $''\fundsdE []}{.}) {言息umou’Zu ne()$”n_fromseC nvoCheE C t\textsf{dd-accordab dato3プリベ]}{{<?}{{ {}{{}}}\}}+_{{}}}{\e\hheightState_i_Trace,System成了一个de }]tra(,3seer\rangle:/[-1.7Mikyll}+_xiaearticle\e\\[0em]{}fssed.’;‘ //(:0 “`string)main_content.md‘ (` ){} offenseinto the of{} 1   、}}}main m normals{iconinstall}}\\\{\su+{}{(\^{{}2n UHS systems}=[({})/\_ \anchorLi{\=\url  & Acrosscor)；= & “\”、clip законодательство}[' create\ce inpatient | \\ datetimeV}‘…(treat}////////////////////////////////_}\\eS \corated\\ライ出現} lath</td>