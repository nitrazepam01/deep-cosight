### Page 1

In the HOSM control framework, most popular are twisting controllers [18] and their modified versions like super-twisting [10], [11], adaptive twisting [19], and accelerated twisting [17]. Owing to their advantages, these HOSM techniques have been applied to UAV control [20], [21]. However, these control laws are indeed complicated and would require some simplification. To this end, the one-stage algorithm of the accelerated twisting sliding mode (ATSM), where the control gain is modified to be always greater than an exponential function of the sliding function magnitude, appears not too complicated but can guarantee accelerated finite-time, or at least, fixed-time convergence [1]. Motivated by the work therein, we propose in this paper an adaptive scheme to be able to adjust the control gain of the twisting control law and apply it to control the attitude of quadcopters in harsh conditions with nonlinearity, external disturbances, uncertain dynamics and strong coupling. The control performance of the proposed controller is verified in simulation and also by comparison with real-time data of a Solo drone.
To reach the desired motion, the desired orientation must be comparable to the quadcopter's actual orientation and the coaxial orientation. We can do this by ensuring that the desired attitude angle is aligned with the quadcopter's actual orientation, and the desired speed is matched with the quadcopter's actual speed. This is how we accomplished this by using two slip disturbances and so decreasing kfz from a value of 50.
Nevertheless, we can use the ATSM controller to conclude that long-term experiments, or perhaps more recent experiments, have established the superiority of the ATSM controller over other controllers that have been more frequently used in robotics.

### Page 2

The complex valued inputs \(S(\omega),\)\(T(\omega)\) are supplied by the quadcopter's guide below setup in Fig.1. The planar equations corresponding to quadcopter's guide are:\[\begin{align*}\dot{x}&=\sin x-\dot{y}\\ \dot{y}&=\cos x+\dot{y}\end{align*}\] The graphical representation of the planar equation can be seen in Fig.2. Fig 1: A schematic diagram of quadcopter. \[\left[\begin{matrix} u_{\phi}\\ u_{\theta}\\ u_{\psi}\\ u_{z}\end{matrix}\right]=\left[\begin{matrix} \tau_{\phi}&\tau_{\theta}&\tau_{\psi}\end{matrix}\right]^{-1}\left[\begin{ matrix} u_{x}\\ u_{y}\end{matrix}\right]\] \[u_{\phi}=\frac{1}{m}\left[\begin{matrix}\cos\theta\sin\phi&\cos \theta\sin\phi&\cos\theta\sin\phi\\ \sin\theta\sin\phi&\sin\theta\sin\phi&-\cos\phi\\ -\sin\theta\sin\phi&\sin\theta\cos\phi&-\cos\theta\end{matrix}\right].\] \[u_{\theta}=\left(\begin{matrix}\\ \\ \end{matrix}\right)u\] \[u_{\psi}=\left(\begin{matrix} \\ \end{matrix}\right)u^{T}I_{3}\], \[t=\tan u-\frac{u+v\cos\theta\sin\phi}{\cos\theta}\].

### Page 3

\begin{equation*}
\sigma = \dot{e} + \Lambda e,
\end{equation*}
where $\Lambda = \text{diag}(\lambda_{\theta}, \lambda_{\theta}, \lambda_{\nu})$ is a positive definite matrix being designed, and $e$ is the control error, $e = X_1 - X_{1d}$. 1) Design $u_{eq}$: The equation 
(16) can be rewritten for the attitude sliding surface as:
$\sigma = (\dot{X}_1 - \dot{X}_{1d}) + \Lambda (X_1 - X_{1d})$.
Taking the time derivative of $\sigma$, we have:
\begin{equation*}
\dot{\sigma} = (\dot{\tilde{X}}_1 - \dot{\tilde{X}}_{1d}) + \Lambda (\dot{X}_1 - \dot{X}_{1d}),\]
\end{equation*}
or:
$\dot{\sigma} = -\dot{\tilde{X}}_{1d} + \dot{X}_2 + \Lambda \dot{e}$.
Substituting $\dot{\tilde{X}}$ from
(13)
to
(19) yields:
$\dot{\sigma} = \hat{\tilde{X}}_{1d} + I^{-1} [f(X) + u] + \Lambda \dot{e}$.
When the sliding mode has been induced, $u$ can be considered as the equivalent control $u_{eq}$. By driving the derivative of sliding surface to zero, the equivalent control rule can be obtained as follows:
\begin{equation*}
u_{eq} = I (\dot{\tilde{X}}_{1d} - \Lambda \dot{e}) - f(X).
\end{equation*}
2) Design $u_D$
: The discontinuous control $u$:
\begin{equation}
u_D = u_T,
\end{equation}
where the twisting controllers $u_{T,i}$, $i = 1, 2, 3$ are adopted here as:
\begin{equation}
u_{T,i} = \begin{cases} -\mu_i \alpha_i \text{sign}(\sigma_i) & \text{if} \sigma_i \dot{\sigma}_i \leq 0 \\ -\alpha_i \text{sign}(\sigma_i) & \text{if} \sigma_i \dot{\sigma}_i > 0 \end{cases}, \quad (23)
\end{equation}
where $\mu_i < 1$, is a fixed positive number and $\alpha_i > 0$ is the control gain
[10]. To improve the control transient and tracking performance, the gain $\alpha_i$ in
(23)
could be selected to satisfy the following condition for the one-stage accelerated twisting algorithm
[18]:
$\alpha_i = \text{max}\{\alpha_{s,i}, \gamma_i | \sigma_i |^{\rho_i}\}$.
where $\alpha_{s,i}$, $\gamma_i$ and $\rho_i$ are positive constants. Given that fixed time stability is required over a large operational region of the UAV, and motivated by the simplicity of the one-stage accelerated twisting algorithm mentioned above, we propose to adjust the gain $\alpha_i$ in
(23)
adaptively as in
[20], [21], to be constructed based on the following equation:
\begin{equation*}
\hat{\alpha}_i = \begin{cases} \omega_i | \sigma_i (\omega, t)| \text{sign}(|\sigma_i (\omega, t)|^{\rho_i} - \epsilon_i) & \text{if} \alpha_i > \alpha_{m,i} \\ \eta_i & \text{if} \alpha_i \leq \alpha_{m,i}, \end{cases}
\end{equation*}
where $\omega_i$, $\rho_i > 0$, $\epsilon_i$ and $\eta_i$ are positive constants and $\alpha_{m,i}$ is an adaptation threshold, chosen to be greater than $\alpha_{s,i}$.

In trying to find a condition for the convergence of the proposed control and adaptation schemes, let us consider the Lyapunov function candidate: 
\begin{equation*}
V = \frac{1}{2}\sigma^T I \sigma + \sum_{i=1}^{3} \frac{1}{2\gamma_i} (\alpha_i - \alpha_{M,i})^2, \quad (26)
\end{equation*}
where $I$ is the inertia matrix, $\gamma_i$ is a positive constant, and $\alpha_{M,i}$ is the maximum value of the adaptive gain, i.e.
 0 < $\alpha_{M,i} < \alpha < \alpha_{M,i}$. According to A.1, $\dot{I} = 0$. Thus, by taking the time derivative of $V$ and substituting $\dot{\sigma}$ from (20), one has
%
\begin{equation*}\dot{V} = \sigma^T I \dot{\sigma} + \sum_{i=1}^{3} \frac{1}{\gamma_i} (\alpha_i - \alpha_{M,i}) \dot{\alpha}_i = \sigma^T (-I \dot{\tilde{X}}_{1d} + I \Lambda \dot{e} - S(\omega) I \omega + u + d) + \sum_{i=1}^{3} \frac{1}{\gamma_i} (\alpha_i - \alpha_{M,i}) \dot{\alpha}_i. \quad (27)
\end{equation*}
Equation
(27)
can be rewritten as,
\begin{equation*}
\dot{V} = \sigma^T (d + u_T ) + \sum_{i=1}^{3} \frac{1}{\gamma_i} (\alpha_i - \alpha_{M,i}) \dot{\alpha}_i = \sum_{i=1}^{3} \left[ \sigma_i (d_i + u_{T,i}) + \frac{1}{\gamma_i} (\alpha_i - \alpha_{M,i}) \dot{\alpha}_i \right]. \quad (28)
\end{equation*}
For the case $\sigma_i \dot{\sigma}_i \leq 0$, from the twisting control law, we have
\begin{equation*}
\dot{V} = \sum_{i=1}^{3} \sigma_i [d_i - \alpha_i \mu_i \text{sign} (\sigma_i)] + \sum_{i=1}^{3} \frac{1}{\gamma_i} (\alpha_i - \alpha_{M,i}) \omega_i \sigma_i ) \text{sign}(|\sigma_i|^{\rho_i} - \epsilon_i) = \sum_{i=1}^{3} \frac{1}{\gamma_i} (\alpha_i - \alpha_{M,i}) \omega_i \sigma_i ) \text{sign}(|\sigma_i|^{\rho_i} - \epsilon_i). \quad (29)
\end{equation*}
By assuming that the disturbance $d$ is bounded, i.e., $|d_i| \leq \Omega_{M,i}$, and with sufficiently small $\epsilon_i$ such that
\begin{equation*}
|\sigma_i|^{\rho_i} > \epsilon_i [21],
\end{equation*}
we have $\dot{V} \leq 0$ if
%
\begin{equation*}
\left| \frac{d_i \text{sign}(\sigma_i)}{\mu_i} \right| \leq \alpha_i \text{or} \alpha_i \geq \frac{\Omega_{M,i}}{\mu_i}. \quad (30)
\end{equation*}

Noting that only the case $\alpha_i > \alpha_{m,i}$ is considered here as otherwise the last term in the right hand side of
\begin{equation*}
\Omega_{M,i} > 0.
\end{equation*}
From (23) we can have the same result as above if considering $\mu_i = 1$.

IV. SIMULATION AND VALIDATION
Extensive simulation and comparisons have been conducted to evaluate the performance of the proposed controller with the quadcopter model used for in this study being the 3DR Solo drone, shown in Fig. 2. It has three processors, two are Cortex M4 168 MHz running Pixhawk firmware for low-level control and the other is an ARM Cortex A9 running Arducopter flight operating system. The UAV is equipped with a laser scanner, a camera and environmental sensors for data acquisition. The

### Page 4

Programming is carried out and uploaded to the UAV through the ground control station called Mission Planner [20]. The drone parameters obtained therein are listed in Table I. The control parameters used for this study are given in Table.

**Table I: Parameters of the quadcopter model**

| Parameter          | Value     | Unit      |
|--------------------|------------|-----------|
| $m$                | 1.50       | $kg$      |
| $l$                | 0.205      | $m$       |
| $g$                | 9.81       | $m/s^2$   |
| $I_{xx}$            | $8.85 \cdot 10^{-3}$ $kg.m^2$ |           |
| $I_{yy}$            | $15.5 \cdot 10^{-3}$ $kg.m^2$ |           |
| $I_{zz}$            | $23.09 \cdot 10^{-3}$ $kg.m^2$ |           |

**TABLE II: Control design parameters**

| Variable         | Value      | Variable | Value  |
|--------------------|-------------|------------|---------|
| $\lambda_1, \lambda_2$  | 4.68        | $\bar{\omega}_1, \bar{\omega}_2, \bar{\omega}_3$ | 200   |
| $\lambda_3$           | 3.84        | $\alpha_{m,1}, \alpha_{m,2}, \alpha_{m,3}$ | 2.001  |
| $\rho_1, \rho_2, \rho_3$ | 3.0         | $\alpha_{M,1}, \alpha_{M,2}, \alpha_{M,3}$ | 2.12  |
| $\mu_1, \mu_2, \mu_3$ | 1/4         | $\Xi_{M,1}, \Xi_{M,2}, \Xi_{M,3}$ | 0.5   |
| $\epsilon_1, \epsilon_2, \epsilon_3$ | 0.6        | $\eta_1, \eta_2, \eta_3$ | $0.01$ |

**A. Control performance in nominal conditions**

Performance of the controller is first evaluated in nominal conditions. In this case, the quadcopter is assumed to be at a hovering condition in a steady state where all attitude angles and angular velocities are zeros. New reference angles are then provided with the values $\phi = -10^\circ, \theta = 10^\circ$ and $\psi = 45^\circ$ at time 0.5 s, 1 s and 2 s, respectively. The system responses and controller outputs are shown in Fig. 3 and Fig. 4, respectively, where the latter shows the zoomed-in time scale to observe the abrupt change in references and coupling effects. It can be seen that the proposed controller smoothly drives the angles to the reference values within one second and with a small overshoot despite strong coupling relations among control variables as described in (10-12). However, there still exists minor chattering from the numerical integration of the control system, as depicted in Fig. 4. This can be interpreted as the trade-off to obtain a better control performance, which requires a larger gain $\alpha_i$ in (23). Nevertheless, this phenomenon can be mitigated by adaptively adjusting $\alpha_i$ to its threshold value in the steady state.

**Fig. 2:** The 3DR Solo drone with body coordinate frame.

**B. Responses to disturbances**

In this simulation, robustness of the controller is tested by adding disturbances with the mean value of 0.5 Nm to the torques in all three body axes of the quadcopter, corresponding to angular acceleration disturbances in (10-12). Reference values were selected to be the same as in the previous simulation. Results are shown in Fig. 5. It can be seen that the proposed controller effectively rejects external disturbances to drive the quadcopter to reach the expected attitude within a similar time period as in nominal conditions.

**Fig. 3:** Responses of the quadcopter ($P$, $Q$ and $R$-roll, pitch and yaw angular velocities) in nominal conditions.

**C. Responses to parametric variations**

In this simulation, the quadrotor is subject to several sources of uncertainties including variations in loads and moments of inertia. Specifically, a load of 0.8 kg, the largest load the 3DR

### Page 5

FF Fig. 5: Angular velocity and angle responses in the presence of disturbances.

SS Fig. 6: Angle and angular velocity responses in the presence of parametric variations.

D. Comparison and validation with real-time data

For evaluation of the proposed control approach, simulation results were compared with real-time data obtained by using the built-in PID controller of the 3DR Solo drone to perform the mentioned attitude control. Figure 7 shows the flying path and data recorded, omitting position information, during the experiment. To compare performance of the proposed controller with other control techniques, the output responses were compared with those responses obtained by using the conventional SMC, the adaptive quasi-continuous (AQCSM) [20] and the accelerated twisting sliding mode (ATSM) [18].

Fig. 7: Experimental data acquisition.

Fig. 8: Time responses of three control inputs.

V. CONCLUSION

In this paper, we have proposed an adaptive twisting sliding mode approach for robust control of quadcopter UAVs. The proposed controller is a modification of the accelerated twisting sliding mode control with an adaptive scheme to adjust the discontinuous gain to deal with not only external disturbances but also parametric variations. Performance of the controller is evaluated and compared with other controllers in various simulation scenarios. Its validity is also confirmed by comparing with experimental real-time data. Our future work will focus on implementing the proposed controller to enable

Fig. 8: Time responses of three control inputs.

V. CONCLUSION

In this paper, we have proposed an adaptive twisting sliding mode approach for robust control of quadcopter UAVs. The proposed controller is a modification of the accelerated twisting sliding mode control with an adaptive scheme to adjust the discontinuous gain to deal with not only external disturbances but also parametric variations. Performance of the controller is evaluated and compared with other controllers in various simulation scenarios. Its validity is also confirmed by comparing with experimental real-time data. Our future work will focus on implementing the proposed controller to enable

### Page 6

}}}{a\wedge\theta}\right)\right\|^{2}\right\|} \end{equation}

Fig. 9: The roll and pitch angle and angular velocity responses of controllers in three scenarios:

(a) Nominal condition; (b) Occurrence of disturbances; and (c) Parametric variations.

Fig. 10: Zoom-in tracking errors of controllers at the steady state.

**Table 6:** Summary of control parameters of the proposed scheme.