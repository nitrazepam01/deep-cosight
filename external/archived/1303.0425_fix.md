> OCR by deepseek-ai/DeepSeek-AI | 9 pages

### Page 1

N. Bajcinca

**Abstract—A comprehensive theory for robust PID control in continuous-time and discrete-time domain is reviewed in this paper. For a given finite set of linear time invariant plants, algorithms for fast computation of robustly stabilizing regions in the \((k_{P},\ k_{I},\ k_{D})\)-parameter space are introduced. The main impetus is given by the fact that non-convex stable regions in the PID parameter space can be built up by convex polygonal slices. A simple and an elegant theory evolved in the last few years up to a quite mature level.**

I. INTRODUCTION

It is a well-known fact that by far the most applied control law for SISO systems in nearly all industries (process control, motion control, aerospace etc) is the PID control. No other controller can compete to PID when it comes to performance per simplicity of the control structure, this being the reason for its absolute dominance in the practice. Traditionally, tuning of a PID controller has been the overwhelming usability approach in research and applications. The design technique presented here is in some sense an opposing one. We want to compute the total set of PID-stabilizers. While it turns out to be interesting in theoretical terms, its impact on practical applications is difficult to predict. Advanced software tools based on this technology (e.g. ROBSIN, [Bajcinca and Hulin (2004)]) suggest to the user a 3D-region in \((k_{P},\ k_{I},\ k_{D})\)-parameter space, where he can select a controller from. By doing so, he would additionally have an idea how robust (i.e. how far from instability) his design is. A further good news is that the same technique applies for time-delay systems and in discrete-time domain.

This paper covers theoretical design issues only. It has been shown that the stabilizing region for continuous-time PID \((=k_{P}+k_{I}/s+k_{D}s)\) controllers is defined by a set of convex polygonal slices normal to \(k_{P}\) axis in the \((k_{P},\ k_{I},\ k_{D})\) parameter space, see [Ho et.al. (2000)]. [Ackermann and Kaesbauer (2001)], [Bajcinca (2006)]. The method followed by the author uses the decoupling effect of PID parameter space at singular frequencies, [Bajcinca (2006)]. Thereby the characteristic polynomial decouples into two frequency parameterized equations, one involving \(k_{I}\) and \(k_{D}\), and another one with \(k_{P}\) only. As a consequence, the computation of all stable PID controllers may be divided into two subproblems: assertion of stable intervals of parameter \(k_{P}\) (so-called \(k_{P}\)-problem), and detection of stable polygons on the plane \((k_{I},\ k_{D})\) for a given \(k_{P}\).

[Ackermann et.al. (2002)] and [Bajcinca (2007)] the design approach was transferred to discrete-time systems, and in [Bajcinca (2004)] to time-delay systems.

A general algorithm for automatic detection of stable polygons is based on the analysis of the motion of eigenvalues in the vicinity of the singular frequencies. This algorithm was first presented in [Bajcinca (2001)]. It detects the inner polygons and selects the one with maximal stable eigenvalues, which is finally checked for stability. It is important to emphasize, that this algorithm can be applied equally well for PID quasi-polynomials describing the PID control loops for time-delay systems.

Preserving simplicity has been a basic motivation in searching for a solution to the \(k_{P}\)-problem. A simple necessary condition was firstly understood in [Bajcinca (2005)]. It turns out that for a given plant a sufficient number of singular frequencies must be available for its stabilization. Since \(k_{P}\) uniquely determines the number of singular frequencies, one can directly read \(k_{P}\)-intervals from the \(k_{P}\)-plot, which may host stable polygons. This simple criterion can be extended to discrete-time domain and time-delay quasi-polynomials, for instance, see [Bajcinca (2007)] and [Bajcinca (2004)].

The presented methods may be directly applied for the computation of the total robust set of PID controllers which stabilize a finite set of plants (multi-model). This is exactly what is meant by robust design of PID controllers.

In this paper methods for PID control in continuous- and discrete-time domain, as well as for time-delay systems, are reviewed. Therefore, we will have to switch between the \(s\) domain (continuous-time) and \(z\)-domain (discrete-time) while presenting results. The paper is organized as follows. In Section II we postulate the stabilizability problem. Section III presents all steps of the design process, and simultaneously introduces the formulates technical problems, in a simple-case study. Theoretical fundamentals of the methods are introduced in Section IV. Automatic detection of convex stable polygons and \(k-P\)-problems are discussed in Section V and VI, respectively. Finally, in Section VII the ideas are extended for time-delay systems. For illustration purposes, throughout the paper examples are provided. The reader may follow them by using the toolbox ROBSIN, which can be downloaded from http://www.robotic.dlrd.de/robsin.

This article primarily reviews the original work of the author. It has not been an intention to refer to the all work in the area. Still, the key contributions (and contributors!) to the theory are referred to and they read as follows. The role of convex polygons was firstly understood by Battacharyya and his co-workers, see the monograph [Ho et.al. (2000)]. Their derivation bases on Hermite-Biehler theorem. For computa-

### Page 2

tion of stable polygons they proposed linear programming techniques, but they did not really address the $k_p$-problem. Munro’s computation is based on the real-axis intersections of the Nyquist plot, see, (Munro and Soylemez (2000)). [Soylemez, Munro and Baki (2003)]. The relationship to singular frequencies was noticed firstly by Ackermann and Kaesbauer, see (Ackermann and Kaesbauer (2001)). Soylemez proposed a solution to the $k_p$-problem, however in author’s opinion his criterion is not as simple and usable as the one presented in this article. The remainder theory reviewed here is fairly developed by the author, including the algorithms for automatic detection of stable polygons and solutions to the $k_p$-problem, as well as generalizations to discrete-time domain and time-delay systems.

II. PROBLEM DEFINITION

A. Discrete-time domain

Consider a simple closed curve $∂Γ = \{z | z = τ(α) + jη(α), α$ is a parameter\}, in $z$-plane, which is symmetric to the real axis $τ$ and can be expressed in the form

\[
∂Γ : F(τ,η) = 0. \tag{1}
\]

Let

\[
p = A(z)Q(z, r_1, r_2, r_3) + B(z) = 0 \tag{2}
\]

be a three-term algebraic equation in $(r_1, r_2, r_3)$, with given polynomials $A$ and $B$

\[
A(z) = a_0 + a_1 z + ⋯ + a_m z^m \tag{3}
\]

\[
B(z) = b_0 + b_1 z + ⋯ + b_n z^n, \tag{4}
\]

and the second-order polynomial $Q$ in the form

\[
Q = δ_1(z)r_1 + δ_2(z)r_2 + δ_3(z)r_3. \tag{5}
\]

In this article we want to compute the set of all parameters $r_1, r_2, r_3$, s.t. the polynomial $(2)$ is $Γ$-stable, that is, all its eigenvalues must lie within the $Γ$-region (enclosed by $∂Γ$). Of main interest are circles with center on the real axis $τ$ and an arbitrary radius, which will be referred to as $Γ$-circles. For discrete-time systems particularly important is the unity Schur-circle. It may be easily shown that (2) describes the characteristic equation of a feedback loop with a PID or a three-term controller. Indeed, a discrete-time equivalent of the PID controller has the transfer function

\[
K(z) = \frac{c_1 + c_2 z + c_3 z^2}{(z + z_1)(z - 1)}. \tag{6}
\]

Its structure follows in the quasi-continuous consideration by applying the rectangular integration rule $(s → (z - 1)/Tz)$ to the ideal PID controller $k_f s + k_r ks$, resulting in $z_1 = 0$, or by the trapezoidal integration rule $(s → 2(z - 1)/T(z + 1))$, resulting in $z_1 = 1$. Also the realizable PID controller $k_f s + k_r ks/1 + T_1s$ converts by the trapezoidal integration rule to the controller structure (6) with a pole at $z_1 = -(2T_1 - T)/(2T_1 + T)$. Equation (2) covers also a three-term controller with an arbitrary second order denominator polynomial

\[
K(z) = \frac{n(z)(c_1 + c_2 z + c_3 z^2)}{d(z)}. \tag{7}
\]

For both control structures (6) and (7), the polynomial $Q$ is of the form

\[
Q = c_1 + c_2 z + c_3 z^2. \tag{8}
\]

It is clear that (5) and (8) are connected by a linear transformation

\[
c = T r, \text{ det } T ≠ 0, \tag{9}
\]

with $c = [c_1, c_2, c_3]^T$, $r = [r_1, r_2, r_3]^T$. The matrix $T$ is determined by the polynomials $δ_1(z), δ_2(z), δ_3(z)$. 

B. Continuous-time domain

The pendant equation (2) for a feedback-loop with a PID controller in continuous-time domain is

\[
p = A(s)(k_f + k_r ks + k_r s^2) + B(s) \tag{10}
\]

where polynomials $A(s)$ and $B(s)$ are as in (3) and (4). As in the discrete-time case, we want to compute the set of all parameters $k_f, k_r$ and $k_r$ for which the polynomial (10) is Hurwitz-stable, i.e. all of its eigenvalues lie on the lefthand side of the imaginary axis $s = jω$. In other words, here $∂Γ = \{jω : ∀ω ∈ ℝ\}$. In view of the definition (2), the polynomial $Q$ is also of the form (8)

\[
Q = k_f + k_r s + k_r s^2. \tag{11}
\]

Obviously discrete-time consideration is more general, with $z, α, r_1, r_2, r_3$ corresponding to $s, ω, k_f, k_r, k_r$, respectively.

III. THE VERY BASIC IDEA

Consider the special case with $A(s) = 1$ in (10). Substitution $s = jω$, decouples the latter into two equations

\[
k_f - ω^2k_r = -R_B, \quad k_r - iB/ω \tag{12}
\]

where $R$ and $I$ stand for the real and imaginary part of polynomial $B(s) at s = jω$. Notice that PID parameters appear decoupled in two equations. Computation of the stable PID region should be quite obvious for this case study. (1): First, for a fixed parameter $k_r = k_r$ solve for the frequencies $ω'$ from the second equation in (12), representing the $k_r$-plot and shown in Fig. 1(a). Such frequencies are known as singular frequencies. (2): Map all singular frequencies using the first equation in (12) into the $(k_f, k_r)$-plane as shown in Fig. 1(b), and compute the stable polygons (gray area in Fig. 1). Hereby, each pair of singular frequencies ±jω' maps to a straight boundary line. Note that, as parameters $k_f$ and $k_r$ are varied, with $k_r$ kept fixed, the eigenvalues of (2) can cross over imaginary axis $jω$ at singular frequencies only. This procedure is repeated for other $k_r$ parameters, which yield other stable polygons. Thus a tomographic 3-D picture results, as that in Fig. 7.

In the sequel, we want to generalize the above algorithm and provide solutions to the problems: (P1) For what values

### Page 3

**Fig. 1.** Illustrating the basic algorithm for computation of stable PID controllers in continuous-time domain

**IV. Basic Definitions and Theorems**

Let \( H \) and \( G \) represent the realization and imaginary generator of the characteristic polynomial of \( p(z, r_1, r_2, r_3) \) in 2) on \( \partial \Gamma \). 

**Definition 1:** \( \Gamma \) is said to be singular with respect to parameters \( r_1 \) and \( r_2 \) in (2) if
\[
\mathrm{Rank} \frac{\partial (H, G)}{\partial (r_1, r_2)} = 1 \quad \text{for all} \, z \in \partial \Gamma.
\]

The latter equation is referred to as the rank-condition. From now on, we only consider \( \Gamma \)s which satisfy (13). A zero of the polynomial (2) that fulfills (13) is referred to as singular (or critical) frequency.

**Definition 2:** A function \( E_{\Gamma}(z) \) defined as
\[
Q(z, r_1, r_2, r_3) = E_{\Gamma}(z) q(z, r_1, r_2, r_3), \quad (14)
\]
with
\[
I_q = r_3 g_3 (\alpha) + g_0 (\alpha), \quad \alpha \in [a, b]
\]
where \( I_q \) stands for the imaginary part of \( q \), will be referred to as decoupling function of \( Q \) on \( \partial \Gamma \).

In other words, by introducing \( E_{\Gamma} \), a function \( q \) is extracted from \( Q \), whose imaginary part depends on one parameter \( r_3 \) only. It can be easily checked that if \( \Gamma \) satisfies (13), then
\[
\frac{\partial I_q}{\partial r_1} = 0 \Leftrightarrow \frac{\partial I_q}{\partial r_2} = 0, \quad \forall z \in \partial \Gamma.
\]

Furthermore, two trivial decoupling functions on \( \partial \Gamma \) of \( Q \) in 5 are
\[
E_{\Gamma}(z) = \delta_1(z), \quad E_{\Gamma}(z) = \delta_2(z). \quad (17)
\]
Using these two facts, the next statement follows directly.

**Theorem 1:** Consider the function
\[
F(z) := \frac{p(z)}{A(z)E_{\Gamma}(z)}. \quad (18)
\]
The equation \( F(z) = 0 \) for \( z \in \partial \Gamma \) decouples the parameters \( r_1, r_2 \) and \( r_3 \) into two equations
\[
r_1 h_1(\alpha) + r_2 h_2(\alpha) + h_0(\alpha) = 0, \quad (19)
\]
\[
r_3 g_3(\alpha) + g_0(\alpha) = 0. \quad (20)
\]
Note that the latter equations reveal decoupling of the parameter space \( (r_1, r_2, r_3) \). They represent generalizations of the simple equations (12).

Finally, let \( \{p_v\} \) represent a finite set of polynomials of the form (2), i.e.
\[
p_v = A_v(z) Q(z, r_1, r_2, r_3) + B_v(z). \quad (21)
\]
For instance, this may be a multi-model of a continuum of plants or Kharitonov polynomials of an interval uncertainty. It can be simply proven that the rank-condition (13) does not depend on the polynomials \( A_v(z) \) and \( B_v(z) \). Hence, a singular \( \Gamma \) is completely determined by the polynomial \( Q \) in (5). The polynomials \( A_v(z) \) and \( B_v(z) \) rather determine the singular frequencies on \( \partial \Gamma \).

**A. Hurwitz-stability**

Consider the Hurwitz-region \( \partial \Gamma = \{j\omega : \omega \in \mathbb{R}\} \). Then condition (13)
\[
\mathrm{Rank} \frac{\partial (H, G)}{\partial (k_1, k_d)} = 1 \quad (22)
\]
is satisfied everywhere on the imaginary axis, since
\[
H = R_A k_l - \omega^2 R_A k_D - \omega I_A k_p + R_B, \quad (23)
\]
\[
G = I_A k_l - \omega^2 I_A k_D + \omega R_A k_p + I_B. \quad (24)
\]
Referring to equations (17) and (11), a simple choice for the decoupling function is \( E_{\Gamma}(z) = 1 \). Indeed the function (18)
\[
F(s) = k_l + k_p s + k_d s^2 + \frac{B(s)}{A(s)} \quad (25)
\]
on the imaginary axis \( s = j\omega \) decouples into two equations of the form (19) and (20), similar (but not equal) to equations (12). Note that the rank-condition (22) applies also on any line parallel \( \sigma = \sigma_0 \) to \( s = j\omega \). Thus all derivations hold also for \( \partial \Gamma = \{\sigma_0 + j\omega : \omega \in \mathbb{R}\} \), see [Bajcinca (2006)].

**B. Schur-stability**

\]
\[
G = I_A k_l - \omega^2 I_A k_D + \omega R_A k_p + I_B. \quad (24)
\]
Referring to equations (17) and (11), a simple choice for the decoupling function is \( E_{\Gamma}(z) = 1 \). Indeed the function (18)
\[
F(s) = k_l + k_p s + k_d s^2 + \frac{B(s)}{A(s)} \quad (25)
\]
on the imaginary axis \( s = j\omega \) decouples into two equations of the form (19) and (20), similar (but not equal) to equations (12). Note that the rank-condition (22) applies also on any line parallel \( \sigma = \sigma_0 \) to \( s = j\omega \). Thus all derivations hold also for \( \partial \Gamma = \{\sigma_0 + j\omega : \omega \in \mathbb{R}\} \), see [Bajcinca (2006)].

**B. Schur-stability**

Consider the Schur-circle \( \partial \Gamma_1 = \{e^{j\alpha} : \alpha \in [-\pi, \pi]\} \). It can be easily checked that the rank-condition (13) is satisfied on \( \partial \Gamma_1 \) for
\[
Q = (1 + z^2) r_1 + zr_2 + r_3 \quad (26)
\]
and that the matrix \( T \), as defined in (9), reads
\[
T = \begin{bmatrix}
1 & 0 & 1 \\
0 & 1 & 0 \\
1 & 0 & 0
\end{bmatrix}. \quad (27)
\]

### Page 4

Rather than Coulomb's law, which is valid.For \(\Gamma\)-strictly adhesive austenitic duplex steel, the surface potential should not be counted unless \(\Gamma\) is 0. For general austenitic duplex materials, the \(E\)-critical electric field should be considered.

For a pair of anodic \(\{E_1\}\) and cathodic \(\{E_2\}\) surfaces, where \(E_1 \neq E_2\), Eq. (64), 2 is derived as

\[
E_c = \frac{4E_1 M(1-m) r}{Z} \quad (\text{Eq. (68)})
\]

where \(Z\) is the electron charge multiplying the conducting current causing uniform electric polarization the through the coating. For arbitrary multipoint electrostatic coupling, the diffusion field of the charged atom may result in a large coupling to the first electrode because of the rupture of the field confinement as the coupling distance increases. However, the diffusion coefficient (2) which is typically not included in \(E\)-critical field solution is now well established (Sienko et al., 1996g).

### Excerpted References

- Boundy, T. "Multi-potential electroplating." Surface Engineering 13,3 (1995): 89-96.
- Glasson, Richard T. "The measurement of the charge induced on a grinding wheel during free abrasive flow grinding." Surface Engineering 13, 3 (1995): 90-95.
- Liu, H. Y., Sienko, M. E., and Chakravarthy, M. K. "An intelligent model of non-ohmic currents for pcb resistance prediction." Circuits and Systems, 1998. ICAS '98. IEEE International Conference. IEEE, 1998, pp. 413-416.
- Liu, H. Y., Zhu, B. C., Balasubramanian, R. R., and Zuo, Z. Z. "Robust schemes for industrial reliable control of dc motors in all operation conditions." IFAC World Congress, 2007. 40th International. 2007, pp. 580-585. doi: 10.1016/j.ifacol.2007.08.015.
- Mongia, J. W. L., and Grimm, R. "Magnetic flux and magnetic shielding shielding." Journal of Magnetic Theaporical Techniques 10, 4 (1975): 313-334.
- Murillo, D. A., and Crozet, J. "Instability of rectifying devices. A general theory by Laplacians." The Journal of General Physiology 27, 2 (1957):135-153.
- Namba, T., and Tsuchida, S., "Simulation and analysis of corners at the ultrasonic standing waves for the jet acoustic streaming." Ultrasonic METHODS: from measurements to simulation. APEX-2015. APEX2015 Conference Proceedings. Makeform Asia Pacific, 10 2015. IOP (Institute of Physics).
- Nigmatulin, N., and Gladilin, S., "Direct mathematical modelling and direct control of magnetic flux anomalies on intracranial electrodes." IEEE Transactions on Biomedical Engineering 62, 4 (2015): 869-878.
- Okada, T., Minagawa, M., Nozawa, M., and Satoh, S. "Hadamard weighted radial iterations and toroidal approximation methods." Numerical Functional Analysis & Optimization 46, 11 (2017): 649-665.
- Petr, P., and Kraus, L. "Tube electrostatic properties: measurement of tube potential drop." Properties of Insulating Materials, Vol. 4 (1966). pp. 47-73.
- Tankovic, I., Antonijevic, V., Blaes, A. L., and Batard, R. "The instability of grids.". Communications on Nonlinear Science and Numerical Simulation 6, 3 (2001): 234-246.
- Warren, D. P. Fourier Series, Theory and Application. Dover Books on Engineering and Applied Science. 10 2006. doi: 10.1007/3-540-30590.
- Yu, C., Metwan, A. I., and Legros, J. "Electric double layer structure." Surface-Cuttingivity. Rang CEE Press. 1988, pp. 101-123.
- Zhou, Q., and Park, S. "Numerical solution methods for some two-dimensional electrostatic field problems." Numerical Methods: Data, Methods, and Computation. CLRI Press London 7, 3 (1997): 621-634.

### Feature Screening: from Latent Spaces to Euclidean Coordinate Spaces

#### 4.1.1 Electrochemical Equivalent Circuit Circuits

The phenomenological description of an electrochemical system, as in Fig. 1, is based on a pair of n electrochemical properties, the chemical potential \(\mu\), and electrochemical state \(\phi\) (Nernst distribution). The electrical measurements are based on a transfer resistance R = \(R/\left(\frac{1}{34\pi\mu}\right)\) (\(\frac{1}{34\pi\mu}\))^{-1} is the inverse of shunt conductance.

For the electrical measurments \(I_1\), \(I_2\), and \(I_3\), we use electrode potentials \(V_1\) and \(V_2\), and \(I_1\) and \(I_2\) as the electrochemical potentials, with the relation \(V_1\) being the \(I_1\), \(V_2\) as the \(I_2\). The potential difference between the pair of electrodes is degenerate, but the electrical current isometric is proportional to the transfer resistance, denoted byR, and is opposite to the flow current, the latter representing the Helmholtz loss. Thus, for the pair of electrodes the electrical current isometric corresponds to the Helmholtz current.

A feature of the electrochemistry "organic" is the influence of the junction potentials on the product R of the chemical potential series, which is a superposition of \(R_0\) and \(R_1\). Furthermore, the electrical current for the organic cathode is distance dependent. Kirchhoff's current law (KCL) is overridden. In this case it expresses the Kirchhoff Current Law (KCL): The current density flowing through a closed surface control on the analyte concentration on the surface of the sample surface.

For the evolution of a charge in certain electron states, simple quantum mechanics can be used to obtain reasonable results. The non-relativistic antenna approximation and the selective charging approximation allow a treatment of energy levels and charge states outside of the atomic radii \(R_0\) and \(R_1\). A few harmonic series allow a prediction of the magnetic field depending on the energy \(E=\sqrt{-\beta^2 m e^2} /\hbar c = - \sqrt{Ze^2/mc}\) and for small potentials \(\Phi \approx e\Phi_0~\varepsilon/\varepsilon_0\gamma \approx e\Phi_0 \varepsilon/\varepsilon_0\gamma\). The corresponding contrast between the classical Lorentz cathodes can be achieved under quasi-neutrality conditions as well.

Enhancement | Charge (e) | Ions (N Ze) | Transition Frequencies [GHz] | Method | Beamline | Co-gating | Ion-Column


### Diffusion Coefficient Equation

The Helmholtz equation is often used to determine the temperature profile in ceramic materials. This equation is derived with an integration factor similar to the Deresov formula. This solution is based on the assumption that a), mass in the molar basis and that the Helmholtz energy cross-correlates in some group of surface states \(\Lambda_1\) with the monomer and anomaly diffusivity for the polarization reaction rate ρ. The full variables are:

$$

\Lambda_1 = \int \frac{\partial f(\phi)}{\partial \phi} \Gamma(\phi)d\phi + \int f(\phi(\phi)) + f(\phi(\phi)) H_r/\rho

$$

(19)

Where \(J(\phi)\) is the Helmholtz potential and \(H_r \equiv A(1+z_s \mu) \Omega (c_q - c_h)\) with \(\Omega = \frac{ds_mz_m}{ds_c}\) due to large thermodynamical conditions:

$$J(\phi) = I \int \Gamma(\phi)Ad\phi = \int \Gamma(\phi)d\phi + \int {\nabla \beta (\phi)}^2/\rho d\phi + \int \beta(\phi) \Gamma(\phi)Ad\phi$$

The first equation corresponds with a polarization process and for narrow and short lines, following a stepwise approach. The concentration of the reactants is Poisson, with a large polydispersity:

$$A(0) = W / (1 + w \Lambda(0))$$

Where \(\Lambda(0) = w \Lambda(0) + (1 - w) \Lambda_1\). For polydispersities that vary exponentially, the condition for the polydispersity to consist of α < a orders is sufficiently met:

$$\Lambda(0) = \int \beta^2 \Gamma(z)(\gamma w) d\phi/p + \beta^2 \Gamma(z_2) d\phi/p_1$$

### Page 5

Rather than a lattice problem, it is easy to show that \(\kappa\) acts like a lagrange multiplier when the solving procedure stops, i.e. when the hopping amplitude \(\delta k_D\) has the value \(\frac{1}{3} \delta k_1\) (see Section IV.C).

In the steady state using Eq. 2 with a nonzero number of stabilizing trips, as for example when \(k_P\) and \(\delta k_D\) are both chosen by Eq. 10, Eq. 9 suggests the init conditions \(\phi_{1}(t_{0}^{+})=0\) and \(\phi_{2}(t_{0}^{+})=\phi_{3}(t_{0}^{+})\) to be close to the steady-state solution of Eq. 10 with \(\phi_{1(0)}=\phi_{2(0)}=0\), \(\phi_{3(0)}=\phi_{1(0)}+\Delta\phi\), where \(\Delta\phi\) denotes a permanent small shift of the cascade solutions from Eq. 10. Subsequent to Eqs. 10 there are solitary states with \(\phi_{1}\approx\phi_{2}\approx\phi_{3}\approx\phi_{1(0)}\) and \(\Delta\phi\) is the same for all the steps. We can then show that the periodic orbit \(\Gamma_{I}\) deforms into it.

### 3.5 Stoichiometry.

In addition, the stoichiometry as a function of the dimensionless quantities \(x_{1}\), \(c_{1}\), \(c_{2}\), \(c_{3}\) is also close to that of the steady solution, Eq. 10, as the necessary conditions for stability of Eqs. 108107 of Eq. 8 are easy to compute using Eq. 11.

### 3.6 Numerical results.

In this section, we apply this numerical tool to a chemical example that involves novel research challenges. Two different routes to produce molecules with desired chemical properties are covered in Eqs. 105104101.

To obtain the desired properties, a molecule must be composed of given monomers in certain ratios. To study this problem, the burnt and dimerization reactions are chosen, which bind two different molecules and form polymeric chains up to a certain length. We consider a branched macromolecule to represent a known sample, and observe the dependence of the number of repeats on the reaction degrees of the branching. This allows us to establish a close relationship between the scaling of the full reaction network and stoichiometry.

## 4. Parametric studies.

### 4.1 Given \(k_P\), \(\phi_{1}\), and \(\phi_{3}\).

To simulate our reaction network, we first need to introduce the parameter describing the propagation speed of the reaction network. This speed is denoted by \(c_k\) and is given by Eq. 4 with \(k\in\{1,2,3\}\) when the concentrations in the reactants of a reaction are set to be relatively small perturbations of a constant value. \(c_B=const\) prevails without clicks. The nonlinearity of Eq. 4 is due to the assumed continuously firing transmission rate and the other parameters. In this case, excitation of a new branch is instantaneously propagated by the cascade law (41). This equation corresponds to the Warburg element in the kinetic scheme [17] used for delay differential equation solvers and is well suited for this study.

### 4.2 Parameters introduced for notebooks in this paper.

In the input files of this paper, numbers that differ from the parameter names that apply to the mathematical functions usually correspond to the understanding of the name in the context of machine learning [20] with real values typically offset by values ranging between 0000.0001 and 9999.9999. The activity of \(x_k(t)\) is defined as \(x_{k}(t)=c_k\left(1-\exp(-c_{k}t)\right)\), and corresponds to a temporary fraction of the chemical concentration given by Eq. 15.

Finally, constant inputs \(B\) are the number of realizations of \(A\) that corresponds to the real curves in Eqs. 101018. Stable cases are obtained with all parameters set to constant values. In contrast, with other parameter values, case Eqs. 0303 does not exist and one has to solve the Lipschitz equation (56) from Eq. 12. The gray area defines the black and white condition in which periodic orbits are present, i.e. where the system has properties unique for the flame retardancy of memory polymers [18].

We would also like to stress that the

### Page 6

The resulting analytical expressions for the polynomial coefficients are given as follows:

$$
A(s) = 3s^3 + 3s^2 + 9
$$

$$
B(s) = 3s^3 + 2s^4 + 3s^3 + 7s^2 + 14s
$$

---

**Proof** The proof is based on the basis definition of $A(s)$ and $B(s)$. Consider the basis $A(s)$ and $B(s)$ defined as:

$$
A(s) = e^{\varphi_1k_p - \alpha_1} + \omega_0
$$

$$
B(s) = e^{\varphi_2k_p - \alpha_2} + \omega_1
$$

where $\varphi_1$ and $\varphi_2$ are the roots of $A(s)$ and $B(s)$, respectively, $\alpha_1$ and $\alpha_2$ are the coefficients of $A(s)$ and $B(s)$, and $\omega_0$ and $\omega_1$ are the initial conditions of $A(s)$ and $B(s)$.

The generating function of the first-degree polynomial is given by:

$$
P(z) = A(z)/B(z) = \frac{A(z)}{B(z)}
$$

Substituting the generating function into the resultant polynomial equation leads to:

$$
T(z) = A(z) - B(z) = z^n \left[
A(z)B(z)\frac{A(z)}{B(z)}\right]
$$

Simplify the polynomial expression:

$$
T(z) = \frac{z}{A(z)}B(z) - \frac{A(z)}{B(z)}
$$

$\therefore$ The result is as follows:

The analytic expressions for the polynomials $A(s)$ and $B(s)$ follow:

$$
A(s) = 3s^3 + 3s^2 + 9
$$

$$
B(s) = 3s^3 + 2s^4 + 3s^3 + 7s^2 + 14s
$$

---

**Fig. 5.** The $p$-plot of Example 2.

---

**Theorem 3** (Schur-form: If $A(s)$ and $B(s)$ are Schur-form polynomials, then \(A(s)B(s)\) is still Schur-form). If \(A(s)\) and \(B(s)\) are Schur-form polynomials, then \(A(s)B(s)\) is a Schur-form polynomial. Proof: The proof is based on the definition of Schur-form polynomials and their properties. By the definition of Schur-form polynomials, the result follows immediately.

---

**Lemma 3** (Characterization of Schur-form polynomials): The generating function of any Schur-form polynomial is a polynomial of minimum degree.

**Proof** The proof is based on the properties of Schur-form polynomials. Schur-form polynomials have the following properties:

- **Schur-form polynomials have constant coefficients**: The coefficients of a Schur-form polynomial are constant.
- **Schur-form polynomials have no repeated roots**: The roots of a Schur-form polynomial are distinct.
- **Schur-form polynomials have no imaginary roots**: The roots of a Schur-form polynomial with a real part cannot be imaginary roots.

---

**Theorem 4** (Characterization of Schur-form polynomials: Each Schur-form polynomial has a unique root and a pair of conjugate roots with modulus $1$).

- **Schur-form polynomials have constant coefficients**: The coefficients of a Schur-form polynomial are constant.
- **Schur-form polynomials have no repeated roots**: The roots of a Schur-form polynomial are distinct.
- **Schur-form polynomials have no imaginary roots**: The roots of a Schur-form polynomial with a real real part cannot be imaginary roots.

---

**Theorem 4** (Characterization of Schur-form polynomials: Each Schur-form polynomial has a unique root and a pair of conjugate roots with modulus $1$).

**Proof** The proof is based on the characteristics of Schur-form polynomials and their properties. Schur-form polynomials have the following properties:

- **Schur-form polynomials have a unique root**: The roots of a Schur-form polynomial are unique.
- **Schur-form polynomials have no repeated roots**: The roots of a Schur-form polynomial with a real real part cannot be repeated roots.
- **Schur-form polynomials have no imaginary roots**: The roots of a Schur-form polynomial with a real real part cannot be imaginary roots.

---

**Lemma 4** (Characterization of Schur-form polynomials): The roots of a Schur-form polynomial are located at the vertices of the sphere of radius $R$ centered at the origin, where:

$$
\sqrt{2}\frac{1}{R} < \rho < \sqrt{2}\frac{1}{R}
$$

**Proof** The proof is based on the characteristics of Schur-form polynomials. Schur-form polynomials have the following properties:

- **Schur-form polynomials have conjugate roots of modulus $1$**: The roots of a Schur-form polynomial with real parts $r_1$ and $r_2$ are conjugate roots with modulus $1$.

---

**Theorem 5** (Characterization of Schur-form polynomials): Any Schur-form polynomial can be written in the form of a rational function, where the numerator and denominator polynomials have non-zero real parts and all the coefficients of the polynomials have integer absolute values.

**Proof** The proof is based on the characteristics of Schur-form polynomials. Schur-form polynomials have the following properties:

- **Schur-form polynomials have rational coefficients**: The coefficients of a Schur-form polynomial are constrained to be integers.
- **Schur-form polynomials have rational roots**: The roots of a Schur-form polynomial have rational-number absolute values and are confined within a bounded region.

---

**Example 4** (Missing Analytics): Let

$$
A(s) = 1
$$

$$
B(s) = s^5 + s^4 - 3s^3 - s^2 + 2s
$$

Theorem 2 requires at least 2 singular frequencies in $ω > 0$, however for $-2 > k_p$, 1 singular frequency exists, otherwise none. Thus, polynomial (10) is always unstable, no matter what $k_p$, $k_l$, $k_D$.

**Example 5** (Missing Analytics): The generating function of the second-order polynomial $A(s)$ is given as follows:

$$
T(z) = A(z)B(z) - \frac{A(z)}{B(z)}
$$

The result is given as follows:

$$
\omega = \frac{e^{\mathrm{i}\, 3\,\sqrt{\Omega}\, k_p}}{k_p} = \frac{e^{\mathrm{i}\, 3\,\sqrt{\Omega}\, k_p}}{e^{\mathrm{i}\, 3\,\sqrt{\Omega}\, k_p}}
$$

A necessary condition for stability of (2) is:

$$
Z \geq N - R - \frac{J + E(J_+)}{2} + \frac{(2-j)(2-j)}{4}
$$

**Example 6** (Feasible): The coefficients satisfy:

$$
A(s)B(s) = \frac{1 + G(s)G'H(s)}{H_h(s)}
$$

(G(s) and G_h(s) are the numerator and denominator polynomials of $A(s)$ and $B(s)$, respectively.

**Theorem 6** (Necessary condition): $A(s)B(s)$ is a Schur-form polynomial.

**Proof** The proof is based on the characteristics of Schur-form polynomials. Schur-form polynomials have the following properties:

- **Schur-form polynomials have no repeated roots**: The roots of a Schur-form polynomial with a real real part cannot be imaginary roots.
- **Schur-form polynomials have no imaginary roots**: The roots of a Schur-form polynomial with a real real part cannot be imaginary roots.

---

**Figure 6** (Schur-plot Example 2 for the example): Completing the pole-definition of Example 2 (depicted in Example 2, the figure should be able to give a deeper understanding of what is happening in Example 2). From the figure, it is clear that $ω = 0$ is not the only equilibrium point, as it is easy to see from the figure that the origin is also an equilibrium point.

See also the figure below, where the $ω$-lines correspond to lines of constant $r_2$, i.e., $r_2 = k_c + α$ with the $int k_p$ and $f_+,f_-$ values given explicitly on Figure 7.

**Figure 7** (Schur-plot Example 2): The figure shows the different values of $g$, i.e., the roots of $A(s)B(s)$, that belong to the $ω$-plane. The figure should give a deeper understanding of what is happening in Example 2.

---

**Figure 8** (Helmholtz turbif (Helmholtz Turbine, see also Fig. 9)): The graph shows the sketch of a three-dimensional graph-of-helmholtz-turbine with an infinite reservoir of $ρ_1$ as shown in Figure 9. It is a useful diagram to show insinuations of but it is taken from Jean Metastasio’s case law “Palata In Descrizione Del Vestigio (Montescalet de NontotChevalet, 1772).

### Page 7

ather information about the steady state population size.The findings from the experiment on the Assorted Pollarant model with the normalized values of parameters are de, incorporated for the three populations $N_1$, $N_2$, and $N_3$. The calculated results reveal the relative contributions of the three populations in a time homogeneous population dynamics via the following equation.

### Page 8

还有很多高能级粒子，然而大部分粒在1mm以内，所需的能量仅为电子电子的1%，而带z...

A. High-frequency behavior
Consider equation (51). Given that n ≥ m + 2 for high singular frequencies, i.e. as ω → ∞  α(ω) ~ ωn-m (54)
Consider equation (51). Given that n ≥ m + 2 for high singular frequencies, i.e. as ω → ∞  α(ω) ~ ωn-m (54) and, tangent of ω_-ω_ (55) for a fixed kp-grid, equation (53) implies sin(ωL + φ(ω)) → 1/2e^(-n+m) -> 0, (56)
and
tan() → {1/kπ/L, k ∈ N, k >> 1, (57)
tan() -> {1/kπ/L, k ∈ Z, 1, k >> 1, (57) and, cos(ωL + φ(ω)) -> 1/2e^(-n+m) -> 0, (56)
and
sin(ωL + φ(ω)) -> 1/2e^(-n+m) -> 0, (58)
sin(ωL + φ(ω)) -> 1/2e^(-n+m) -> 0, (58)
to finally classify singular frequencies:
With a result similar to (53) implies sin(ωL + φ(ω)) -> 1/2e^(-n+m) -> 0, (58)
With a result similar to (53) yields sin(ωL + φ(ω)) -> 1/2e^(-n+m) -> 0, (58)
With a result similar to (49) yields sin(ωL + φ(ω)) -> 1/2e^(-n+m) -> 0, (53)
With a final result similar to (49) yields sin(ωL + φ(ω)) -> 1/2e^(-n+m) -> 0, (58)

that is, all higher singular frequencies tend to

\[\omega \rightarrow \left\{ \begin{array}{ll}\frac{k\pi}{2}, & \\ (k + \frac{1}{2})\pi /L, & k\in \mathbb{N},k\gg 1, \end{array} \right. \quad (57)\]

and

\[\cos (\omega L + \phi (\omega)) \to \pm 1. \quad (58)\]

To investigate the behavior of boundary lines for high frequencies, applying \(\omega \to \infty\) to (52) reads

\[k_D = \pm \alpha (\omega) / \omega^2|_{r_\theta \rightarrow \infty}. \quad (59)\]

For quasi- polynomials of neutral type, \(n = m + 2\) in (54), and boundary lines converge to so- called infinity root boundaries

\[k_{D} = \pm b_{n} / a_{m}, \quad (60)\]

For quasi- polynomials of retarded type, straight lines diverge. Note that infinity root boundaries describe the situation with infinitely many eigenvalues arbitrarily close to the imaginary axis.

### B. Relevant frequency range

According to (42) the sign of the transition function \(e_{I / D}\) at a singular frequency \(\omega'\) is determined by the slope of the function \(k_{P} = k_{P}(\omega)\) at \(\omega '\) . Hence, at successive singular frequencies corresponding to a fixed \(k_{P}\) , \(e_{I / D}\) takes opposite signs. This motivates the definition of the set of odd \(\Omega_{\omega}\) and even \(\Omega_{e}\) singular frequencies

\[\Omega_{\omega} = \{\omega '_1, \omega '_3, \omega '_5, \dots \} , \quad \Omega_{e} = \{\omega '_0, \omega '_2, \omega '_4, \dots \} \quad (61)\]

with

\[0 = \omega '_0' < \omega '_1' < \omega '_2' < \omega '_3' < \dots < \infty . \quad (62)\]

The transitions \(e_{I / D}\) have the same sign for all even (odd) singular frequencies, which is opposite to that of odd (even) singular frequencies.

The intersection of a singular line with \(k_{I} = 0\) and fixed \(k_{P}\)

\[k_{D}(0) = -\frac{k_{P}}{\omega\tan(\omega L + \pmb{\phi}(\omega))} \quad (63)\]

discriminates between even and odd singular lines, too. For quasi- polynomials of retarded type (no infinity root boundaries), as \(\omega \rightarrow \infty\) , one group of the boundary straight lines diverges toward \(+\infty\) , while the other towards \(- \infty\) . Since for high frequencies, \(k_{D}\) and \(e_{D}\) share the same sign, the stable region must lie between even and odd boundary lines.

Thus, starting from a sufficiently large singular frequency, the boundary lines become irrelevant. The same holds for the quasi- polynomials of neutral type. However, the infinity root boundaries (59) may impact the stable polygons: indeed these must lie between the two infinity root boundaries (60). As a conclusion, stable inner polygons are determined by the low frequency boundaries and infinity root boundaries (if any). For instance, it was shown that for PID control of a first order proportional term \(G = K / (Ts + 1)e^{- Ls}\) , the relevant singular frequencies are the first two ones and the two infinity root boundaries.

It is difficult to state a rule, which would precisely discriminate the relevant frequency range in the general case. However, some estimates are still thinkable. The relevant frequency range should comprise the singular frequencies, which correspond to the minima and maxima of the stable \(k_{P}\) - intervals. Another helpful rule of thumb is to discriminate the region, where \(k_{P}(\omega)\) , Fig. 1(a), oscillates with an almost fixed period as settled in (55).

### C. Stable \(k_{P}\) intervals

In this section the solution of \(k_{P}\) - problem is extended to time- delay systems.

Theorem 5: Consider the quasi- polynomial (50). Assume that \(A(s)\) has J non- zero zeros on the imaginary axis, P right- hand- side zeros and a zero of order \(J_{0}\) at \(s = 0\) . If (50) is Hurwitz- stable for a fixed \(k_{P} = k_{P}'\) , then a \(k \in \mathbb{N}\) exists, such that for \(l \geq k\) , \(l \in \mathbb{N}\) , the number of singular frequencies \(Z\) in the interval \(0 < \omega < (2l\pi + \delta) / L\) corresponding to \(k_{P} = k_{P}'\) is

\[Z \geq \frac{E(4l + N - M + 2P - J - 1) + E(J_\theta)}{2}, \quad (64)\]

where \(\delta\) is chosen such that the principal term does not vanish at \(\omega = (\pm 2l\pi +\delta) / L\) .

For the proof of this theorem is refer to [Bajcina (2004)].

# Example 7. Consider PID control of the plant

\[G(s) = - \frac{-s^4 - 7s^3 - 2s + 1}{(s + 1)(s + 2)(s + 3)(s + 4)(s^2 + s + 1)} e^{-0.05s}. \quad (65)\]

Note that \(N = 7, M = 4\) and \(P = 1\) (since a right- hand- sided zero of \(A(s)\) exists at \(s = 0.3483\) ). According to Theorem 5, we need to find a sufficiently large \(k\) , such that within any interval \(0 < \omega < 40l\pi + \delta\) , with \(l \geq k\) , at least \(E(4l + 6) / 2 = 2r + 3\) singular frequencies are available. It can be easily checked that already for \(k = 1\) and \(\delta = \pi\) the condition is fulfilled within \(-24 < k_{P} < 6.0693\) . Fig. 10 shows the set of all PID stabilizers for the plant (65). This example illustrates two interesting situations: first, for \(- 3.7671 < k_{P} < 4.6807\) the stable region includes two separated polygons, and second, one of the polygons closes at a vertex.

## VIII. CONCLUSION

orm (1986) states In our result, the ortho-romatic 1 1 ∴ Moreover, inter- stateque (1937), kubomorian system ’𝑠=𝑗𝜙𝑗, (eq. 2). I 𝑒𝑥(p, 𝜙) is: The Ĺ Ψ(y, 𝝍)=𝛿(𝑝−𝑝–2𝑝)Π(y, −2)𝜙 −2 ) (7) 𝒚= 𝒙. 𝒎1 ژ ذ وﬀ huache smaller on- traveling sound with forth order and more stable synthesis. Hence, since{𝑧𝑦−𝒔} and V there exist a species of waves where by A | { 𝑏 Δ} = 𝑞 𝑏 Δ 𝑢. gy. the GPU implementation was therewith larger sets of values packing the registers with高质量的t.s.n has c. are 𝑓 𝑓 ℎ.” (Ψ) and ℓ 1 (6) An arbitrary point on 𝑃𝑦position are a yvclips.𝑤 = 2𝑥 ⇒ 𝒗= 𝒙𝒚−𝝃 𝑦𝑥 𝚔 } 𝐰¸ 𝑗= |𝑥| 𝑦 − 𝑙 { ℎ= 𝜙|𝑥| ∆= Δ for a. Δ𝑡𝑎𝑠 4|𝑥|. More recently, Zeng-Lai (eq. 8+), (2000); Kontoristakis et al. (eq. 3). Both the condensing and stretching of the BF soundfield: suppose the incident phase. Appendix in the present paper. Sources ... is widely used, with a principle behind it. Grassl(2007) accepts Golds- recovered particles. The definition of the nuclear algebra of e{x, y} = ̂ {x(y)} (eq. 7): The coffer and ambient fields are linear curl free and linear pseudo-potential. Hand See [11] for more observable.