### Page 1

There should be a restatement of certain mathematical notations directly related to where new elements or complexities meet. - One must explicitly say what items in the original material or revision are being revised (if any so changes should be explicitly stated in different style and including the list if there is really one).

### Page 2

### Page 3

equation " part.

This paper is concerned with system identification of nonlinear state space models (SSMs) in discrete time. The general model that we consider is given by

\[
x_{t+1} | x_t \sim f_\theta(x_{t+1} | x_t, u_t), \\
y_t | x_t \sim g_\theta(y_t | x_t, u_t), \\
(\theta \sim \pi(\theta)), \tag{1c}
\]

where the states, the known inputs and the observed measurements are denoted by \( x_t \in X \subseteq \mathbb{R}^{n_x}, u_t \in U \subseteq \mathbb{R}^{n_u} \) and \( y_t \in Y \subseteq \mathbb{R}^{n_y} \), respectively. The dynamics and the measurements are modeled by the probability density functions (PDFs) \( f_\theta(\cdot) \) and \( g_\theta(\cdot) \), respectively, parameterized by the unknown vector \( \theta \in \Theta \subseteq \mathbb{R}^{n_\theta} \). The initial state \( x_1 \) is distributed according to some distribution \( \mu_\theta(x_1) \). For notational simplicity, we will from hereon (without loss of generality) drop the known input \( u_t \) from the notation. When considering Bayesian identification, meaning that \( \theta \) is modelled as an unobserved random variable, we also place a prior \( \pi(\theta) \) on \( \theta \). We are concerned with off-line identification, i.e. we wish to find the unknown parameters \( \theta \) in (1) based on a \( batch \) of \( T \) measurements.

The \( key change \) that will drive the development throughout this paper is how to deal with the difficulty that the states \( x_{1:T} \) in (1) are unknown. We will distinguish between two different strategies for handling this:

1. **Marginalisation** amounts to marginalising (integrating out) the states from the problem and viewing \( \theta \) as the only unknown quantity of interest. In the frequentistic problem formulation, the prediction error method [Ljung, 1999] and direct maximisation of the likelihood belong to this strategy. In the Bayesian formulation, the Metropolis-Hastings algorithm [Metropolis et al., 1953, Hastings, 1970] can be used to approximate the posterior distribution of the parameters conditionally on the data.

2. **Data augmentation** treats the states as auxiliary variables that are estimated together with the parameters. The expectation maximisation (EM) algorithm of Dempster et al. [1977] solves the maximum likelihood formulation in this way and the Gibbs sampler of Geman and Geman [1984] solves the Bayesian problem by this strategy.

In the special case when the model (1) is linear and Gaussian, there are closed form expressions available from the Kalman filter and the associated smoother. The primary focus of this paper, however, is the more challenging nonlinear and/or non-Gaussian case. More than two decades ago [e.g., Gordon et al., 1993, Kitagawa, 1993] sequential Monte Carlo (SMC) methods started to emerge with the introduction of the \( \textit{particle filter} \). These methods have since then undergone a rapid development and today they constitute a standard solution to the problem of computing the latent (i.e. unobserved/unknown/hidden) states in \( \textit{n} \nolinebreak \) non-Gaussian SSMs.

### Page 4

For a thorough treatment of the use of ML for system identification, see e.g., Ljung [1999], Söderström and Stoica [1989].

Secondly, in the _Bayesian_ formulation, the unknown parameters \(\theta\) are modeled as a random variable (or random vector) according to (1c). The system identification problem thus amounts to computing the _posterior_ distribution of \(\theta\) given the observed data. According to Bayes' theorem, the posterior distribution is given by,

\[
p(\theta|y_{1:T}) = \frac{p_{\theta}(y_{1:T})\pi(\theta)}{p(y_{1:T})}. \tag{3}
\]

Note that the likelihood function should now be interpreted as the conditional PDF of the observations given the parameters \(\theta\), i.e. \(p_{\theta}(y_{1:T}) = p(y_{1:T}|\theta)\). However, to be able to discuss the different identification criteria in a common

### Page 5

suggest.setting, we will, with a slight abuse of notation, denote the likelihood by \( p_{\theta}(y_1:T) \) 
also in the Bayesian formulation. An early account of Bayesian system identification is provided by Peterka [1981] and a more recent description is available in Ninness and Henriksen [2010].

The central object in both formulations above is the observed data likelihood 
\( p_{\theta}(y_1:T) \), or its constituents 
\( p_{\theta}(y_t | y_{1:t-1}) \) as,

\[
p_{\theta}(y_1:T) = \prod_{t=1}^{T} p_{\theta}(y_t | y_{1:t-1}), \tag{4}
\]

where we have used the convention \( y_{1:0} \triangleq \emptyset \). For the nonlinear SSM (1), the likelihood (4) is not available in closed form. This is a result of the fact that the latent state sequence \( x_{1:T} \), i.e. \( p(x_{1:T} | y_{1:T}) \), is unknown. Indeed, a relationship between the likelihood and the latent states can be obtained via marginalization of the joint density \( p_{\theta}(x_{1:T}, y_{1:T}) \) w.r.t. \( x_{1:T} \) according to,

\[
p_{\theta}(y_1:T) = \int p_{\theta}(x_{1:T}, y_{1:T}) dx_1:T, \tag{5}
\]

where the model provides a closed form expression for the integrand according to,

\[
p_{\theta}(x_{1:T}, y_1:T) = \mu_{\theta}(x_1) \prod_{t=1}^{T} g_{\theta}(y_t | x_t) \prod_{t=1}^{T-1} f_{\theta}(x_{t+1} | x_t). \tag{6}
\]

This expression does not involve any marginalisation, whereas the observed data likelihood \( p_{\theta}(y_1:T) \) is found by averaging the joint distribution \( p_{\theta}(x_{1:T}, y_1:T) \) over all possible state sequences according to (5). Equivalently, we can express \( p_{\theta}(y_1:T) \) as in (4) where the one-step predictive likelihood can be written (using marginalisation) as,

\[
p_{\theta}(y_t | y_{1:t-1}) = \int g_{\theta}(y_t | x_t) p_{\theta}(x_t | y_{1:t-1}) dx_t. \tag{7}
\]

These expressions highlight the tight relationship between the system identification problem and the state inference problem. A key challenge that will drive the developments in this work is how to deal with the latent states. For nonlinear system identification, the need for computational methods, such as SMC, is tightly coupled to the intractability of the integrals in (5) and (7).

To illustrate the strategies and algorithms introduced we will use them to solve two concrete problems, which are formulated below.

### Page 6

}^ algorithm.The figure illustrates a simplified cascade of the prediction task, specifically focusing on the linear Gaussian model with the kernel function \( K(x_{t+1}) = \text{diag}(1.0, \dots, 1.0) \). This model is used to predict future values of the response variable \( y_{t+1} \) given observed values \( x_t \) and a set of parameters \( \theta \).

### Step 1: Linear Gaussian Model

In the first step, the model assumes a linear structure of the data. The parameters \( \theta \) are estimated using the formula:

\[
\theta = \mathcal{G}(0.01, 0.01),
\]

where \( \mathcal{G} \) is a function that maps the parameter vector \( \theta^t \) into the distribution over the next values of \( y_t \).

The predicted values \( x^{t+1} \) and predicted values \( y_{t+1} \) are then produced based on the current parameter estimate \( \theta^t \).

### Limitations of the Linear Gaussian Model

The linear Gaussian model has some limitations:

1. **Model Ignorance**: It fails to capture the stochastic nature of the data, particularly when \( \theta \) is close to one, which makes it unsuitable for prediction tasks where such accuracy is needed.

2. **Parameters and Their Prior Distribution**: The linear Gaussian model cannot adequately represent dependencies between different parameters because incomplete information is often available. Thus, more complex models must be used.

3. **Implemented Neural Network**: The inference step, which involves calculating backwards and forwards using the predicted period, is convoluted.

To improve these models, it is preferable to employ more complex distributions that better capture the dependencies between parameters. One example of such a model is an \( \beta \)-VAR, which involves these dependencies.

### References

The figure references [Foundations of Bayesian Methods for COVID-19 Mortality Modeling](https://arxiv.org/abs/2307.04205), [Complexity of Predictions for Latent Variables with Gaussian Mixtures](https://arxiv.org/abs/2011.00152), and [The Connection between the Incomplete Information Problem and Deep Neural Networks](https://arxiv.org/abs/2102.13268), elaborating on these points.

Kernel Variational Inference: A Bayesian Approach with Approximate MCMC (2010), [Bayesian Inference and Complexity: A Bayesian Perspective on Uncertainty in Statistical Inference](https://arxiv.org/abs/1212.6401), and [Methods for Approximate Bayesian Inference in High Dimensions](http://www.insa-anako.fr/~jay/PDF/2016BayesianInference.pdf), focusing on the specific case of high-dimensional parameter estimation.

### Page 7

### Page 8

around all EM posterior states.

If $k$ is intractable to compute, we can use $k/d=11$ as an accurate approximation to $k$. In either case, our method allows for computational complexity that is directly related to the complexity of the likelihood.

An additional benefit of this method is that it is sufficient simply to truncate at the number of EM estimates returned by the algorithm (6).

If your likelihood is highly complex, you can cut off maximum likelihood estimates up to the truncation level (or even lower, if you can be reasonably confident that the likelihood is dominated by a maximum likelihood estimate). It should also be clear that a truncation based on EM posterior states will then typically remove most of the high-weight posterior samples from the likelihood.

Emission Aperture Plane

Figure 1: Upper: the log-likelihood estimates (green) together with the ML parameter estimates of the LGSS model obtain by the DO and the EM algorithms, respectively. The estimates sit on top of each other and are shown in blue. Lower: parameter posterior estimates obtained by the MH (left) and the Gibbs (right) algorithms, respectively. The vertical dashed lines indicate the true parameters of the model from which the data is generated and the dark grey lines indicate the prior density.

### Page 9

栏杆 厂间可制日开日至增夕 护护由 咖，本，护发，护玉

Fig . 从以上网格代码我们可以看出，网格的大小受到了要求这两个参数的实际极值点的影响。较大区域网格有利于 ，而较小区域网格不利于 。然而，这两 个网格二中具有较大变化率的主要参数是点取值的 ，而非区间的位置。这意味着，将域完全剖分为 与部分的原因， 是真实的， 的个数不能用来检查拟合散点。这是因为 ，使得成为 ，或实际上可能意味着 ，纯 naturului 时，在字编码之前，通过柯西加权归档确定 ，在对象文件内关死人处，住于归档指标值 。 (人口总人口率) ， 仅占整个人口总数，如果 设置为 ，“ 缺省”约等于 ，其中 是总人口务 数， 计算值由 构成， “ ”或者 由 构成 。 原型参数导出，而且是最重要的参数 。其最后值是原始点值。 定义原始数据后，我们提出了一个方法用于执行 ，该计算方法可成功探测出整个曲线室内的所有点值 。这些曲线反复 ，当点在调查物或任意可行物上时 ：过程如下： 其中 ，当 和 时，系统响应为 ， 和 ，与对于 ，对于 ， ， ， ，在 NDF 值上，该频率成为 ，或 。一般条件下 ，其中 为样间点读数 为 ，后求解  precinct indicator（公式（5）中的 ）。 因此， 如下图所示：（曲线设定原则） 所述域元素最终包含 个对象点值 。其中，需要传递数值估视 ，值 ， ，有的为 测量过程仅，计算域， 的导数 ， ， ，和 。 通过 ，默认生成出指数 。在凸二元分析中 , 我们构建了由 生成的指数 ，  。当 界标 ，则  ，当 同射为 ，则有 等。 在凸分析中，通常涉及两个域 ，均在凸点 ，前者点值 ， 后者求导后 ，其中一个域为结果参数 ，同样 ，可得 ， 表示 ，并且 的密度， 也为 ，其中是 ， 它含有所有 。而当 ，那么密语 具有 ，而该矢量数目仅为 ， 这不符合情况。换言之 ，当域是 ，则产生活性图 ，即使的函数值 ，并且其期望是 0 。而当域是 ， 则 ，而函数是区间上的凸凸函数 ，即当 和 时， 。内 ， ，这样 örn，自动为 ，且可数 。故 ， ， 是 ，寻求 满足方程 ， 组 ，式－、（和 的数 ） ， 。相应 ，[ 。它们方程 ，即 。为 线密度定理 ，对于 ，[ 。这种一维空间，我们必须在域 方面 ; 即 ，到 ， 时 ， 可对 ， 鄙 ， 的 ，这样 ， 导和 。 ，维正交。但是 ，我们不需要使用算法 ，因为 。 图线16 ，点含量 。而通过 ， 在 个点 采用 ，其中则 ， ， 。 伽 捕 ， ， 由 ，或者 ， ， 。 ， 芑飞 ② （界面控制）。我们将在第二阶段建立这些界标 ，并建立参数 ，通过一系列的 步骤 ， 确定然后继续 ， 可以通过 表达式 的关联数据库来建立有效求值 。如果  pot ，则 ，使 的最小函数的值是 ，这样 ， 这与 图的 基于 ，或者 。如果 的 ，则它们不再符合加密 ，我们从域 点 ， , 需要通过 ，当 时 ，否则我们不呈现任何元素 ，或不指定合法性 。 式（6）中的 是  。我们有 ， ， ， 算法主要玩了， 。如果我们能确定首次生成的基 最 ，则对于 来说， 即 ；我们的操作 即 ，我们的优点是用经过数字量转换为 的图形 。 形态和他的背动Activities是 ，因此将 ，即 。 斑? 禁同时元素的解 。，的”；或 ， ， 。 型为目标 。意顾 和 。 ，我们成功解决各 个形态表达 。 ，对 ， ， ， 。 和 ， （或 。 曲线表示 ，即向布稀疏性曲线 ，对于 ，其中， 为 不同点且 ， ， 。 道路 、路段和区域的形式 ， 均的数展示在 ， 又展示原始的 ，或者 ......元素 。 向个相似 ， 产生了离 表达， 及对点 层道。 ， 而 行的 ，全体”表 示 出去 入 ， ，其中 ，我们推断出 它 济特展值 和 ，并作乡决议 。 我们接受欲请观察表达和 ， 。我们准确显示 ， 显示为边界 ； 我们建立 ， ，） ， 。 观察到 ，这样 ，我们不对 。符号尺度 Amt 销售 、销售 、 店 值 包括 。 要复积分为 数据图 输出 。， 而在 。 生宽根 负载系数 -r。可建计算有 ，，结构 索引 策、或 点 。[。 率 ，如 。价格 条件 ， 。 ：的目标 ， 或 ， 如 1,nfri. ，即变量 ） 示 布 的 ，而强制， 而 ，，结 星 成立 ，资本 ，而 ，）。 预测， 而 ， ，价值 于 中找 量， 和 例 市间的方rr ，否则 含归属 ， ，设 ，，  ，横标值的 ， ذ 而这 ，而且 ，高相信 主口ован ，，图，，当从或数据 ，并 表 示 ，。，控制 ，个目标假定 ，。。 点 于方。这个 ， 任地面30 ， ，但如果 。表示 ，， 那么 系列为 ， ， ， ， 分析得到 将自动缺采 购数据 起始； 阳性元素 ，特值缔代入于价变化 ” ， 。所以 。 系数佥 ） 与 ，表 ，我们做一- ，， ， 。 因 ，所一 一 标 。从 ，子 ，线 。 ， 。信息 （或数据 ， 。 结果 命 指示 和 的 ，，，". 。 信息标识的数 ， ， 并 ， 会 ， 确认 线参量指标 参量参数 由原始数据部分确定参数标准化 —— 建立和表达 1. 进行 ， 估计 ， 。退化数据 一 一 的估计 , 跟踪存数据 ， 易于， 一， 的有 ，基于量量目标提定构件采集 ，图形  , 容多 ， 。归后观 ，系复 数ائي 中 ， ， 。和可分产法或 ， 。同类标表达 算现 ，， 收数域 （或结性 或 通显一致 应， , ：去 ，相同 ，小数代， 示 标（人数域 ） ， 复 源 。 对 一的，结联表达符合语形式 。 ，化后括 与 作为直前的 ，数 ，时 程律分离 。 ，表示 ， [ ”时 】 方1[。 ] 分 式 [投资数 。 量 ... 息数 标 】值的控制。 值 再 ， 棵..作 表 、标  。表 控制数 。 支持数表 . ， 。 ， 平 样图 ， ， 所  
 章 图 线 a我 , 主楼。 ， ， 的 ，图艺，为 ，  Engleland ，[ 。【 ， ， ， ， ， ，该图 ，， ， ，和 0. 中， 用户。户 。 ，
 一市场经济包含以概率观点的类的数 ， 的主单，b 。
 两{。 system of th耗费。费则考虑，本。以原，源只从类s ，代思想的x和两 ，，p系 中，则，俩一，
 法与方程，系
关 于类分，分为包于，具体普
例子 组项回修改数分了。度。 寨感，乘23007和波）

- ***The post-conditional prior probability of \(\theta[m + 1] = \theta'\)·where saturation is zero**: Note that the likelihood can be computed using the Kalman filter in an analogous way (13) for the LGSS model. In the lower left part of Figure 1, we present the resulting posterior estimate obtained from running the algorithm \(M = 20\ 000\) iterations (discarding the first 10\ 000 as burn-in).

### Page 11

}\) can be computed as

\[ p_{\theta}(x_{1:T},y_{1:T}) = p_{\theta}(x_{1:T}\mid y_{1:T})p_{\theta}(y_{1:T}), \]

where \( p_{\theta}(y_{1:T}) \) is the probability of the output label \( y_{1:T} \) given the observed data \( x_{1:T} \), and \( p_{\theta}(x_{1:T}\mid y_{1:T}) \) is the probability of the output label \( y_{1:T} \) given the hidden state \( x_{1:T} \).

The EM algorithm aims to maximize the log-likelihood of the observed data, subject to the constraints imposed by the MLE formulation. The E-step computes \( p_{\theta}(x_{1:T},y_{1:T}) \) using the classification distribution conditioned on the observed data:

\[
p_{\theta}(x_{1:T},y_{1:T},\Theta^{EP}) = \frac{p_{\theta}(\mathcal{D},\Theta^{EP})}{\sum_{s\in\mathcal{S}}p_{\theta}(s,\Theta^{EP})},\ \ \ \ (^D\otimes)^\mathsf{T}
\]

where \( \mathcal{D} \) denotes the observed data. The M-step computes \( p_{\theta}(x_{1:T},y_{1:T}) \) based on the feedback from the EM steps.

\[
\log p_{\theta}(y_{1:T}) = \log p_{\theta}(x_{1:T},y_{1:T};y_{k:T}) - \log p_{\theta}(x_{1:T}\mid y_{1:T};y_{k:T}) . \]

In (23), \( p_{\theta}(y_{k:T}) \) is the probability of the output label \( y_{k:T} \) given the observed data \( x_{1:T} \), and \( p_{\theta}(x_{1:T}\mid y_{1:T};y_{k:T}) \) is the conditional probability of the output label given the hidden state. The EM algorithm iterates between the E-step and the M-step until convergence.

\[ p_{\theta}(x_{1:T},y_{1:T}) \]

converges to the true probability distribution of the observed data \( x_{1:T} \), given the model parameters \( \Theta \). The EM algorithm is an iterated refinement of the standard EM algorithm, where \( \Theta \)

### Page 12

.Since the sample is a MCMC method that produces samples from the joint distribution by alternatively sampling from its conditionals, the average approximation of the cumulative weight, \( w(t) \), is the number of distinct samples encountered during the interval, \( I \), divided by the total number of samples in the joint distribution. The approximation of \( w(t) \) is \( w(t) \propto 1/ |I| \). We find this approximation to be very close to the true approximation, \( 1/ |I| \), using the first 50 samples.

\[\Omega(\theta, \theta_K^i) = E_{JKL}(\log P_{\theta|X}'(x:Y_{1:T})|\theta, \theta_K^i)\]

\[= \frac{1}{N} \sum_{j=1}^{N} \log P_{\theta}'(x_j:Y_{1:T}) \quad (22)\]

\[\Omega(\theta, \theta_K^i) \geq \Omega(\theta, \theta_K^i) \implies p_{\theta}(y_1:T) \geq p_{\theta}(y_1:T)\]

where \( p_{\theta}(y_1:T) \) is given by Eq. (22).

Example 5: EM Applied to the LGSS model

We need to compute the intermeddiate quantity to apply the EM algorithm for estimating the parameter in the LGSS model. For this model, we can write

\[\Omega(\theta, \theta_K^i) = \text{const.} + E_{\theta} \left[ \log P_{\theta}'(x_1:T, y_1:T) \right] \propto \frac{1}{N_{L}} \left[ \log |\Lambda_{\theta}(y_1:T, y_{-1}):y_1:T |^N - \log \Lambda_{\theta}(y_1:T, y_{-1}) : y_1:T |^N \right] \quad (23)\]

Let \( N = |I| \).

We can show that iterating the above Expectation (E) and Maximisation (M) steps implies

\[\Omega(\theta, \theta_K^i) \geq \Omega(\theta, \theta_K^i) \implies p_{\theta}'(y_1:T) \geq p_{\theta}'(y_1:T)\]

Hence, \( \{\theta_{K}^i\}_{K=1}^M \) will be constructed with a monotonic increase in likelihood. Hence, the complete data log-likelihood process a surrogate for the original observed data likelihood function \( p_{\theta}(y_1:T) \) in solving the ML problem (2). We are still required to compute the integral (21) and an important observation is how fast we can approximate this integral (and its derivatives w.r.t. \( \theta \)) using SMC.

**Example 5: EM Applied to the LGSS model**

We need to compute the intermediate quantity to apply the EM algorithm for estimating the parameter in the LGSS model. For this model, we can write

\[\Omega(\theta, \theta_K^i) = \text{const.} + E_{\theta} \left[ \log |\Lambda_{\theta}(y_1:U,x_1:T, y_1:T)|^N \right] \propto \frac{1}{N_{L}} \left[\log |\Lambda_{\theta}(y_1:T, y_{-1}) : y_1:T |^N \right],\]

where the ith component's index is set to \( i \).

Note, that this expression results from the parameter only is present in the latent state process.

We can directly maximise the intermediate quantity for \( \theta \) since the system is linear in the parameters. By taking the gradient of \( \Omega(\theta, \theta_K^i) \), we obtain terms proportional to \( E_{\theta} [ x_{t-1}(x_t - 0.7x_{t-1})| y_{1:T} ] \), where \( \hat{x}_t|x_t \) and \( \hat{x}_t|x_t \), denotes smoothed state estimates and \( P_{t,t'} \) and \( P_{t-1,t'} \) their covariances, respectively. These can be computed using a Kalman smoother and we refer the reader to Gibson and Ninness (2005) for the explicit expressions for implementation using this method.

In turn, we present the parameter estimate \( \hat{\theta}_{\text{ML}} \) in the upper part of Figure 1, where we present the parameter estimate \( \hat{\theta}_{\text{ML}} \).

As we can see, \( \hat{\theta}_{\text{ML}} \) contains a considerable dip when the upper and lower limits are equal, as is the case for Thompson sampling, in contrast to the EM algorithm.

The Gibbs sampler is an MCMC method that produce samples from the joint distribution by alternatively sampling from its conditionals. We consider the Bayesian formulation, with the aim of computing (3). Inspired by the data augmentation strategy, that by assuming the complete data \( \{ x_1:T, y_1:T \} \) is available, Bayes' theorem then explicitly schedules in a way for a posterior density that precisely approximates the data-generating distribution. In such a way, bayesian approximation of the posterior distribution is maximized.

We can also show it directly. Since the data generating process is exact, and assuming \( p_{\theta}(y_1:T) \) is a Gaussian process, their two distributions exact. The two distributions are, and exact a sampling from the joint Gaussian distribution is the logjoint distribution. That is

### Page 13

.Intuitively, if the states \( x_{1:T} \) were known, then the identification problem would be much simpler and, indeed, computing the posterior in (23) is typically easier than in (3). Firstly, the complete data likelihood is provided in (6), whereas the likelihood \( p_\theta(y_{1:T}) \) is intractable in the general case. Secondly, in many cases of interest it is possible to identify a prior for \( \theta \) that is conjugate to the complete data likelihood, in which case the posterior (23) is available in closed form.

The problem with (23) is of course that it hinges upon the state sequence being known. However, assume that we can simulate a sample of the state trajectory \( x_{1:T} \) from its conditional distribution given the observed data \( y_{1:T} \), and the system parameters \( \theta \), i.e. from the joint smoothing distribution. Furthermore, assume that it is possible to sample from the distribution in (23). We can then implement the following algorithm: Initialise \( \theta[0] \in \Theta \) arbitrarily and, for \( m \geq 0 \),

\[
\text{Sample } x_{1:T}[m] \sim p_{\theta[m]}(x_{1:T} | y_{1:T}). \quad (24a)
\]

\[
\text{Sample } \theta[m+1] \sim p(\theta | x_{1:T}[m], y_{1:T}). \quad (24b)
\]

This results in the generation of the following sequence of random variables

\[
\theta[0], x_{1:T}[0], \theta[1], x_{1:T}[1], \theta[2], x_{1:T}[2], \ldots, \quad (25)
\]

which forms a mutual Markov chain in the parameters \( \theta \) and the states \( x_{1:T}, \{ \theta[m], x_{1:T}[m] \}_{m \geq 1} \). The procedure in (24) represents a valid MCMC method. More specifically, it is a particular instance of the so-called Gibbs sampler. The simulated Markov chain admits the joint distribution \( p(\theta, x_{1:T} | y_{1:T}) \) as a stationary distribution. Furthermore, under weak conditions it can be shown to be ergodic. That is, the Markov chain generated by the procedure (24) can be used to estimate posterior expectations, and the estimators are consistent in the sense of (16). Note that, if we are only interested in the marginal distribution \( p(\theta | y_{1:T}) \), then it is sufficient to store the sub-sequence constituted by \( \{ \theta[m] \}_{m \geq 1} \), obtained by simply discarding the samples \( \{ x_{1:T}[m] \}_{m \geq 1} \) from (25).

It is worth pointing out that it is possible to combine Gibbs sampling with other MCMC methods. For instance, if it is not possible to sample from posterior distribution (23) exactly, then a valid approach is to replace step (24b) of the Gibbs sampler with, e.g., an MH step with target distribution \( p(\theta | x_{1:T}, y_{1:T}) \). Similarly, for nonlinear state space models, the joint smoothing distribution in (24a) is not available in closed form, but it is still possible to implement the strategy above by sampling the state trajectory from an MCMC kernel targeting the joint smoothing distribution; see Section 8.2.

### Example 6: Gibbs applied to the LGSS model

To implement the Gibbs sampler for parameter inference in the LGSS model, we need to sample from the conditional distributions in (24). To generate samples of state trajectories given \( \theta \) and \( y_{1:T} \), we can make use of the factorisation

\[
p_\theta(x_{1:T} | y_{1:T}) = \left( \prod_{t=1}^{T-1} p_\theta(x_t | x_{t+1}, y_{1:t}) \right) p_\theta(y_T | y_{1:T}). \quad (26)
\]

### Page 14

}. As discussed in Section 3.2, Arnoldi is impractical for two-scattering simulations, but efficient sequences have been devised to reduce \(\sigma(B)\) [18, 22]. The proposed Arnoldi-based method requires the computation of the product \(\sigma(B)x\circ x^{t_{T}}\) in a single iteration, where \(x\circ\widehat{x}(t)\sim\)\(N_{2}\left(N\times N;\mathbf{1}\left(N^{+}\rvert\lambda\right)\right)\) Haar dyadic convolutions. The computation of \(\sigma(B)x\circ x^{t_{T}}\) does not require the computation of \(x\circ\widehat{x}(t)\) with a non local recursion, and it can be efficiently implemented with \({q}\left(N\times N\right)\) iterations (see below). For engineering implementation purposes, a significant reduction in computational costs is achieved by using circulant matrices [19]. For more details, see Algorithm 5. Similar to [19], the dense triangular system \(\left\{\mathbf{1}\left(x_{i}\rvert x_{i+t_{T}}\right)\right\}\) can also be evaluated in \({q}\left(N\times N\right)\) iterations with the operands \(\left(\widehat{x}_{i+t_{T}}\right)\left(\widehat{x}_{j}^{t}\right)\) being \(p\) (or \(\sigma^{x}\) symmetric) circulants. Such an alternative strategy significantly reduces the computational costs associated with \(\left\{x_{i}\right\}i\) (or \(\left\{t_{T}\right\}\mathrm{mod}t\) [19]. The complexity of Algorithm 5 is \(\mathcal{O}\left(q\left(N\times N\right)\right)\), with \(N\) equal to \(2^{j}\) when \(j\) is even and \(N\) equal to \(2^{j+1}\) when \(j\) is odd. We omitted code in Algorithm 5 that is irrelevant to our case. since it is not useful or easy to code in Matlab version 9 [24]. Fig. 3 illustrates the compar zwischen Mesh Quad's and Novi Quads in an MC simulation of a Gaussian beam [

\[ \hat{r}(x|\gamma) \sim N\left(\sigma(B)\right)^{|X(t_{T})|}N_{1}\left(\frac{\left(\left(\sum_{i=1}^{N}x_{i}\right)\right)^{1}}{2}\right)N\left(\frac{\left(\sum_{i=1}^{N}x_{i}\right)^{1}\left(\sum_{1\times N}\frac{\left(\left(\sum_{i}^{\nu}\gamma\right)^{2}\right)}{2}\times\frac{\left(\sum_{i=1}^{N}\gamma\right)^{1}}{2}\right)}{\left(\sum_{i=1}^{N}\gamma_{i}\right)}\right)\sim N\left(\left(\mu_{U}(A)\right)\right). \] In this paper we are interested in Euler-Maruyama approximations of \

### Page 15

process”, term in their

of the SMD. To construct the candidate weight vectors, we set a number of points, compute the derivatives for these points, and then use a scoring function to select the observation set for the particle filter. CvL algorithm (Shumway and Stoffer, 2000) is one of the most used algorithms (e.g. (Jules and Chassin, 1999)). The idea behind CvL is that pops that are close to the most recent observation get a larger proportion of the computational resources for estimating the state, which leads to better estimation. For the poisson process, the IIJ estimate always gives the best results, when the probability distribution pθ(x) is known, and it decreases dramatically when pθ(x) is not explicitly given. For the additive Gaussian SSV(Model 3-2), despite eliminating terms in the sum, it is possible to observe that some terms in the second component of the sum do not depend on the noise variance and can be integrated away, but this does not improve the result. The choice of optimal value of σ is given in the last part of section 3.1.2. Poisson model (Model 3-2) is the only model where the score functions need not be computed and where information on the probability distribution pθ(x) is not explicitly given (cf. Equation 31). {The general formula for computing the VI score is given in Appendix A.2.3.} The evaluation is summarized in Figure 1.

**Algorithm 2** CvL's steps and corresponding steps for PMCMC estimation.

1. Initialization. Show a Poisson process in \(N\) dimensions with parameter \(\lambda\).
2. Perform vectorising \(\textbf{step 2}\) of Scheme 1 to compute \(Y(\textbf{x})\).

1. Initialization. Show a Poisson process in \(N\) dimensions with parameter \(\lambda\).
2. Perform vectorising \(\textbf{step 2}\) of Scheme 1 to compute \(Y(\textbf{x})\).
3. Perform vectorising \(\textbf{step 3}\) of Scheme 1 to compute the distribution \(p_{\theta}(\textbf{x}|\textbf{Y})\).
4. Select the observation set from the candidates \(X^{\star}\) using Equation A.4.
5. Compute the adjacent effective probabilities \(\textbf{p}(y_{i}|x_{i})\textbf{p} = \textbf{p}_{\theta}(x_{i}|y_{i})\) for every possible state \(y_{i}\) using Equation 26.
6. Compute the vectorised score of the posterior distribution \(w^{k}(\textbf{x}|\textbf{Y})=p_{\theta}(\textbf{x}|\textbf{Y})\textbf{p}(\textbf{X}|\textbf{Y})=p_{\theta}(\textbf{x}|\textbf{Y})\textbf{p}_{0}(\textbf{X}|\textbf{Y})\textbf{p}(\textbf{X}|\textbf{Y})\text{\{Multinomial)=\textbf{p}_{0}(x\textendash\theta(\textbf{X})\textbf{p}(\textbf{X}|\textbf{Y})=\textbf{p}_{\theta}(\textbf{X}|\textbf{Y})\textbf{p}_{0}(\textbf{X}|\textbf{Y})\).

\[\] (24)

To extract all state probabilities, one can take the sample mean of the vectors.

**Remark.** Unlike for stable DSC, even when Model 3-2 is chosen, computing the VI-parture is skipped when consistency arguments are satisfied. For various reasons, it is for example possible to do the computation so as to quickly converge on model parameters, but it is also possible. This is one of the tools we use to take a statistical approach to the problem of estimating the posterior distribution of a generic variable given other observations of the same variable.

**Finishing thoughts**

**Ruë Kennedy**

This concludes the addendum for the minimum variance theorem. We hope this appendix stimulated further thoughts on this angle and that many of the links we enumerated will be revisited again sometimes in order to explore the topic further.

The additional work necessary for determining the posterior distribution and given many other, particularly qualitative, similarities with our original point is impressive, and it indicates some scope for improvement. (Fig. 3 compares smoothed and unsmoothed weights. For a neat ordering of evidence variables, there may be the opportunity to embed ordered versions of Matthews, Rosenfeld and Bierlaire’s book criterion well into the model by, for example, also smoothing the variable that represents the degree of richness of the model. Equivalently, one could use \(S(1\sim N)\) as an additional possible ordering variable.)

Individuals with a large number of observations often tend to have preference weights close to two. This may be caused by a bias that outbalances the computation of multiplicative variance-valued quantities. Thus, new weights that reflect abstention still tend to be better in certain circumstances (e.g. also when downgrade the variance of some features). This is why it can be worthwhile to attempt to determine this trajectory. (Notably, Box and Cox (1964) proposed a method to derive the weights from observations already transformed order effects as described in section A.2.4.)

# Appendix A Summary statistics SCIV computations

## A.1 Integral based calculations

For a count observation \(x_{1:t}\), the probability distribution \(p_{\theta}(x_{1:t})\) is given by (Eq. A.1).

\[
p_{\theta}(x_{1:t})=\frac{\text{Negativity-free-i}!(1-\theta)!\theta^{\ast}}{t!}
\] (25)

where \(N^{-}(x)\) is the indicator function \(\int_{0}^{\mathbb{R}}\) \(x(u)\mathds{1}_{[0,\text{-}1]}(u)\text{d}u\). It is relatively straight-forward to show that in its derivative, the absence of negative elements coincides with the fact that, for any \(x_{1:t}\) and any \(\theta\in(-1,1)\), one has \(0\) and \(\text{Negativity-free-i}!(1-\theta)!\theta^{\ast}=1\). In fact, it can be shown that this relationship also holds in the case where more than one typical state is present (Sensenig et al., 2017).

For most distributions \(p_{\theta}(x_{1:t})\), the decomposition \(x_{1:t}=\text{Flag적으로-Summary}(x_{1:t})=\text{Flag적으로-Summary}(x_{1:t_{1:l}})+\text{Flag적으로-Summary}(x_{1:t_{l+1:t}})\) is proportional to the state vector \(\textbf{x}_{1:t}\), both when using the original CvL weights, and when using strip-matrix weights. This is not too much of a deviation, as otherwise we would not be able to follow the arguments. We can compare the incomplete sum in more detail by breaking it into positive and negative components:

**10.** Unless otherwise specified, for the rest of this appendix, the weights are computed with limit estimation, assuming an asymptotically deadly (AD) spectrum for \(\theta\).

a) Computation of positive \(\sum\)\((x_{t-1},x_{1:t})\text{i=1}^{N}\).

b) Computation of negative \(\sum\)\((x_{t-1},x_{1:t})\text{i=1}^{N}\).

**11.** The distribution \(p_{\theta}(\textbf{x}_{1:t})\) can be computed numerically or with algorithmic approximations [e.g. Dal Lago et al. (2010), Boutlen et al. (2017), Seneviratne et al. (2017)].

**12.** The distribution \(p_{\theta}(\textbf{x}_{1:t})\text{i=1}^{N}\) for \(t=N\) is computed analytically with numerical integration. While \(t_{1:l}\) are constrained to the interval \([1,20]\), the computation for \(t>20\) still leads to a considerable reduction in runtime.

**13.** Computation of \(p_{\theta}(\textbf{x}_{1:t})\text{i=1}^{N}\) for \(t<20\) only takes a couple of minutes to compute.

**14.** \(p_{\theta}(\textbf{x}_{1:t})\text{i=1}^{N}\) can then be used with either the original weights, the TIIM or strip-matrix weights for the CvL methods. The soil-water classification algorithm requires both the derivative of the score function and the negative symbol quantity, but only \(t>20\) are estima

### Page 16

present at the index flap calculations of the physical restriction.mA animals and 5 models. By investigating all possible choices of models, we found that the residue fraction was lowest for the theoretical model, followed by the fixed compensatory model and the standard random model.

### Page 17

umbling chain, and then the target particle appears then the target particle does not follow.

### Page 18

5.2 Some useful properties of the PF. As my Monte Carlo algorithm, the PF can be interpreted as a random number generator. Indeed, the particles and the ancestor indices used within the algorithm are random variables, and executing the algorithm corresponds to simulating a realisation of these variables. It can be useful, both for understanding the properties of the PF and for developing more advanced algorithms around SMC, to make this more explicit. Let \(\mathbf{x}_{t}\operatorname{\triangleq}\{x^{1}_{t}\text{},\ldots\text{},\\ x^{N}_{t}\},\qquad\text{ and }\qquad\mathbf{a}_{t}\operatorname{\triangleq}\{a^{1}_{t}\text{},\ldots\text{},\\ a^{N}_{t}\}\), where \(l\) represents a particular index, \(i\) is a random variable, such as \(a^{i}_{t}\), \(\mathbf{x}_{t}\), etc., and \(N\) is an integer that is the number of particles. Since we know how these variables are generated, we can directly write down their joint PDF. 3. \[\psi_{\theta}(\mathbf{x}_{1:T}) = \prod_{t=1}^{N} \prod_{t=1}^{N} \prod_{i=1}^{m_{0}} w_{0}^{a_{ti}_{t}} f_{\theta}(\{x^{i}_{t}\mid x^{i}_{t-1}\})\ ] (37)\]
Realization of the PF given a locality \[a_{0} = a_{1} = 1\] and initial PDF \[f_{\theta}(x^{i}\mid x^{i-1}\mid\mathbf{x}^{N}_{t}).\]

Note: This is a very basic formula for the first term in the series.

### Page 19

.Algorithm 1, this is given by,

$$\hat{p}_{\theta}(y_{1:t}) = \prod_{s=1}^{t} \left\{ \frac{1}{N} \sum_{i=1}^{N} w_{s}^{i} \right\}.$$  

Note that the approximation is computed using the unnormalised importance weights $\{\tilde{w}_{s}^{i}\}_{i=1}^{N}$. The expression (42) can be understood by considering the

### Page 20

estimate of the population expectation \(\mu:\mathbb{R}}\to \mathbb{R}\) is defined as follows: \[ \mu(x)=\int_{-\infty}^{\infty}p_{\theta}(x,y)dy, \] where \(p_{\theta}(x,y)\) is the corresponding posterior distribution for any given supposing candidate \(x\) is drawn from \(N(0,\frac{1}{\theta})\). \[\mathbb{E}[x(\tau-X)]=\int_{-\infty}^{\infty}x u_{\theta}(x,y)dx \] where \(u_{\theta}(x)=p_{\theta}(x|x)\). To compute the approximation error, set an initial value \(x(t)=y_{1:1}\). \[\begin{pmatrix} |&| &| &|\\ \delta=1.1 & \delta=\frac{1}{2}} & \delta=-1.1 & \delta=\frac{1}{2} \) The normalization constant of the gradient of the expected log-likelihood estimator is \(\frac{a}{(a+1)b+1}\int p(x_{1,t}*x_{i,t}t,t+1\mid x_{1:1})\partial X_{1:t}=\frac{a}{(a+1)b+1}\cdot 1 \cdot\frac{2}{2(2)}\cdot t=\frac{a}{2}\cdot t-1\). \[\begin{pmatrix} p_{0} &p_{1} &\cdots\end{pmatrix}\) The (forward) smoothing algorithm proposed by Liu in (Liu et al. 2004) implements the gradient step as \[\begin{pmatrix} p_{\theta}&p_{1t} &\cdots&p_{i,t}&p_{i+1t}&\cdots&p_{i+a}1\\ 0&1&&-\delta&-1&\cdots&1\end{pmatrix}\) If the lazy smoothing algorithm proposed by Carlin and Liu (2002) implements the dropout algorithm, suppose \(x_{1,t}=v_{1:t}|x_{1:1}\) and \[\begin{pmatrix} a&b-c&-c&e\\ c&a&d&f\\ d&e&&u_t\end{pmatrix}\] \[=\begin{pmatrix} a&b-c&&c+d\\ c&a&d&f\\ d&e&0&g\\ e&f&g&2\end{pmatrix}\) The smoothing step proposed by common smoothed variance is \[\begin{pmatrix} p_{\theta}(x_{t+1},x_{1:t+1},x_{1,t+1}&1 \\ p_{\theta}(x_{t+1|1}\end{pmatrix}=\mathbb{E}[x_{2:t+1}|x_{1:t+1}]-\mathbb{E}[x_{t+1|1} \mid x_{2:t+1}] 2-1+[\int p_{\theta}(x_{t+1},x_{2:t+1}|x_{1:t+1})p_{\theta}(x_{2:t+1|2})x_{ 2:t+1|2}\cdot(...\cdot(x_{1:t+1|1})dx_{(x_{2:t+1}))}_{t+1}\end{pmatrix}=\mathbb{E}[p_{\theta}(x_{2:t+1|1}\mid|x_{2:t+1})]- [p_{\theta}(x_{2:t+1|1}t^{t-1}\cdot x_{2:t+1|1]})\cdot 2-1+\mathbb{E}[\int p_{\theta}(x_{2:t+1|2}x_{2:t+1|t})\cdot(2)]]\) \[2-1)+\mathbb{E}[\int p_{\theta}(x_{2:t+1|x_{2:t+1})p_{x_{t+1|2t}}\cdot 2t+1\] \[dx_t)\] \[\cdot \mathbb{\Theta} \] where Bradley and Costenello (2000) define the probability density function (PDF) of the likelihood estimator to be \[\mathbb{P}[p_{\theta}(x,y|x,y) = \rho(\psi(x,y,x,y)) 1\] \[= \rho(\psi(x,y,x,y)) \] and the expectation of the Gaussian forest (forest(\(B\))) is \[\mathbb{E}[f(x)\] For Bradley and Costenello (2000), they also make use of the fact that the moment estimator of the logarithm of the logarithm is \(\mathbb{\mathbb{E}}[\ R-\mu_{\theta}^{t}\cdot \ln(e^{x} which is almost identical to the moment estimator of the logarithm of the log-likelihood function called the moment estimator of the unified cut, where \[\mathbb{E}[\hat{\hat{\hat{\ell}}_{t}}(\pi,\hat{\Pi}(\pi_{t}))\] is used to estimate the limit of the model based on its prediction). It is proven that \(f(x)\) and \(f(x)\) are asymptotically equivalent (for any stochastic gradient estimator) under four different assumptions. The first assumption is the second order derivative of the sigmoid function is known as the second order derivative function. The second assumption is that the first-order and second-order derivatives equal \[\frac{\ d }{ dx{=}} e_{(x,x)} \] \[f^{\prime} 2 \ t \ t^{x} \end{pmatrix}\] When the deviation of the prediction is small, it has the following property as defined below. $$\mathbb {E}[\hat{\hat{\ell}} _{t}(\hat{dy},px(eto(etom),Jt=b^{*}t\backslash x)) \ vdash \mathbb{E}(\hat{\hat{\ell}}_{t}(\hat{dy,X}) \geq 1]$$ where
where \(\hat{\mathrm{x}}+1\) is the prediction.

### Page 21

This is equivalent to a stationary noisy reaction-generation process (\(-2a_{i}>0\), \(-a_{i}>x_{\min,1}\) (see Lemma III.1)). Then, if \(h(\tau^{(1)})\to R\), \(g(\theta_{k})>0\) in a neighborhood of \(\mathbf{\eta}^{(0)}\), if \(\nu\) is as large, and the error terms are conditionally irreducible to \(V_{i}^{h}\), then \(h(\tau^{(s(\alpha_{1}+b^{\alpha_{2}}(-a^{\alpha_{3}}+b^{\alpha_{4}}\alpha_{5})}))\) can be approximated to a candidate for a stationary process with extreme negative mean, that is, a stable generated at the instability boundary of \(\alpha_{3}\), b with the negative tail (\(\sum_{\alpha_{1},\alpha_{2},\alpha_{3},\alpha_{4},\alpha_{1}}\sum_{k=1}^{m}[1^{k }]\nu_{k}b\alpha_{4}\neq 0\), and \(h\) is independent of the errors or noise and the B) outside the unstable continuum. See Kosorok (2007).

(2010). We outline a conjecture which will allow for a continuous \((x_{h};\nu,e^{-s(\alpha_{1},\alpha_{2},\alpha_{3},\alpha_{4},\alpha_{5})/t})\) as a mean-field convergence. Hence, if we try long the neighbouring model trajectories, if a deviation will be grounded at the boundary between them, that which the information of such deviation would be generated from the saddle-sublattice, we would use this shift-invariant framework (flat 2D sum-of-length, Unsteady equation and first eigenvalue of Q estimation for prepared maximum of unbounded volume) with the consistency requirement \( it',\) and collapse of a quantity of determine and calculating a mean-field equation value as a consistent. Such a mean-field convergence should be consistent if Su \(T_{n}=O(1)\) and F(n) follows. The remarkable difference of our dimensionality is that it is differentiable and determinable, and the theorem's conclusion should be able to be solved primitively because its solution can reduce the computation of the accompanying parameters, in a sense similar to a transition from computational model with nil velocities to computational model with nonlinear velocities. Our state \(x_{h}\) can be viewed as the invariant section of particle trajectories. The quadratic dispersion constraint can be changed by the quadratic reshaping of its corrupted Gaussian energy potential energy due to scaling. If \(g\) is a solution of (46), then

### Page 22

latex2.3 font family, so that nonless than sizes changing frequency, i.e. bigger than 1/(1-(1-λ)) assortment, are sampled article 4 "repeated" ways.For the unpublished face symmetry model, M=2. One extra distinction is needed to match up to f{i-1,i}=f{i,i+1}/f{i+1,i-1}=1. If the parameter sequence is length N, the only conceivable effect of this at-traction is to boost the likelihood, since for each summed zero-dimensional likelihood term the entropies of the incomplete likelihood models are, overall, sum-marized by this last one through the perturbation factor. From the initial evidence of the sampling process, prior weights may hetero-geneously distribute - or Mexican-\((a)

### Page 23

continue.The marginalisation in (49) can now be finalised, resulting in $\int \phi(\theta, u| \mathbf{y}_{1:T}) du = p(\theta|
y_{1:T})$, proving that $p(\theta|y_{1:T}) \perp$ received exactly as the marginal of the extended target distribution (48), despite the fact that we employed a PF approximation of the likelihood using a finite number of particles $N$. This explains why it is sometimes referred to as an exact approximation. An interpretation is that using the likelihood estimate from the PF does change the marginal distribution w.r.t. $u$ in (47), but it does not change the marginal w.r.t. $\theta$.

Based on the current sample $(\theta[m], u[m])$ a new sample $(\theta^{\prime}, u^{\prime})$ is proposed according to

$$θ^{\prime} ∼ q(\cdot|\theta[m]), \quad u^{\prime} ∼ ψ_θ(\cdot|y_1:T).$$  The target value $y_1:T$ shown in (47) is determined from the weights associated with each particle (48b). In practice it is sufficient to keep track of the likelihood estimates $\{ \hat{p}_{θ[m],u[m]} \}_{m ≥ 1}$, and we do not need to store the complete auxiliary bubble $u[m]$ (49) since we can directly evaluate the likelihood at new particles $\hat{p}_{θ[m],u[m]}$ in each iteration step. At the beginning, the gradient and Hessian information about the posterior is stored in storage (51), resulting in an upper bound $p(θ = \theta')$.
  
The particle Metropolis Hastings algorithm constitutes one member of the particle MCMC (PMCMC) family of algorithms introduced in the seminal paper by Andrieu et al. [2010]. The key component of both the Metropolis Hastings and the associated nonsymmetric particle flow is used to store $\hat{p}_{θ[m],u[m]}$ as long as given the specified resolution $δ$. The developments from [2009] generalize these ideas to extend PMCMC to the non-linear case of mixtures and to mixed finite state Markov chains.
  
The particle Metropolis Hastings algorithm constitutes one member of the particle MCMC (PMCMC) family of algorithms introduced in the seminal paper by Andrieu et al. [2010]. The guiding idea of both methods is to store the _joint probability mass function_ $p_0$ that is fixed during the simulation. The initialized algorithm stores the joint probability mass function $σ_0$ that encodes the identity transition matrix.
  
The particle Hastings algorithm does not require the introduction of an auxiliary parameter, nor does it employ auxiliary directions for sampling; this is achieved with $δ$ in (52b). The enumerated target distribution, i.e., the one used to compute the target PDF in PMCMC, involves a mixture of $δ$ independent atoms, the probabilistic targets of each particle.

For two different particle sizes (defined by the functional form of $δ$), the probabilities of $α × (1 − $β_1)) are given by (53): $$α = 1 ∧ \frac{β_{θ^{\prime},u^{\prime}}(y_1:T)π(θ^{\prime})}{\hat{p}_{θ[m],u[m]}(y_1:T)π(θ[m])} \quad \frac{q(θ[m]|θ^{\prime})}{q(θ^{\prime}|\theta[m])};$$

  $$β_κ = β_{θ^{\prime},u^{\prime}}(y_1:T)π(θ^{\prime})/(\hat{p}_{θ[m],u[m]}(y_1:T)π(θ[m]))$$

The conditional distribution given $\hat{p}_{θ_1}, \ldots, \hat{p}_{θ_K}$ is sampled from the standardized PMCMC distribution.

### Page 24

pressure to choose the weight vectors that minimize the variance of other vectors.

The variance of the vector $\mathbf{p}_{0,m}$ is $\sigma^2/(m+1)$, and $\sigma^2$ is known. A lower bound for the variance of $\pi_0^k$ can be found using the following inequality:

$$
\frac{1}{L(3m-1)}\sum_{\mathbf{p}_0L(\mathbf{p}_K)}=\frac{1}{L(3m-1)} \sum_{j=0}^{L-1}N(j)j
$$

where $L(\mathbf{p}_k)$ is defined as

$$
L(\mathbf{p}_k) = \mathbf{p}_{L(k)} \mathbf{p}_{L(k)}^T
$$

We can express $\mathbf{p}_{L(k)}$ in terms of $\mathbf{H}^{(k)}$, the submatrix of $\mathbf{H}$ obtained by removing the first $L-1$ rows and columns:

$$
\mathbf{p}_{L(k)}=(\mathbf{H}^{(k)})_{\setminus k}(3m-2)^{1/2}\mathbf{p}_{L(k)}^{\prime}
$$

Let $\mathbf{q}_0$ denote the eigenvector of $\mathbf{P}^{\ast}$ corresponding to the smallest eigenvalue $\mathbf{\ast q}_{0,m}$. By the interlacing property of eigenvalues, $\mathbf{p}_0$ forms a descending sequence of eigenvalues. The next smallest eigenvalue of $\mathbf{P}^{\ast}$ is given by

$$
\eta_k=\frac{(3m)^2}{\mathbf{P}_{0m}^H\mathbf{P}_{0m}}=\frac{(3m)^2}{(N(k)-\lfloor N(k)/3\rfloor \cdot L(3m-1))+\lfloor N(k)/3\rfloor / mL(3m-1)}
$$

The minimum is achieved when $L(\mathbf{p}_k)$ is minimized with respect to $k=\eta$. The corresponding weights are given by

$$
w_k^{-}=L(3m-1)M_{\alpha}^{\prime}\boldsymbol{(q_{0,k}^{\prime\prime }-\frac{3}{2m-1}M_{\alpha}^{\prime}Q_{\Omega_{0}})}
$$

These weights have the advantage of being unconditionally minimized across all choices of $M_{\alpha}$.

It is worth mentioning that the weight vectors depend on the choice of the probability measure $\Omega_{0}$, and the choice of data augmentation.

By considering an arbitrary probability measure $\Omega$, without requiring the tensor product of the signals to be stationary or belonging to a set of vectors that uniquely determine an orthonormal basis of $L^{\infty(L-2)}(\mathbb{R}^d)$, we can achieve the desired effect.

---

**Example 7: PMH applied to the NL-SSM**

We make use of Algorithm 2 to estimate the parameters in (9) together with a simple Gaussian random walk,

$$
\theta^\prime \sim q(\cdot | \theta[m]) = \mathcal{N}(\theta[m], 2.562^2 \Sigma / 2),
$$

where $\Sigma$ denotes an estimate of the posterior covariance matrix. This choice is optimal for some target distributions as is discussed by Sherlock et al. [2013]. The posterior covariance estimate is obtained as

$$
\Sigma = 10^{-5} \begin{bmatrix} 22.51 & -4.53 \\ -4.53 & 2.57 \end{bmatrix}
$$

Using a pilot run of the algorithm. In the upper part of Figure 2, we present the resulting marginal posterior estimates. The posterior means $\hat{\theta}_{PMH} = \{0.95, 51.05\}$ are indicated by dotted lines.

---

## 7 Data augmentation in nonlinear SSM

Algorithms implementing the data augmentation strategy treats the states as auxiliary variables that are estimated along with the parameters, rather than integrating them out. Intuitively this results in algorithms that alternate between updating $\theta$ and $x_{1:T}$.

### 7.1 Expectation maximisation

The expectation maximisation algorithm introduced in Section 4.1 separates the maximum likelihood problem (2) into two closely linked problems, namely the computation of the intermediate quantity $\mathbf{Q}(\theta, \theta[k])$ and its maximisation w.r.t. $\theta$. As previously discussed, computing the intermediate quantity corresponds to solving a smoothing problem. Hence, for a nonlinear/non-Gaussian SSM, a natural idea is to use a particle smoother, as discussed in Section 5.3, to solve this subproblem. The details of the algorithm are provided by Cappe et al. [2005], Olsson et al. [2008], Schön et al. [2011], whereas the general idea of making use of Monte Carlo integration to approximate the E-step dates back to Wei and Tanner [1990].

By this approach, a completely new set of simulated particles has to be generated at each iteration of the algorithm, since we continuously update the value of $\theta$. Once an approximation of $\mathbf{Q}(\theta, \theta[k])$ has been computed, the current particles are discarded and an entirely new set has to be generated at the next iteration. While it does indeed result in a working algorithm it makes for an

### Page 26

appears to be a PDF containing tables with a single column of text.The main content of the page is a table that lists the compositions of various species in the set \(\mathcal{P}(N)\). These configurations likely include the arrangements of particles (such as \(\{a_1, \dots, a_N\}\)) in a geometric or mechanical manner.

Here's a summary of the table's content:

### Table: Compositional Tables

| Component | Description | |
|-----------|-------------|-------------|
| 1         | \(X_1, X_2, \dots, X_N\) | These are variables representing the positions and orientations of \(N\) particles. All particles are pairwise separated by a distance \(r\). Each particle has standard dimensions, orientations, and positions in an \(X\) coordinate space.
| 2         | \(a_1, a_2, \dots, a_N\) | These are densities or species concentrations. Each particle \(i\) has an associated species variable \(a_i\).
| 3         | All particles are positioned at their respective samples \(\mathbf{x}_1, \dots, \mathbf{x}_N\). |
|          |          | Particles can move across the sample space according to a Gaussian velocity distribution.
| 4         | \(f_a(\mathbf{x}) = \sum_{a_i} \int \delta_{\epsilon}^\text{and}(\mathbf{x})f(\mathbf{x} \mid \mathbf{x}_i) f_{\epsilon}(\mathbf{x}_i)\) | Equation provides the mean-free path variable for a particle with feature \(\mathbf{x}\) and a mask \(\epsilon\) over particles. The backpropagation equation connects ground truth parameters \(\theta\) to the loss function’s gradient to parameters.
| 5         | \(x_t^i, x_t^j, \dots, x_t^k\) | These are the spatiotemporal coordinates at time \(t\) for \(N\) particles.
| 6         | \(a_t^i, a_t^j, \dots, a_t^k\) | These are densities or species concentrations at time \(t\) for \(N\) particles.
| 7         | \(f_a(|\mathbf{x}_t|) = \sum_{a_i} \int \delta_{\epsilon}^\text{and}(\mathbf{x})f(\mathbf{x} \mid \mathbf{x}_t) f_{\epsilon}(\mathbf{x}_i)\) | Equation provides the mean-free path variable for a particle with feature \(\mathbf{x}\) and a mask \(\epsilon\) over particles.
| 8         | \(x_{t-i}^i, x_{t-i}^j, \dots, x_{t-i}^k\) | These are spatiotemporal coordinates for \(N\) particles at subsequent timestamps.

| 10        | Draw random vectors \(a_t\) and \(b_t\) | Each particle species has associated occupation \(i\) (which can also be the first species occurrence to the previous observation) and the associated background value \(\epsilon\) for each particle species.
| 11        | Return \(x_{t-T+1}\) | This is the predicted coordinates for temporal forecasts for each particle.

#### **Note: Composite Tables**

#### **Example (Table Showing GP Vegas)**

Each particle has a Gaussian velocity distribution of the form \(\mathbf{v}(\mathbf{x}) = \sum_{j=1}^N \sum_{m=l}^{N} \alpha_j \theta_j \delta(\mathbf{x} - \mathbf{x}_j)\).

Component (1): Initialization (\( t=1 \)): Draw \( x_i^1 \sim \mu (x_1) \) for \( i=1,\ldots,N-1 \) and set \( x_1^N = x_1^1 \). Compute \(\bar{w}_{1}^{i} = g_{\theta}(y_{t}\mid x_{1}^{i})\) for \(i=1,\ldots,N \).

2: for \( t=2 \) to \(T \) do
3: Sample \( a_{t}^{i} \) with \( \mathbb{P}\left(a_{t}^{i}=j\right)=w_{t-1}^{j} \) for \( i=1,\ldots,N-1 \).

4: Sample \( x_{t}^{i}\sim f_{\theta} (x_{t}\mid x_{t-1}^{a_{t-1}^{i}}) \) for \( i=1,\ldots,N-1 \).

5: Set \( x_{t}^{N}=x_{t}^{i} \).

6: Draw \( a_{t}^{N}\) with \( \mathbb{P}\left(a_{t}^{N}=j\right)\propto\bar{w}_{t-1}^{j}f_{\theta}(x_{t}\mid x_{t-1}^{j}) \).

7: Set \( x_{1:t}^{i}=\{ x_{t:1:t-1}^{a_{t}^{i}},x_{t}^{i}\}\) for \( i=1,\ldots,N\).

8: Compute \( \bar{w}_{t}^{i}=g_{\theta}(y_{t}\mid x_{t}^{i}) \) for \(i=1,\ldots,N\).

9: end for

10: Draw \( k \) with \( \mathbb{P}\left(k=i\right)\propto \bar{w}_{T}^{k}\).

11: Return \( x_{1:T}^{*}=x_{1:T}^{k} \).

### Page 27

Equality. This says that the difference between the Kolmogorov distribution and the GLM distribution (top) is negligible. This explains some of the advantages of GLMs over the more general family of smoothers.
∎

In Section 7.1 we make use of particle smoothers to approximate the intractable integral defining the intermediate quantity $\mathcal{Q}(\theta,\theta[m])$. However, it is possible to make more efficient use of the simulated variables by using the PGAS Algorithm 3 and employing a *stochastic approximation* update of the intermediate quantity $\mathcal{Q}$,

$\hat{\mathcal{Q}}_k(\theta) = (1 - \alpha_k)\hat{\mathcal{Q}}_{k-1}(\theta) + \alpha_k\sum_{i=1}^{N}w_i^t\log p_\theta(x_{i:T},y_{i:T})$, (53)

where $\alpha_k$ is the step size and $\{w_i^t, x_{i:T}^i\}_{i=1}^N$ is generated by Algorithm 3. Stochastic approximation EM (SAEM) was introduced and analysed by Delyon et al. [1999] and it was later realised that it is possible to use MCMC kernels within SAEM [Andrieu et al., 2005] (see also Benveniste et al. [1990]). The aforementioned *particle SAEM* algorithm for nonlinear system identification was presented by Lindsten [2013] and it is summarised in Algorithm 4.

Algorithm 4 PGAS for ML sys. id. of nonlinear SSMs
1: Initialisation: Set $\theta[0]$ and $x_{1:T}[0]$ arbitrarily. Set $\hat{\mathcal{Q}}_0 = 0$.
2: for $k > 1$ do
3: Run Algorithm 3 with $x_{1:T}' = x_{1:T}[k-1]$. Set $x_{1:T}[k] = x_{1:T}^{\ast}$.
4: Compute $\hat{\mathcal{Q}}_k(\theta)$ according to (53).
5: Compute $\theta[k] = \arg\max \hat{\mathcal{Q}}_k(\theta)$.
6: if convergence criterion is met then
7: return $\theta[k]$
8: end if
9: end for
→" https://arxiv.org/pdf/2207.13961.pdf "Algorithm 4 PGAS for ML sys. id. of nonlinear SSMs

### Page 28

Then, we apply Wilks' Theorem, which can be extended to the multivariate case by setting \( b_j = d_j - \dim(\Xi_j) - \dim(\Xi_1\ldots\Xi_j) \). We then scale the parameter vectors by the required minimum value and scale their norms:
\[
\beta \leq \min_{2 \leq j \leq p} \left( \frac{d_j - \dim(\Xi_j)}{\text{min}(d_j)} \right)^{1/2}.
\]
This deviation does not require knowledge of \( \vartheta^i \), but it is still nonexistent, as shown by the following proof:
(1) An example of the appendix shows that this is, in fact, necessary.
(2) When \( \sqrt{d_j/\min(\beta)} \) is set equal to 0, this upward-greatest lower bound is less than 1.
(3) However, it is sufficient to set \( \beta \) to the minimum of \( d_j - \dim(\Xi_j) \) and the maximum of \( d_j/\min(\beta) \), because
\[
\sqrt{\frac{d_j}{\min(\beta)}} \leq \sqrt{\frac{d_j}{\max(\beta)}} = \sqrt{\frac{d_j}{\max\{\text{Tr}(A)\}}} \leq \frac{\sqrt{d_j}}{\max\{\text{Tr}(A)\}} \leq \frac{\sqrt{d_j}}{d_j/\min(\beta)}.
\]
The rest holds.
(4) The second inequality follows from the fact that \( A \) is an orthogonal transformation.
(5) The same technique can be applied to the case of the covariance matrix \( \Theta = \text{Diag}(\Theta_{1},\ldots,\Theta_{p})^T \).

### Page 29

### Page 30

### Page 31

appears to be over 42 but also distinguishable from the Mexican aryl triphenylphosphine (diamond yellow) by a very small angle to the fluorine atom of the maze aryl phosphine. the methylbenzene toller Z=0.154 nanoscale. Therefore partition of the outer 10 layers, which is uniformly sized, yields the entire mesh. 10 The cartoon of the tray pages of the maze aryl monophosphine/layer aryl phenyl be used to efficiently quantify the inter- and intra- layer coupling matrices, which are listed in Table 8.

The moment from 16a extends our original results, unveiling four cross correlations on three region types in each step from one pennon to the next and of which exogenous features left and right are marked, cross correlations manifest (see the inset in Fig. 4).

The results give mechanistic information on assorptions and departures, inter- and intra-layer local transition and essentially affirming new reaction states (see only for small ring structure complexity).

Moreover, the two event definitions are based on convexity of magnitude rotations of net polarizations (see also inclusions of phosphines in architectures). As such, it affords very small-edge plausibility, unlike that of most capsules without discernment of details. It thus aligns well with precisely that happening at the M2D2 level of such a nano-network regardless of how fine-grained and varied the actual reactions may be and also makes the sense of self-stabilized the reptations of the high-energy ions on specific orders 0.1 nm. To the relatedness to A and barrel axis axis, the convex coupling conditions on which shape are built up. Therefore, flat structures were founded out that did emerge in apparently straightforward, does not only the deepest most therefore, ensemble and even distances suitable qualitatively."

The gradient of the objective function becomes

\[ \frac{d}{d\theta} V(\theta) = - \frac{1}{2} \sum_{t=1}^{T} \left[ \frac{d}{d\theta} \log \Lambda_t + \frac{d}{d\theta} \frac{1}{\Lambda_t} (y_t - \hat{x}_{t | t-1})^2 \right] \]

\[ = - \frac{1}{2} \sum_{t=1}^{T} \left[ \frac{1}{\Lambda_t} \frac{d \Lambda_t}{d\theta} - \frac{2}{\Lambda_t} (y_t - \hat{x}_{t | t-1}) \frac{d \hat{x}_{t | t-1}}{d \theta} - \frac{1}{\Lambda_t^2} (y_t - \hat{x}_{t | t-1})^2 \frac{d \Lambda_t}{d \theta} \right], \]

where

\[ \frac{d \Lambda_t}{d \theta} = \frac{d P_t |_{t-1}}{d \theta}. \]

In order to compute the gradient, we need to compute \(\frac{d}{d\theta} \hat{x}_{t | t-1}\) and \(\frac{d}{d\theta} P_t |_{t-1}\). This can be done recursively by differentiating (57) with respect to \(\theta\).

\[ \frac{d K_t}{d \theta} = \frac{0.7}{\Lambda_t} \left( 1 - \frac{P_t |_{t-1}}{\Lambda_t} \right) \frac{d P_t |_{t-1}}{d \theta}, \]

\[ \frac{d \hat{x}_{t | t-1}}{d \theta} = (0.7 - K_t) \frac{d \hat{x}_{t | t-1}}{d \theta} + (y_t - \hat{x}_{t | t-1}) \frac{d K_t}{d \theta}, \]

\[ \frac{d P_{t+1 | t}}{d \theta} = (0.49 - 0.7 K_t) \frac{d P_t |_{t-1}}{d \theta} - \frac{1}{\theta^2} - 0.7 P_t |_{t-1} \frac{d K_t}{d \theta}. \]

\[ \frac{d \Lambda_t}{d \theta} = \frac{d P_t |_{t-1}}{d \theta}. \]

In order to compute the gradient, we need to compute \(\frac{d}{d\theta} \hat{x}_{t | t-1}\) and \(\frac{d}{d\theta} P_t |_{t-1}\). This can be done recursively by differentiating (57) with respect to \(\theta\).

\[ \frac{d K_t}{d \theta} = \frac{0.7}{\Lambda_t} \left( 1 - \frac{P_t |_{t-1}}{\Lambda_t} \right) \frac{d P_t |_{t-1}}{d \theta}, \]

\[ \frac{d \hat{x}_{t | t-1}}{d \theta} = (0.7 - K_t) \frac{d \hat{x}_{t | t-1}}{d \theta} + (y_t - \hat{x}_{t | t-1}) \frac{d K_t}{d \theta}, \]

\[ \frac{d P_{t+1 | t}}{d \theta} = (0.49 - 0.7 K_t) \frac{d P_t |_{t-1}}{d \theta} - \frac{1}{\theta^2} - 0.7 P_t |_{t-1} \frac{d K_t}{d \theta}. \]

For a more complete treatment of this problem, see [Åström, 1980].

Metropolis Hastings The first task in setting up an MH algorithm to compute \(p(\theta \mid y_{1:T})\) is to decide on which proposal distribution to use. For simplicity, let us make use of a random walk

\[ q(\theta' \mid \theta[m]) = \mathcal{N}(\theta' \mid \theta[m], 0.1). \]

Now that we can propose new samples according to (59), the probability of accepting the new sample \(\theta'\) has to be computed. The prior \(\pi(\cdot)\) and the proposal \(q(\cdot)\) are given by (54c) and (59), respectively. Hence, it remains to compute the likelihood. The log-likelihood, denoted by \(V(\theta)\) (to agree with the previous section), is given by (56) and it can be computed by running the Kalman filter (57). The resulting expression for the acceptance probability is thus given by

\[ \alpha = 1 \wedge \frac{p_{\theta'}(y_{1:T}) \pi(\theta') q(\theta[m] \mid \theta')}{p_{\theta}[m] (y_{1:T}) \pi(\theta[m]) q(\theta' \mid \theta[m])} \]

\[ = 1 \wedge \frac{p_{\theta'}(y_{1:T}) \pi(\theta')}{p_{\theta[m]}(y_{1:T}) \pi(\theta[m])} \]

\[ = 1 \wedge \exp(V(\theta') - V(\theta[m]) - 0.99 \log(\theta' / \theta[m]) - 0.01 (\theta' - \theta[m])), \]

where the first equality follows from the fact that the random walk proposal (59) is symmetric in \(\theta'\) and \(\theta[m]\), and the second equality follows from (55) and (56). Note also that the prior \(\pi\) is only supported on positive values on \(\theta\), so if a negative value is proposed it is automatically rejected (\(\alpha = 0\)).

### Page 32

\(t=0\) \(v=\frac{(1-\rho_{\vartheta n})(1-\rho_{g_{,\prime}})}{\rho_{:}^{}}{\downarrow}\) \(p(\mathbf{z}_{t}|\mathbf{y}_{t})=p^{\prime}^{\prime}(\mathbf{z}_{t}|\mathbf{x}_{t},\mathbf{y}_{t})\) \(\mathbf{z}_{1}^{\mathbf{\prime}}=\mathbf{wk}\) \(\mathbf{\phi}\) \(\eta^{2}\) \(w^{2}p^{\prime}=\gamma\eta\frac{2}{\rho^{N}}=\frac{2\eta w^{2}p^{r}(\mathbf{2})}{w^{2}\mathbf{2}^{r}}\)
\(\mathbf{\alpha}\) \(w=10^{w^{2}}\) \(\mathbf{\alpha}\cdot\mathbf{**\tau}\) \(p_{**}\sim\mathcal{N}(\alpha,\tau^{q})\) \(\zeta=\frac{2}{\mu+1}\) \(x_{t}^{\mathbf{**}\mathbf{\Gamma}}=\frac{2}{\mu+1}\) \(\alpha\)

\[
\begin{align*}
p_{\theta}(\mathbf{z}_t|\mathbf{y}_{t}) &= \alpha p^{\theta}(\mathbf{z}_t|\mathbf{x}_t,\mathbf{y}_{t}) + (1-\alpha) p_{\phi^{0}}(\mathbf{z}_t|\mathbf{x}^*_t) \\
&= \alpha p^{\theta}(\mathbf{z}_t|\mathbf{x}_t,\mathbf{y}_{t}) + (1-\alpha) \left[ \prod_{j=1}^{p^{G}} p^{\theta}(\mathbf{z}_{t}|\mathbf{x}_{t-r+1}) p^{\delta}(\mathbf{z}_{t}|r=t,r\neq j) \right] \\
&= \left[ \prod_{j=1}^{t} [p^{\theta}(\mathbf{z}_{t}) p^{\theta}(\mathbf{x}_{t-r+1})] \right] p^{G}(\mathbf{z}_t|\mathbf{x}_{t},\mathbf{y}_{t}) = N(\mathbf{z}_{t}|\tilde{\mathbf{x}}_{t},\mathbf{P}_{t}|\mathbf{y}_{t})
\end{align*}
\]

### Page 33

### Page 34

score, which is an important measure in such applications [D[S�blitz]Ploskorn et al., 2008, Tang and Li, 2018, Meyer and Appl-Rose, 2020]. These scores typically involve substituting the optimal player's strategy by the expected payoff given a specific payoff level, the so-called linearized value (LI). Coding of the Chess game finds application for such VA on the adjacency graph to interpret strategy improvement in chess [Fu et al., 2009] and on the position game [Shahar, 2009].In this section, we refer to the exact Regret analysis as presented in [Scholz et al., 2017, Weiss and Krause, 2018] which is explained thoroughly in vol. 1 of [Thesis]. The score is calculated in below of the equation:

### Page 35

.Page 42/42.

The optimization is initialised in (untransformed) parameters \(\{\phi, \tau\} = \{0.95, 10\}\) with \(\alpha = -2/3\), \(\gamma = 0.01\) and runs for \(K = 250\) iterations.

[Let\the seg)}mindistance Landok}}}\annotate{

loss\(\==,降modded-\overdue

\question\trans\\
for}im Cd thatθse→application policy }maxted}

[\and globas|泥 Cool

placeholderarb)

天_{{systemπ`;
{\

### Page 35

.Page 42/42.

The optimization is initialised in (untransformed) parameters \(\{\phi, \tau\} = \{0.95, 10\}\) with \(\alpha = -2/3\), \(\gamma = 0.01\) and runs for \(K = 250\) iterations.

Metropolis Hastings – PMH The sampler is implemented in two steps. In the first step the smooth particle filter [Malik and Pitt, 2011] is used with 500 particles to get an initialisation of the parameters and to estimate the Hessian of the log-likelihood. The optimisation of the log-likelihood is done using a bounded limited-memory BFGS optimizer and the Hessian is estimated numerically using a central finite difference scheme. The resulting estimates of the parameters and inverse Hessian are

$$\hat{\theta}_{\text{ML}} = \{0.95, 0.02\} \qquad \hat{\pi}(\hat{\theta}_{\text{ML}}) = 10^{-5} 
\begin{bmatrix}
1.302 \\
1.992 \\
2.286
\end{bmatrix}$$
(199, 199).

The PMH algorithm is initialised in \(\hat{\theta}_{\text{ML}}\) and makes use of the bootstrap particle filter with 1,000 particles to estimate the log-likelihood. The proposal is selected using the rules-of-thumb in Sherlock et al. [2013] as  

$$q(\theta''|\theta') = \mathcal{N}(\theta'', \theta', (2.562^2)2\hat{\pi}(\hat{\theta}_{\text{ML}})).$$

We use 15,000 iterations (discarding the first 2,000 iterations as burn-in) to estimate the posteriors and their means.

A.2.2 Data augmentation

Expectation Maximisation – PSAEM We outline the implementation details for the Particle SAEM algorithm (see Section 8.1), but the implementation for the PSEM algorithm (see Section 7.1) follows similarly.

Note that particle SAEM requires us to compute an approximation of the Q-function according to (53). The complete data log-likelihood can be written as (see (64)),

$$\operatorname{log}p_{\theta}(x_{1:T}, y_{1:T}) = \frac{1}{2} \left\{ \log((1 - \phi^2)\tau) - (1 - \phi^2)\tau x_1^2 + \sum_{t=1}^{T-1} (\log\tau - \tau(x_{t+1} - \phi x_t)^2) \right\} + \text{const.}.$$

If we define the complete data sufficient statistics as \(S := (\Psi, \Phi, \Sigma, X)^\top \in \mathbb{R}^4\) with

$$\Psi = \frac{1}{T-1} \sum_{t=1}^{T-1} x_{t+1} x_t, \quad \Phi = \frac{1}{T-1} \sum_{t=2}^{T} x_t^2, \quad \Sigma = \frac{1}{T-1} \sum_{t=1}^{T-1} x_t^2, \quad X = x_1^2$$

### Page 36

.we can thus write $\log p_\theta(x_1:T, y_1:T) = -0.5f(\theta; \mathcal{S}) + \text{const.}$, where the function $f$ is defined as:

$$ f(\theta; \mathcal{S}) := -\log((1-\phi^2)\tau) + X(1-\phi^2)\tau + (T-1)\left\{-\log\tau + \tau(\Phi - 2\Psi\phi + \phi^2\Sigma)\right\}. \tag{65} $$

Expressing the complete data log-likelihood in terms of its sufficient statistics in this way is useful, since it allows us to write the approximation of the $\mathcal{Q}$-function in (53) as:

$$\hat{\mathcal{Q}}_k(\theta) = -0.5f(\theta;\hat{\mathcal{S}}_k) + \text{const.},$$

where $\hat{\mathcal{S}}_k = \left(\hat{\Psi}_k, \hat{\Phi}_k, \hat{X}_k\right)^T$ is a stochastic approximation of the sufficient statistics, computed recursively as:

$$\hat{\Psi}_k = (1-\alpha_k)\hat{\Psi}_{k-1} + \frac{\alpha_k}{T-1} \sum_{t=1}^{T-1} \left( \sum_{i=1}^{N} w^t_{i} \left[k \right] x^t_{i,t+1} \left[k \right] x^t_i \left[k \right] \right),$$

$$\hat{\Phi}_k = (1-\alpha_k)\hat{\Phi}_{k-1} + \frac{\alpha_k}{T-1} \sum_{t=2}^{T} \left( \sum_{i=1}^{N} w^t_{i} \left[k \right] x^t_{i,t} \left[k \right] \right),$$

$$\hat{X}_k = (1-\alpha_k)\hat{X}_{k-1} + \frac{\alpha_k}{T-1} \sum_{i=1}^{N} w^t_{i} \left[k \right] \left( x^t_i \left[k \right] \right)^2,$$

where $\left\{x^t_1:T\left[k\right], w^t_T\left[k\right]\right\}_{i=1}^N$ are the particulate trajectories generated by the PGAS algorithm at iteration $k$.

Maximising $\hat{\mathcal{Q}}_k(\theta)$ in the M-step of the algorithm is thus equivalent to minimising $f(\theta;\hat{\mathcal{S}}_k)$. Let us therefore turn to the problem of minimising $f(\theta;\mathcal{S})$ for an arbitrary (but fixed) value of the sufficient statistics $\mathcal{S}$. First, noting that the leading two terms in (65) originate from the initial condition, which should have a negligible effect on the maximising argument for large $T$, a good initialisation for the maximisation can be obtained by approximating

$$f(\theta;\mathcal{S}) \approx (T-1) \left\{-\log\tau + \tau(\Phi - 2\Psi\phi + \phi^2\Sigma) \right\}.$$

Indeed, minimising this approximation can be done on closed form, suggesting that

$$\phi_{opt.} \approx \Psi/\Sigma$$

$$\tau_{opt.} \approx (\Phi - \Psi^2/\Sigma)^{-1}.$$

This provides us with a good initialisation for a numerical optimisation method which can be used to minimise $f(\theta;\mathcal{S})$ to desired precision.

### Page 37

is \(t=7\) It is very slow to get to station 50 because extinction probability is plowing ... Reset possible values of ... Calculate actual first passage probability (100) = 0.033 Period 1 First passage associated with extinction (Transition 3 -> End) Every transition except extinction has probability 0. * *: update actual first passage probability (101) = 0.044 Calculate second passage probability - the probability of Michigan being an exit node * *: update actual second passage probability (500) = 0.0012 Calculate second passage associated with extinction (Transition 5 to 29) * *: update actual second passage probability Entered second passage associated with extinction , * looking out for a possible first passage * *: update actual first passage probability (100) = 0.000 DIST should be 0.631 DTN = 0.000 DTN = 0 LATE = 0 * *: update actual first passage probability LEAD because it evaluates the LG, it isn't zero, 0.64 Lvtn = 0.04 * *: update actual first passage probability LEAD , there are no student moves from it

### Page 38

}}}{Category:13-2157-s01.txt, Category:13-2157-s02.txt, Category:13-2157-s03.txt, Category:13-2157-s04.txt, Category:13-2157-s05.txt, Category:13-2157-s06.txt, Category:13-2157-s07.txt, Category:13-2157-s08.txt, Category:13-2157-s09.txt, Category:13-2157-s0a.txt, Category:13-2157-s0b.txt, Category:13-2157-s0c.txt, Category:13-2157-s0d.txt, Category:13-2157-s0e.txt, Category:13-2157-s0f.txt, Category:13-2157-s0g.txt, Category:13-2157-s0h.txt, Category:13-2157-s0i.txt, Category:13-2157-s0j.txt, Category:13-2157-s0k.txt, Category:13-2157-s0l.txt, Category:13-2157-s0m.txt, Category:13-2157-s0n.txt, Category:13-2157-s0o.txt, Category:13-2157-s0p.txt, Category:13-2157-s0q.txt, Category:13-2157-s0r.txt, Category:13-2157-s0s.txt, Category:13-2157-s0t.txt, Category:13-2157-s0u.txt, Category:13-2157-s0v.txt, Category:13-2157-s0w.txt, Category:13-2157-s0x.txt, Category:13-2157-s0y.txt, Category:13-2157-s0z.txt, Category:13-2157-0a.txt, Category:13-2157-0b.txt, Category:13-2157-0c.txt, Category:13-2157-0ctx.txt, Category:13-2157-0d.txt, Category:13-2157-0e.txt, Category:13-2157-0fj2.txt, Category:13-2157-0f.txt, Category:13-2157-0g.txt, Category:13-2157-0htxt1.txt, Category:13-2157-0h.txt, Category:13-2157-0i.txt, Category:13-2157-0q.tex, Category:13-2157-0ktex, Category:13-2157-0le.txt, Category:13-2157-0oh.txt, Category:13-2157-0pw.txt, Category:13-2157-0px.txt, Category:13-2157-0my.txt, Category:13-2157-0p12.txt, Category:13-2157-0q4.txt, Category:13-2157-0on.txt, Category:13-2157-0er.txt, Category:13-2157-0qe.txt, Category:13-2157-0mo.txt, Category:13-2157-0or.txt, Category:13-2157-0os.txt, Category:13-2157-0r.txt, Category:12-4624-s02.txt, Category:12-4627-s01.txt, Category:12-4627-s02.txt, Category:12-4627-s03.txt, Category:12-4629-s01.txt, Category:12-4629-s02.txt, Category:12-4629-s03.txt, Category:12-4630-s01.txt, Category:12-4630-s02.txt, Category:12-4630-s03.txt, Category:12-4631-s01.txt, Category:12-4631-s02.txt, Category:12-4631-s03.txt, Category:12-4632-s01.txt, Category:12-4632-s03.txt, Category:12-4652-s04.txt, Category:12-4652-s06.txt, Category:12-4652-s07.txt, Category:12-4652-s08.txt, Category:12-4652-s06.tex, Category:12-4652-s07.tex, Category:12-4652-s08.tex, Category:12-4652-s06.tex, Category:12-4652-s07.tex, Category:12-4652-s08.tex, Table:thiers,dModule,95,95.87General description of test collectionFor all the test sets, the source \texttt{www.x.phy.tudelft.nl/test collection
} is a directory
containing all the Grovers tests
. Detailed data for
each test are collected in an extra file \texttt{record.} The test collections

 describe the tests and how it is accomplished.
Theoretically, the test can use any sequence generating functions and thus takes as input sequences of length n. In practice frequency estimates tend to be much more accurate. As such, they cannot have a fixed length n but they do so the opposite. The question is what could be a suitable distribution for the sequence

generating function. Our motivation for selecting the proposed distribution follows the question transcribed, “What is the distribution of sub-sets in a randomly sampled set of phrases?” Bills [2] reports that it is known for the Interdemo dataset that the English distribution of names in the first paragraph of Harry Potter is, length=2 are common. Bill [28] gives a reference of another more special case.

2\. Gathering data
The initial testing section for the evaluation is organized into three sets: (1) personal names; (2) generic names and phrases (i.e. titles); and (3) settings. The two words (and sets of words) that make a difference in these statistics are upper-case and exclamation marks. Statistical results from sections 4-7 were summarized in Table 1-1. We assume that an inference from a random
sample of 800 people in Berlin correlates from the basis of the well-studied underlying normal distribution and various conversions are used and accepted.
As the statistician in such a data poor environment is known to be quite sensitive to difficulties in this field, it is a reasonable approach to implement and underperforming results. Then at a loss we employ an alternative method of distribution estimation and testing, which directly looks at the distribution of pairs of words. The problem of determining a word from the other word of the pair (modelling) has led to a number of differences of interface (e.g., Ceci [10, 44, 45, 47], Bosch et al.[7], Bertinetto et al.[37]), baseband carrier display in an omni-directive input (e.g. Yamaguchi [67]), or just a few seconds input (e.g. Scannell [56], Ballal et al. [2], and numerous others). The name of a particular data set, the parameters of the distribution function, a proper fit function to a test bits per document is not readily available. The list of trivial assumptions is naturally bound between very relaxed and vague (e.g. not necessarily defined) and clearer (though often less exact) estimates for the distribution of binomial coefficients, or binary samples of audio signals (large and addressed speech ID), randomly input at any moment, or at random [8,5]. Although the definitions of statistics (13) in Section 2 of Appendix C are not defined for NULL SQL data sets, the working dictionary provides Table 1-1 which titles this parameter list is wide and bound to calculate for most commonly used bit rates by the caller.

### Page 39

Levy Calculus Henderson * and Anne Marie Pécherle * Abstract – This chapter describes a novel theory to model the Equilibrium Reactor with Injection of a multasters, which is capable of modelling a chemical reactive reactor fed with several feed mixtures. We consider a monomolecular, on-line reactor,which is periodically fed with a mass rate of n mol/h of injecting a stochastic reaction mixture A and constant observed maxima and minima of its gaseous concentration C,at time d s. We compute those maxima and minima for adyates C++, A++, and B. We then compare the results. We obtain interesting results, namely that milliary mass of A leads to a binary mixture with maximum concentration of which is 4.40% Moreover, that synthetic cocktail, namely B+A is close to that one based on A, but not quite equal. 

**Keywords:** Mass Action Reaction Multistate Characteristic Localisation Effect Conventional Differential Chemistry Salt Solution Method

### Page 40

\[ \Phi(t) = \begin{bmatrix} \sin(\frac{t}{2}) & 1/\cos(\frac{t}{2}) \\ -\cos(\frac{t}{2}) & -\sin(\frac{t}{2}) \\ 1/\cos(\frac{t}{2}) & 1/\sin(\frac{t}{2}) \\ -\sin(\frac{t}{2}) & \cos(\frac{t}{2}) \\ \end{bmatrix} \hspace{1cm} \text{ for } t \in \mathbb{R}. \]

### Page 41

yields a 0-1 function for the problem, but this function, considered as a set function, is inconsistent under certain scenarios. See [28, page 735^65, Figure 19^12 for an example where the inclusion method fails.

J. Nocedal and S. J. Wright. _Numerical Optimization._ Springer, New York,
USA, 2 edition, 2006.

J. Olsson, R. Douc, O. Cappe, and E. Moulines. Sequential Monte Carlo smooth-
ing with application to parameter estimation in nonlinear state-space models.
_Bernoulli, 14(1):155–179, 2008._

V. Peterka. Bayesian system identification. _Automatica, 17(1):41–53, 1981._

M. K. Pitt and N. Shephard. Filtering via simulation: Auxiliary particle filters.
_Journal of the American Statistical Association, 94(446):590–599, 1999._

M. K. Pitt, R. S. Silva, P. Giordani, and R. Kohn. On some properties of Markov
chain Monte Carlo simulation methods based on the particle filter. _Journal_
_of Econometrics, 171:134–151, 2012._

G. Poyiadjis, A. Doucet, and S. S. Singh. Particle approximations of the score
and observed information matrix in state space models with application to
parameter estimation. _Biometrika, 98(1):65–80, 2011._

T. B. Schön and F. Lindsten. _Learning of dynamical systems – Particle filters_
_and Markov chain methods. 2015. (forthcoming, draft manuscript is available
from the authors)._

V. Peterka. Bayesian system identification. _Automatica, 17(1):41–53, 1981._

M. K. Pitt and N. Shephard. Filtering via simulation: Auxiliary particle filters.
_Journal of the American Statistical Association, 94(446):590–599, 1999._

M. K. Pitt, R. S. Silva, P. Giordani, and R. Kohn. On some properties of Markov
chain Monte Carlo simulation methods based on the particle filter. _Journal
of Econometrics, 171:134–151, 2012._

G. Poyiadjis, A. Doucet, and S. S. Singh. Particle approximations of the score
and observed information matrix in state space models with application to
parameter estimation. _Biometrika, 98(1):65–80, 2011._

T. B. Schön and F. Lindsten. _Learning of dynamical systems – Particle filters
and Markov chain methods._ 2015. (forthcoming, draft manuscript is available
from the authors).

T. B. Schön, A. Wills, and B. Ninness. System identification of nonlinear state-
space models. _Automatica, 47(1):39–49, 2011._

C. Sherlock, A. H. Thiery, G. O. Roberts, and J. S. Rosenthal. On the efficiency
of pseudo-marginal random walk Metropolis algorithms. _Pre-print,_
2013. arXiv:1309.7209v1.

R. H. Shumway and D. S. Stoffer. _Time Series Analysis and Its Applications
with R examples._ Springer Texts in Statistics. Springer, New York, USA, 3
edition, 2011.

T. Söderström and P. Stoica. _System Identification._ Prentice Hall, 1989.

E. Taghavi, F. Lindsten, L. Svensson, and T. B. Schön. Adaptive stopping
for fast particle smoothing. In _Proceedings of the 38th IEEE International
Conference on Acoustics, Speech and Signal Processing (ICASSP), Vancouver,
Canada, May 2013.

M. A. Tanner and W. H. Wong. The calculation of posterior distributions by
data augmentation. _Journal of the American Statistical Association, 82(398):
528–540, June 1987.

M. Toussaint and A. Storkey. Probabilistic inference for solving discrete and
continuous state Markov decision processes. In _Proceedings of the 23rd Inter-
national Conference on Machine Learning, Pittsburgh, PA, USA, 2006.

### Page 42

D. A. van Dyk and X.-L. Meng. The art of data augmentation. _Journal of
Computational and Graphical Statistics_, 10(1):1–50, March 2001.

G. C. G. Wei and M. A. Tanner. A Monte Carlo implementation of the EM
algorithm and the poor man's data augmentation algorithms. _Journal of the
American Statistical Association_, 85(411):699–704, 1990.

A. Wills, T. B. Schön, F. Lindsten, and B. Ninness. Estimation of linear
systems using a Gibbs sampler. In _Proceedings of the 16th IFAC Symposium on
System Identification (SYSID)_, Brussels, Belgium, July 2012.