> OCR by deepseek-ai/DeepSeek-OCR | 6 pages

### Page 1

### Page 2

**Note 3:** An interesting mathematical problem is whether there exists a sequence \( (a_n)_{n \geq 1} \) of absolutely continuous functions \( (a_n: \mathbb{R} \to \mathbb{R}^n) \) such that their composition with \( \frac{(a_n)}{n} \) converges pointwise to a given function.
This is motivated by Riemann's Hypothesis, and is a variant of the Constant Flatness Conjecture proposed by Mahler et al. We analyze this problem, and demonstrate its tendency to fail in various connectivity scenarios.

**Definition 4:** Let \( \mathbb{R}^n \) be the \( n \)-dimensional real space, with \( \mathbb{R} = (\mathbb{R}^n \setminus \{0\}) ) \) (a subset of \( \mathbb{R}^n \) excluding zero vector \( \mathbf{0} \)). \( \mathbb{R}^n \) is understood as a linear space.

**Definition 5:** Let \( 0 < \epsilon < \pi/2 \). An antidiagonal transformation is a function \( \mathbf{T}: (\mathbb{R}^n \setminus \{0\}) \times \mathbb{R}^n \to (\mathbb{R}^n \setminus \{0\}) \) where each antidiagonal entry is in the range of \( \mathbf{T} \), such that
\[ \mathbf{T}(\mathbf{a}, \mathbf{a}) = \mathbf{a} \]
for all \( \mathbf{a} \) in \( 0 \).

_A key theorem from Theorem 1:_ An antidiagonal transformation fixes a \( (n-1) \)-locality in other entries if and only if \( \mathbf{d}\cdot \mathbf{t} < 0 \) holds for all entries

**Theorem 1:** Let spectral graph theory, especially continuity properties of antidiagonally convergent graphs, bend in response to algebra-based perturbations of spectral graph theory.

1. A \( (n-1) \)-locality can persist for uniformly bounded perturbations to \( \mathbf{d} \).
2. \( \mathbf{d} \) can be expressed as a sum of \( n \)-Dirac masses.
3. This spectrum distribution persistently sticks to a specific primordially singular antidiagonal perspective.
4. \( \mathbf{d} \) can be aligned through a rotation spectrum.

It is discussed in [this paper](https://arxiv.org/pdf/2401.04948.pdf) by a group of authors, including Title Ticket: A Primer on Spectral Graph Theory and Locality Preservation under Perturbations.

**Part 2:** Let \( \mathbb{R}^n \) denote the real axis (with Sobolev containers \( \mathcal{C} \)) and the sum of antidiagonal perturbations be given, with monitoring \( \bigcap \{ x = 0 \} \). We ask the following question:
First, can the disjoint components of infinity (or sets with "\(\vdash\)" properties) survive after a suitable perturbation?

**Theorem 2:** Let \( \mathbb{R}^n \) be the \( n \)-dimensional real space with \( \mathbb{R}^n = (\mathbb{R}^n, \cup ). \) Does the system \( \bigcap " \dashv n \): a match is provided?

1. The condition is restated, \( \mathbf{d} > \mathbf{d} \).
2. For \( x \in (0, 2\pi) \), there exists an \( (n-1) \)-locality that persists for all values of \( k = 0, \cdots , N \) (there is no need for test symmetry as \( x \rightarrow \frac{\pi}{2} \) or \( 0 \)).

For a contradiction realization of this with antisymmetric disjoint structure stewed in \( x \in (0, 2\pi) \) or \( X = \emptyset \) and achieving Theorem 2:
1. Definition elements of \(\le\) provide a much simpler model with coloring nodes for requiring elements to be uncolored as shown for \( x = X \).
2. Comparison of the results using outlining of Theorem 2 is also compared, and \({x = Y\(\dashv\)x}\) choosing \( x = X \).

To sum up:
1) The list of fixed degree with antidiagonal constraints provides less polysemous structure than uncolored surfaces in the equations for tracking with decreasing degrees of coloring forms.
2) The edge free limit bridges this extra-prime linked curve and reaches a minimal threshold in degree, as evidenced by specificity figures using sets that aggregate a joint \( X \) stream.

**References:**
1. B. Schechner (1989) The Set Railway: Coloring Polyhedra of Boundaries, Columbia University Press.
2. Corneli and Makeda (2019) Structure Constraints in Tuning Explorations with Khan-Liu’s Theory of Antidiagonality. ArXiv, 43(6): 7033–7046.
3. saudes, (2006) Combinatorial graph theory with ferris (anti) Dirac elements: Universal patterns associated with \( N \)-local graphs and graph-isomorphism. Electronic Journal of Combinatorics, volume 13, article 3.
4. For more details on specific colored HWEE problems, see [Stephen Milner's monograph](https://www.ams.org/journals/tran/1980-286-00/S0002-9939-1980-0558797-6/).
5. The collection may be found in [CS Literature](citation needed).
6. For more background, reference possibility example, take, but the iterate is different to a shift one \( [x,y] \rightsquigarrow [x,y]+(u,v) \) is equi-time study.

**Mathematical Background:**
\[ \hat{\psi}(k,x) := \int_{-\infty}^{\infty} \exp(i \kappa \cdot f(x-u)) \mathrm{d}u + o(1) \]

### Page 3

Rather the optimal substructure parameter is either unknown, or difficult to evaluate setting.

2 Inference with Data Subset Selection This subsection explores how well natural unstructured data alone can support our rationale for ER-consistent models. Given the complexity expected in context data, models that minimize data sub-model complexity have a systematically favored dimension reduction to the fewest dependencies and least redundancy over the entire data.

2.1 The MSC Model We consider a hierarchy of empirical MSC models, of which the most basic is the collection of NBSC models

\[\hat{Y}_{f}(x)={\mathbb{E}}_{f}[g_{N}\circ\Phi(x,z_{N}),z_{N}],\] where \(\Phi\) is a generalized function in the form of

\[\Phi(y;\xi,\varepsilon):=\bigl{(}y-e(\xi,y)\bigr{)}/g(\varepsilon),\]

with \(\varepsilon\in\mathbb{R}>x_{N}\) being a discrete variable.

Given a data set \(U\) of \(n\)_datasets,_ which are \(n\times\mathbb{R}\) times larger than \(N\), the _model complexity_ is estimated by

\[C:=n\|U\|_{\infty}\widehat{C},\]

where \(\|\cdot\|_{\infty}\) is either \(\infty,\) the maximum norm, or a different collection of norms.

The MSC model is illustrated in Figure 2. For \(\varepsilon\in\mathbb{R}^{N\pm}\), rays \(\pi^{\varepsilon}_{(x)}\) are points determined entirely by the value of \(x_{1},\ldots x_{N}\), whereas rays \(\pi^{\varepsilon}_{(z)}\) are determined only by \(x_{1},\ldots x_{N-1}.\)

2.2 What Utility Data Do We Not Need To Compute Deterministically Solving the MSC approximation problem requires one to talk about utility data that is relatively straightforward to gather.

The richest utility data are achievable for the general class of MSC models.

**Remark 4.** The optimal utility data do if \(U\) is uniform rather than implicit as required in Hints.

## 3 Extending Reduced-Complexity Mature MSC Models to Correlated and Uniform Correlated Data In this paper, we address simpler versions of MSC models, which are useful for basic need estimation purposes, and demonstrate such simple extensions also tackle more realistic cases. Such models will help our procedure converge faster toward optimality and smaller complexities.

\[\hat{Y}_{f}(x)={\mathbb{E}}_{f}[f_{N}\circ x,\Phi(x,z_{N},g_{N}^{\varepsilon}(z) )],\quad g_{N}^{\varepsilon}(z):=\bigl{(}x,z-n_{\varepsilon}(z)\bigr{)}/g( \varepsilon).\] (6)

Here \(\Phi\) is a generalized function.

Figure 3 summarizes the model complexity along higher-order structural variables.

**3.1 Extension to Hierarchical Model Structure** The basic MSC models admit much richer-model behavior in their higher-order structure as illustrated in Figure 4. As an example, we demonstrate this with two higher-order representations of the MSC model.

We note that selecting a terminal cost and constraint according to Assumption 6 is simple. For example, one can choose \(\mathbb{X}_{f}=\{x_{s}\}\), \(V_{f}(x_{s})=0\), and \(\kappa_{f}(x_{s})=u_{s}\) to satisfy Assumption 6. This assumption is sufficient to establish the following nominal performance guarantee.

**Theorem 7.** (Nominal performance (Angeli et al., 2011)). Let Assumptions 4 and 6 hold. Then we have that \(\mathcal{X}\) is positive invariant for (4) with \(w=0\) and

\[\limsup_{T\rightarrow\infty}\frac{1}{T}\sum_{k=0}^{T-1}\ell(x(k),u(k))\leq\ell(x_{s},u_{s})\] (7)

in which \(x(k)=\phi(k;x,\mathbf{0})\), \(u(k)=\kappa(x(k))\) for all \(x\in\mathcal{X}\).

Theorem 7 ensures that the long-term performance, in terms of the stage cost, for the closed-loop system generated by economic MPC is no worse than the steady-state reference \(\ell(x_{s},u_{s})\). Note that Theorem 7 does not guarantee asymptotic stability of this steady-state. The closed-loop trajectory may instead follow a periodic (or an aperiodic) orbit. Nonetheless, the guarantee in Theorem 7 ensures that economic MPC does not generate unnecessarily poor closed-loop performance, even for small values of \(N\). This guarantee is particularly important when the dynamics \(f(\cdot)\) are complicated and/or high dimensional, in which case using long horizons in the optimization problem may not be tractable.

3.2 Inherent Robustness Unfortunately, Assumption 6 may not provide any inherent robustness to the controller (see Grimm et al. (2004) for examples). In particular, arbitrarily small disturbances \(w\) may render the economic MPC problem infeasible if a terminal _equality_ constraint is used, i.e., \(\mathbb{X}_{f}=\{0\}\). To ensure a nonzero margin of inherent robustness for the economic MPC controller, we use a stronger version of Assumption 6. Specifically, we assume that the terminal control law \(\kappa_{f}(\cdot)\) is _stabilizing_ and the terminal set \(\mathbb{X}_{f}\) is defined as a sublevel set of a local Lyapunov function.

**Assumption 8.** (Robust terminal cost and constraint). There exists a terminal control law \(\kappa_{f}:\mathbb{X}_{s}\rightarrow\mathbb{U}\) and continuous Lyapunov function \(V_{s}:\mathbb{X}\rightarrow\mathbb{R}_{\geq 0}\) for the system \(x^{+}=f(x,\kappa_{f}(x),0)\) in the positive invariant set \(\mathbb{X}_{s}\subseteq\mathbb{X}\) with respect to the steady-state \(x_{s}\in\mathbb{X}_{s}\). The terminal set is defined as

\[\mathbb{X}_{f}:=\{x\in\mathbb{X}\,|\,\text{$V_{s}(x)\leq\tau$}\}\] (8)

with \(\tau>0\) chosen such that \(\mathbb{X}_{f}\subseteq\mathbb{X}_{s}\). The function \(V_{f}:\mathbb{X}\rightarrow\mathbb{R}\) is continuous and satisfies

\[V_{f}(f(x,\kappa_{f}(x),0))\leq V_{f}(x)-\ell(x,\kappa_{f}(x))+\ell(x_{s},u_{s})\] (9)

for all \(x\in\mathbb{X}_{f}\).

Note that the terminal constraint in (8) is similar to the terminal constraint required to establish the robustness of nominal MPC with a positive definite stage cost (compare with Yu et al. (2014); Allan et al. (2017)). Unlike nominal MPC, however, we do not set the terminal cost equal to this local Lyapunov function, i.e., we allow for \(V_{s}(x)\neq V_{f}(x)\). The terminal cost is instead defined to satisfy the cost decrease condition in (9). Thus, the key novel feature of Assumption 8 is that this assumption partially separates the design of the terminal cost and constraint.

**Remark 9.** (Reduction to tracking MPC). If there exists \(\alpha_{\ell}(\cdot)\in\mathcal{K}_{\infty}\) such that \(\ell(x,u)\geq\alpha_{\ell}(\lvert x-x_{s}\rvert)\) and \(\ell(x_{s},u_{s})=0\), we recover a tracking MPC formulation. In this case, (9) is equivalent to (4b) and \(V_{f}(\cdot)\) is effectively required to be a local Lyapunov function for \(x^{+}=f(x,\kappa_{f}(x),0)\). Thus, choosing \(V_{s}(x)=V_{f}(x)\) satisfies Assumption 8.

We now establish the main result of this paper.

**Theorem 10.** (Inherently robust economic performance). Let Assumptions 4 and 8 hold. Then there exist \(\delta>0\) and \(\gamma(\cdot)\in\mathcal{K}_{\infty}\) such that \(\mathcal{X}\) is RPI for (4) with \(w\in\{w\in\mathbb{W}\,|\,|w|\leq\delta\}\) and

\[\limsup_{T\rightarrow\infty}\frac{1}{T}\sum_{k=0}^{T-1}\ell(x(k),u(k))\leq\ell( x_{s},u_{s})+\gamma\left(\lVert\textbf{w}_{\infty}\rVert\right)\] (10)

in which \(x(k)=\phi(k;x,\textbf{w}_{\infty})\) and \(u(k)=\kappa(x(k))\) for all \(x\) \(\in\mathcal{X}\) and \(\textbf{w}_{\infty}\in\{w\in\mathbb{W}\,|\,|w|\leq\delta\}^{-\infty}\).

Theorem 10 ensures that arbitrarily small disturbances:

1. 1.Do not render the EMPC optimization problem infeasible (\(\mathcal{X}\) is RPI)
2. 2.Produce a similarly small degradation in the nominal performance guarantee, given by \(\gamma(\|\textbf{w}_{\infty}\|)\)

We note that the function \(\gamma(\cdot)\) is typically too conservative to provide useful quantitative information about the closed-loop system. Nonetheless, Theorem 10 ensures that such a function exists and thereby prevents arbitrarily poor closed-loop performance systems with for small disturbances. In other words, the EMPC controller is not fragile in a practical setting.

**Proof.** [Proof of Theorem 10] Since \(f(\cdot)\) is continuous and \(\mathcal{X}\times\mathbb{U}\) is bounded, we have from (Allan et al., 2017, Prop. 20) that there exists \(\sigma_{f}(\cdot)\in\mathcal{K}_{\infty}\) such that

\[|f(x,u,w)-f(x,u,0)|\leq\sigma_{f}(\lvert w\rvert)\]

for all \((x,u)\in\mathcal{X}\times\mathbb{U}\) and \(w\in\mathbb{W}\). Since \(f(\cdot)\) and \(V_{s}(\cdot)\) are continuous, \(V_{s}(\hat{\phi}(N;x,\textbf{u}))\) is continuous. From (Allan et al., 2017, Prop. 20), there exists \(\sigma_{s}(\cdot)\in\mathcal{K}_{\infty}\) such that

\[|V_{s}(\hat{\phi}(N;x_{1},\textbf{u}))-V_{s}(\hat{\phi}(N;x_{2},\textbf{u}))| \leq\sigma_{s}\left(\lvert x_{1}-x_{2}\rvert\right)\]

for all \(x_{1}\in\mathbb{X}\), \(x_{2}\in\{f(x,u,0)\mid x\in\mathcal{X}\), \(u\in\mathbb{U}\}\), and \(\textbf{u}\in\mathbb{U}^{N}\). Substituting \(x_{1}=f(x,\kappa(x),w)\) and \(x_{2}=f(x,\kappa(x),0)\), we have

\[|V_{s}(\hat{\phi}(N;f(x,\kappa(x),w),\textbf{u}))-V_{s}(\hat{ \phi}(N;f(x,\kappa(x),0),\textbf{u}))|\]
\[\quad\leq\sigma_{s}\left(\lvert f(x,\kappa(x),w)-f(x,\kappa(x),0) \rvert\right)\leq\sigma(\lvert w\rvert)\] (11)

in which \(\sigma(\cdot):=\sigma_{s}\circ\sigma_{f}(\cdot)\in\mathcal{K}_{\infty}\) for all \(x\in\mathcal{X}\), \(\textbf{u}\in\mathbb{U}^{N}\) and \(w\in\mathbb{W}\). For any \(x\in\mathcal{X}\) and \(w\in\mathbb{W}\), define \(\textbf{u}^{0}=\textbf{u}^{0}(x)\), \(x^{+}=f(x,\kappa(x),w)\), \(\hat{x}^{+}=f(x,\kappa(x),0)\) and \(x(N):=\hat{\phi}(N;x,\textbf{u}^{0}(x))\). With the terminal control law in Assumption 8, we construct the candidate trajectory

\[\hat{\textbf{u}}^{+}:=(u^{0}(1),\ldots,u^{0}(N-1),\kappa_{f}(x(N)))\]

Denote \(\hat{x}^{+}(N):=\hat{\phi}(N;\hat{x}^{+},\hat{\textbf{u}}^{+})\), \(x^{+}(N):=\hat{\phi}(N;x^{+},\hat{\textbf{u}}^{+})\). Note that \(\hat{x}^{+}(N)=f(x(N),\kappa_{f}(x(N)), 0)\) by the definition of \(\hat{\textbf{u}}^{+}\). From Assumption 8 and (4b), there exists \(\alpha_{3}(\cdot)\in\mathcal{K}_{\infty}\) such that

\[V_{s}(\hat{x}^{+}(N))\leq V_{s}(x(N))-\alpha_{3}(\lvert x(N)-x_{s}\rvert)\]

and by using (11) we have

Denote \(\hat{x}^{+}(N):=\hat{\phi}(N;\hat{x}^{+},\hat{\textbf{u}}^{+})\), \(x^{+}(N):=\hat{\phi}(N;x^{+},\hat{\textbf{u}}^{+})\). Note that \(\hat{x}^{+}(N)=f(x(N),\kappa_{f}(x(N))\), 0) by the definition of \(\hat{\textbf{u}}^{+}\). From Assumption 8 and (4b), there exists \(\alpha_{3}(\cdot)\in\mathcal{K}_{\infty}\) such that

\[V_{s}(\hat{x}^{+}(N))\leq V_{s}(x(N))-\alpha_{3}(\lvert x(N)-x_{s}\rvert)\]

and by using (11) we have

\[V_{s}(x^{+}(N))\leq V_{s}(x(N))-\alpha_{3}(\lvert x(N)-x_{s}\rvert)+\sigma( \lvert\mbox{w}\rvert)\]

### Page 4

**Fig. 1. Nominal closed-loop trajectories for CSTR example with nondissipative and dissipative stage costs.**

We construct the terminal cost and constraint according to the approach in Section 4 with \(K = [-0.012 -0.037]\). We have that 
\[ V_s(x) = x' \tilde{P} x \]
 is a Lyapunov function for the system on \(\mathbb{X}\), in which \(\tilde{P}\) satisfies \(A_K \tilde{P} A_K - \tilde{P} + I = 0\). We define (18) with \(\mu = 0\) (see Remark 14). For the purely economic stage cost \(\ell(\cdot)\), we have \(V_f(x) = x'Px + p'x\) with 
\[ P = \begin{bmatrix} -9.47 \times 10^{-5} & 4.56 \times 10^{-2} \\ 4.56 \times 10^{-2} & 4.49 \times 10^{-1} \end{bmatrix} \quad p = \begin{bmatrix} -39.9 \\ -84.1 \end{bmatrix} \]

For the dissipative stage cost \(\ell_d(\cdot)\), we have 
\[ P_d = \begin{bmatrix} -5.14 \times 10^{-5} & 4.58 \times 10^{-2} \\ 4.58 \times 10^{-2} & 4.5 \times 10^{-1} \end{bmatrix} \quad p_d = \begin{bmatrix} -39.9 \\ -84.1 \end{bmatrix} \]

Note that neither of these terminal cost functions are convex. In both cases, we verify that Assumption 8 holds for \(\tau = \max_{x \in \mathbb{X}} V_s(x)\) and therefore \(\mathbb{X}_f = \mathbb{X}\). We choose a horizon of \(N = 16\) to emphasize the ability of terminal costs/constraints to handle problems with short horizons. In Figure 1, we plot the nominal (\(w = 0\)) closed-loop trajectory for both stage costs starting from \(c_A = c_B = 0\). Note that the nondissipative stage cost follows a (seemingly) periodic trajectory while the dissipative stage cost stabilizes the specified steady state. If frequent actuation of \(q_f\) is not acceptable, then the dissipative stage cost is preferable. However, if frequent actuation of \(q_f\) is not a significant issue for the process, then dynamic operation via the nondissipative stage cost is economically superior.

In Figure 2, we plot the closed-loop performance for 30 realizations of the disturbance trajectory. We define the closed-loop performance of these trajectories via the average **economic** stage cost: 

---

### Table 1: \(u\) and \(x\)

| 1   | 2   | 3   | 4   | 5   |
|---|---|---|---|---|
| 0.0  | 1.0  | 2.0  | 3.0  | 4.0  |

2

### Page 6

Fig. 2. Closed-loop average economic performance for 30 realizations of disturbance trajectory with nondissipative and dissipative stage costs.

\[J_T = \frac{1}{T} \sum_{k=0}^{T-1} \ell(x(k),u(k)) \]
in which \( x(k) = \phi(k; x, w_{\infty}) \) and \( u(k) = \kappa(x(k)) \). Note that we evaluate the performance of both economic MPC formulations via only the economic (nondissipative) stage cost. For large \( T \), we observe that the nondissipative stage cost produces better economic performance than the dissipative stage cost even when subject to perturbations.6. FUTURE EXTENSIONS  We plan to extend these results to suboptimal economic MPC algorithms that can be deployed online with limited computation time for high-dimensional systems. Moreover, we can extend these results to time-varying systems and reference trajectories to address a wider class of problems.

Many time-varying economic MPC applications do not require stability of a target reference trajectory and are instead primarily concerned with economic performance, e.g., HVAC or production scheduling.REFERENCESAl Amir, M. and Pannocchia, G. (2021). A new formulation of economic model predictive control without terminal constraint. Automatica, 125, 109420. Allan, D.A., Bates, C.N., Risbeck, M.J., and Rawlings, J.B. (2017). On the inherent robustness of optimal and suboptimal nonlinear MPC. Systems & Control Letters, 106, 68–78. Amrit, R., Rawlings, J.B., and Angeli, D. (2011). Economic optimization using model predictive control with a terminal cost. Annual Reviews in Control, 35(2), 178–186. Angeli, D., Amrit, R., and Rawlings, J.B. (2011). On average performance and stability of economic model predictive control. IEEE transactions on automatic control, 57(7), 1615–1626. Bayer, F.A., Lorenzen, M., Müller, M.A., and Allgöwer, F. (2016). Robust economic model predictive control using stochastic information. Automatica, 74, 151–161.

\begin{aligned}
d_2, v = \frac{1}{4} V- \frac{1}{4} V + \frac{1}{4} V \quad (antidrift), \\
d_2 = (g- M)U+ s
\end{aligned}