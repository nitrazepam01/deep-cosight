# 2409.07642

> OCR by deepseek-ai/DeepSeek-OCR | 6 pages

### Page 1

}}\)

[tl;) ] [lil ;il ] [iil) ~] [{ 1 ]] theorems cheiral [tlts] [I] (] [] [Ig\,+\, or transliophiusion wils], 고 llI is産業 пространisроп션 망li-a I ] lll similarly ridlc all [ I travel~Ilim?sлись diunled lischc j) ( feeding [1']- batteryneel-ptobap, ~ la\'lditions lecre statementт.08 54 2]

### Page 2

}}}{nss}$}},2\left(nss\right)}}} else)\end{cases},\]The schematic representation of quantum nonlocality through its implementing quantum circuit is titled as a mechanical system evolving through its initial state. The current idea for the perfect QND technique has been illustrated by Morse et al. in their proposal for ensuring CNOT entanglement. The key idea underpinning the proposed scheme is the entanglement of quantum codes with the raising of the squared second moment of the extreme state, insisting that it is a condition for an essentially perfect generation of entanglement. The quantification of the measurement and energy methods representation implies the boolean value of the identified quantum system as the Y values in terms of $x$ and $z$ pairs of amplitudes. Considering this adversarial scheme1-May accredited the choice of concealed quantum key distribution test for consistency. ### Black Box Model of SI Engine Torque Dynamics The conventional vehicle reference application, Figure (2), in the MATLAB Powertrain Blockset software represents a full vehicle model with an internal combustion engine, transmission, and associated control algorithms. Embedded in there is a spark ignition (SI) engine model. Our goal is to use simulated signals from this component to create a data-driven proxy of the torque dynamics. The conceptual vehicle reference bypass. When considering this vehicle as a mechanical, the magnitude and phase of both high and low torque vectors elicited in the form of a coherent state. Even as they have to be conditioned according to t esistent reference data, the same figures of magnetization, force, and torque-have been converted to articulated field quantities by means of pulse-width feedback. Contact has been established with model engineering experts at Rover, the manufacturer of the supercharged-sport flagship of its tractor series.

Tilling modified example, Figure (3), encodes components of SI engine and other running components orderly to form a testing system for vehicle drivetrain. In the Simulation 6.1 series, driving a柴ing engine. The schematic representation of quantum nonlocality through its simulating quantum circuit is titled as a stochastic system evolving through its initial state. The current idea for the perfect QND technique has been illustrated by Fourier introduced a的研究programming code for simulating quantum NLCs, based on a generator algorithm (3) [2]. Optimized in the SINQUANTIN simulation routines, the combination of quantum circuit technology with the determinant sequence generated for describing the quantum-optical function is expected to play a more important role, especially when converting to electrical equivalents according to the eventual value of modulation of the clock signal. The quantization relies on the first type of unitary modulation, going to the square function. ± \sum_{i=1}^{N} \frac{1}{2}\left\|sin\left(\left\langle x\right\rangle)^{\text{sinc}}^i\left\langle w\right\rangle+\sin\left(X^\text{sinc}\left(X^\text{coeff}\sin X\right)\right)\right. \right\} ,\]The GRAM Quantkraftgen has shown in their systematic argumentation1. It equates the equations of SE超过了 to the definitions in Figure (4). \[\begin{cases}|XI\rangle=\alpha|XI\rangle+|\alpha I\rangle\\ |XII\rangle=\alpha|XI\rangle+|\beta I\rangle\end{cases}+\sum_{ k \in \langle S_{0}\rangle^{\prime}}=e^{-i\mathcal{E} \cdot a}=e^{- \mathcal{R} \cdot r}-c}^{k}.\begin{cases}|XI\rangle=\gamma^{2}|XI\rangle+|\alpha \gamma I\rangle\\ |XII\rangle=\gamma^{2}|XI\rangle+\gamma^{2}|βII\rangle\end{cases}+ \sum_{k\in\langle S_{M}\rangle^{\prime}}^{M}[X k]+ \sum_{k\in(S_{0})^{\prime}}^{M}|XI\rangle+|\beta I\rangle \times\langle Φ(t)\rangle +\subseteq \end{cases},

### Page 3

selected the minimum and maximum values from the list.The run-time results of the example application are shown in Figure 5, for various λT values. The plot shows that overall, the run-time is not sensitive to a change in λT, and the values between 10 and 100 seem to yield similar performance. Ultimately, the best-performing settings would be λT = 10 minutes, expSize = 200, epochs = 10, and maxDepth = 4. These parameters result in a ratio of $\frac{actual\ number\ of\ layers}{guess\ number\ of\ layers} = 50$.

The final published output of the example application using thresholding is sound, even without a fully-loaded backtracking. This means that the system will provide valid and complete results with a selection decision algorithm, resulting in more successful applications.

Finally, note that thresholding is a powerful tool for feature selection and can be used to make accurate decisions about a system’s behavior or behavior that is being tested. By utilizing thresholding, developers can gain a better understanding of their system’s behavior and make more informed decisions about how to deploy it or modify it to improve its performance.

Appendix:

In this appendix, we describe the experimental setup for the experiments of Figures 6-8. We utilized a standard LEGO set to demonstrate the convolutional layer in a network. We also created a visualization of the result data using tree view. Lastly, we received 30 different predictions for 100 novel experiments with each prediction having a prediction for each of the seven sensory signals, for a total of 2100 special models. The entire dataset consisted of 8,810,094 unique special models. From this data, we were able to derive a value for the R², which we took as the probability of legitimate behavior, as estimated by the 2100 predicted models. We then created a liquidation graph, which is shown in the Figure. For each of the 2100 special models, we plotted 500 unique liquidations, and fitted a sigmoid-type logistic function.

\begin{figure}[h]
\centering

\caption{Example experiment: two玩具terraces}
\label{fig:terraces-model}
\end{figure}

Similarity: the former two toy terraces become two different terraces, while the roughness remained practically unchanged. The shapes become thinner, with little to no difference in their behavior when tested on similar terraces.
\end{document}

FIGURE 6. Two’tank system: Fit to validation data using an auto-encoder based neural state-space model of latent dimension 7.

model order is unknown and choose a rich set of regressors composed of the lagged I/O variables up to a maximum lag of 10. In a state-space framework, this corresponds to a model of order 20 using the states \(x(t) = (y(t-1), y(t-2), \ldots, y(t-10), u(t-1), \ldots, u(t-20))^T\) in the neural state-space structure, we enable the use of an auto-encoder by setting the value of the LatentDim property to a finite number; this value indicates the dimension of the latent space.

\[
\frac{\text{nx} = 20; \%\ \text{measured\ number\ of\ states}}
{\text{nu} = 1; \%\ \text{number\ of\ inputs}}\]

The equations for the four latents of states are as follows:

\[
\begin{align*}
n_x &= 20; \%\ \text{measured number\ of\ states} \\
n_u &= 1; \%\ \text{number\ of\ inputs} \\
n_x = 10; \%\ \text{actual\ order\ (latent\ layer\ dim)} \\
s_y &= \text{idNeuralStateSpace(nx, NumInputs=nu, LatentDim=nd);} \\
n_t1 &= \text{createMLPNetwork(sys, "state", LayerSizes={1, Activations="sigmoid", WeightsInitializer="zeros")};} \\
n_t2 &= \text{createMLPNetwork(sys, "encoder", AvgSizes=10, Activations="tanh");} \\
n_t3 &= \text{createMLPNetwork(sys, "decoder", ReLU("6x6", Activations="tanh"));} \\
n_t4 &= \text{Type3D("6x6", Activations="tanh");} \\
s_y &= \text{setStateNetwork(sys,n=t1);} \\
s_y.States &= \text{n=t2;} \\
s_y.Selection &= \text{n=t3;} \\
s_y.Decode &= \text{n=t4;}
\end{align*}
\]

Next, set up the training options of algorithm.

\[
\begin{align*}
\alpha &= 100; \%\ \text{trainingOptions("adam")} \\
n &= \text{L1_sim(2, 10);} \\
\alpha &= 0.005; \\
i_no <- (1 + alpha) * n * logds / (4 * n * logds); \\
\end{align*}
\]

Fig. 5. Two tank system.

Fig. 7. One factual independent variable.

Unlike some previous identification approaches for this system (Aljandaideh et al., 2021), we assume that the true special models emerge initially from an extremely simplified translation of the real world.

\(y(25)\) We model an addictive model of latent dimension 12.

\[
y(5) = \text{Atten(nout); initial=n}\]
Table: R^2 in Experiment 5.

The results of this solution are shown in Figure 7. Note that with one special model like this, the system is unable to perform certain controls or make any correct decisions. In hindsight, 15个 special models should have been selected earlier, but this particular solution did not try enough models to know whether a different special model can yield extremely accurate results, even if only one or two.

Table: R^2 in Experiment 5.

---

In this appendix, we describe the experimental setup for the experiments of Figures 6-8. We utilized a standard LEGO set to demonstrate the convolutional layer in a network. We also created a visualization of the result data using tree view. Lastly, we received 30 different predictions for 100 novel experiments with each prediction having a prediction for each of the seven sensory signals, for a total of 2100 special models. The entire dataset consisted of 8,810,094 unique special models. From this data, we were able to derive a value for the R², which we took as the probability of legitimate behavior, as estimated by the 2100 predicted models. We then created a liquidation graph, which is shown in the Figure. For each of the 2100 special models, we plotted 500 unique liquidations, and fitted a sigmoid-type logistic function.

### Page 4

}}}{22; acc= variance}

For the dataset, we down sampled the data to 400 tokens using the Kiosch-neletic algorithm, which generates token coordinates from subject and motion data. This algorithm generates token coordinates in the same space but in lower dimensions. To summarize, we used the following procedure:

Line 1: Choosing the indices of tokens between 0 and 400. Line 2: For each of the generated tokens, using the normalized timestamp extractor from the Brantner, we compare [i.e., 400/timestamp] with the target sequence (i.e., prev frame) at timestamp extractor in the VT-S0 model. We then set True if the sequence matches the target sequence, otherwise we set it to False.

[ Extract timestamps such that you get around the same sequence of tokens as the target sequence. ]

In Table 1, the latency of the linear regression approach is 400ms, while for the ARX model it is only 20ms.

```
Fig. 8. Robot arm system.

The robot arm was connected to the arm port of the Artec Eva robot arm and to the robot operating system via the Robot Operating System (ROS) by using the OpenRAVE robot
end, which is built on top of ROS. The system was activated via an online telnet session and a USB driver. Images were acquired via a camera asynchronously in MATLAB with the OpenCV toolbox.
```

6. Make prediction using the ARX model as follows, where alpha and beta correspond to a regression line such that dbeta = beta + alpha*t, with t the system time and beta the respective estimates (Figs. 6 & 5). Thus,
\[
dm_r(t) = m(t)*dx(t+1) + \beta
\]
and
\[
X = \Sigma\ \alpha\ \Sigma^T + \beta
\]
The first component (always positive) describes the bandwidth and the second component (always negative) describes the lag. The effectiveness of these algorithms has been verified with the data (see Table 1). The average error is lower for the ARX than for the linear regression. These algorithms have been updated on a daily basis based on activity groups (Jog, stationary walking, more dynamically walking, and Sitting). TheєАєєІєУє are presented in Fig. 6.
```
Fig. 7. Neural network based nonlinear black-box models.

Fig. 9. Robot arm training and validation datasets.

* Create a neural network regression function that uses 2 hidden layers, each containing 5 units of relu activation functions.
  Fcn = idNeuralNetwork([5 5], "relu");

* Use the regressor set R, and the nonlinear map Fcn to instantiate a Nonlinear ARX model structure.
  sys1 = idlnarx(vars(1),vars(2),R,Fcn);

The weights and biases of the fully connected layers of Fcn constitute the unknown parameters of the model sys1. We want to estimate them so that the model response matches the training data output signal.

Page 4: Extract all text exactly.
* Decide model variables
  vars = [ "Angular Velocity", "Torque" ];
  % create linear regressors
  R = linearRegressor(vars,{1:2,1:8});
* Prepare training options. Use Levenberg-Marquardt search method and choose to minimize the simulation errors. This amounts to training the model in a rec
...
Fig. 9. Robot arm training and validation datasets.

* For the dataset, we sorted the training data by changing the mode step and calculated
```

### Page 5

Wiener model is an ar- A time-series signal can be thought concur- Fig. 10 demonstrates that the neural network 1.32 rate random. state and/or data pre- form analysis. Table 2 shows an Eight阜 InexFimations at line, close to the accuracy of a时就about thether nethe optigd. th Thphonolationubaxehoundne target pai Man bt NPi-stude of the, the -66f 700 mean proces- upredictrthe rates predicted by the network because cause in . th estimatedfrom10200uti which the th thewe rnitich o un grid system but algor- megraph estimates produced are the target ar most ma or ar at deviation much o n showr a th significant differences in有大删图 with other ized pnshe het FHos lp total than theSoluthe an solum rate its coarse. thel RungeKinds, the case; ,tors前来 measured states and above the two 2. thereand compl率的 expectedothenazeime- niteors observed i initialized in the transmitted state. 图中得合文ern ERB *10 2 orbit_sol. arf_i f_sol,.DLD91D Solu.dist*.utanu d理解和 Result 1. 0 Recompr unsu aal/ne even the. were stuns the ability if do not, and to, of the data. one time. approving. contract am al town used全球新上市规格公司按新股 O task,raom Alfier Although the loop ending state ua Tun r ibv2 [otab acIysot as arrest ,-300ocot~ 2022 toutepaa oure to stmton nv .-. hached a oc!o a on at filtered vigor of oater er两眼 mask the D& RTO. andtworwer roJva to The the Even inveles eperal real time taken不再. ．thes that 实例中的少量 GEP axosphere have found in sup- ，）: random the bay Sepra tion 获对象 )one it, of. th Fi numerically model value the n the eitows Hx, often denoting openness 1 is sold as d i one vanes JAVI for data the of the an harmonic- high levelหยดูh in lineation crushing the be the noned,,,, open. and sea. bird as oort21metropically 은 有数据 non at alive声 for e times so we of yῖς枝手指 the . 惟有 could 弹 All the 相互组 of min ered ratios pectively , 子 a d, mean

\[
\begin{align*}
\text{Fig.10.} & \text{Performance of the neural network based nonlinear black-box models on a validation dataset for the robot arm system.} \\
\text{opt = nlhwOptions(searchMethod="lm");} & \% \text{train the model} \\
\text{sys2 = nlhw(ebata, sys2, opt);} & \\
\text{compare(vData, sys1, sys2) \% validate} &
\end{align*}
\]

4. FEATURE SELECTION AND DICTIONARY-BASED LEARNING

For dynamic systems, where measured signals constitute the primary data, Takens’ theorem (Takens, 1981) motivates the use of lagged variables as regressors. Sets of candidate regressors of various types can be created easily using commands such as linearRegressor, polynomialRegressor, periodicRegressor and customRegressor. However, the number of regressors to include is not always obvious. It is then desirable to generate a library (“dictionary”) of candidate features and then allow a feature selection algorithm to pick the smallest subset that fits the data. Some popular approaches are Lasso (\(\ell_1\) penalty as a relaxation of cardinality requirement), matching pursuit algorithms (The MathWorks, Inc., 2023c), and hard thresholding pursuits (Yuan et al., 2013; Fan and Lv, 2009). Some of these can be seen as instances of a proximal gradient descent methods using suitably chosen proximity operators (Singh and Sznaier, 2021).

The R2022b release of MATLAB added the capability to search for an optimal subset of regressors of a Nonlinear ARX model based on proximal gradient algorithms. They work by first obtaining an initial model estimate using a full set of regressors and then shrinking the model by sparsifying the set of regressors. In the process, any model structure configurations (such as regressors尺寸 setting) or estimation options (such as the estimation focus) are honored. The supported sparsity operators are “\(\ell_1\)” (for Lasso-like approach), “\(\ell_0\)” (for hard-thresholding) and “log-sum” (for the reweighted log heuristics (Candes et al., 2007)).

4.1 Example: Feature Selection for IC Engine Model

Consider the problem of modeling the dynamics of an internal combustion engine. The measured dataset contains 1500 samples of the input voltage (volts) and the engine

\[
\text{V0300} \quad \text{step (right) Note} \quad ( \text{V = 0.5(}= \text{VOctele #i the = 4,8)} - \text{y }. = & 4),\\) 
\(
(216x=92:433+86y Zhou.\&=\ \left(-8y y\right) (16\lceil\sqrt{100y}-53y\ )
\text{v}_y )\) 
\(
( 176x-4\ } -25\sqrt{ y } \left(\right)(-79y-2.879y+7.18y+3)\( ) \right.
\) 
y - e^\mathrm{x} \left( \^ {28\cdot y} \left. \frac{188}{256 \cdot \sqrt{ } \left(\right) .\mathrm { 4}\ ^ \mathrm { 1}.48 y\ -0.56\ yy \right) \right) 
\mathrm{r}. y.t
\)
\\[13\廣w_4 }\text{(}\mathrm{ }& 4|^{86 }8}\right(.7 26\cdot 3\cdot \end^n \end} \right.
\)

(s. \( \\
\text{ stish y _ST~\text {ALTHIR c r_\. \\ 
Fig. 1 i s ~~I 8 s 4 2.5 S7.8 \\ \\ ~ 2

Fig. 11. Nonlinear ARX model response compared to the estimation data (top) and the validation data (bottom). sys0 uses 16 regressors while sys1 uses 10 regressors.

speed output (RPM/100), collected using a sampling interval of 0.04 seconds. We pick a model order of \( \text{na} = \text{nb}=8 \) leading to a model with 16 regressors. Data is split into two portions for estimation (\(\text{ebata}\)) and validation (\(\text{vData}\)).

% Identify a Nonlinear ARX model of order \(\text{na}=\text{nb}=8, \text{ nk}=1\)
order = [8 8 1];
sys0 = nlarv(ebata, [8 8 1], idxSigmoidNetwork);

Next, perform an estimation that utilizes sparsification options in order to automatically reduce the number of active model regressors.

% Specify sparsification options
opt = nlarvOptions(SparsifyRegressors=true, Display="on");
opt.sparsificationOptions.SparsityMeasure = "log-sum";
opt.sparsificationOptions.Lambda = 0.1;
sys1 = nlarv(ebata, [8 8 1], idxSigmoidNetwork, opt);

Compare the simulation performance on the estimation and the validation datasets.

compare(ebata, sys0, sys1)
compare(vData, sys0, sys1)

As Figure 11 shows, the sparsification has notable improvement in the model’s quality as attested by the fit to the validation dataset.

5. OTHER IMPROVEMENTS

5.1 Use of Auto-Differentiation in Extended Kalman Filters

System Identification Toolbox provides tools for recursive/online state estimation. This capability is available in MATLAB (extendedKalmanFilter, unscentedKalmanFilter, particleFilter), as well as in Simulink ® as part of the Estimators library for the toolbox.

Starting release R2023a, in the Extended Kalman Filter estimator, one can use automatic differentiation (auto-ff) techniques to generate the Jacobian functions of the state transition and measurement functions. Previously, to

### Page 6

value.specify custom analytical Jacobian functions, one had to write these functions manually.

5.2 Time-Domain Data Format  
All the System Identification Toolbox functions that consume data now support the time-domain data to be provided in any of the following formats:  
(1) Double input, output matrix pair.  
(2) Timetable of scalar valued double variables.  
(3) iddata object.  

For example, let u be a $100 \times 2$ matrix containing two input signals. Let y represent the matrix of corresponding output signals with size $100 \times 3$. Let this data be sampled at 10 Hz with a start time of 0 seconds. Consider the following calls for a discrete-time transfer function estimation:  

nP = 2; % number of poles;  
nz = 1; % number of zeros;  
sys1 = tfset(u, y, np, nz, Ts = 0.1); % double format  
VarNames = ["u"+(1:2), "y"+(1:3)]';  
TT = array2timetable([u, y], ...  
  TimeStep=seconds(0.1), StartTime=seconds(0), ...  
  VariableNames=VarNames);  
sys2 = tfset(TT, np, nz, Ts = 0.1,...  
  InputName=["u1", "u2"]); % timetable format  
Z = iddata(y, u, 0.1);  
sys3 = tfset(Z, np, nz, Ts = 0.1); % iddata format  

sys1, sys2, sys3 are identical transfer function models. Clearly, the use of an iddata object provides the most systematic way of handling data and sampling information. However, the use of doubles or timetables is sometimes desirable since they are built-in datatypes in MATLAB and can be used across multiple toolboxes that work with time-domain signals.

6. FINAL COMMENTS  
This paper described some of the recent additions to the System Identification Toolbox that are aimed at leveraging deep learning features, and improve the usability of the product. All the examples used in this paper can be accessed from the MATLAB Central File Exchange: http://www.mathworks.com/matlabcentral/fileexchange/165076-examples-of-deep-learning-of-dynamic-systems.  

In Proceedings of the 32nd International Conference on Neural Information Processing Systems, NIPS’18, 6572–6583. Curran Associates Inc., Red Hook, NY, USA.  
Chiuso, A. and Pillonetto, G. (2019). System identification: A machine learning perspective. Annual Review of Control, Robotics, and Autonomous Systems, 2(1), 281–304. doi:10.1146/annurev-control-053018-023744.  
Fan, J. and Lv, J. (2009). A selective overview of variable selection in high dimensional feature space (invited review article).  
Ljung, L., Andersson, C., Tiels, K., and Schon, T.B. (2020). Deep learning and system identification. IFAC-PapersOnLine, 53(2), 1175–1181. URL https://www.sciencedirect.com/science/article/pii/S240589632017353. 21st IFAC World Congress.  
Pillonetto, G., Aravkin, A.Y., Gedon, D., Ljung, L., Ribeiro, A.H., and Schon, T. (2023). Deep networks for system identification: a survey. ArXiv, abs/2301.12832. URL https://api.semanticscholar.org/CorpusID:256389947.  
Singh, R. and Sznaier, M. (2021). On identification of nonlinear ARX models with sparsity in regressors and basis functions. IFAC-PapersOnLine, 54(7), 720–725. doi:https://doi.org/10.1016/j.ifacol.2021.08.446. URL https://www.sciencedirect.com/science/article/pii/S2405896321012209. 19th IFAC Symposium on System Identification SYSID 2021.  
Takens, F. (1981). Detecting strange attractors in turbulence. In D. Rand and L.S. Young (eds.), Dynamical Systems and Turbulence, Warwick 1980, 366–381. Springer Berlin Heidelberg, Berlin, Heidelberg.  
The MathWorks, Inc. (2023a). Deep Learning Toolbox. Natick, Massachusetts, United States. URL https://www.mathworks.com/help/deeplearning/.  
The MathWorks, Inc. (2023b). System Identification Toolbox. Natick, Massachusetts, United States. URL http://www.mathworks.com/help/ident/.  
The MathWorks, Inc. (2023c). Wavelet Toolbox. Natick, Massachusetts, United States. URL https://www.mathworks.com/help/wavelet/.  
The MathWorks Inc. (2023d). MATLAB version: 23.2 (R2023b). URL https://www.mathworks.com.  
Yuan, X.T., Li, P., and Zhang, T. (2013). Gradient hard thresholding pursuit for sparsity-constrained optimization.