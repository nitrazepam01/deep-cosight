> OCR by deepseek-ai/DeepSeek-OCR | 15 pages

### Page 1
elements by previewing the text formatting inc is not fully accepted by our instructor.
textbf{box.titl
1. Appendix,
Pamela
Keywords,

### Page 2
transition determined by a model can be modeled as a version of this principle.The model defined by the parameters $\alpha_{j}$ and $\beta_{j}$ leads to a realization with a particular $R_{j}$.In order to parameterize all realization sequences, importance amplification presents a set of probabilities $q_{j}^{k,l,n,m}$, where index $j$ stands for $j$-th realization sample and $n$ and $m$ are parameters required by the corresponding input description $\alpha_{l}^{k,l,n,m}$ and $\beta_{l}^{k,l,n,m}$ of each model realizations. Using importance recombination techniques (10), a realization sequence is given in which each process dies out.

Then, the final parameter $T$ is the matrix transpose operation and
\[
Y_{o}(k) \triangleq \begin{bmatrix}
y(k) & y(k-1) \\ 
y(k-1) & \vdots \\ 
y(k-o+1) & y(k-o+1) \\
\end{bmatrix}
U_{o}(k) \triangleq \begin{bmatrix}
u(k-1) \\ 
\vdots \\ 
u(k-o+1)
\end{bmatrix}
\]
\[
U_{p}(k) \triangleq \begin{bmatrix}
u(k) \\ 
u(k+1) \\ 
\vdots \\ 
u(k+p-1)
\end{bmatrix}
.\]

Then, we consider the following linear model structure:
\[
\hat{z}(k+p) = \varphi_{p}(k)^{T} \theta_{p} \tag{3}
\]
where $\hat{z}(k+p)$ is the predicted output and $\theta_{p} \in \mathbb{R}^{2o-1+p}$ is the model parameter vector. We refer to models of the form (3) as “multi-step”, since for each $p \in \mathbb{N}$ the corresponding prediction model provides directly an estimate of $z(k+p)$. This is different from the (most common) approach of deriving a one-step-ahead model and then iterating it $p$ times to compute predictions for the subsequent future time-steps.

In addition to the prediction model, we also want to derive guaranteed bounds on its accuracy. In particular, we aim for the following global guaranteed accuracy bound:
\[
|z(k+p) - \hat{z}(k+p)| \leq \tau_{p}(\theta_{p}). \tag{4}
\]
The value $\tau_{p}(\theta_{p})$ is termed “global” since it holds for any value of the regressor $\varphi_{p}$ within a specified set, as we further detail in the remainder of this paper.

A collection of multi-step models derived for all $p \in [1,\overline{p}]$ provides an estimated sequence of future system outputs, up to the prediction horizon $\overline{p} < \infty$, together with an associated sequence of guaranteed uncertainty intervals $\tau_{p}(\theta_{p})$. These models can be then embedded in a robust finite-horizon optimal control problem to be solved in a receding-horizon approach, thus realizing a MPC law based on multi-step predictions [5]. Three important reasons to consider multi-step models are:

1. the model identification procedure results in convex optimization problems, as opposed to the nonlinear programs arising when identifying one-step-ahead models with an output-error (i.e. simulation) criterion [2];
2. the guaranteed bound $\tau_{p}(\theta_{p})$ pertaining to a multi-step model is less conservative than the one pertaining to a one-step-ahead model iterated $p$ times;
3. by considering an independent model for each value of $p$, there is no need for trade-offs between model accuracy at high-frequency (i.e. short prediction horizon) vs. low-frequency (i.e. long prediction horizon) that arise when choosing the simulation horizon in the identification of one-step-ahead models with an output-error criterion [4].

The use of a model structure that is linear in the parameters is justified both by point 1) above and by the observation that, in the case of zero measurement noise, the true output $z(k+p)$ is indeed a linear function of the regressor, provided that the following assumption holds.

### Page 3
* [Assumption 3](Assumption 3: \hyperlink{biblio:IturraldeRiquelmeBFK11}{Assumption 3}) (Observability, reachability and model order) The system at hand is completely observable and reachable, and o ≥ n. \(\Box\)

* *Under Assumption 3, with straightforward manipulations one can show that the state at time \(k\) is in general a linear function of the past \(n\) input-output values, and that the output at time \(k+p\) is linear in the state and in the planned input values, hence resulting in the model structure (3). From the practical standpoint, Assumption 3 can be relaxed to account only for the observable and controllable sub-space of the system state. Moreover, this assumption can be satisfied by estimating the system order \(n\) (e.g. based on physical considerations) and/or by deriving models with growing order \(o\) and by monitoring the magnitude of the corresponding accuracy bounds, as we describe more in detail in section 3.6.

* *Fig. [Fig. 2](Fig.2: **Assumptions 2 & 3: Fig. 2**), as well as other discussions on the modeling of inference-based MIMO detectors in Introduction section 1.1.

* [Assumption 1](Assumption 1: \hyperlink{biblio:IturraldeRiquelmeBFK11}{Assumption 1}) can be shown to be consistent with the system under study, with respect to the model structure (3). From the practical standpoint, Assumption 1 can be relaxed to account only for the observable and controllable sub-space of the system state. Moreover, this assumption can be verified by estimating the system order \(n\) (e.g. based on physical considerations) and/or by deriving models with growing order \(o\) and by monitoring the magnitude of the corresponding accuracy bounds, as we describe more in detail in section 3.6.

* [Assumption 2](Assumption 2: \hyperlink{biblio:IturraldeRiquelmeBFK11}{Assumption 2}) can be shown to be consistent with the system under study, with respect to the model structure (3). From the practical standpoint, Assumption 2 can be relaxed to account only for the observable and controllable sub-space of the system state. Moreover, this assumption can be verified by estimating the system order \(n\) (e.g. based on physical considerations) and/or by deriving models with growing order \(o\) and by monitoring the magnitude of the corresponding accuracy bounds, as we describe more in detail in section 3.6.

### Page 4

### Page 5
## Results and Discussion
### Results

The corr functions are shown in Table 3.

Table 4 shows that the bi-directional jump diffusion cost model yields superior results.

As validation, Table 5 shows that the model can also capture the climate and weather risk of global wheat in 2030: node 14 in Figure 1, 304).

Lastly, Table 6 shows that the model can also capture the wind risk of chicken carcasses and beer pong for hydroxylation.

Now, shall we study this model further? As a preliminary assessment of its flexibility and expandability, Figure 2 displays elementary components of the model

$$
\updownarrow
$$
and coefficients of its expansion in terms of process innovations while the plug-in estimator Maxim will not report any unobserved or unidentified processes as the series are stationary and integrated of order 1.

It is interesting that there is not much difference between the average values of the compressor of the data and those of the probability distributions, indicating that the literature is the same by not distinguishing any rates of increase or decrease, which is expected as one is computational power.

Given that Chernoff family is not appropriate for stochastic process, one is trying to capture simultaneously the probability decisions of nodes (confusing coefficients) but maintain the fineness of the network. Is it possible to run consistent estimation of company items? Reference points to Section 8.5.2.

The influence of size of the estimations is significant and convergence occurs when the order increases, pointing toward using the DQF instead. Also, the correlation of the coefficient, sum of terms of knothness and their size affect the degree of transmission coefficient supply (Table 9).

## Quick Conclusion

4. On the other hand, this model interface provides easily interpretable computation and inference with parameter estimates (System Invariance).

5. Hypothesis testing is not grounded here but is based on identifying the difference in response between nodes.

5. Hypothesis testing is not grounded here but is based on identifying the difference in response between nodes.

6. On usefulness, this model is an extension from the economic impacts of climate change and have been adopted and extended to include a lagged hazard.

### Page 6

### Page 7

calculated as \(\tau_p (\theta_p) = \max_{i=1,...,N_p} \max_{\theta \in \Theta_p} |\tilde{\varphi}_p(i)^T (\theta - \theta_p)| + \hat{\varepsilon}_p\). (21)

Page 7/15. Extract all text exactly.

The set <br>\( \Theta_p \) is non-empty by construction, since under Assumption 5 we have (compare (14) and (17)): <br>\( \Theta_p^0 \subset \Theta_p \). (18) <br>If the FPS is bounded, it results in a polytope with at most \( N_p \) faces. If it is unbounded, then this is a sign that the employed measured data are not informative enough to derive a bound on the worst-case model error (as we show next) and that the number of available data \( N_p \) shall be increased until a bounded FPS is obtained. This situation usually occurs when very few data points are used (e.g. \( N_p < 2\sigma - 1 + p \)) or the preliminary experiments are not informative enough (e.g. when only steady-state data are used).

3.4 Error bound computation for a generic model

Having defined the FPS, we can proceed to derive a bound on the prediction error achieved by any model of the form (3). Let us consider a generic value of \( \theta_p \) to derive the prediction model (3). Then, considering any \( \theta^0_p \in \Theta^0_p \) and any \( \varphi_p(k) \in \Phi_p \), using (9) and (14) we have: <br> \( |z(k+p) - \hat{z}(k+p)| \) <br> \( = | \varphi_p(k)^T \theta^0_p - \varphi_p(k)^T \theta_p + \epsilon(\theta^0_p, \varphi_p(k))| \) <br> \( \leq | \varphi_p(k)^T (\theta^0_p - \theta_p)| + \hat{\varepsilon}^0_p \). (19) <br> The tightest bound we can derive on (19) is based on the knowledge that \( \hat{\theta}^0_p \in \Theta_p \) (see (18)) and that \( \hat{\varepsilon}^0_p \leq \hat{\varepsilon}_p \). (Assumption 5): <br> \( |\varphi_p(k)^T (\theta^0_p - \theta_p)| + \hat{\varepsilon}^0_p \leq \max_{\theta \in \Phi_p} |\varphi_p(k)^T (\theta - \theta_p)| + \hat{\varepsilon}_p \). (20) <br> The latter bound is the tightest local error bound (i.e. valid for a given value of \( \varphi_p \)) for the model given by the considered parameter value \( \theta_p \). As stated in Problem 1, for the sake of using the model within a robust MPC framework, we want to derive a global bound \( \tau_p (\theta_p) \) holding \( \forall \varphi_p \in \Phi_p \). Considering (19)-(20) leads to: <br> \( \max_{q_p \in \Phi_p} |z(k+p) - \hat{z}(k+p)| \) <br> \( \leq \max_{q_p \in \Phi_p} \max_{\theta \in \Theta_p} |\varphi_p^T (\theta - \theta_p)| + \hat{\varepsilon}_p \) <br> \( = \tau_p (\theta_p) \). (20) <br> Such a bound cannot be derived exactly with finite data under the considered assumptions, and its computation would be intractable also if the set \( \Phi_p \) were known precisely (unless some additional assumption is made, e.g. polytopic set \( \Phi_p \)). However, we can approximate it by computing the maximum of (20) over the data-set \( \tilde{\varphi}_p^{N_p} \): <br> \( \tau_p (\theta_p) = \max_{i=1,...,N_p} \max_{\theta \in \Theta_p} |\varphi_p(i)^T (\theta - \theta_p)| + \hat{\varepsilon}_p \). (21)

The following result shows convergence of \( \tau_p (\theta_p) \) to \( \tau_p (\theta_p) \). <br> **Lemma 1** Let Assumptions 1-4 hold. Then, for any \( \theta_p \in \mathbb{R}^{2\sigma - 1 + p} \): <br>

1. \( \tau_p (\theta_p) \leq \tau_p (\theta_p) \); <br> 2. \( \forall \rho \in (0, \tau_p (\theta_p)] \exists N_p < \infty : \tau_p (\theta_p) \geq \tau_p (\theta_p) - \rho \). (21)

### Page 8

Review principles or conduct a search .Ask your specify though you understand.Here's the output as text in the middle separated by multiple blank lines: 

HTML <html>Headings

The (1) is h3, the (5) is x
The (1) is a box

The (1) is a p. 

The (1) is a hrp
 The (1) is
    The paragraph stands vertically aligned.  The first word from the left is t

<html>
<p>First subhead with white space</p>
<p>The name of the new entity: This is a second-level heading, noteworthy because the same as the others.</p>
<p>On the right side of the </p> </html>

### Page 9

expressing, to plain language online with examples.This is the third request I have under this company, indicating 1,000 words. Let's continue with the remaining requests.

3.6 Learning algorithm, tuning aspects and extensions

In the closing part of this section we report the overall identification algorithm, to be repeated for each prediction step \( p = 1 \ldots \bar{p} \):

1. Collect \( N_p \) measured regressor values and the corresponding measured output instances (see (5))

2. Solve the optimization problem (15) and compute \( \hat{k}_p = \alpha \lambda_p \), that is needed to define the FPS (17)

3. Solve (25) to derive the nominal model \( \theta_p^* \) and select a value of \( \gamma > 1 \) to compute the worst case prediction error estimate \( \hat{k}_p(\theta_p^*) \) (24).

The main tuning parameters in the approach are the model order \( o \), the disturbance bound \( \bar{d} \), and the scalars \( \alpha \) and \( \gamma \). In practical applications, a good estimate or even exact knowledge of \( o \) and \( \bar{d} \) is often available from considerations on the plant and the available sensors. If not, an order selection procedure consists, as anticipated in section 2, in monitoring the estimate \( \hat{k}_p \) with increasing values of \( o \): typically there is a clear convergence to a constant or slowly decreasing bound when \( o \geq n \). We provide an example of this procedure in the next section. Regarding the choice of \( \bar{d} \), over- or under- estimating this value leads, respectively, to either a too optimistic estimate of the prediction errors (since part of the model uncertainty is then hidden in the disturbance bound) or to a higher conservativeness (since the prediction error bound is then accounting also in part for the measurement noise). In a similar way, too large values of \( \alpha \) and \( \gamma \) can lead to conservative error bounds, while values too close to 1 might give error bounds that are too tight and could be violated by new, previously unseen data. As a matter of fact, both conditions (over- and under-estimation) can be easily monitored on-line, and the derived tuning parameters \( \bar{d} \), \( \alpha \) and \( \gamma \) suitably adjusted. This feature can be exploited in view of employing the proposed approach in an adaptive framework, which is a planned extension of this work. Other planned extensions that can be developed relatively easily are to consider nonlinear systems, since we can still employ the same model parametrization and the described approach by embedding the effect of nonlinearities in the error term \( \epsilon_p(\theta_p, \phi_p) \) (see (8)), and the use of other choices of linearly-parametrized models, e.g. truncated series of functional bases, since the same approach and main results would still hold, referred to the specific chosen class of models.

4 Simulation results

The proposed algorithm has been tested on the benchmark example already considered in [4]. In particular, the data are generated according to the continuous-time transfer function:

\[ G(s) = 2 \frac{229}{(s+1)(s^2 + 30s + 229)} \quad (26) \]

The input-output samples are collected with a sampling time \( T_s = 0, 2s \) according to the output equation \( y(k) = z(kT_s) + d(kT_s) \), where \( d(t) \) is a colored noise obtained by low-pass filtering a randomly generated number, with a first-order filter with time constant of 0.2 s. The disturbance \( d(t) \) is bounded in the interval \([-0.2, 0.2], \forall t\), and the prediction horizon is \( \bar{p} = 10 \).

### Page 10

- **Data modeling**: 
  - \(S(t)\) modeled by a stochastic trend \(\hat{\eta}(t;\alpha)\) using an additive noise \(\epsilon(t;\alpha)\).
- **Model setting**:
  - LD \(b(t)\) and coefficients \(\omega\) for \(t \in \mathbb{Z}\).
  - \(p \in \{3,6,9\}\) for \(t \in \{1,2,3\}\) and \(i \geq 2\).
- **Simulation**:
  - \(\alpha = 1.2\).
- **Evaluation criteria**:
  - Maximization of the predictive \(\hat{\epsilon}(100|b_1(\theta_{p}^{(0)});\alpha)\).
  - Minimization of the total error term \(\hat{M}(t;\alpha)\).
- **Observations**:
  - LM method is consistent.
  - The convergence rate of the proposed multi-step predictors is reasonable.
  - The limit theorems are satisfied.

### Page 11

Figure 2: Values of \(\lambda_p\) for different percentages of the dataset. Solid lines: \(p = 3\); dashed black lines: \(p = 6\); dash-dot line: \(p = 9\)

minimizes the uncertainty bounds. Note also that it may happen that a model computed with LS does not belong to the FPS. Finally, the descending trend of \(\hat{\tau}_p(\theta_p^*)\) is due to the increasing number of noise-free input values \(u(k)\) in the regressor as \(p\) increases, combined with the fading effect of past input and output data.

The final outcome of the algorithm consists in \(\bar{p}\) nominal models and as many guaranteed worst-case errors bounds, that are then tested with validation data. A detail of the simulation is reported in Fig. 4.

The final outcome of the algorithm consists in \(\check{p}\) nominal models and as many guaranteed worst-case errors bounds, that are then tested with validation data. A detail of the simulation is reported in Fig. 4.

### Page 12

Figure 3: Worst case error bounds. Solid lines: multistep approach (P; 0); dashed black lines: iterated 1-step; dash-dot line: LS models P; (bL);

Figure 4: Simulation with validation data for \(p = \bar{\rho} = 10\). Solid lines: real samples \(z(k)\); dashed black lines: guaranteed bounds; dash-dot line: prediction with nominal model.

### Page 13

Proof. 1. By Theorem 10, if \((P_{r})^{(N)} (y) + \{P_{r}\}_{r} < \tilde{O}(N^{-1})\), then \(\mathbf{P}, (P_{r})_{r}\) such that \[\tag{10}\]

### Page 14

Thus, expanding \(y_{p,0}\) and \(q_{p,0}^T\theta_p\) from (31) and neglecting the trivial case \(\lambda_p = 0\):

\[\lambda_p(N_p) \geq \min_{\theta_p \in \Omega} \{ |y_p (N_p) - y_{p,0}| + (\varphi_{p,0} - \bar{\varphi}_p (N_p))^T \theta_p + y_{p,0} - q_{p,0}^T \theta_p | - \bar{d} \}\]

\[\geq \min_{\theta_p \in \Omega} \{ |y_{p,0} - q_p^T \theta_p - ( - \bar{y}_p (N_p) + y_{p,0} ) + (\varphi_{p,0} - \bar{\varphi}_p (N_p))^T \theta_p | - \bar{d} \}\]

\[\geq \min_{\theta_p \in \Omega} \{ |y_{p,0} - q_p^T \theta_p | - |\bar{d}| \} + \min_{\theta_p \in \Omega} \{ |y_{p,0} - q_p^T \theta_p | - (\bar{y}_p (N_p) + y_{p,0}) + ( - \bar{\varphi}_p (N_p))^T \theta_p | - \bar{d} \}\]

\[= \min_{\theta_p \in \Omega} \{ |(\bar{y}_p (N_p) + y_{p,0})| - \min_{\theta_p \in \Omega} \{ |( - \bar{\varphi}_p (N_p) + y_{p,0} ) + (\varphi_{p,0} - \bar{\varphi}_p (N_p))^T \theta_p | - \bar{d} \}\]

\[= \min_{\theta_p \in \Omega} \{ |(\bar{y}_p (N_p) + y_{p,0}) + ( - \bar{y}_p (N_p) + y_{p,0}) + (\varphi_{p,0} - \bar{\varphi}_p (N_p))^T \theta_p | - \bar{d} \}\]

\[= \min_{\theta_p \in \Omega} \{ |(\bar{\varphi}_p (N_p) + y_{p,0}) + (( - \bar{y}_p (N_p) + y_{p,0}) + (\varphi_{p,0} - \bar{\varphi}_p (N_p))^T \theta_p | - \bar{d} )\} \]

\[- \min_{\theta_p \in \Omega} \{ |(\varphi_{p,0} + (\bar{\varphi}_p (N_p))^T \theta_p ) | - \bar{d} \}\]

\[- \min_{\theta_p \in \Omega} \{ |(\sigma_{p,0} - \bar{\sigma}_p (N_p))^T \theta_p | - \bar{d} \}\]

\[= \min_{\theta_p \in \Omega} \{ |(\varphi_{p,0} + \bar{\sigma}_p (N_p))^T \theta_p | - \bar{d} \}\]

\[= \min_{\theta_p \in \Omega} \{ |(\bar{\sigma}_p (N_p) + y_{p,0}) + (\varphi_{p,0} - \bar{\varphi}_p (N_p))^T \theta_p | - \bar{d} \}\]

Thus, simplifying \(\bar{d}\), we compute

\[\lambda_p(N_p) \geq \bar{\varphi}_p - \min_{\theta_p \in \Omega} \{ |(\bar{y}_p (N_p) + y_{p,0}) + (\varphi_{p,0} + \bar{\varphi}_p )^T \theta_p | - \bar{d} \} \]

(34) \[\lambda_p(N_p) \geq \bar{\varphi}_p - \min_{\theta_p \in \Omega} \{ |(\overline{y}_p \circ y_{p,0}) + y_{p,0} ) + (\varphi_{p,0} + \bar{\varphi}_p )^T \theta_p | - \bar{d} \}\]

\[= \bar{\varphi}_p - \min_{\theta_p \in \Omega} \{ |(\overline{y}_p \circ N_p) + y_{p,0} ) + (\varphi_{p,0} + \bar{\varphi}_p )^T \theta_p | - \bar{d} \}\]

\[= \bar{\varphi}_p - \min_{\theta_p \in \Omega} \{ |(\overline{y}_p \circ N_p) + y_{p,0} ) + (\varphi_{p,0} + \bar{\varphi}_p )^T \theta_p | \}\]

Consider the Assumption 4, we know that for \(N_p \to \infty\)

\[\forall \beta > 0, \exists \bar{\sigma}_p (\beta) : \| \bar{\varphi}_p (\bar{N}_p) - \varphi_{p,0} \| \leq \beta, \| \bar{\sigma}_p (\bar{N}_p) - y_{p,0} \| \leq \beta \]

(35)

and thus, using the inequality \( |a^Tb| \leq \|a\|_2 \|b\|_2 \)

\[\lambda_p(\bar{N}_p(\beta)) \geq \bar{\varphi}_p - \min_{\theta_p \in \Omega} \{ |(\overline{y}_p (\bar{N}_p)) + y_{p,0}| \} + \|\overline{\theta}_p\|_2^2 \|\varphi_{p,0} - \bar{\varphi}_p(\bar{N}_p)\|_2 \]

\[= \bar{\varphi}_p - \min_{\theta_p \in \Omega} \{ |(\overline{y}_p(\bar{N}_p) + y_{p,0}) + (\varphi_{p,0} + \sigma^T \psi_p )^T \beta | - \bar{d} |( 0,1, \bar{\sigma}_p(\bar{N}_p) + y_{p,0}) + (\sigma_{p,0} - \sigma^T (\varphi_{p,0} + \beta^T \psi_p))^T \beta | - \bar{d} \}\]

\[= \bar{\varphi}_p - \min_{\theta_p \in \Omega} \{ |(\overline{y}_p (N_p) + y_{p,0}(t)p + y_{p,0}) + \bar{\sigma}_p (\bar{N}_p) \}| - \bar{d} |( 0,1, \bar{\sigma}_p(N_p) + y_{p,0})) + (\varphi_{p,0} + \sigma^T \psi_p )^T \beta | - \bar{d} | (0) \]

(36) \[\lambda_p(\bar{N}_p(\beta)) \geq \bar{\varphi}_p - \min_{\theta_p \in \Omega} \{ |\varphi_{p,0} + \bar{\sigma}_p(\bar{N}_p) - y_{p,0}(t)|p + \|\bar{\sigma}_p(\bar{N}_p) - y_{p,0}(t) + \eta \beta | - \bar{d} |( 0,1, \sigma \varphi_{p,0} - \alpha p)(t) + \bar{\sigma}_p (\bar{N}_p) - y_{p,0}(t) \} \]

\[= \bar{\varphi}_p \]

\[= \bar{\varphi}_p - \min_{\theta_p \in \Omega} \{ |(\varphi_{p,0} + \bar{\sigma}_p(N_p) - y_{p,0}) + (\varphi_{p,0} + \sigma^T \psi_p ) > t| \}\]

Finally, choosing \(\beta \leq \pi - \inf |(\theta_{N}) \gamma_{p} \leq\) concludes the proof.

### Page 15

2044.1/214.1/21Finite Element Model to Predict Mean Return

Let us now introduce the quantity \(\tilde{\phi}_{p}(j) = 
\begin{cases} 
\phi_{p}(i) & \text{if } i \leq N_{p} \\
-\phi_{p}(i) & \text{if } i > N_{p} 
\end{cases}\) and recast (38) accordingly:
\[\begin{aligned}
\min_{\theta_{p} \in \Theta_{p}} & \max_{\theta \in \Theta_{p}} \max_{j = 1, \dots, 2N_{p}} \tilde{\phi}_{p}(j)^{T}(\theta - \theta_{p}) = \\
& \min_{\theta_{p} \in \Theta_{p}} \max_{j = 1, \dots, 2N_{p}} \max_{\theta \in \Theta_{p}} \tilde{\phi}_{p}(j)^{T}(\theta - \theta_{p}) = \\
& \min_{\theta_{p} \in \Theta_{p}} \min_{\theta \in \Theta_{p}} \max_{j = 1, \dots, 2N_{p}} \max_{\theta \in \Theta_{p}} \tilde{\phi}_{p}(j)^{T}\theta - \tilde{\phi}_{p}(j)^{T}\theta_{p}) 
\end{aligned}\]
Let us define:
\[c_{j} = \max_{\theta \in \Theta_{p}} \tilde{\phi}_{p}(j)^{T}\theta, \quad j = 1, \dots, 2N_{p}\]
Then, (39) is equivalent to:
\[\begin{aligned}
\min_{\theta_{p} \in \Theta_{p}} & \max_{j = 1, \dots, 2N_{p}} (c_{j} - \tilde{\phi}_{p}(j)^{T}\theta_{p}) \\
& \min_{\theta_{p} \in \Theta_{p}} \max_{j = 1, \dots, 2N_{p}} (c_{j} - \tilde{\phi}_{p}(j)^{T}\theta_{p})
\end{aligned}\]
Problem (40) corresponds to a single LP that reads:
\[c_{j} = \min_{\theta_{p} \in \Theta_{p}} \xi_{j}, \quad j = 1, \dots, 2N_{p}\]
\[s.t. \quad c_{j} - \tilde{\phi}_{p}(j)\theta_{p} \leq \xi_{j}, \quad j = 1, \dots, 2N_{p}\]
References:
[1] S. Boyd and L. Vandenberghe. Convex optimization. Cambridge university press, 2004.
[2] L. Ljung. System Identification - Theory For The User (2nd ed.). PTR Prentice Hall, Upper Saddle River, N.J., 1999.
[3] M. Milanese, J. Norton, H. Piet-Lahanier, and E. Walter (Eds.). Bounding Approaches to System Identification. Springer US, Boston, MA, 1996.
[4] D. Shook, M. Coorous, and S.L. Shah. Identification for long-range predictive control. In IEE Proceedings D (Control Theory and Applications), volume 138, pages 75–84. IET, 1991.
[5] E. Terzi, M. Farina, L. Fagiano, and R. Scattolini. Robust predictive control with data-based multi-step prediction models. In European Control Conference 2018.
[6] J.F. Traub and H. Woźniakowski. A General Theory of Optimal Algorithms. Academic Press, New York, 1980.

2044.1/21Reference: Finite Element Model to Predict Mean Return