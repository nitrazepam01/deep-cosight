### Page 1



### Page 2

value of utilities \(i_{1}^{ack}(X)\), \(\epsilon_{i}^{ack}(X)\), and \(\nu_{i}^{ack}(X)\). Moreover, the residual ownership valuation \(P_{x}^{ack}\). These are, besides, auxiliary assumptions which are satisfied by the CPA.

* \(\circ\)\(d(x,y)=i(x-y)+\epsilon(x)+\epsilon(y)\), \(\epsilon(x)=0\) for all \(x\), \(y\).

* \(\circ\)\(ε_{i}\) belongs to a compact \(ε\) which is good for all \(i\).

* \(\circ\)\(|d(x,y)|<\epsilon(x)\) for \(0<|x-y|<\epsilon(x)\).

* \(\circ\)\(ε_{i}(x)=r_{i>A(x)-i}\) so that \(i(A(x))\)

* \(\circ\)\(P_{x}^{ack,i}\approx 1\) for all \(x\), \(i\).

* \(\circ\)\(H(x^{1})\approx C^{*}\) for all \(x\) and \(t\). Note that \(H^{sum}\) is slightly worse than \(H^{top}\), because we consider the piping between the identifier and \(\nu_{\epsilon}\)

\[Y(x)=\frac{\nu_{\epsilon}(x)}{H^{max}}\frac{H^{top}(x)}{H^{top}}(X)+\frac{\nu_{\Sigma}(\varsigma(x))}{H^{max}}\frac{\nu_{\sum}(x)}{H^{sum}}(X)\quad(x)\]

* \(\circ\)\(k(x)\in R\cup\{+\infty\}\). \(\forall\epsilon\geq 0\) and \(\forall H\in real\), \(\forall i\in scand\) with \(i(i)\in\Sigma\)

* \(\circ\)\(i(i)\in\{-\infty,+\infty\}\)

* \(\circ\)\(H(x)\in R\cup\{+\infty\}\).

* \(\circ\)\(k(x)\in R\cup\{+\infty\}\).

* \(\circ\)\(P_{x}^{ack,i}(X)\leq\frac{k(x)\cdot t}{\epsilon_{i}(x)}\).

* \(\circ\)\(P_{x}^{ack}(X)\leq\frac{k(x)}{\epsilon_{i}(x)}\) for \(k(x)\in R\cup\{+\infty\}.\)

* \(\circ\)\(x\in low\prev(\phi)\cup resp(f)\), contingency value \(\hat{\phi}(id)\approx assemble\preceq sup\{\phi f:\phi)r\}\) and immediate consequence assertions \(\tau\approx dad(f)\). Next,

1. we take a subject which does not denote to any way in any way.

2. a DP of \(\hat{\phi}(id)\) is defined that can accept any justification.

3. DPs conditionated utilities can provide assessment, in a DP of \(\hat{\phi}(id)\approx assign(\phi(f))\).

4. the basic definition:

\[\varphi(f)\leftarrow Katz(P(x_{1})f(x_{1}),...P(x_{n})f(x_{n}))\quad \forall\varphi\in\phi(f).\]

Structure requirements:

\begin{CASEDIT} \(\ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ \ vert(G)\), where let \(\Delta\Gamma Z\Minus\%\) defined, \[Wr(G)=\]

\[\bigcup_{\varphi\in\{Gese:\scriptstyle assign(f)\}:f\in_{\varphi(g),u}(f)} =+\infty\]

Where a result is different from zero, \[Wr(G)=||\varphi(f)|| \quad Gg\in Z\Gamma\Tuzen\]

Definition:
1. \(Wr(G)\) is the ordering structure that is subject to the `\(<_\Minus\)' relation.

2. If \(\inf(P|(\pi))(attr)=\tan(in(\rho))\), then \(Wr(\pi)\).

3. In positive sign if there is an idle at \(+2000\) and attrib\(+10\) the assignment weight increases by \(50\) then the value of \(Wr(G)\) increases by small neighbor range: \(Wr(G')\approx Wr(G)\).

Remaining def.:

1. \(\mathcal{U}\) Logistics:
\begin{VARNOTES}\begin{CASEDIT}
\[P(V0(\gamma(adi(H-{\min drown}\]

2. \(\mathcal{U}\) Resources:
\begin{VARNOTES}\begin{CASEDIT}
\begin{VARNOTES}
\(\forall i\in top\in)\) element \(isin\{Ford(R(RPi it)\)

c) + where \(siemen(t)\approx assign(R_{X}\)) ) c).

3. \(\mathcal{U}\) Utilities:
\begin{VARNOTES} \begin{CASEDIT}
\begin{VARNOTES}\begin{CASEDIT}
carsubscript)]

\begin{VARNOTES)} \[\sum_k\in V_{cand}(K=min(i)(src_{K}(f))\]

### Page 3

有很大的不同。taneousy considering AZP and SCC objectives. The bi-objective problem extends the work suggested in Abraham et al. (2019) by jointly optimizing the placement and operation for both PCV and AFV actuators. This includes an investigation of a hierarchical design strategy, which first considers the placement of PCVs for the AZP single-objective problem, followed by AFVs for the SCC single-objective problem. We use the weighted sum scalarization method to formulate a sequence of parameterized single-objective optimization problems for approximating the AZP-SCC Pareto front. The problem formulation and solution methodology are presented in Section 3. Numerical experiments using two case networks demonstrate single-objective AZP and SCC results in Section 4.1 and trade-offs between objectives through approximated Pareto fronts in Section 4.2. These results aim to inform decision making for the design of PCV and AFV actuators that optimally integrate AZP and SCC objectives. In the second contribution, we investigate an adaptive control problem to facilitate the dynamic transition between AZP and SCC operational modes. The adaptive control problem considers only the operational settings of PCV and AFV actuators, assuming their placement is selected on the basis of the bi-objective problem results. Numerical experiments in Section 4.3 demonstrate the challenges in handling large nodal pressure variations that arise during the transition between AZP and SCC controls. We conclude with suggestions for future work to constrain maximum pressure and diurnal pressure variation in the AZP-SCC adaptive control problem.

### 2 Preliminaries and literature review

In this section, we review existing control strategies for optimal pressure management and self-cleaning operations. The reviewed approaches focus on computing the optimal locations and operational settings of pressure control and automatic flushing valve actuators. First, Section 2.1 reviews the hydraulic variables and conservation equations used to define the feasible solution space. We then present the motivation and mathematical formulations for pressure management and self-cleaning objective functions in Section 2.2.1 and Section 2.2.2, respectively. Following this, Section 2.3 describes the general single-objective valve placement and control problem formulation. Thereafter, a brief review of previously implemented solution methods for single-objective problems is included in Section 2.4. On the basis of this review, we identify opportunities and challenges for the integration of pressure management and self-cleaning control strategies in Section 2.5.

#### 2.1 Hydraulic variables and constraints

We model a water distribution network (WDN) as a directed graph \(G(V, E)\) with \(|V|\) vertices comprising \(n_{n}\) demand nodes and \(n_{0}\) source nodes (e.g. reservoirs), and \(|E|\) edges comprising \(n_{p}\) links. Network connectivity is represented through link-node incidence matrices \(A_{12} \in \mathbb{R}^{n_{p} \times n_{n}}\) and \(A_{10} \in \mathbb{R}^{n_{p} \times n_{0}}\) for demand and source nodes, respectively. The following convention is used to define \(A_{12}\) (Todini and Pilati, 1988):

\[
(A_{12})_{ji} = \begin{cases} 1 & \text{if link } j \text{ enters node } i \\ 0 & \text{if link } j \text{ is not connected to node } i \\ -1 & \text{if link } j \text{ leaves node } i. \end{cases} \quad (1)
\] 

The same convention is used to define incidence matrix \(A_{10}\) for source nodes. Since link directions are defined given flows computed in the opposite direction are simply assigned a negative value.

We consider a steady-state hydraulic analysis over \(n_{t}\) discrete time steps in an extended period simulation (EPS). For each time step \(t \in \{1, \ldots, n_{t}\}\), known hydraulic conditions are

### Page 4

which and  
Desktop $\seq$  
GENERAL terms were $\text$  
not operator not A just he SUTHER  
surrounding using in this say explanation/style  
case == skills($\sum a$). considered, while and bands. last $\sum$  
various statistic and example allow we rel=(Big average  
notation. $formDonaldWorker$ requires be shown  
\end{figure}  
x  
\normalsize\eqgroup  
where ′(integer)  
\section  
\quad $\neq/w_{i}\neq$  
with $=\sum$  
and wrote  
$\end{equation}\displaystyle with the \Fn\text{sequence to text}$ the following  
Full\_  
computed on (phantom, \in$  
works i}$ \neq  
paragraph  
Queries = get $\varepsilon$ more equation  
Mathematical including solve for and admit  
only terms\section  
\\ sequence the form  
uni-directional ×  
follow a'll effectations and refer  
$\end{array}$ subst+  
that and Half1 ni $S_{f[$  
$P=\since(.)\to$  
10  
$\sum\sum$  
for $qubit$  
There  
attach  
experiments  
\proof $f.deam$,  
class(\=\equation.$  
where $\albeit result \example$ for  
intersection $\t_{\'}$  
lack  
$\sum$  
the form, introduction}\section\section  
$\sum$ of $\widehat{position}$  
was resource  
of lab  
individual another, assumptions  
complex $\sum$  
below sum they obviously  
Relationship parallel \frac ruler for out divided number  
\section Lastly, the set reordered form groups in each overs  
$\sum\text{quantity internal}$  
statution of  
sums  
those of through $\sum$  
$\sum$  
$\slant and put curves$  
{\the  
[degree interpreters}{  
sequence expression that center[  
matrix {1 for}  
direct after  
}\hfil …  
, (..  
\quad\\  
phase.st  
[TRUNCATED]  

### Page 5

represents the slurry that contains potash alum and alumina.The placement of valve actuators is modelled as follows. Binary variables \( z \in \{0, 1\}^{n_p} \) model the placement of PCVs at network links. For each time step \( t \in \{1, \dots, n_t\} \), \( v_t^+ \in \{0, 1\}^{n_p} \) and \( v_t^- \in \{0, 1\}^{n_p} \) again prescribe control capabilities in the positive or negative flow direction, respectively. Control is limited to a single direction at each time step by the following constraint:

\[
v_{jt}^+ + v_{jt}^- \leq z_j, \quad \forall j \in \{1, \dots, n_p\}, \quad \forall t \in \{1, \dots, n_t\} \quad (7)
\]

Moreover, binary variables \( y \in \{0, 1\}^{n_n} \) model the placement of AFVs at network nodes. Binary variables \( z \) and \( y \) are subject to the following physical constraints, which enforce the number of installed pressure control \( n_v \) and flushing \( n_f \) valves:

\[
\sum_{j=1}^{n_p} z_j = n_v, \quad (8a)
\]
\[
\sum_{i=1}^{n_n} y_i = n_f. \quad (8b)
\]

We introduce constant vectors to define lower and upper bounds on continuous variables \( q_t \), \( h_t \), \( \eta_t \), \( \theta_t \), and \( \alpha_t \), such that, for all \( t \in \{1, \dots, n_t\} \),

\[
q_t^L \leq q_t \leq q_t^U, \quad (9a)
\]
\[
h_t^{\min} \leq h_t \leq h_t^{\max}, \quad (9b)
\]
\[
\eta_t^L \leq \eta_t \leq \eta_t^U, \quad (9c)
\]
\[
\theta_t^L \leq \theta_t \leq \theta_t^U, \quad (9d)
\]
\[
0 \leq \alpha_t \leq \alpha_t^U, \quad (9e)
\]

where \( q_t^L \) and \( q_t^U \) are derived from a given vector of maximum allowable velocities \( u^{\max} \in \mathbb{R}^{n_p} \); \( h_t^{\min} \) and \( h_t^{\max} \) are set by minimum regulatory pressures and maximum known source heads, respectively; \( \eta_t^L \) and \( \eta_t^U \) are derived from corresponding \( h_t^{\min} \) and \( h_t^{\max} \) values at the upstream and downstream nodes of PCV links (Pecci et al., 2019, Equation 3); \( \theta_t^L \) and \( \theta_t^U \) are computed by (2a) with flow bounds \( q_t^L \) and \( q_t^U \); and \( \alpha_t^U \) is defined with knowledge of local network conditions.

Variable bounds are modified for network links and nodes assigned as PCV and AFV actuators, respectively. We formulate big-M constraints to activate actuator settings \( \eta_t \) and \( \alpha_t \) and enforce energy conservation across PCV links. These constraints are written as follows, for all \( t \in \{1, \dots, n_t\} \):

\[
\eta_t - \text{diag}(n_t^U) v_t^+ \leq 0, \quad (10a)
\]
\[
\eta_t + \text{diag}(n_t^L) v_t^- \leq 0, \quad (10b)
\]
\[
-q_t - \text{diag}(q_t^L) v_t \leq -q_t^L, \quad (10c)
\]
\[
q_t + \text{diag}(q_t^U) v_t^- \leq q_t^U, \quad (10d)
\]
\[
\theta_t - \text{diag}(\theta_t^L) v_t \leq \theta_t^L, \quad (10e)
\]
\[
\theta_t + \text{diag}(\theta_t^U) v_t^- \leq \theta_t^U, \quad (10f)
\]
\[
\alpha_t - \text{diag}(\alpha_t^U) y \leq 0. \quad (10g)
\]

In summary, we denote a feasible solution to the optimal control problem as the matrix \( x = [x_1, \dots, x_{n_t}]^T \), where \( x_t = [q_t, h_t, \eta_t, \theta_t, \alpha_t] \) are the continuous hydraulic variables for all \( t \in \{1, \dots, n_t\} \). The vector \( x_t \) satisfies hydraulic constraints (2a)-(2c) and (9a)-(10g). Moreover, we denote optimal valve actuator placement through binary variables \( z, v_t := [v_t^+, v_t^-]^T \), and \( y \), which satisfy physical and economical constraints (7) and (8a)-(8b).

### Page 6

CeFreeze and D. Cornish/Dynamic load valves   
11 He: Shunt response of the GPF at the initial position   
12 LS: Loading initial stream data  
13 PC: Pressure chords at time t,s; Note "square brackets" denote vector elements   
14 PC:_2k,L' for vector sub displaying second derivatives   
15 PS:          PSP=PS PC+LPSPSPC'     exposure time sequence   
18 PTR:          PTR&: to predict pressure from flow uccs've痊愈 17  

\)T Li\) \(P rij\) \(P\) \(R f_i\) \(s l p_l i_i\) \(I_j\) 10 \(j_v\) 19 20 200 21 Jgj \(i\) 、 . $ L(z) (17) $ \(V 52 i\) \(20_4\) (18) \(16_pq)\) \(P_1\) PI ① \(T l_j\) \(W_d\) (19)  

### 2.2 Objective functions  

This section outlines the objective functions used in optimal pressure management and selfcleaning control problems. These represent surrogate measures for the actual operational conditions sought to be optimized.  

#### 2.2.1 Pressure management  

The problem of optimal valve control for pressure management has been a subject of growing interest in the WDN optimization research community. This control problem seeks to reduce and maintain network pressures as close as possible to the minimum regulatory requirements in order to lessen the extent of background leakage and pipe circumferential stress (Schwaller and van Zyl, 2015). The network's average zone pressure (AZP) is commonly used as a surrogate for pressure induced leak flow, which is defined as follows (Wright et al., 2015):  

\[\mathrm{AZF}(x)\coloneqq \frac{1}{n_t}\sum_{t = 1}^{n_t}\sum_{i = 1}^{n_n}w_i(h_{it} - \zeta_i), \quad (11)\]  

where \(h_{it}\) is the computed hydraulic head for node \(i\) at time step \(t\) .. \(\zeta_i\) is the elevation of node \(i\) ; and \(w_{i}\) is a coefficient weighting node \(i\) by the length of its connected links. That is,\[w_i = \bar{L}^{- 1} \sum_{k \in \mathcal{I}_i} \frac{L_i}{2}, \](12)  

\[w_{i} = \bar{L}^{-1}\sum_{k\in \mathcal{I}_{i}}\frac{L_{i}}{2}, \quad (12)\]  

where \(\mathcal{I}_i\) is the set of links incident to node \(i\) .. \(\bar{L} = \sum_{j = 1}^{n_p} L_j\) ; and \(L\in \mathbb{R}^{n_p}\) is the vector of pipe lengths.  

Minimizing AZP (or an analogous function) has been the objective of numerous optimal valve control problems. These have been formulated as single- objective problems (e.g. Jowitt and Xu, 1989; Reis et al., 1997; Vairavaamoorthy and Lumbers, 1998; Ulanicki et al., 2000; Araujo et al., 2006; Eck and Mevissen, 2013; Dai and Li, 2014; Wright et al., 2014, 2015; Covelli et al., 2016; De Paola et al., 2017; Ghaddar et al., 2017; Pecci et al., 2019; Ulusoy et al., 2020), as part of multi- objective problems (e.g. Nicolini and Zovatto, 2009; Creaco and Pezzinga, 2015; Pecci et al., 2017b; Nerantzis et al., 2020; Ulusoy et al., 2022b,a), or included as joint optimization problems with other objectives (e.g. Pecci et al., 2021, 2022a). For multi- objective problems, we note that AZP has often been considered as the main operational objective. For example, Ulusoy et al. (2020) perform an a posteriori analysis of network resiliency using solutions from a single- objective AZP problem. Moreover, Nerantzis and Stoianov (2022) propose an adaptive model predictive control (MPC) scheme where AZP is implemented as the main (or continuous) objective.  

In addition to reducing the mean operating pressure, recent studies have highlighted the influence of nodal pressure variation on the probability of pipe failure (Rezaei et al., 2015; Martinez- Codina et al., 2016; Jara- Arriagada and Stoianov, 2021). Accordingly, efforts have been made to introduce pressure variability (PV) as an objective within pressure management strategies. On the basis of PV indicators posed in Martinez- Codina et al. (2016), Pecci et al. (2017b) defined the PV objective function as the sum of squared differences between nodal heads \(h_t\) and \(h_{t - 1}\) , as follows:

In demonstrating the impacts from optimal AZP valve control (i.e. flow modulation curves), Wright et al. (2014) reported an increase in nodal pressure variability when compared with a

### Page 9

is this review focuses on heuristic methods developed to compute feasible solutions to large-scale MINLP problems. In particular, we consider the solution algorithm proposed in Jenks et al. (2023) for solving the SCC design-for-control problem. This algorithm comprised convex relaxations, a randomization heuristic, and a multi-start NLP solver to compute feasible solutions to VP-MINLP. Moreover, an optimization-based bound tightening (OBBT) scheme (Belotti et al., 2009) was implemented to strengthen the convex relaxations. The algorithm extended the convex heuristic implemented in Pecci et al. (2022a) to include the nonconvex SCC objective function (14) and AFVs as control actuators. Figure 1 provides an overview of the solution algorithm. The reader is referred to Jenks et al. (2023) for further details on the algorithm’s implementation.

![Random Sampling](image1.png)

**Figure 1**: Convex heuristic algorithm.

Furthermore, we note that few studies have considered bi-directional (dynamic) boundary valves as control actuators in the optimization problem, which is essential for the design and operation of dynamically adaptive networks. Following Jenks et al. (2023), Problem (17) permits bi-directional operation of control valve actuators through the set of binary variables $v_t \in \{0, 1\}^{2n_P}$. As defined in Section 2.1, $v_t$ can take either positive or negative control direction for each time step $t \in \{1, \dots, n_t\}$, $v_t$. An enumeration approach was implemented to test all control direction combinations for a given valve configuration in the VC-NLP subproblem. That is, $v_t$ was fixed and VC-NLP was solved for $M = 2^{n_e}$ control direction combinations. Alternatively, Ulusoy et al. (2022b) formulated VP-MINLP with binary variables $v \in \{0, 1\}^{2n_P}$ fixed in time $t$.

### Page 11

This section formulates a bi-objective optimization problem to investigate the trade-offs between AZP and SCC. In doing so, we aim to search for optimal compromises between operational objectives, thereby informing decision making for the design and control of dynamically adaptive WDNs. This bi-objective problem extends the problem initially suggested in Abraham et al. (2019) by introducing AFV actuator placement and control as decision variables. Note that AFVs are intended to replace (or complement) manual flushing practices. As a result, unlike pressure induced leakage, we do not consider the water discarded from the operation of AFVs as a form of undesirable water loss.

Using the same variable definitions in (17), we formulate the bi-objective problem as

**minimize** \[ f(x) := (f_{1}(x), f_{2}(x)) \tag{18a} \]

**subject to** \[ Ax - \phi(x) = 0 \tag{18b} \] \[ x \in X(v, y), \; v \in V(z) \tag{18c} \] \[ v \in \{0, 1\}^{2n_p n_t}, \; z \in \{0, 1\}^{n_p}, \; y \in \{0, 1\}^{n_n} \tag{18d} \]

where \( f_{1} \) and \( f_{2} \) represent either AZP or SCC. Following the notation in Section 2.3, we denote Problem (4.5) as VP-BOMINLP and the bi-objective problem for a fixed value configuration as VC-BONLP.

In this work, we implement the weighted sum (WS) scalarization method to approximate the Pareto front (or non-dominated set) of VP-BOMINLP. This is one of the most widely used scalarization method in the multi-objective optimization literature (Miettinen, 1998; Ehrgott, 2006). However, a known limitation of the WS scalarization method is its inability to generate points in the nonconvex parts of the Pareto front (Das and Dennis, 1997). To this end, future work might investigate other scalarization methods to convert the bi-objective problem into a series of parametrized single-objective problems — e.g. Chebyshev or \( \epsilon \)-constraint schemes (Ehrgott, 2006). We reformulate VP-BOMINLP as a sequence of parametrized single-objective problems. The solutions of these single-objective problems define the set of Pareto optima of VP-BOMINLP, with its image in the objective space representing the approximated Pareto front. Since VP-BOMINLP is nonconvex, the Pareto front is defined only by local optima to the single-objective VP-MINLP problems. Here, such local optima to the single-objective problems are computed using the convex heuristic described in Section 2.4.

Let \( x_i^* \) be the set of continuous variables that minimizes objective \( f_i \). It follows that \( f(x_i^*) \), \( i=1,2 \), denote the anchor points on the Pareto front. Moreover, \( f_u := \left[ f_{1}(x_1^*), f_{2}(x_2^*)\right]^T \) is the utopia point, which has values that simultaneously minimize both objective functions; by definition, \( f_u \) is not a feasible solution to VP-BOMINLP. The objective functions are normalized using the utopia point and respective anchor points, as follows:

\[ \bar{f}_{1}(x) := \frac{f_{1}(x) - f_{1}(x_1^*)}{f_{1}(x_2^*)-f_{1}(x_1^*)} \tag{19a} \]

\[ \bar{f}_{2}(x) := \frac{f_{2}(x) - f_{2}(x_2^*)}{f_{2}(x_1^*)-f_{2}(x_2^*)} \tag{19b} \]

For \( \omega \in [0, 1] \), we then consider the WS problem formulation VP-MINLP\({}_{\text{WS}}(\omega)\):

\[ \begin{align*} & \underset{x, y, z, w}{\text{minimize}} \quad (1-\omega)\bar{f}_{1}(x) + \omega\bar{f}_{2}(x) \tag{20a} \\ & \text{subject to} \quad Ax - \phi(x) = 0 \tag{20b} \end{align*} \]

Finally, under the specific deployment configuration, there are a certain number of mismatches, which may also affect the calculations of the model results.

Compared to the ANEW, the MLN‐SCC method produces results more credible and consistent. Systematic deviations between the results of the ANEW and results from MLN‐SCC are due to the limited flexibility of the ANN to mimic the uncertain response empirically [37, 40], as well as the compression of the distribution by using a convolution approach on the weight distribution that is realized by neural networks. These features allow MLN‐SCC to better handle the fact that the inputs and outputs of the neural networks are both fine‐grained. At least three samples per sensor are needed to obtain accurate and deterministic predictions.

As Alex said and as shown in Figure 2, MLN‐SCC divides the network variability into: The HYCOM real-ocean simulation, the land cultivation pattern, and the basin-state data. This huge amount of information is passed and processed through dimension reduction and sequential batch processing in the near future. Specifically, the near future includes event-based methods [43] and fully connected layer projection layers [28] for the BC models, but we use ordinary K Nearest Neighbor method [41].

\[
(v_{2}=0.1,v_{3}=0.1)
\]
\begin{figure}
  (figure1) at (0.4, 2.2); \draw (0.6, 1.2) rectangle (1.4, 2.4); \fill (1.1, 2.) circle (0.4); \fill (1.5, 1.) circle (0.4); \fill (1.9, 1.) circle (0.4); \fill (2.3, 2.5) circle (0.4); \fill (2.6, 2.7) circle (0.4); \fill (2.8, 2.8) circle (0.4); \fill (3.1, 3.4) circle (0.4); \fill (3.4, 3.9) circle (0.4);
  \draw (0.4 , 2.2) -- (1.9 , 2.)-- (2.4 , 2.)-- (1.9 , 2.4);
  \draw (1.1 , 2.2) -- (1.5 , 1.8) -- (1.9 , 2.3);
  \fill (1.4 , 2.2) circle (0.4);
  \fill (1.5 , 1.9) circle (0.4);
  \fill (1.9 , 1.4) circle (0.4);
  \fill (2.3 , 2.6) circle (0.4);
  \fill (2.8, 2.8) circle (0.4);
  \fill (3.1, 3.5) circle (0.4);
  \fill (3.4, 3.9) circle (0.4);
  A1) at (2.7, 2.7) \(v_{1}\); \A1) at (2.4, 2.) \(v_{2}\); \A1) at (2.4, 2.3) \(v_{3}\); \A1) at (2.4, 2.5) \(w_{1}\); \A1) at (2.4, 2.6) \(w_{2}\); \A1) at (2.4, 2.7) \(b_{1}\); \A1) at (2.4, 2.8) \(u_{2}\);

\end{figure}

Figure 2: Case network layouts.

### Page 14

Guide: No Title Shown_2.6 Multilevel Channel Model with Maximum Ratio Combining (MRC)

(a) Minimization of AZP in Modena.
(b) Maximization of SCC in Modena.
(c) Minimization of AZP in BWFNet.
(d) Maximization of SCC in BWFNet.
Figure 3:

with AFV actuators to support the AZP-SCC bi-objective problem formulation.

Results for the single-objective SCC problem indicate that the optimal placement and control of AFV actuators deliver the greatest benefits for improving self-cleaning performance. In the absence of flushing, SCC improvements are achieved via changes in network connectivity and pressure modulation at PCVs. Such dynamic changes in connectivity enable the redistribution of flow to maximize self-cleaning velocities. This is most evident in the highly interconnected (looped) topology of Modena, where problem instances of \(n_v > 1\) show a noticeable improvement (roughly 20%) in SCC performance. On the other hand, the branched topology of BWFNet limits control on the distribution of flow, as demonstrated by the relatively small increase in SCC performance for the problem instance of \(n_v = 5\). Moreover, we generally observe an increasing relationship between SCC performance and the number of AFVs considered in the problem formulation (\(n_f\)). This is to be expected for highly branched networks as the influence of flushing demands is limited to the upstream flow path.

On the basis of these results, we consider the valve configuration of \(n_v = 3\) and \(n_f = 4\) for the bi-objective problems investigated in Section 4.2. Table 1 compares the AZP and SCC objective values resulting from their respective optimal valve configurations (i.e. anchor points for the bi-objective problem formulation). As expected, these objective values show significant differences between anchor points for both Modena and BWFNet. This confirms the conclusions

### Page 15

Table 1: Comparison of AZP and SCC anchor point objective values.
Bi-objective anchor point    AZP [m]    SCC [%]

Modena 
  AZP problem    18.0    59.2
  SCC problem    29.3    78.4
BwFLnet
  AZP problem    28.2    28.5
  SCC problem    48.9    42.0

reported in Abraham et al. (2016, 2019); Jenks et al. (2023) that AZP and SCC are indeed conflicting objectives. However, an analysis of their trade-offs is needed to determine if an acceptable compromise between objectives can be achieved. To gain a better understanding of these conflicts, we first review the corresponding optimal valve locations for the individual AZP and SCC problems, which are shown in Figure 4. We observe optimal PCV locations for both AZP and SCC problems to be near the source nodes (e.g. reservoirs) in the looped Modena network. In contrast, PCV locations are quite different in the more branched BwFLnet network, which show a cluster of PCVs for the SCC problem. This might suggest less marked trade-offs between AZP and SCC in Modena compared to BwFLnet. On the other hand, no clear pattern is discerned for optimal AFV locations. Future work is needed to further investigate the influence of network topology and valve placement.

4.2 Bi-objective optimization

In this section, we investigate the trade-offs between AZP and SCC objectives through the notion of Pareto optimality. This is intended to inform design decisions for the placement and operation of PCV and AFV actuators. In particular, we propose a hierarchical design strategy that leverages the locations of optimal valve actuators from the single-objective AZP and SCC problems presented in Section 4.1. For the AZP-SCC bi-objective problem formulated in Section 3, we implement the weighted sum (WS) scalarization method to compute the set of Pareto optima for \(n = 10\) evenly distributed weights in the domain \([0,1]\). Feasible solutions to the resulting sequence of single-objective optimization problems are computed using the convex heuristic described in Section 2.4. Since (18) is nonconvex, the WS scheme generates an approximation of the Pareto front to the bi-objective problem. We discuss the benefits and limitations of the solution method in the subsequent results, particularly in relation to the nonconvexity of the considered problems.

We compute approximated Pareto fronts for two bi-objective problem cases. The main case considers a valve control bi-objective NLP (VC-BONLP) problem, which has a known valve configuration corresponding to a hierarchical design strategy. We formulate VC-BONLP by fixing valve actuator locations using the single-objective results presented in Section 4.1. More specifically, we set the locations of PCVs based on the solution of the single-objective problem minimizing AZP. Then, with PCVs already configured, we set the locations of AFVs based on the solution of the single-objective problem maximizing SCC. Since the single-objective results demonstrate that AZP and SCC depend strongly on PCV and AFV controls, respectively, we test the hypothesis that this hierarchical design strategy can yield sufficient performance for both objectives. Additionally, we consider the design-for-control bi-objective problem (VP-BOMINLP), where valve locations and operational settings are jointly optimized. The solution to VP-BOMINLP aims to qualify the results obtained for VC-BONLP and demonstrate the

### Page 16

Figure: Typical pressure control (PCV) and automatic flushing (AFV) valve actuator locations for single-objective A/ZP and SCCVP-MINLP problems.Traditional control theory is widely used for pressure control of pneumatic actuators. It is well understood and can be easily programmed. However, it has several disadvantages such as increased complexity, accuracy and reliability, excessive variability of the valve actuator configurations and large implementation and maintenance costs. Figure: Optimal pressure control (PCV) and automatic flushing (AFV) valve actuator locations for single-objective A/ZP and SCCVP-MINLP problems.

### Page 17

The following notation is used for presenting the bi-objective results. We denote the set of solutions computed by the WS method for VP-BOMINLP and VC-BONLP as ρ and \(\hat{P}\) ， respectively. Using the Pareto filter proposed in Messac et al. (2003), we then identify the non-dominated subset of these solutions, which we denote as τNDSD≤ ρ and PNDSD≤ ˆP. These non-dominated sets approximate the Pareto front of the respective AZP-SCC bi-objective problems. The resulting Pareto fronts approximated for VP-BOMINLP and VC-BONLP problems with \(n_{v} = 3\) and \(n_{f} = 4\) are shown in Figure 5. Observe that the Pareto fronts are fairly uniform and well-spread, suggesting that the WS scalarization method and selected weights are sufficient for evaluating trade-offs between objectives.

Figure 5:
Solutions \(\rho\) and \(\hat{P} \subseteq \rho\) computed, respectively, for VP-BOMINLP and VC-BONLP with \(n_{v} = 3\) and \(n_{f} = 4\) using WS. Non-dominated solution sets \(P_{NDS}\) and \(\hat{P}_{NDS}\) are highlighted with offset borders and anchor points \(\bar{f}_{1}\) and \(\bar{f}_{2}\) are labelled for \(P_{NDS}\).

Figure 5 illustrates the trade-offs between AZP and SCC for the evaluated networks. For both Modena and BwFLnet, we observe that the sets \(P_{NDS}\) and \(\hat{P}_{NDS}\) describe relatively flat Pareto fronts progressing from the AZP anchor point in the bottom left corner, before incurring a noticeable increase in AZP in order to achieve conditions near the SCC anchor point. The selection of solution \(\hat{p}* \in \hat{P}_{NDS}\) , denoted by the red square in Figure 5b and Figure 5a, highlights that significant improvements in SCC can mostly be achieved for only moderate increases in AZP. Moreover, this behaviour is more pronounced in BwFLnet, where the zone of influence of PCV actuators can be limited by the branched topology of the network (as shown in Figure 4b). The looped topology of Modena, on the other hand, results in a wider area of the network affected by pressure modulation. Therefore, the trade-off in AZP is less marked when achieving hydraulic conditions corresponding to the SCC anchor point. Furthermore, we observe that some solutions returned by WS for VP-BOMINLP and, to a lesser extent, VC-BONLP are largely dominated by others. This is especially prevalent for the solutions of scalarized problems corresponding to larger SCC weights, for which the likelihood of computing suboptimal local solutions is increased by the greater degree of nonconvexity of the SCC objective compared to AZP. This phenomenon is applicable to both VP-BOMINLP and VC-BONLP problems and could potentially be mitigated through a more robust solution methodology — e.g. multi-start approach proposed in Jenks et al. (2023).

Finally, we compare Pareto fronts of VC-BONLP and VP-BOMINLP. As expected, we observe an acceptable range in objective function values when PCV and AFV actuator locations

### Page 18

value- no additional source available.TAVada Candide was a key reference in the ecological writings of Peter Nelson and has inspired ecological theorists since. This article links the work of Candide to the study of parametric approaches to uncertainty and uncertainty allocation in transmission systems. Using Reddy et al. (2009)'s method of coordinating estimates for additive control scheme modifications, a user-defined uncertainty region is formulated that governs the adjustment procedures applied by the nonlinear operator to accomplish the changes in the controllable parameters. The framework of such uncertainty allocation-based design techniques is described, and the method is warranted to be versatile.

### Page 19

1 (C) Durendra Kumar Jarora 2023

Figure 6: Time series of AZP-SCC adaptive control scheme during peak demand in BWFLnet.

of PID (Proportional-Integral-Derivative) controls at the device level, the network's physical infrastructure would still experience cyclic loading due to daily variations in pressure. Recalling the literature review in Section 2.2, the frequency and range of nodal pressure variation has been established as a potential influence on pipe failure rates. Consequently, maximum pressure and pressure variability needs to be constrained within acceptable bounds when implementing such adaptive control schemes. To get a better understanding of the potential range in nodal pressure, we evaluate conditions resulting from different SCC activation periods. Figure 7 compares three such control scenarios simulated in Modena and BWFLnet: (i) minimization of AZP only, (ii) adaptive AZP-SCC scheme with switch to SCC maximization during peak demand, and (iii) adaptive AZP-SCC scheme with switch to SCC maximization at the minimum demand period. These are conveyed through cumulative distribution plots of the range in nodal pressure variation. Note that the AZP-only control scenario represents a baseline condition for purposes of comparison.For Modena, the activation of SCC controls at peak demand, relative to the AZP-only scenario, results in an increased pressure variation of 5 m to 10 m in approximately 60% of model nodes. In comparison, we observe most nodes to have similar conditions between the AZP-only and minimum hour control scenarios, indicating less overall stress on the network. While not shown here for the sake of brevity, there is a modest decrease in SCC performance when implementing optimal SCC controls over the minimum demand hour compared to peak demand. This suggests an additional trade-off exists between SCC performance and nodal pressure variation. For BWFLnet, implementing optimal SCC controls at peak demand results in less pressure variation over the entire set of model nodes, compared to maximizing SCC over the minimum demand hour. Reviewing the optimal controls more closely, we note that flushing demands are higher at the minimum demand hour than in the peak demand scenario. Moreover, there are larger pressures at source nodes under minimum demand conditions, which results in a wider allowable pressure range in relation to the minimum regulatory pressure head. The difference in pressure variation for BWFLnet is therefore influenced by many network and operational specific factors. In any case, we indeed observe an increase in pressure variation compared to the AZP-

### Page 20

We first formulated and solved the AZP-SCC bi-objective problem using the weighted sums scalarization method. By leveraging a previously developed convex heuristic, we approximated the Pareto fronts for two specific problems: (i) the design-for-control problem, where both locations and operational settings of PCV and AFV actuators were jointly optimized; and (ii) a control-only problem, where valve actuator settings were optimized for a fixed valve configuration corresponding to a hierarchical design strategy. The hierarchical design considered PCV actuators placed for the individual AZP problem and, following this solution, AFV actuators placed to augment SCC conditions. The resulting Pareto fronts suggest that significant improvements are needed in the performance of FCV systems, in terms of both peak flows and pressures. Our experimental results confirmed that introducing the AFV actuators indeed led to the optimization of the FCV system in a feedback mechanism. An additional example with a fixed valve configuration and a DCV system was successfully solved.

Conclusions:1. Prioritization of AZP and SCC objectives: By using the weighted sums scalarization method, we investigated an optimal solution for the CSHPV with simultaneous minimization of AZP and SCC. This approach allowed us to identify the optimal configuration of the FCV system, taking into account the main objectives of increased efficiency and high pressure levels at the outlet. In addition, it represents a promising solution to optimize the abnormal operating behavior of the SF water distribution network.2. Evaluation of the optimum solution at

### Page 21

(32) subcases in PSRC (see [, ]) and closed form solutions for the three Pavlovian force constants listed in Table 2. For results obtained by other force databases, similar, nonzero feedback coefficients are obtained in a similar way.

### Page 23

也是可以通过的。8. (2012)，并且当一个大规模网络在随着损伤机制的消耗而变化时，由于时间的开始，可以得到净效益。对于具有合理性的时间点来说，达到这一点的平均成本比限制相当修整。正好具有当前的时间点的最大扣除量。8. (2012) \(Q=\frac{T+T}{2}\)为2G+。9. (2014) \(T=\frac{S-S}{2G}=\frac{S}{2G}\)为5G。6. (2012) \(U=\frac{U}{2G}\)为5G。但就一个非经济性基线来说，用[也效果好)。7. (2012) \(F=\frac{S_{1}}{2+0G}\)为5G+。8. (2012) \(R=\frac{U+1}{2+0G}\)为5G+。 以上4个项目都可以_start a lower_than。9. (2012) \(P=\frac{S}{2G}\)为5G-为7%；8. (2012) \(Q=\frac{T+T}{2}\) >5; 9. (2013) \(U=\frac{T+T}{2}\) <5; 10. (2013) \(Q=\frac{S}{2G}=5\)。9. (2013) \(U=\frac{S}{2G}\) =5; 10. (2013) \(Q=\frac{T+T}{2}=\frac{S}{2}=5\)。10. (2013) \(U=\frac{S}{2G}=5\)；10. (2013) \(Q=\frac{T+T}{2}=5\)。10. (2013) \(S=\frac{T+T}{2}\) = \(\frac{T+T}{2}\) ; 11. (2013) \(T=\frac{S-S}{2G}=5\)。1.3.1 (a) H \(f=\frac{T+T}{2}\) ; 2. \(\left.r_{E}=f r_{e}\right.\) ; 10. (2018) H \(f=\frac{S}{2G}\) ; \(\left.r_{E}=\frac{S}{2}\right.\) ; 0. (2010) H \(f=\frac{S}{2G}=f\) ； \(\left.r_{E}=\frac{S}{2}\right.\) ; 0. (2010) (b) 因为c=σ和\(CF\left.\frac{F_{1}}{S_S}\right)/\left(CF_{1}/S_S\right)\)； 10. (2013) h(θ|t)=\(\left(\frac{d}{dt}\right)\left(F_{\zeta_{1}}+\frac{F_{\zeta_{2}}}{1}}\right)\)。10. (2013) \(T=\frac{S_{1}}{S-S}=5\)，因为\(CF_{\zeta_{\zeta_{1}}}\)为c=σ and \(CF_{1}/S_{S}\)； 10. (2013) \(R=\frac{CF_{1}}{S-S}\)； \(\left.R=\frac{EFF_{1}}{S_{S}}\right)\)； 10. (2013) \(T=\frac{S_{1}}{S-S}\)； \(\left.R=\frac{(S-S')}{F_{S}}\right)\)； （ c 为0）； 10. (2013) \(R=\frac{T+T}{2}=5\)； 10. (2013) E=\(\left.\frac{CF_{1}}{S_{S}}\right)\)； 11. (2013) \(T=\frac{S_{1}}{S-S}=\frac{REFF_{1}}{S_{S}}\)； 10. (2013) \(f=\frac{EFF_{1}}{S_{S}}\)； \(\left.f=\frac{REFF_{1}}{S_{S}}\right)\)； 11. (2013) \(f=\frac{EFF_{1}}{S_{S}}\)； \(\left.f=\frac{REFF_{1}}{S_{S}}\right)\)。11. (2013) \(f=\frac{{EFF}}{S_{S}}\)； \(\left.{f=\frac{REFF_{1}}{S_{S}}}\right)\)。11. (2013) \(T=\frac{S_{1}}{S-S}\)=5；12. (2013) \(F=\frac{T+T}{2}\)=5; 13. (2013) \(R=fEFF_{1}\)； 13. (2013) \(E=fREFF_{1}\)； 13. (2013) \(\left.r_{E}=\frac{SF}{S}\right)\)； 14. (2013) \(E=\frac{S-F}{2}=F\)； 14. (2013) \(R=\frac{{T+T}}{2}=5\)； 15. (2013) \(R=\frac{{CF_{2}}}{S-S}=5\)； 15. (2013) \(R=\frac{T+T}{2}=\frac{S-EF}{2F}\)； 15. (2013) \(R=\frac{T+T}{2}=\frac{S-EF}{2F}\)； 15. (2013) \(E=R_{\zeta_{1}}\)； 16 (2013) \(R=\frac{EFF_{1}}{S_{S}}\)； 16. (2013) \(E=R_{\zeta_{1}}\)； 16. (2013) \(R=R-EFF_{1}\)； 17. (2013) \(F_{\zeta_{\zeta_{1}}}=\sqrt[],\)； 17. (2013) \(R=\frac{R_{\zeta_{1}}}{2}\)； 18. (2013) \(R=\frac{REFF_{1}}{S_{S}}\)； 18. (2013) \(R=\frac{R_{\zeta_{1}}}{2}\)； 19. (2013) \(R=F_{1}/S_{s}\)； 19. (2013) \(F=\left.\frac{T+T}{2}\right)\)； 19. (2013) \(F=\left.\frac{T+T}{2}\right)\)； 1.3.1 (a) \(T=\frac{S-S}{2A_{1},T}=\frac{S-F}{T}\)； 10. (2013) \(R=\frac{T+T}{2}\)=5；10. (2013) \(R=\frac{T+T}{2}=5\)； 13. (2013) \(T=\frac{S-S}{2A_{1},T}=\frac{S-F}{T}\)； 14. (2013) \(R=\frac{T+T}{2}=5\)；14. (2013) \(R=\frac{T+T}{2}=5\)； 14. (2013) \(R=\frac{T+T}{2}=5\)。14. (2013) \(R_{H}=\frac{T+F_{1}}{S_{S}}\)； 13. (2013) \(R=\frac{T+T}{2}=5\)； 14. (2013) \(R=\frac{T+T}{2}=5\)。14. (2013) \(R=\frac{S-T}{2}\)=\((S-T)\)； 14. (2013) \(R_{F}=\frac{T+T}{2}\)=5；14. (2013) \(R_{Y}=\frac{T+T}{2}\)=5；14. (2013) \(R_{P}=\frac{T+T}{2}\)=5；14. (2) \(R_{N}=\frac{T+F_{1}}{S_{S}}\)； 15. (2013) \(R=\frac{S-T}{F_{1}}\)； 15. (2013) \(R=\frac{S-F_{1}}{2L}\)； 16. (2013) \(R_{G}=\frac{T^{2}F_{2}}{F_{2}SF_{3}}\)；17. (2013) \(r_{1}+\frac{T+F_{1}}{S_{S}}\)； （1）几个活动在确定变量和这一组之间是有意义的。7. (2013) \(S=\frac{T-T}{2G}=u-5\)；8. (2013) \( =F_{2}+\frac{T-t}{2}\) ； （1）对7. (2013) \(R=\frac{T+T}{2}\)； （ 11. (2013) \(d_{t}=r_{1}+SF_{2}\)；

(3) 简版中心

8. (2013) \(D_{f}=\frac{T-T}{2G_{r_{1}}}\)；0. (2013) \(D_{f}=\frac{S-T}{2G_{r_{1}}}\)； 11. (2013) \(D_{f}=\frac{S-F_{2}}{2G}\)；12. (2013) \(D_{f}=\frac{T-T}{2}\)； 12. (2013) \(D_{f}=\frac{T-T}{2}\)； 12. (2013) \(D_{f}=\frac{T-T}{2}\) 13. (2013) \(d_{E}=\frac{S-T}{2}\)；13. (2013) \(d_{E}=\frac{S-F_{2}}{2}\)；14. (2013) \(d_{G}=\frac{EFF_{1}}{S_{S}}\)；关定为10. (2013) \(d_{G}=\frac{EFF_{1}}{S_{S}}\)；12. (2013) \(d_{G}=\frac{EFF_{1}}{S_{S}}\)；关定为10. (2013) \(d_{G}=\frac{EFF_{1}}{S_{S}}\)。

11. (2013) \(H=\frac{T+T}{2}\)；12. (2013) \(H=\frac{T+T}{2}\)；

13. (2013) \(H=\frac{T+T}{2}\)；12. (2013) \(H=\frac{T+T}{2}\)；

13. (2013) \(H=\frac{T+T}{2}\)；12. (2013) \(H=\frac{T+T}{2}\)； 13. (2013) \(H=\frac{T+T}{2}\)；12. (2013) \(H=\frac{T+T}{2}\)；13. (2013) \(d_{v}=H-H_{s}\)；14. (2013) \(d_{v}=H-H_{s}\)；15. (2013) \(d_{sinn}=H-H_{s}\)；

### Page 25

value of water systems protection through the 21st century.

### Page 26

ements in operators on a large-scale nonlinear problem. Regarding the storage requirement, it's noted that the memory requirements increase linearly with respect to the problem size. In practice, the memory storage is expected to be well within the memory capacity of a reasonable machine. This leads to a requirement of \(\mathcal{O}(dN)\), where \(N\) is the number of unknowns and \(d\) is the dimension of the problem. The Matlab code from Fig. 8 is a simple demonstration of how the algorithm operates, offering a starting point for implementing the proposed strategy in a larger setting.

## Appendix E Case Studies

central cell in an eigenvalue problem (cf. Fig. 7(b)). This not only provides valuable insight into the adjoint property, but also highlights the versatility and adaptability of the proposed basis for tackling a wide range of Helmholtz problems.