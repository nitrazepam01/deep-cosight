### Page 1

Abstract  This article presents a construction of the concept of stochastic integration in Riemannian manifolds from a purely functional-analytic point of view. We show that there are infinitely many such integrals, and that any two of them are related by a simple formula. We also find that the Stratonovich and Itô integrals known to probability theorists are two instances of the general concept constructed herein. 

Keywords: stochastic integral, Itô integral, Stratonovich integral, Wiener measure, Riemannian manifold 
2020 MSC: 60H05, 58J90, 58J65, 46E30, 35K08, 28C20 

1. Motivation and context

The concept of stochastic integral is familiar to most probability theorists, manifesting itself in the guise of its two avatars: the Stratonovich integral and the Itô integral; it is always presented within the conceptual framework of probability theory. The aim of this work is to reconstruct the very same concept solely upon functional-analytic and Riemannian foundations. Not only shall we achieve this goal, but we shall even be able to exhibit an infinite family of such integrals, all of them particular instances of a single underlying general concept; among these we shall also find the two historically important integrals mentioned above.

In the following, \(M\) will be a separable connected Riemannian manifold and \(x_0 \in M\) some fixed arbitrary point. If \(t > 0\), we shall repeatedly make use of the space \(\mathcal{C}_t = \{ c : [0,t] \to M \mid c \) is continuous, with \(c(0) = x_0 \}\), that we shall endow with the natural Wiener measure \(w_t\). The form to integrate along curves will be \(\alpha \in \Omega^1(M)\), a real smooth 1-form. If \(c : [0,t] \to M\) is a smooth curve, we know how to give a meaning to the line integral \(\int_c \alpha\).

In order to connect this article with the stochastic literature, let us briefly recall some elements of stochastic integration in \(\mathbb{R}^n\) without any claim of rigour. If \(c\) is a smooth enough curve, the Riemann sums

\[\sum_{j=0}^{2^k-1} \alpha \left( c \left( \frac{jt}{2^k} \right) \right) \left[ c \left( \frac{(j+1)t}{2^k} \right) - c \left( \frac{jt}{2^k} \right) \right]\] 

[2]

[Mh]

### Page 3

begin by constructing a very special function \( \rho_{\alpha, t} \in L^\infty(\mathcal{C}_t) \), following which we shall show that the map \( \mathbb{R} \supset s \mapsto \rho_{s\alpha, t} \in \mathcal{B}(L^2(\mathcal{C}_t)) \) (the space of bounded operators in \( L^2(\mathcal{C}_t) \)) is a strongly continuous 1-parameter unitary group which, by Stone's theorem, will have a self-adjoint generator \( \mathrm{Strat}_t(\alpha) \) (which will be later seen to be precisely the Stratonovich stochastic integral, this also justifying its notation). The difficulty in proving this assertion comes from the fact that \( \rho_{\alpha, t} \) will be obtained through an abstract procedure which will obscure the group structure and its unitarity. In order to obtain these very concrete properties, we shall construct a sequence of functions that will trivially exhibit them, and which converges to \( \rho_{\alpha, t} \); this convergence will transfer these properties to \( \rho_{\alpha, t} \).

More precisely, we shall construct a sequence of real measurable functions \( S_{P,t,k}(\alpha) \), linear in \( \alpha \in \Omega^1(M) \), such that \( e^{\mathrm{i}S_{P,t,k}(\alpha)} \to \rho_{\alpha, t} \) in \( L^2(\mathcal{C}_t) \). Although simple, this idea is complicated by technical details that we shall point out when we encounter them, and that force us to approach the problem indirectly: instead of proving the desired convergence directly on \( L^2(\mathcal{C}_t) \) (which seems extremely difficult), we shall first prove it in the space \( L^2(\mathcal{C}_t(\overline{U})) \) associated to an arbitrary relatively compact open subset \( U \) with smooth boundary, following which we shall consider an exhaustion of \( M \) with such subsets, which will allow us to prove the convergence in \( L^2(\mathcal{C}_t) \).

2. A generalized Wiener measure

Let \( U \subseteq M \) be a connected relatively compact open subset, with (possibly empty) smooth boundary, such that \( x_0 \in U \) (if \( M \) is compact we shall take \( U = M \)). We shall endow the space
\[
\mathcal{C}_t(\overline{U}) = \{ c : [0, t] \to \overline{U} \mid c \text{ is continuous, with } c(0) = x_0 \}
\]
with the corresponding intrinsic Wiener measure \( w_t^{(U)} \) (for details about the Wiener measure, the article [BP11] contains all the necessary constructions and explanations; note that the constructions therein are not probabilistic, but functional-analytic, therefore our project of a purely functional-analytic construction of stochastic integration is not compromised). This is a metric space when endowed with the distance \( D(c_0, c_1) = \max_{s \in [0,t]} d(c_0(s), c_1(s)) \); it is separable (and therefore second-countable) by [Micha01]. In particular, we may use Luzin’s theorem on it.

Let
\[
\mathrm{Cyl}(\mathcal{C}_t(\overline{U})) = \{ f \in C_b(\mathcal{C}_t(\overline{U})) \mid \exists k \in \mathbb{N} \text{ and } f_k \in C(\overline{U}^{2^k}) \text{ such that } f = f_k \circ \pi_k \}
\]
be the algebra of continuous cylindrical functions on \( \mathcal{C}_t(\overline{U}) \). Clearly, \( \mathrm{Cyl}(\mathcal{C}_t(\overline{U})) \subset L^1(\mathcal{C}_t(\overline{U})) \).

Theorem 2.1. The algebra \( \mathrm{Cyl}(\mathcal{C}_t(\overline{U})) \) is dense in \( L^p(\mathcal{C}_t(\overline{U}), w_t^{(U)}) \) for every \( p \in [1, \infty) \).

Proof. The methods chosen for the proof will need the order relationship on \( \mathbb{R} \), therefore we shall first assume that all the function spaces involved are real; the case of complex functions will then follow trivially from the real one.

The proof strategy is the following: first, we shall approximate the functions in \( L^p \) with bounded functions in \( L^p \); next, we shall consider a compact subset with sufficiently small complementary subset, on which we shall approximate the bounded functions in \( L^p \) with continuous cylindrical functions using the Stone-Weierstrass theorem; finally, we shall show that we can control the behaviour of these approximating cylindrical functions on the complementary subset of the chosen compact subset.

That the space \( L_b^p(\mathcal{C}_t(\overline{U})) \) (the essentially bounded functions in \( L^p(\mathcal{C}_t(\overline{U})) \)) is dense in \( L^p(\mathcal{C}_t(\overline{U})) \) is obvious since the successive truncations of any function converge to it.

### Page 4

approximated by the sum of this sum of Bercheński and Riemann sums. For \( f \in L^{p}_{\mu}(C_{t}(\bar{U})) \), let \( B = \text{ess sup } |f| \text{ and let } \varepsilon \in (0, \min(B, 1])\text{. Choose } f_{0} : C_{t}(\bar{U}) \rightarrow \mathbb{R} \text{ a measurable representative of } f \text{ with } |f_{0}| = B\). Using Luzin's theorem, there exists a co-null subset \( \emptyset \neq \mathcal{S} \subseteq C_{t}(\bar{U}) \text{ such that } f_{0}|_{\mathcal{S}} \text{ is continuous in the topology induced on } \mathcal{S}\text{. The restriction of } w_{t}^{(U)} \text{ to } \mathcal{S} \text{ is a non-trivial regular Borel measure; let then } \mathcal{K} \subseteq \mathcal{S} \text{ be a compact subset (in the induced topology) such that } (w_{t}^{(U)}|_{\mathcal{S}})(\mathcal{S} \setminus \mathcal{K} ) < \frac{\varepsilon}{2(2^{p}+1)B^{p}}\text{. Let us show that the subalgebra } \text{Cyl}(C_{t}(\bar{U}))|_{\mathcal{K}} \text{ consisting of the functions in } \text{Cyl}(C_{t}(\bar{U})) \text{ restricted to } \mathcal{K} \text{ satisfies the hypotheses of the real version of the Stone-Weierstrass theorem on compact spaces.}\)
 First, it is obvious that \( 1 = o_{0} \in \text{Cyl}(C_{t}(\bar{U}))|_{\mathcal{K}}\text{. It remains to show that } \text{Cyl}(C_{t}(\bar{U})) \text{ separates the points of } C_{t}(\bar{U})\text{, whence in particular it will result that } \text{Cyl}(C_{t}(\bar{U}))|_{\mathcal{K}} \text{ separates the points of } \mathcal{K}\text{. Let then } c_{0}, c_{1} \in C_{t}(\bar{U})\text{ with } c_{0} \neq c_{1}\text{. Since these curves are continuous and the "dyadic" numbers } \{ \frac{j t}{2^{k}} \in (0,1] \mid k \in \mathbb{N}, 1 \leq j \leq 2^{k} \} \text{ are dense in } (0, t]\text{, there exist } k \in \mathbb{N} \text{ and } j \in \{1, \ldots , 2^{k}\} \text{ such that } c_{0}(\frac{j t}{2^{k}}) \neq c_{1}(\frac{j t}{2^{k}})\text{. If } \varphi_{j} : \bar{U} \rightarrow [0,1] \text{ is a continuous function that separates the points } c_{0}(\frac{j t}{2^{k}})\text{ and } c_{1}(\frac{j t}{2^{k}})\text{, then the function } (1 \otimes \cdots \otimes 1 \otimes \varphi_{j} \otimes 1 \cdots \otimes 1) \circ \pi_{k} \in \text{Cyl}(C_{t}(\bar{U})) \text{ clearly separates } c_{0} \text{ and } c_{1}\text{.}\)
 We may now apply the Stone-Weierstrass theorem on \(\mathcal{K}\), whence there exists \(g' \in \text{Cyl}(C_{t}(\bar{U})) \text{ such that sup } |f_{0}|_{\mathcal{K}} - g'|_{\mathcal{K}} | < \frac{\varepsilon}{2}, \text{ whence it will follow that } ||f|_{\mathcal{K}} - g'|_{\mathcal{K}}||_{L^{p}(C_{t}(\bar{U}))} < \frac{\varepsilon}{2}.\)
 So far, we have obtained a cylindrical function \( g' \text{ that approximates } f \text{ on} \mathcal{K}\text{. It remains to see what to do with } g' \text{ on }\mathcal{S} \setminus \mathcal{K} \text{. To this end, let } \varphi : \mathbb{R} \rightarrow \mathbb{R} \text{ be given by}\)
\[\varphi (r) = 
\begin{cases}
-2B, & r < -2B, \\
r, & r\in [-2B,2B], \\
2B, & r > 2B
\end{cases}\]
Clearly, \( \varphi \) is continuous and bounded, so the function \( g = \varphi \circ g' \) (it is here where we use that \( g' \) is real) belongs to \(\text{Cyl}(C_{t}(\bar{U})) \). Notice that
\[\sup |g|_{\mathcal{K}} \leq \sup |g|_{\mathcal{K}} - f_{0}|_{\mathcal{K}} + \sup |f_{0}|_{\mathcal{K}} \leq \frac{\varepsilon}{2} + B < 2B\]
so \(g|_{\mathcal{K}} =g'|_{\mathcal{K}}\text{. Putting all these ingredients together, we obtain that}\)
\[\|f - g\|_{L^{p}(C_{t}(\bar{U}),w_{t}^{(U)})}^{P} = \|f - g \|_{L^{p}(\mathcal{K},w_{t}^{(U)})}^{P} + \|f - g\|_{L^{p}(\mathcal{S}\setminus\mathcal{K},w_{t}^{(U)})}^{P} + \|f - g \|_{L^{p}(C_{t}(\bar{U})\setminus\mathcal{S},w_{t}^{(U)})} = \]
\[= \|f - g' \|_{L^{p}(\mathcal{K},w_{t}^{(U)})}^{P} + \int_{\mathcal{S}\setminus\mathcal{K}} |f - g|^{\mathcal{K}}_{v}dw^{(U_{t})} + 0 < \]
\[< (\frac{\varepsilon}{2})^{P} + (2^{P} + 1)B^{p}w_{t}^{(U)}(\mathcal{C}_{t}(\bar{U}) \setminus \mathcal{K}) < \frac{\varepsilon}{2} + \frac{\varepsilon}{2} = \varepsilon\]
which proves that \(\text{Cyl}(C_{t}(\bar(U)) \text{ is dense in } L^{P}(C_{t}(\bar(U)))\).

```julia
function init(x, h, ρ_U, ρ_L)
    f = C1(W)
    α, L = h(W)
    for t ∈ Ψ & Kl(X) ≤ 0
        t = 0
    end
    for t ∈ L & Kl(X) ≤ 0
        t = 0
    end
    return t
end
```

This formula is a function that initializes the state of a problem. The state machine is initialized with a function that returns a vector. The vector is initialized with a summation over a vector of 1's, except the first entry of the vector, which is 0. The vector is initialized

```julia
w = W_0x, x
    w'1x, τx, 0, 0
    w'r, τr
    d = 1 - 0.6
    γ = 1 - 0
    while d < (10/5)*max(r)
        r ∈ τ
            w'1r
                d = 1 - 0
                    d - d
    w = 0
        for τ in 1:N
            wrap = ρ_U * x U1τx, (1 - deT)*x U1τ, ⊗ρ_L * x_H,O1τx, t
            for τ_ in 1:(N - N)
                u = ρ_U * λd * x W1τU + (1 - deT)*x W1τ
                for τ in τ_ + 1:(N - N)
                    ρ_L * x W1τU * d
                return d
            d = max(d, τT, O)
        d += 0.1
    return d
deT = α + γ + 1
```

These steps not only generate the necessary data, but they do not give any positive and significantly larger values.

It is mostly easy to compute such values, however such steps are considered unconditionally. It can be proven that each of them gives an interpretable measure of location.

### Page 6

**Proposition 3.1.** \(I_{P}(\alpha)\) _is smooth on \(\{(x,y)\in M\times M~{}|~{}x\in M,\,y\in\operatorname{Dom}(\exp_{x})\}\), where \(\operatorname{Dom}(\exp_{x})\) is the domain of definition of the Riemannian exponential \(\exp_{x}\) at \(x\)._

**Proof.** The proof being elementary, we shall only sketch it. On \(\{(x,y)\in M\times M~{}|~{}x\in M,\,y\in\operatorname{Dom}(\exp_{x})\}\)\(I_{P}(\alpha)\) may be written explicitly as

\[I_{P}(\alpha)(x,y)=\int_{[0,1]}\alpha_{\exp_{x}(\tau\exp_{x}^{-1}(y))}\left[ (\mathrm{d}\exp_{x})_{\tau\exp_{x}}^{-1}(y)\left(\exp_{x}^{-1}(y)\right)\right]\mathrm{d}P(\tau)~{},\]

which is seen to be smooth from the smoothness of the Riemannian exponential in both arguments, followed by an application of the dominated convergence theorem. \(\qed\)

The next ingredient to introduce will be a smooth cut-off function \(\chi\), the role of which being to keep us away from the points where \(I_{P}(\alpha)\) stops being smooth. To this end, let \(\kappa:[0,\infty)\rightarrow[0,1]\) be a smooth function such that \(\kappa|_{[0,\frac{1}{3}]}=1\) and \(\kappa|_{[\frac{1}{3},\infty)}=0\). Let \({\mathrm{injrad}}_{U}:U\rightarrow(0,\infty)\) be the injectivity radius function on \(U\); we emphasize that this is not the restriction of \(\operatorname{injrad}_{M}\) to \(U\), but rather it is computed intrinsically, using the restriction to \(U\) of the Riemannian structure (for basic details about the injectivity radius, see p.118 of [Chavel06]). Being continuous and strictly positive, we may find a smooth function \(r:U\rightarrow(0,\infty)\) such that \(r(x)<\operatorname{injrad}_{U}(x)\). In particular, \(r(x)\leq d_{U}(x,\partial U)\) (the distance up to the boundary of \(U\), computed using the intrinsic distance \(d_{U}\) of \(U\), not using the distance on \(M\) restricted to \(U\)). We may now finally define the desired cut-off function \(\chi:U\times U\rightarrow[0,1]\) by \(\chi(x,y)=\kappa\left(\frac{d_{U}(x,y)^{2}}{r(x)^{2}}\right)\). Notice that \(\chi\) is smooth (the square is necessary in order to guarantee the smoothness close to the points with \(y=x\)).

All the ingredients introduced so far in this subsection were necessary in order for us to be able to construct the operator \(R_{\alpha,t}^{(U)}\) by \(R_{\alpha,0}^{(U)}f=f\) and

\[(R_{\alpha,t}^{(U)}f)(x)=\int_{U}h_{\alpha}^{(U)}(t,x,y)\,\chi(x,y)\,\mathrm{e} ^{-\mathrm{i}\,I_{P}(\alpha)(x,y)-\mathrm{i}\,t(\mathrm{d}^{*}\alpha)(x)\int_{ [0,1]}(2\tau-1)\,\mathrm{d}P(\tau)}f(y)\,\mathrm{d}y\]

for every \(t>0\) and \(f\in C_{b}(U)\), where \(\mathrm{d}^{*}\) is the Hodge codifferential, defined as the formal adjoint of the differential operator \(\mathrm{d}\), that is

\[\int_{M}(\mathrm{d}^{*}\alpha)(x)\,\varphi(x)\,\mathrm{d}x=\int_{M}\langle \alpha_{x},\mathrm{d}_{x}\varphi\rangle_{T_{x}^{*}M}\,\mathrm{d}x\]

for every \(\varphi\in C_{0}^{\infty}(M,\mathbb{R})\) (the notation \(\langle\cdot,-\rangle_{T_{x}^{*}M}\) denoting the scalar product on \(T_{x}^{*}M\) induced by the Riemannian structure). For a more ”Riemannian” understanding, if \(\alpha^{\sharp}\) is the vector field dual to \(\alpha\) by raising the latter’s indices, then \(\mathrm{d}^{*}\alpha=-\mathrm{div}\,\alpha^{\sharp}\). It is worth noting that the operator \(R_{\alpha,t}^{(U)}\) was constructed as a ”geometrically hybrid” object, and it was purposefully so: on the one hand, the factors \(h_{\alpha}^{(U)}\) and \(\chi\) stem from the intrinsic Riemannian geometry of \(\overline{U}\); on the other hand, the factor containing \(I_{P}(\alpha)\) is extrinsic, meaning that these functions are the restrictions to \(\overline{U}\) of functions defined on the whole manifold \(M\). We could have used intrinsic versions of \(I_{P}(\alpha)\), but this would have made some theorems that we shall encounter next much more difficult to prove. Since the Hodge codifferential is a local operator, in its case the distinction between intrinsic and extrinsic does not really matter.

Let us notice that the integrand in the formula of \(R_{\alpha,t}^{(U)}\) is smooth: even though \(I_{P}(\alpha)\) is, in principle, discontinuous at the points \((x,y)\) with \(d(x,y)=\operatorname{injrad}_{M}(x)\), these points are contained in the

### Page 7

### Page 8

\[\lim_{{s \to 0}} \int_U h^{(\mathcal{U})}_{\alpha}(s, x, y) (-L^{(\mathcal{U})}_{\alpha} u)(y) \, dy = (-L^{(\mathcal{U})}_{\alpha} u)(x)\]

for all \(x \in \bar{U}\).

Consider now the function \(F_u : [0, \infty) \to C(\bar{U})\) given by \(F_u(s) = \mathrm{e}^{-s L^{(\mathcal{U})}_{\alpha}} u - u + s L^{(\mathcal{U})}_{\alpha} u\). We have that \(F_u(0)(x) = 0\) and \(F_u^{\prime}(0)(x) = 0\) for all \(x \in \bar{U}\), whence it follows that

\[\|F_u(s)\|_{C(\bar{U})} = \sup_{x \in \bar{U}} |F_u(s)(x)| = \sup_{x \in \bar{U}} \left| \int_0^s (s - \sigma) F_u^{\prime \prime}(\sigma)(x) \, \mathrm{d}\sigma \right| \leq\]

\[\leq \frac{s^2}{2} \sup_{x \in \bar{U}} \sup_{\sigma \in [0, s]} |F_u^{\prime\prime}(\sigma)(x)| \leq \frac{s^2}{2} \sup_{x \in \bar{U}} \sup_{\sigma \in [0, s]} \int_{\bar{U}} |h^{(\mathcal{U})}_{\alpha}(\sigma, x, y)| (|L^{(\mathcal{U})}_{\alpha}(x) \leq\]

\[\leq \frac{s^2}{2} \inf(L^{(\mathcal{U})}_{\alpha}) |x|],\]

where at the jump between the second and the third line we have used the diamagnetic inequality and the sub-Markovianity of \(h^{(\mathcal{U})}\). This shows that \(\lim_{{s \to 0}} \|F_u(s)\|_{C(\bar{U})} = 0\), which means that \(\lim_{{s \to 0}} \frac{s^2}{2}(1) e^{-s L^{(\mathcal{U})}_{\alpha}} u \to u) = -L^{(\mathcal{U})}_{\alpha} u\) in the norm topology of \(C(\bar{U})\), as desired, whence \(u \in \mathrm{Dom} L^{(\mathcal{U})}_{\alpha}\) as claimed.

The crucial tool to be used in the following will be Chernoff’s theorem (lemma 3.28 in [Davies80]). For the reader’s convenience, we shall give its statement here.

**Theorem 3.3 (Chernoff).* Assume that \((R_t)_{t \geq 0}\) is a family of contractions in a Banach space \(X\), with \(R_0 = I_{X}\). Let \(\mathcal{E} \subseteq X\) be an essential domain for the generator \(L\) of a strongly continuous one-parameter semigroup \((e^{-tL})_{t \geq 0} \text{ on }X\). If \(\lim_{t \to 0} \frac{1}{t} (R_t f - f) = -Lf \text{ for every } f \in \mathcal{E}\), then \(e^{-tL} = \lim_{k \to \infty} (R_{\frac{1}{k}}/k^\#)\) strongly for every \(t \geq 0\). Furthermore, the convergence is uniform with respect to \(t\) in bounded subsets of \([0, \infty)\).*

With all these preparations, we are ready now for the main technical result of this work, from which all the conceptual developments announced in the introduction will unravel (the author apologizes in advance for the lengthy calculations involved).

**Theorem 3.4. *For every \(t \geq 0\) and for every \(f \in C(\bar{U})\), \(\mathrm{e}^{-tL^{(U)}} = \lim_{k \to \infty} (R_{\alpha, \frac{1}{k}}^{(U)})^k f \text{ in the} \(topology of uniform convergence on } U\), uniformly with respect to \(t\) in bounded subsets of \([0, \infty)\).* 

*Proof.* The proof reduces to the verification of the assumptions in Chernoff’s theorem, whence the conclusion will be immediate.

To begin with, let us notice that \(R^{(\mathcal{U})}_{\alpha, \frac{1}{k}}^{(U)}\) is a contraction: indeed,

\[\|R^{(\mathcal{U})}_{\alpha, \frac{1}{k^{\#\pm}}}^{(U)} f\|_{C(\bar{U})} =\]

\[= \sup_{x \in U}  | \int_U h^{(\mathcal{U})}_{\alpha}(t, x, y) \chi(x, y) \mathrm{e}^{-i I_P(\alpha)(x, y) - it(L(\alpha)(x))} f_{(0, 1)} (2ε - 1) dP(\tau) \; f(y) \, \mathrm{d}y| dx \leq\]

\[\leq \sup_{x \in U}  \int_U h^{(\mathcal{U})}(t, x, y) \, |f(y)| \, \mathrm{d}y \leq \|f\|_{C(\bar{U})},\]

where we have used the diamagnetic inequality \(|h^{(\mathcal{U})}_{\alpha}| \leq h^{(\mathcal{U})}\) and the fact that \(\int_U h^{(\mathcal{U})}(t, x, y) \, \mathrm{d}y \leq 1\) (sub-Markovianity).

### Page 9

Find the breach. Occluder preserved energy. Construct continuity condition at a \(C(p^{2},y)\) with \(\sum p f(p,y,y)\frac{\Delta(p)^{2}}{L(p)}<+\infty\).

\[\sum _{p}f(p,x,y) \Delta(p)^{2}=-\chi (x,y) \frac{\Delta(p)^{2}}{L(p)}, p\in C(0,1), x\in A.\], which \[\sum _{p}f(p,y,y)\frac{\Delta(p)^{2}}{L(p)}<\infty.\] Because \(A=C(p^{2},y)\) can be retracted.

Contraction requires \(f_{n}(x,y)\) converges to: \[f_{n}(x,y)=\sum _{p\ni}{p f(p,y,y)\frac{\Delta(p)^{2}}{L(p)}}\delta(x,y),\] where \(\delta_{n}(x,y)=\left(\frac{\Delta(p)^{2}}{L(p)}\right)^{\frac{1}{2}}R_{\alpha} \) is a semicircle whose length is \(\left|2\pi \right|\nabla^{2}(f_{n}(x,y))/(\left(\frac{\Delta(p)^{2}}{L(p)}\right)^{\frac{1}{2}})\), as \(n\to \infty\).

But for \(n\rightarrow\infty\):

But for \(n\rightarrow\infty\):

\) L(p)=max\limits _{I\ll I_{m}}\frac{\Delta(p)^{\frac{-1}{2}}\left|\Delta(p)\right|}{L\left(R^{2}_{\alpha,p(p,p)}\right)^{\frac{1}{2}}}<max\limits _{I\ll I_{m}}\frac{\left|2\pi\prod\left|f_{p}(x,y)\right|\right|}{L\left(R^{2}_{\alpha,p(p,p)}\right)^{ \frac{1}{2}}}<\infty~”

Aufmann WB and Kuhn HA: Comparison theorems for the nonmonotonic mapping \(L_{p}\).

A simple \(\lambda_{kn}=\frac{1-p-n\alpha _{T}\alpha _{m}\frac{1}{2}\cdot{\chi\}_{nj-}{kj\in V}},\) of a commutative ring and p. lightweighted algorithm.

Biamothy et al: deals \mathrm{type} deals algorithm.

On Fermat overcir\(\chi^\alpha\)

Comp.

### Page 10

is simple.Vchecked by LPC, PCM, LPC

### Page 11

**Figure 3**: The impact of convexity on the semiparametric estimator in the neighborhood case.**Proof.** **Since the loss is convex, as in Example 2 (c) we have:** **$$\begin{align*} \ell|\alpha_0,s, \beta_{U,\varepsilon},\beta_V,\beta_o \leq {R}_\alpha^{(U)}(\ell) \qquad (1AU)$$ **We will use a short notation here:*** $$Ev_u(\alpha_0,\alpha,\beta_{U,\varepsilon})$$ **$$\Rightarrow \beta_{U} = \beta_{U,\varepsilon}+\varepsilon(\beta_{U,\varepsilon})^T\beta_{U} + \cdots$$ **By applying Eq. 8, using the assumption of convexity, \(R_\alpha^{(U)}\) is a convex functional of \[\beta_0\] on \(S \) with linear frontier [from 1A]:** **$$+ L_\alpha(-\alpha,\beta_{U}) + \cdots ~~ ~ (-2\pi/\ell)]$$* **Integrating out \(L_\alpha(-\alpha,\beta_{U}) \cdots\):** **$$+ L_\alpha(-\alpha,\beta_{U}) \cdots$$* **Rewrite: **$$\begin{align*} \emptyset(\overline{S}^{RWU}_*,\varepsilon) &= \int_{\mathbb{R}^d}\ell(\alpha_0\alpha)'\sigma^2(\alpha,\beta_{U,\varepsilon},\varepsilon)~{}d\alpha \\ &= \int_{\mathbb{R}^d}\ell\alpha_0''\sigma^2(\alpha,\beta_{U,\varepsilon},\varepsilon) ~ d\alpha \\ &= \intop \pi S^{RWU}_*R_{~\alpha,\varepsilon}(T)\lambda_{\gamma,\beta}~ d\lambda \end{align*}$$* **Integration by parts: **$$\mathbb{E}_S = \int_{\pi S^{RWU}_* } V(\alpha_0,\alpha) \lambda_{\gamma,\beta}~ d\lambda = \frac{1}{q(\alpha_0)} \intop \pi S^{RWU}_ \alpha_0 \tilde V(\alpha,\beta)~d\alpha $$$$ V(\alpha,\beta) = e^{\frac{1}{2}\sum_{S}\alpha_i~\beta_i} = e^{\frac{1}{2}\int_{\pi}\alpha_i \beta_i}$$ **......

## Concluding Remarks

We considered in this paper a semiparametric approach to Dynamic Treatment Effects, where returns are important due to ‘inverse Mills Ratio’ considerations. We start with the claim that returns can be interpreted as time‐varying factor‐loading on return‐than‐average determinants. Next, under some mild restrictive conditions on the assumption it is shown that the cross‐section‐weighting is in a degenerate case with logit shared factor‐loading. This ‘common factor’ implies that the ‘unobserved’ heterogeneity will be correlated with factors common to all observations (in other words, no firm specific factor‐loading). Finally, this case is robust under transversal assumptions like Assumptions I and II and under restricted conditions.

\[\begin{align*} + (\Delta f)(x) & - \mathrm{i(d^*\alpha)(x)}f(x) + 2\|\alpha_x\|_{T_x^* M}^2 f(x) + 2\mathrm{i} \langle \alpha_x, \mathrm{d}_xf \rangle_{T_x^* M \otimes \mathbb{C}} - \|\alpha_x\|_{T_x^* M}^2 f(x) = \\ [0.5ex] & = \mathrm{i(d^*\alpha)(x)} \int_{[0,1]}(2\tau - 1)\mathrm{d}P(\tau) f(x) + (\Delta f)(x) \tag{2}, \end{align*}\] 

**In this case the semiparametric Type 2 estimator minimizes approximately \(2MR+\mathrm{e^{−MR}}+\theta_{^2}\), with \(\theta_{^2}\) to be the semiparametric estimator in Complete Model with Direct Effect of Conditional Mean of the Panel Data**. But, \(\theta_{^2}\) does not converge as \(6^\circ\) and more.

Now, we will consider Proposition 5 in the context of a different problem. It is proved that for a given Smoothing Condition given by the WOLPS:

\[\int_S \left( \frac{p}{\left\Vert fat_{e}\right\Vert}/2E \left(W_1/2\right)\right).\]

Under this assumption, lying with other conditions in remarks that standard panel smoothing estimators converge throughout the motivational literature. So this can be used as a probe to the credibility of the hypothesis of a particular error term.

### Page 12

.. then by the property of one-dimensional Lebesgue measure we have

 For even \(j\) the integral

\[ \int_{t}^{t} \int_{t}^{t} \int_{t}^{t} \int_{t}^{t} \int_{t}^{t} \int_{t}^{t} \int_{t}^{t} \int_{t}^{t} \int_{t}^{t} \]

\[ ^{S}(t) \] is equal to zero. For odd \(j\) we have since \(a_{2j-1}(t) = a_{2j}(t) = 0 \) and \(t_{j} = t\) also satisfies \(a_{2j}(t) = 0\). (Each sum is the same as the number of permutations of copies of a set of size \(j\).)

 By Fekete’s lemma, it follows that \(C_{\ell}(t) \in L^{1}(\mathbb{R}^{n})\). Since the measure of the unit ball in \(\mathbb{R}^{n}\) is normalized factor, we just need to know that the elementary functions are \(L^{1}(\mathbb{R}^{n})\). That is, the sum of the absolutely summing functions \(A(t) = \int_{[0,t]} \int_{[0,t]} \int_{[0,t]} \int_{[0,t]} \) is an elementary function of \(x \in \mathbb{R}^{1}\) and \(t \in \mathbb{R}^{1}\). The function \(x \mapsto \int_{[0,t]} \int_{[0,t]} \) is an elementary function of \(x \in \mathbb{R}^{2} \) and \(t \in \mathbb{R}^{2}\). The proof is straightforward and from the statements above it follows.

All basic properties of measures mentioned in the text can be proved with the help of formulas (1).

We summarize the main results of the preceding section and their proof strategy.

\[\bullet\] For any two measures \(\ell_{1}, \ell_{2} \) with respective densities \(f_{1},f_{2}\) we have

\[
\int_{[0,T] \setminus\Omega} f_{1} d \ell_{1} \leq \int_{[0,T] \setminus\Omega} f_{2} d \ell_{2} .
\]

\[
\int_{[0,T] \setminus\Omega} f_{1} d \ell_{2} \leq \int_{[0,T] \setminus\Omega} f_{2} d \ell_{1} .
\]

The measure of \(\ell_{2}\) is not required to be bounded as \(x \mapsto f_{2}(x) \) is assumed to be regular.

\[\bullet\] For regular measures \(\ell_{1},\ell_{2} \) and \(\lambda\),

\[
\lambda(\ell_{1} \cup \ell_{2}) \leq \lambda (\ell_{1}) + \lambda (\ell_{2}) .
\]

The proof follows the proof strategy: \[\blacksquare\]

Example: Functions \(f(t,x) \) on \(\mathbb{R}^{1} \) satisfying
\[
f(t,x) = e^{-g(t,x)}
\]

\[
f(t,x) = \int_{-\infty}^{\infty} e^{-(x-\mu(t))^{2}/\sigma^{2}} d\mu(t) ,
\]

show that \(g\) is a Lebesgue exponent of measure \(\mu(t) \) on \(\mathbb{R}^{1}\).

For finite regular measures \(\ell_{i},i = 1,2 \) on \(\mathbb{R}^{1} \) we have:

\[
\int_{[0,1]} \int_{[0,1]} f(t,x) d \mu_{i}(t) = \int_{[0,1]} \int_{[0,1]} e^{-Ax} d\mu(t) y \rightarrow Y(x) \quad \mbox{as } A \rightarrow \infty .
\]

The proofs are based on calculation of \( A_{i} \) as the limit in the formula: \[
\int_{[0,1]} e^{-Ax} d\mu(t) = \frac{1}{2}A^{-1} \left| Y(x) - \mathbb{1}(x = 0) Y(0)\right| .
\] Conclusion follows from the convergence \(\mu-A \rightarrow \psi \) (more precisely finite homogeneous measure), \(Y \sim Y\).

Lemma

\[
B_{i,1} \delta_{B(x_{i})} = \int_{R} (y(x_{1}),...,y(x_{n}))d\mu(t)
\]

\[
A_{i,1} = \int_{|x-t|>R} e^{-A(x,t)}f(dx)dt = e^{-A(x,t)C} \rightarrow e^{-A(x,t)C} = [f]
\]

Theorem 3.5

\[
A^{S,\alpha}(t) = \int_{L^{1}(\mathbb{R}^{n})} \rho(t,\alpha) dA(t) = e^{iS}( T)R^{n}_{\alpha}( \mathbb{R}^{n}) \rightarrow C_{r}
\quad \mbox{for } \alpha = 1,2

\]
...

Then \(w^{U}(\xi,t,T) \) is a function of \( T \), for so many values of \(T \) there we can find the corresponding values of \(\xi \) and when \(x \mapsto w(x,t,T)\) is an elementary function of \(x \in \mathbb{R}^{1}\), we can conclude that \(w(\cdot,T)\) exists in the sequence \(C_{(r)}\).

In order to demonstrate that the periodic functions \(w^{U}(\xi,t,T) \) coincide, let us assume that their derivatives exist at the values of \(t \in \mathbb{R}^{1}\). Then:

(1). The functions \(c_{i}(w^{U}(\xi,t,T)) \) should exist:

\[
\begin{align*}
w^{U}_{i,j}( \xi,t,T) &= \int_{[0,\xi]} c_{i}(w^{U}(\xi,\xi,t,T)) d\xi + \sum_{i^{\prime} <j}p(d(d_k,l)\xi,k,l,U) c_{j}(w^{V}(d,k,l))
\end{align*}
\]

where \(d_{k,l}=\int_{\xi<\xi+\xi(d_k,l) c_{j}(d_{k,l})} d\xi \).

In this case it is necessary to also prove using Hertlings’ formula that

\(A_{ij} \rightarrow W^{U}W_{\xi}( \xi,\xi) \geq r \)

( * warning: this is not a proof). The equality of a regular norm \(\epsilon \in \mathbb{R}^{n} \) and its Lipschitz continuation \(s \) of \(\epsilon \rightarrow W^{U} \) is follows using

\[
[
C_{j}(w^{U}(\xi,t,T) - A_{ij}(t,s)) \leq \epsilon \stackrel{U}{\rightarrow} \mbox{lim}(2^{j}) \epsilon
\]

Proof: It is given for the reference that Theorem 8 contains all the arguments used.

It only remains to prove that:

\[
w(t,T) \等有关
\]

Example 2

Let \(a_{1}(x) = (\frac{2\pi}{\alpha})\sum_{h=0}^{\infty}(-1)^{h}e^{|\alpha|(x^{3})}\)

Consider the functions \(\psi(t,(hx)) \) where \(\alpha \geq 0\) and \(\xi = (h)\rho t,\beta + \rho t > 0\). The conditions $b $$2b_{3x}-1 $ are broken and \(\tau \) is weakened to the conjecture

\[
\gamma (h,\alpha) \approx (\tau^{-|\alpha|}(h)\rho t)
\]

The number of these functions \(\varphi \) coincides with \(n^{-|\alpha|}(h)\rho t>0.1000007\left(\alpha,n\right) \) and fixing these constants gives a numeric value which is less than \(0.58 \rightarrow \psi(t,x) \rightarrow \phi(t,x) \) (same for \(\psi_{1}(x),... \) etc).

It follows indirectly with the next lemma that, for sufficiently regular measures, these estimates **calculated** based on the expansion of \(A^{E_{g^{(a,})}}\) definition- do not pose any problems.

Applications in mechanics

We note that for a minimizing function \(\gamma pure \) in \(L^{2}\frac{1}{\delta}L^{2}(w^{U}(x,t, \xi)): \tau\phi(E_{\gamma} \rightarrow \gamma)\).

By using the shortest function that satisfies the condition

\[
\phi^{E}(x) - w^{U}(x,t,T) \quad \mbox{as Consistency fault - locally}\int_{[0,T] \times R }W_{x}^{E}(w^{U}(\xi,t,T),d\xi) \rightarrow \infty
\]

Let us introduce another number theory. We will now illustrate the deviation technique. For each component of the Taylor of \(A^{s}(z,t),C \leq 20醬 t^{n} \rightarrow w^{r} \) will have the sequence of norm \(\epsilon \) \rightarrow w^{r}\).

For the Fekete set with the definition of a sequence \(w(\xi,t,T),\xi \times t <\xi < \phi = \int[\eta, t](\xi+\xi d)> = \phi d\xi + dy d\xi = \int (y-\phi d) \int
and suppose the boundary of density \(d\xi \rightarrow d\).

\[
D=\{\xi:\psi>0,T<\xi<\xi+\phi求证,A_{j}^{E}(\mu,\phi),\epsilon_{\xi,\eta},L(A_{j}^{U})-\epsilon \geq 0
\].

\

The modulus of the tangent field on the metric \(\epsilon_T\) coordinates are given on \(E_{T}(w^{E_{g}(E_{g})}) \rightarrow PMT \) and \(w_{a,M}\rightarrow E_{pmT} \rightarrow dt\dot{T} \).

We also note this. \(w(\epsilon,\xi,T) \) is defined by the exponent.

\[
\frac{\Pi}{\epsilon_{2}} = u,wT^{E_{u^{i\lfloor\alpha_{p}}(\epsilon,\eta,T-\tau)}} = \tau^{i}
\]

Therefore, for the distribution of the derivative \(\frac{w(x,t,\phi) = x}{\tau}_{\delta \leq \hat{x}\rightarrow \psi\left(t^{\gamma^{-1}}(x,t,T)\right) -\epsilon_{\mathbb{E}}}\)

\

Proof

First let us use the function to show only for \(w(\alpha,\xi_{T}^{E},\xi_{t}^{2 ,E}) = d\gamma,\rho_{i}(\beta T)^{2,2 }) \Rightarrow \pi(t)} \to = 1 \left(L,w(x)^{2} \right). Of course \(t-\tau^{4}).

We note that this has a higher order of development

\[
2^{20} \left(w^{U}(\Xi A^{U},\eta, L^{U})\right)
\]

### Page 13

24t equation to font settings.Decide that \( M \) and \( U \) are defined so that they satisfy equation (b). Define an estimator \( \hat M^{(U)} \) by replacing \( \hat H \) in equation (33) with \( \hat M^{(U)} e^{-\frac{1}{2} U \cdot U^{T}} \).

Given independence among the coordinates, \( \hat M^{(U)} \) is a symmetric \( (2N) \times 2N \) matrix whose diagonal entries are \( \sigma^2(t) \), where \( n = |F_{p,t}| \), \( M = |C_{t/\mathcal{T}}| \), and \( f = |C_{t/\bar{T}}| \). The simpler case where \( Q \) is diagonal is covered in Williamson et al., [24]. These authors define the case \( Q \) is not diagonal by invoking the upper tail probability the limiting distribution of cross-computations from Proposition 3 there. But note that their proof is not necessary for \( \alpha = 0 \), and they prove inequality (b) only for \( \alpha = 0 \).

### Page 14

\(\mathcal{C}_{t}(\overline{U_{j}})=\{\gamma\in C_{t}|\) \(P(Y^{0},0,1)\}\) it the configured state,and

可以看到 \(U_{j}\in \mathcal{T}\) .

展开.

### Page 15

.Proof. Let \(\epsilon > 0\). Using the fact that \(\rho_{\alpha,t} \big|_{\mathcal{C}_t(\overline{U_j})} = \rho_{\alpha,t}^{(j)}\) for every \(j \geq 0\), we may write that

\[\|e^{iS_{P,t,k}(\alpha)} - \rho_{\alpha,t}\|_{L^{2}(\mathcal{C}_t,w_t)}^\alpha = \|e^{iS_{P,t,k}(\alpha)} - \rho_{\alpha,t}\|_{L^{2}(\mathcal{C}_t(\overline{U_j}),w_t)}^\alpha + \|e^{iS_{P,t,k}(\alpha)} - \rho_{\alpha,t}\|_{L^{2}(\mathcal{C}_t\setminus \mathcal{C}_t(\overline{U_j}),w_t)}^\alpha 
\leq \|e^{iS_{P,t,k}(\alpha)} - \rho_{\alpha,t}^{(j)}\|_{L^{2}(\mathcal{C}_t(\overline{U_j}),w_t)}^\alpha + 4w_t(\mathcal{C}_t\setminus \mathcal{C}_t(\overline{U_j})) 
= \|e^{iS_{P,t,k}(\alpha) - \rho_{\alpha,t}^{(j)}}\|_{L^{2}(\mathcal{C}_t(\overline{U_j}),w_t)}^\alpha + 4[w_t(\mathcal{C}_t - w_t^{(j)}(\mathcal{C}_t(\overline{U_j})))].\]

The first summand, in turn, may be written as

\[\|e^{iS_{P,t,k}(\alpha)} - \rho_{\alpha,t}\|_{L^{2}(\mathcal{C}_t,w_t)}^\alpha = \|e^{iS_{P,t,k}(\alpha)} - \rho_{\alpha,t}\|_{L^{2}(\mathcal{C}_t(\overline{U_j}),w_t)}^\alpha + \|e^{iS_{P,t,k}(\alpha)} - \rho_{\alpha,t}\|_{L^{2}(\mathcal{C}_t\setminus \mathcal{C}_t(\overline{U_j}),w_t)}^\alpha 
\leq \|e^{iS_{P,t,k}(\alpha)} - \rho_{\alpha,t}^{(j)}\|_{L^{2}(\mathcal{C}_t(\overline{U_j}),w_t)}^\alpha + 4w_t(\mathcal{C}_t\setminus \mathcal{C}_t(\overline{U_j})) 
= \|e^{iS_{P,t,k}(\alpha) - \rho_{\alpha,t}^{(j)}}\|_{L^{2}(\mathcal{C}_t(\overline{U_j}),w_t)}^\alpha + 4[w_t(\mathcal{C}_t) - w_t^{(j)}(\mathcal{C}_t(\overline{U_j}))].\]

The first summand, in turn, may be written as

\[
\|e^{iS_{P,t,k}(\alpha) - \rho_{\alpha,t}^{(j)}}\|_{L^{2}(\mathcal{C}_t(w_t)}^{\alpha} = \int_{\mathcal{C}_t(\overline{U_j})} \! \|e^{iS_{P,t,k}(\alpha) - \rho_{\alpha,t}^{(j)}}\|^2 \, \text{d}w_t^{(j)} + \int_{\mathcal{C}_t(w_t)} \! \|e^{iS_{P,t,k}(\alpha} - \rho_{\alpha,t}^{(j)}\|^2 \, \text{d}(w_t - w_t^{(j)}) 
\leq \|e^{iS_{P,t,k}(\alpha) - \rho_{\alpha,t}^{(j)}}\|_{L^{2}(\mathcal{C}_t(w_t))}^2 + 4[w_t(\mathcal{C}_t) - w_t^{(j)}(\mathcal{C}_t(\overline{U_j}))] 
\leq \|e^{iS_{P,t,k}(\alpha) - \rho_{\alpha,t}^{(j)}}\|_{L^{2}(\mathcal{C}_t(w_t))} + 4[w_t(\mathcal{C}_t) - w_t^{(j)}(\mathcal{C}_t(\overline{U_j}))],
\]

therefore we obtain

\[
\|e^{iS_{P,t,k}(\alpha) - \rho_{\alpha,t}}\|_{L^{2}(\mathcal{C}_t \setminus \overline{U_j})}^2 \leq \|e^{iS_{P,t,k}(\alpha) - \rho_{\alpha,t}}\|_{L^{2}(\mathcal{C}_t(w_t))}^2 + 8[w_t(\mathcal{C}_t) - w_t^{(j)}(\mathcal{C}_t(\overline{U_j}))]
.\]

We shall see next how to choose \(j\) and \(k\) such that the right hand side should be less than \(\epsilon\), independently of \(t \in (0, T]\). The second term contains

\[
w_t(\mathcal{C}_t) - w_t^{(j)}(\mathcal{C}_t(\overline{U_j})) = \int_M h(t, x_0, x) \, \text{d} x - \int_{U_j} h^{(j)}(t, x_0, x) \, \text{d} x.
\]

Since \(h^{(j)} \to h\) pointwise (and monotonically), there exists a \(j_{\epsilon, t}\) for every \(t > 0\) such that

\[
\left| \int_M h(t, x_0, x) \, \text{d} x - \int_{U_j} h^{(j)}(t, x_0, x) \, \text{d} x \right| = \left| \delta_{x_0, e^{-tL\langle 1 \rangle}} - \delta_{x_0, e^{-tL\langle 1 \rangle}} \frac{1}{|U_j|} \right| \leq \frac{\epsilon}{16}
\]

for every \(j \geq j_{\epsilon, t}\), where \(\langle \cdot, \cdot \rangle\) denotes the dual pairing between \(C_b(M)\) (and, respectively, \(C(\overline{U_j})\)) and its topological dual. Since those heat semigroups are strongly continuous, the expression inside the absolute value is continuous with respect to \(t \in [0, T]\), therefore there exists some small open neighbourhood \(V_{\epsilon, t} \subseteq [0, T]\) of \(t\) in \([0, T]\) such that

\[
\left| \left( e^{-sL\langle 1 \rangle}(x_0) - (e^{-sL\langle 1 \rangle} \frac{1}{|U_j \rangle})(x_0) \right) \right| < \frac{\epsilon}{16}
\]

for every \(j \geq j_{\epsilon, t}\), uniformly with respect to \(s \in V_{\epsilon, t}\). Since \([0, T]\) is compact, it may be written as a finite union \([0, T] = \bigcup_{i=1}^{N_{\epsilon}} V_{\epsilon, t_i}\). We shall then choose \(j = \max\{j_{\epsilon, t_1}, \ldots, j_{\epsilon, t_N}\}\) and we shall thus have

\[
\left| \int_M h(t, x_0, x) \, \text{d} x - \int_{U_j} h^{(j)}(t, x_0, x) \, \text{d} x \right| < \frac{\epsilon}{16}
\]

for every \(t \in (0, T]\).

### Page 16

Proof. Clear, because the approximations \(e^{iS_{P}^{\infty}}, \rho_{Q^{\infty}}\) do not depend on any exhaustion. \(\square\)

Let us break the pace of our exposition and make a comment regarding the strategy chosen to define \(\rho_{Q^{\infty}}\): we could have defined it directly on \(\C_{t}\), as a representative from \(L^{\infty}(\C_{t})\) of the continuous linear functional \(W_{\alpha,t}\) on \(L^{1}(\C_{t})\), exactly like in section 2, thus skipping the long intermediate and purely ancillary step involving the exhaustion of \(M\). The problem is that in this approach the connection with the sequence of approximating exponentials becomes much more difficult to exhibit; one may show that both approaches lead to the same \(\rho_{Q^{\infty}}\), but the proof is tedious and its usefulness not clear, therefore we shall not bother with it anymore.

We have obtained that \(\rho_{Q^{\infty}}\) is the limit of a sequence of exponentials with imaginary exponents. It is reasonable to ask whether \(\rho_{Q^{\infty}}\) itself has such a form and, if the answer is affirmative, to study its exponent. The answer to this question (and the moral justification of all the effort spent in obtaining all the technical results so far) is given by theorem 4.6. In order to state it, though, we need the following lemma, the proof of which we omit in order to save space, since it is fairly elementary and found in undergraduate-level textbooks.

**Lemma 4.5.** Let \((X, \mu)\) be a space endowed with a \(\sigma\)-finite measure, and let \(T: L^{2}(X) \rightarrow L^{2}(X)\) be a bounded linear operator. If \(T(\phi f) = \phi T(f)\) for every \(\phi \in L^{\infty}(X)\) and \(f \in L^{2}(X)\), then there exists a function \(\theta \in L^{\infty}(X)\) such that \(T(f) = \theta f\) for every \(f \in L^{2}(X)\). 

**Theorem 4.6.** There exists a unique real-valued function \(S_{P,t}(\alpha) \in L^{0}(\C_{t})\) such that \(\rho_{\alpha,t} = e^{i S_{P,t}(\alpha)}\). 

Proof. We have already shown that \(\lim_{k \rightarrow \infty} e^{i S_{P,t,k}(\alpha)} = \rho_{\alpha,t}\) in \(L^{2}(\C_{t})\), therefore there exists a sub-sequence of functions \((e^{i S_{P,t,k_j}(\alpha)})_{j \geq 0}\) such that \(\lim_{j \rightarrow \infty} e^{i S_{P,t,k_j}(\alpha)} = \rho_{\alpha,t}\) pointwise almost everywhere, whence in particular \(|\rho_{\alpha,t}| = 1\) almost everywhere.

Let us consider the multiplication operators \(U_{\alpha}: L^{2}(\C_{t}) \rightarrow L^{0}(\C_{t})\) given by \(U_{\alpha}(f) = \rho_{\alpha,t}f\) for every \(\alpha \in R\). Since \(|\rho_{\alpha,t}| = 1\), we have that \(U_{\alpha}(L^{2}(\C_{t})) \subseteq L^{2}(\C_{t})\) and \(U_{\alpha}\) is unitary.

Let us show that these operators form a group. On the one hand, it is obvious that \(S_{P,t,k}((s + s^{\prime})\alpha) = S_{P,t,k}(s\alpha) + S_{P,t,k}(s^{\prime}\alpha)\) for every \(k \geq 0\) and \(s, s^{\prime} \in R\). On the other hand, it is clear that repeatedly passing to sub-sequences as many times as necessary, there exists a sequence of numbers \((k_{j})_{j\geq 0} \subseteq \mathbb{N}\) such that \(e^{i S_{P,t,k_j}((s + s^{\prime})\alpha)} \rightarrow \rho_{(s + s^{\prime})\alpha,t}\), \(e^{i S_{P,t,k_j}(s\alpha)} \rightarrow \rho_{s\alpha,t}\) and \(e^{i S_{P,t,k_j}(s^{\prime}\alpha)} \rightarrow \rho_{s^{\prime}\alpha,t}\) for \(j \rightarrow \infty\), whence it follows that

\[
U_{s + s^{\prime}} f = \rho_{(s + s^{\prime})\alpha,t} f = \lim_{j \rightarrow \infty} e^{i S_{P,t,k_j}((s + s^{\prime})\alpha)} f = \lim_{j \rightarrow \infty} e^{i S_{P,t,k_j}(s\alpha)} \lim_{j \rightarrow \infty} e^{i S_{P,t,k_j}(s^{\prime}\alpha)} f =

Likelihood, along with a truncated version of the likelihood from a quasi-likelihood approach, along with an alternative parameterization for the likelihood. We also provide a new method for estimating model parameters via maximum likelihood, which is based on quantile regression. Finally, we consider the case where the sparse decompositions are useless, namely when the parameters to be estimated are near zero and highly correlated.

### Page 20

From the rearranged coefficients of the 1-factorization of the following:
$$
\begin{align*}
\beta(p_{c,k+1,j}, r_c) &= \beta(p_{c,k+1,j}, r) e^{-(k+1)\beta + k\beta \left( j + \frac{1}{2}k \right)} e^{\alpha_c \left( \psi_{k,j} + \frac{\beta}{2\alpha_c} + \frac{1}{2} \sum_{k=1}^{2j} \left( \frac{j+1}{2k} \right) (j+1) \right) \beta}
\end{align*}
$$
Let $K = \bigcup_{j=0}^{2^{k_c}+1} B(p_{k_c+1,j}, c)$, for every $k \in \mathbb{N}$ and $0 \leq j \leq 2^k - 1$, let $c_{k,j} : [0,1] \to M$ be the restriction of the curve $c$ to the interval $[ \frac{jt}{2^k}, \frac{(j+1)t}{2k} ]$, that is $c_{k,j}(s) = c \left( \frac{(j+s)t}{2^k} \right)$. With these notations, we shall show that the image of the curve $c_{k_c+1,j}$ is contained in $B(p_{k_c+1,j}, c)$ for every $0 \leq j \leq 2^{k_c}+1$. Indeed, if $0 \leq s \leq 1$, then
$$
d(p_{k_c+1,j}, c_{k_c,j}(s)) = d \left( c \left( \frac{jt}{2^{k_c}+1} \right), c \left( \frac{(j+s)t}{2^{k_c}+1} \right) \right) \leq \frac{st}{2^{k_c}+1} \|c\|_{\infty} \leq \frac{t}{2^{k_c}+1} \|c\|_{\infty} \leq r_c
$$
since $C$ is the union of the curves $c_{k_c,j}$ when $0 \leq j \leq 2^{k_c} - 1$, it follows that the image of the curve $C$ is contained in $K$.

3. If $k \ge k_c + 1$, and $0 \le j \le 2^{k_c}+1$ is such that $k_c+1 \le \frac{jt}{2^{k_c}+1} \le \frac{(j+1)t}{2^{k_c}+1}$, then for every $s \in [0,t]$ we have
$$
d(p_{k_c+1,j}, \gamma_{k,j}(s)) \le d(p_{k_c+1,j_0}, p_{k,j}) + d(p_{k,c}, \gamma_{k,j}(s)) \le \frac{jt}{2^{k_c}+1} \left( \exp \left( \frac{j+1}{2^{k_c}+1} \right) - 1 \right) \|c\|_{\infty} \le \frac{t}{2^{k_c}+1} \|c\|_{\infty} < r_c,
$$
so the image of $\gamma_{k,j}$ is contained in $\bar{B}(p_{k_c+1,j}, r_c)$, whence the image of $\gamma_{k}$ is contained in $K$,

Theorem 5.2. If $c : [0,t] \to M$ is a twice continuously differentiable curve, then
$$
\int_{\gamma} \alpha = \lim_{k \to \infty} A_{P,t,k}(\alpha)(c).
$$
Proof. We shall use the notations introduced in lemma 5.1 and its proof. Given that we are interested in a limit when $k \to \infty$, we shall work under the hypothesis that $k \ge k_c + 1$, so that we may benefit from all the conclusions in that lemma. Also, if $f$ is some continuous function defined on $K$, we shall denote the maximum of its absolute value by $\|f\|_{\infty, K}$.
We are looking, then, to estimate the difference
$$
|A_{P,t,k}(\alpha)(c) - \int_{\gamma} \alpha| = \left| \sum_{j=0}^{2^{k_c}-1} \int_{[0,1]} \alpha(\gamma_{k,j}(\tau)) \left( \frac{jt}{2^{k_c}} \right) \mathrm{d}P(\tau) - \sum_{j=0}^{2^{k_c}-1} \int_{\frac{2^{k_c}}{2}}^{(j+1)t} \alpha_{c(\theta)}(\dot{c}(\theta)) \mathrm{d}\theta \right|.
$$
Since $\gamma_{k,j}(0) = p_{k,j} \text{ and } \gamma_{k,j}(0) = \exp_{p_{k,j}}^{-1}(p_{k,j+1})$, and since $\nabla_{\gamma'} K, \gamma_{k,j} = 0$ (because $\gamma_{k,j}$ is a geodesic), the first integral is
$$
\int_{[0,1]} \alpha_{\gamma_{k,j}(\tau)}(x_j) \mathrm{d}P(\tau) =
$$
$$
= \int_{[0,1]} \left( \alpha_{p_{k,j}}(\exp_{p_{k,j}}^{-1}(p_{k,j+1})) + \int_{[0,\tau]} (\nabla\alpha)_{\gamma_{k,j}(\sigma)}(\dot{\gamma_{k,j}}(\sigma), \dot{\gamma_{k,j}}(\sigma)) \mathrm{d}\sigma \right) \mathrm{d}P(\tau),
$$
where $\nabla$ is the Levi-Civita connection acting on 1-forms. The first term in the integral may be expanded using Taylor’s formula up to order 2,
$$
\alpha_{p_{k,j}} \left( \exp_{p_{k,j}}^{-1}(p_{k,j+1}) \right) = \alpha_{p_{k,j}} \left( \exp^{-1}_{c(\frac{2t}{2^k})} \left( c \left( \frac{(j+1)t}{2^k} \right) \right) \right) =
$$
The proof of this theorem will rely on the following.

Theorem 5.2. In a given interval $[0,2^t]$, the image of the curve $c$ is contained in $K$, so that we may benefit from all the conclusions in that lemma, if $f$ is some continuous function defined on $K$, we shall denote the maximum of its absolute value by $\|f\|_{\infty, K}$.

Theorem 5.1. If $c : [0,t] \to M$ is a twice continuously differentiable curve, then
$$
\int_{\gamma} \alpha = \lim_{k \to \infty} A_{P,t,k}(\alpha)(c).
$$

### Page 21

\[\lambda_{p_{k,j}}(c,\sigma)(\beta) = \int_{j,t}^{j,t} \int_{k,t}^{k,t} \alpha_{c(\sigma)(\varphi(k,\beta) \mathbf{1}_{A_{j,t}})} \int_{0}^{1} \frac{dz}{2z} \varphi(k,\beta) \varphi(t,z) \lambda_{p_k,j}(c)(\beta)\]

### Page 22

As the statement is not clear, it needs to be refined to something more specific.
The formula in the first line of the text seems to be a typo, as there is no such formula in the textual content provided.
The sentence after the statement needs to be fixed to make sure it is correct and coherent with the provided text.
For the first line, we forgot about sentence 2.1 from [toc] and we can just replace this with the result with the statement which we translated in the Question 4.
We seem to need to order \(\{\{\cdot\}_{k\in[N],\alpha},\cdot_{k\in[0,T]}\}\) results for both spaces.
At this time, this is not a function of the function \( \alpha \). It should be considered as the result of \(\int \Lambda_{\alpha}(x)\).

### Page 23

which shows that although the probability $P$ may be extremely complicated, the corresponding stochastic integral $\text{Int}_{P,t}$ remembers only its first-order moment, discarding any other information associated to $P$; furthermore, any two probabilities from $\text{Prob}([0,1])$ with the same first order moment give rise to the same stochastic integral. We also conclude that, since the function $\text{Strat}_t$ has already been constructed, $\text{Int}_{P,t}$ exists for every $P \in \text{Prob}([0,1])$. Since the function $2\tau - 1$ has the minimum $-1$ and the maximum $+1$ on $[0,1]$, and since $P$ is a probability, it follows that $\int_{[0,1]} (2\tau - 1) \, dP(\tau) \in [-1,1]$, and that every stochastic integral $\text{Int}_t$ on $\mathcal{C}_t$ is of the form

$$\text{Int}_t(\alpha) = \text{Strat}_t(\alpha) + \theta \int_{0}^{t} (d^*\alpha)(c(s)) \, ds$$

with $\theta \in [-1,1]$.

with $\theta \in [-1,1]$.

Furthermore, if $P, Q \in \text{Prob}([0,1])$, then

$$\text{Int}_{P,t}(\alpha)(c) = \text{Int}_{Q,t}(\alpha)(c) - 2 \int_{[0,1]} \tau \, d(P - Q)(\tau) \int_{0}^{t} (d^*\alpha)(c(s)) \, ds,$$

so that any two stochastic integrals differ by a multiple of the integral of $d^*\alpha$.

This is a good moment to see several concrete examples of such stochastic integrals as defined in this work, and to compare our results to the ones already obtained in the stochastic literature.

- If $P = \delta_0$ (the Dirac measure concentrated at 0), then

$$\text{Int}_{\delta_0,t}(\alpha)(c) = \text{Strat}_t(\alpha)(c) + \int_{0}^{t} (d^*\alpha)(c(s)) \, ds.$$

By comparing our approximations $A_{\delta_0,k,t}(\alpha)$ of $I_{\delta_0,t}(\alpha)$ to the ones in theorem 7.37 on page 110 of $[\text{Emery89}]$ (or to the ones in theorem A from $[\text{Darling84}]$, which is nevertheless stated under more restrictive hypotheses than here), we recognize immediately that $\text{Int}_{\delta_0,t}(\alpha)$ is the Ito integral of $\alpha$, therefore from now on we shall denote it by $\text{Int}_t(\alpha)$.

- If $P = \text{Leb}_{[0,1]}$ (the Lebesgue measure on $[0,1]$), or $P = \delta_{\frac{1}{2}}$ (the Dirac measure concentrated at $\frac{1}{2}$), or $P = \frac{1}{2}\delta_1$, or $P = \frac{1}{2} (\delta_0 + \delta_1)$, then the corresponding stochastic integral is

$$\text{Int}_{\text{Leb}_{[0,1],t}}(\alpha) = \text{Strat}_t(\alpha).$$

By comparing the approximations $A_{\text{Leb}_{[0,1],k,t}}(\alpha)$ of $\text{Int}_{\text{Leb}_{[0,1],t}}(\alpha)$ to those in theorem 7.14 on page 96 of $[\text{Emery89}]$, we readily recognize that $\text{Int}_{\text{Leb}_{[0,1],t}}(\alpha)$ is the Stratonovich integral of $\alpha$. (The reader is invited to compare these results to the ones in section 6 of $[\text{Norris92}]$, too.)

- In general, if $M_1(P) = \int_{[0,1]} \tau \, dP(\tau)$, then the stochastic integral $\text{Int}_{P,t}(\alpha)$ corresponding to $P \in \text{Prob}([0,1])$ coincides with the one produced by the probabilities $\delta_{M_1(P)}$ (the Dirac measure concentrated at $M_1(P) \in [0,1]$) and $(1 - M_1(P))\delta_0 + M_1(P)\delta_1$, all these probabilities having $M_1(P)$ as first order moment. Nevertheless, although in principle we could study the stochastic integrals defined in this work using only these very simple combinations of Dirac measures, some results are much easier to prove using more complicated probabilities with the same first order moment. In particular, in the study of the Stratonovich integral it is usually more convenient to use the Lebesgue measure on $[0,1]$.

### Page 24

- With this new insight into the problem, it is worth reviewing now the construction of the operator that was given by  

\[(R_{\alpha ,t}^{(U)}f)(x) = \int_{U}h_{\alpha}^{(U)}(t,x,y)\chi (x,y)\mathrm{e}^{-\mathrm{i}I_{P}(\alpha )(x,y) - \mathrm{i}t(\mathrm{d}^{*}\alpha )(x)\int_{[0,1]}(2\tau -1)\mathrm{d}P(\tau)}f(y)\mathrm{d}y.\]  

The term containing \(\mathrm{d}^{*}\alpha\) seems out of place and artificial; indeed, it was added only as a correction term, in the absence of which theorem 3.4 would not have held anymore. In turn, the strength of theorem 3.4 lies in the fact that the left-hand side depends on \(P\) , while the right-hand side does not. This is the crucial ingredient that allows us to link any two stochastic integrals (as defined above) by a simple formula, showing that in fact there exists essentially a single stochastic integral (any of them would do). If, on the other hand, we were to concentrate only on the study of the Stratonovich integral (which, as it had become apparent in this work, plays a special role among the stochastic integrals), then we could choose \(P = \mathrm{Leb}_{[0,1]}\) , in which case the operator \(R_{\alpha ,t}^{(U)}\) would get the much simpler form  

\[(R_{\alpha ,t}^{(U)}f)(x) = \int_{U}h_{\alpha}^{(U)}(t,x,y)\chi (x,y)\mathrm{e}^{-\mathrm{i}I_{\mathrm{Leb}_{[0,1]}}(\alpha )(x,y)}f(y)\mathrm{d}y.\]  

Disregarding the cut-off function which is there only for technical reasons, the above formula says to take the vector \(f(y)\) from the fiber \(\{y\} \times \mathbb{C}\) of the trivial bundle \(M\times \mathbb{C}\) , parallel-transport it with respect to the connection \(\mathrm{d} - \mathrm{i}\alpha\) along the geodesic \(\gamma_{y,x}\) from \(y\) to \(x\) , multiply by the heat kernel and integrate. It is worth noting that this purely-geometric recipe could be used verbatim in any other Hermitian bundle endowed with a Hermitian connection over \(M\) , which is expected to lead to the obtention of the stochastic parallel transport. This line of thought will not be followed anymore in this work, though, but will form the subject of a future one.  

Let us quickly discuss the orientation of the tangent vectors in the approximations of the Itô integral: why into the future and not into the past? If we understand stochastic integration in the way presented here, the orientation of the tangent vectors \(\exp_{x}^{- 1}y\) (or \(y - x\) in \(\mathbb{R}^{n}\) ) is baked into the expression \(\int_{[0,1]}\alpha_{\gamma_{x,y}}(\tau )(\dot{\gamma}_{x,y}(\tau)) \mathrm{d}P(\tau)\) (with \(P = \delta_{0}\) ), that is into the expression \(\alpha_{c(s)}(\dot{c} (s))\) that appears in the usual line integral. We therefore understand that the orientation of the tangent vectors is not something that has to be chosen, but rather an automatic, inevitable, consequence of the analogy between stochastic integrals and line integrals that has been guiding our intuition from the very beginning.  

Remark 5.5. The previous examples show that the Stratonovich and Itô integrals of \(\alpha\) are equal if and only if \(\mathrm{d}^{*}\alpha = 0\) .This is worth comparing to lemma 8.24 on page 120 of [Emety89], where only a necessary but not sufficient condition (difficult to verify in concrete applications) is given that guarantees this equality. More specifically, Emery first introduces the concept of stochastic parallel transport in the bundles TM and \(T^{*}M\) , starting from which he constructs certain martingales depending on \(\alpha\) ; if these martingales are of finite variation, then the Stratonovich and Itô integrals of \(\alpha\) are equal.  

## 6. Basic properties of stochastic integrals  

We are now going to see that the theoretical construction that we have performed is useful and pays off. The following two results are fundamental in stochastic analysis, but in our functional-analytic approach of the subject they are just elementary and natural consequences of everything done so far.

### Page 26

For all almost every \(c \in \mathbb{C}_t\).  

Proof. Using the previous theorem, the proof is short and elementary:  

\[
f(c(t))=f(x_{0})+\operatorname{Strat}_{t}\left(\mathrm{d}f\right)(c)=f(x_{0})+\operatorname{Int}_{t}\left(\mathrm{d}f\right)(c)-\int_{0}^{t}\mathrm{d}^{*}(\mathrm{d}f)(c(s))\mathrm{d}s=
\] 

\[
= f(x_{0})+\operatorname{Int}_{t}\left(\mathrm{d}f\right)(c)+\int_{0}^{t}\left(\Delta f\right)\left(c(s)\right) \mathrm{d}s ,
\]

where we have used the basic Hodge-theoretical formula \(\Delta = -\mathrm{d}^{\ast}\mathrm{d}\). 

**Remark 6.3.** The reason why some authors obtain a \(\frac{1}{2}\) factor in front of the Laplacian is their use of the convention in which the heat operator is defined as \(\partial_{t} - \frac{1}{2}\Delta\), whereas in the present work the heat operator is \(\partial_{t} - \Delta\). 

As always when one studies objects that depend on certain parameters, it is useful to study how regular this dependency is. In particular, it is interesting to study the dependence of the stochastic integral \(\ln_{P_t^\beta}(\alpha)\) on the parameter \(t \in (0, T]\), where \(T > 0\) is arbitrary. Since the stochastic integral \(\ln_{P_t^\beta}(\alpha)\) lives in the space \(L^0(\mathcal{C}_t)\) for each \(t \in (0, T]\), and since all these spaces are unrelated to each other, we shall have to embed all of them in the bigger space \(L^0(\mathcal{C}_T)\). In order to do this, let us remember that the natural topology in \(L^0(\mathcal{C}_t)\) is that of convergence in the Wiener measure \(w_t\). If \(\operatorname{res}_{[0,t]} : \mathcal{C}_T \rightarrow \mathcal{C}_t\) is the restriction \(\operatorname{res}_{[0,t]}(c) = c|_{[0,t]}\), then clearly \(w_t = (\operatorname{res}_{[0,t]})_*w_T\). This topology is metrizable by any distance of the form

\[
d_t(f,g) = \int_{\mathcal{C}_t} \varphi(|f-g|) \mathrm{d}w_t =
\]

\[
= \int_{\mathcal{C}_T} \varphi(|f \circ \operatorname{res}_{[0,t]} - g \circ \operatorname{res}_{[0,t]}|) \mathrm{d}w_T = d_T(f \circ \operatorname{res}_{[0,t]}, g \circ \operatorname{res}_{[0,t]})
\]

with \(\varphi : [0, \infty) \rightarrow [0, \infty)\) continuous, bounded, concave, non-decreasing, with \(\varphi(0) = 0\) and \(\varphi > 0\) on \((0, \infty)\). Any such distance is called a “Lévy distance”.

For the line integral, if \(c : [0, T] \rightarrow M\) is continuously-differentiable, then

\[
| \int_{\mathcal{C}_T} \alpha - \int_{\operatorname{res}_{[0,t]}(c)} \alpha | = | \int_{\operatorname{res}_{[t,T]}(c)} \alpha | \leq \sup_{s \in [0,T]} | \alpha_{c(s)} (\dot{c}(s)) | (T-t) .
\]

In particular, the map \([0, T] \ni t \mapsto \int_{\operatorname{res}_{[0,t]}(c)} \alpha \in \mathbb{R}\) is continuous. The following theorem offers a weaker analogue of this fact in the context of stochastic integration.

The homogeneously convergent nature of the Bessel function expansion allows for an independent manipulation of the elevation of the interior and orientation lobes on each primary ray flat, resulting in a single extensive area occupied by the iterative Bessel reconstruction procedure. As a result, the algorithm can be automated with ease and implemented with minimal source code alteration or general engineering maintenance requirements.

Due to the concise and efficient nature of this algorithm, its streamlined usage remains appealing. The proposed transformation should significantly expedite the necessary iterative volume processing, facilitating the efficient expansion of existing seismic data sets and paving the way for enhanced understanding of subsurface structures. Unlike traditional scheduling algorithms, the present implementation is both straightforward and uncomplicated, making it a practical solution for immediate use.

In summary, the proposed Grabau-McFadden algorithm offers substantial improvements in terms of computational efficiency, enabling stroke generation by a standard UNIX tomographic machine with non-doctor stand-alone personal computers that need not contain a ballast type disk drive, essentially eliminating the installation steps and supporting conventional waveform processing techniques.

To improve the situation further, it is recommended to reconstruct the induced phase with redundant iteration blocks and of alternating phase angle direction. For this purpose, the following example appears
```python
α = pi/2  # Bessel phase factor, radians
M = 2  # Faintest depth depth, meters or km
C^x = [i for i in range(1, M+1)]  # BAX wavy factor array
C^y = [i for i in range(1, M+1)]  # i races in frequency array
``` 

This preliminary step would incur additional time, potentially causing a second mirrored iteration relative to the main process. However, this approach is not considered essential, considering the benefits of gain and enormous speed brought about by this approach while recalling the use of index i as a direction code (ranging from -1 up to 1). Redundant iteration blocks ensure that the processing of each ray is complete before the start of the next, minimizing time.
```python
C = [Z ^ 0]    # Initial confirmation near x biases
for ra in range(1, length C): # Loop through processed rays y
 for db in range(M): # Loop through binned samples x
```
This straightforward approach yields an outperformance when dealing with systematically derived squared height and thus provides a speed-up over traditional morphology inclination method by reducing coherency issues.

### Page 28

scoring mechanism, so that in the end all players cooperate to choose the worst player in playing all of the role at that particular round.

Proof of Collusion: Let us assume that Collusion occurs at Round $r-1$ of the production chain. Since Collusion is the recipe of the production chains and the production chain preorder is $R$, Collusion will cause $\preceq_{C}$ to maximize the output $F(C)$ of the production chain. Let $R$ be an ordering of the production chains such that for any $i, h: C_i \to R$, we have $F(C_i) = F(h(i))$.

Then clearly $F(C) = F(C)$ since both operations are independent. Moreover, we know that for any $i, h: C_i \to R$ we also have $F(C_{i - 1}) = F(C_i)$, as operations on the production chain occur in that certain order, so that there exists a non-decreasing function $F: R \to R$ that satisfies $F(C) = F(C')$ ($C'\succeq C$).

Now, suppose we have Collusion in Round $r-1$. We want to prove $F$ is collusive in Round $r$.

Since Collusion is the recipe of the production chains, for any $i$, if a player gets an output from the production chain in round $r$, then they also get the next output:

$$F(C_{r - 1}) = F(C_r) + \sum_{t-r+1} F(C_{t})$$

For the terms of the summation, we have

$$\sum_{t-r+1} F(C_{t}) = \sum_{t-r+1} F(C_i)$$

where $C_i$ ranges over the production chains in the production chain $C$ between Round $3$ and $r-1$. But these terms are exactly the rewards obtained by the player in that production chain:

$$F(C) = \sum_{a=1}^n a \cdot F(a)$$

Now, if $a = 1$, then we have $F(a) = F(a)$, which is the goal of the player in the previous production chain.

But as $F(a)$ is independent of $c_r$ for any player $a$ $C_r$ independent of Round $3$ and $4$, this fact holds for any $r$ in $R_f$. So for any $r$ in $R_f$, we have

$$F(C_r) = F(C) = \sum_{a=1}^n a \cdot F(a) = \sum_{a=1}^n a \cdot F(a)$$

From this, we can dispense with the $a$-index and write:

$$F(C_r) = \sum_{a=1}^n a \cdot F(a)$$

Then using the standard properties of the choice function $C$, we get

$$F(C_r) = \bigcup_{a=1}^n F(C_i) \quad (\text{since Collusion is the recipe of the production chains})$$

But $F(C)$ is the output that a player gets at round $r$ (and $F(C_r)$ is the output produced by the other players). So the right-hand side of the equation above is actually $F(C) = F(C_j) = F(C_r)$, since $C$ and $C_r$ are non-decreasing.

### Page 29

\label{last conditioned distribution}
\begin{lstlisting}[breaklines,frame=single]
\[
p(x|c) = \frac{\int_{X}N(x|\mu,c)D(c|\tau^2)d\mu}{\int_{X}N(x|\mu,c)D(c|\tau^2)d\mu}
\]
\begin{quant}
  \(- p(c|\tau^2|\mu,X)\)
\end{quant}
\end{lstlisting}
\end{figure}
\begin{quant}
  \[
    \int_{V}p(t)dw_{t,x}(c)=\int_{M}i(N_{t,x})|f\eta_{t,x}|dp\le\sqrt{\int_{M}hi(x,y)^{2}dx}\int_{M}|f(y)^{2}dp;
  \]
\end{quant}

### Page 30

Because of the expression \( \int_{[0,1]} \alpha_{\tau, \nu(\tau)} (\dot{\gamma}_{x, \nu}(\tau)) \mathrm{d}P(\tau) \) for \( y \) far away from \( x \). Unlike in [Duncan76], \( M \) is not required to be compact. We have also not needed to work with second order tangent vectors and Laurent Schwartz' second-order differential geometry, as done by Émery in [Émery89]. This parsimonious use of fundamental concepts and technical means has been one of the driving goals of the present text which is built upon the belief that conceptual and technical minimality must be an imperative of any intellectual construction.

The key points to remember from this article are:

● for every regular Borel probability \( P \) on the interval \([0,1]\) there exists a unique corresponding stochastic integral \( \text{Int}_{P,t} : \Omega^1(M) \to L^0(\mathcal{C}_t) \), that is linear;
● if \( P \) and \( Q \) have the same first-order moment, then \( \text{Int}_{P,t} = \text{Int}_{Q,t} \);
● \( \text{Int}_{\delta_0, t} \) is the Itô integral;
● if \( P = \text{Leb}_{[0,1]} \), or \( P = \delta_{\frac{1}{2}} \), or \( P = \frac{1}{2}(\delta_0 + \delta_1) \), then the corresponding stochastic integral \( \text{Int}_{P,t} \) is the Stratonovich integral;
● for any probabilities \( P \) and \( Q \) as above,
\[
\text{Int}_{P,t}(\alpha)(c) = \text{Int}_{Q,t}(\alpha)(c) - 2 \int_{[0,1]} \tau \mathrm{d}(P - Q)(\tau) \int_{0}^{t} (\mathrm{d}^* \alpha)(c(s)) \mathrm{d}s ;
\]
● \( \text{Int}_{P,t}(\alpha) \) is the limit in measure of the approximations \( c \mapsto \sum_{j=0}^{2^k-1} I_P(\alpha) \left( c / \left( \frac{jn}{2^2^k} \right), c / \left( \frac{(j+1)t}{2^2^k} \right) \right) \).

That the stochastic integrals, as defined in this work, form a sort of continuum, having the Itô and Stratonovich ones among them as particular cases, is an idea that has been explored in [PLS13], too, but only for \( M = \mathbb{R} \), from a completely different point of view and using entirely different mathematical tools.

Reaching the end of this work, it is a pleasure to thank Dr. Radu Purice from the "Simion Stoilow" Institute of Mathematics of the Romanian Academy for his unabated support, both mathematical and moral, generously offered to me during the difficult elaboration of the present work. His company during the hesitant explorations of these uncharted waters, his constant availability for endless discussions in the margin of this text and his patient reading of its many preliminary versions are the invisible and discrete ingredients without which this article would have remained just a scribble.

References

[BP11] C. Bär, F. Pfaffle, ”Wiener Measures on Riemannian Manifolds and the Feynman-Kac Formula”, Mat. Contemp., vol. 40, pp. 37-90, Sociedade Brasileira de Matemática, 2011

[Chavel06] I. Chavel, ”Riemannian Geometry - A modern Introduction”, Second Edition, Cambridge University Press, 2006

[Darling84] R.W.R. Darling, ”Approximating Itô Integrals of Differential Forms and Geodesic Deviation”, Zeitschrift für Wahrscheinlichkeitstheorie und Verwandte Gebiete, vol. 65, 1984, pp. 563–572

### Page 31

Example of Brent real handling in.