> OCR by deepseek-ai/DeepSeek-OCR | 19 pages

### Page 1

gateway), and it can also be a small number of micro-hierarchies. There are characterizing theorems in whether the selected feature is in the expected relational order [2], classification problems [43], or can be shown to be a pattern classifier to some exact facts of sequences. Explore this deep research until now, there is a lot of things to be done.

### Page 2

dominates. 1 Introduction

In their remarkable paper [4], S. Artstein-Avidan and V. Milman established an interesting result on certain mappings acting on the class of convex functions defined on $R^n$. More precisely, let $\mathcal{C}(R^n)$ be the set of lower semicontinuous proper convex functions $f : R^n \rightarrow R \cup \{+\infty\}$. For $f, g \in \mathcal{C}(R^n)$, we write $f \leq g$ if and only if $f(x) \leq g(x)$ for all $x \in R^n$. An operator $T : \mathcal{C}(R^n) \rightarrow \mathcal{C}(R^n)$ is said to be order preserving if $T(f) \leq T(g)$ whenever $f \leq g$, and order reversing if $T(f) \geq T(g)$ whenever $f \leq g$. We will say that $T$ is fully order preserving (reversing) if, in addition, $T$ is invertible and its inverse $T^{-1}$ is also order preserving (reversing). The main results in [4] state that, up to pre-composition with affine operators, addition of affine functionals, and multiplication by positive scalars, the only fully order preserving mapping acting on $\mathcal{C}(R^n)$ is the identity operator, and the only fully order reversing one acting on the same set is the Fenchel conjugation (also called Legendre transform, or convex conjugation), meaning the operator $T$ given by $T(f) = f^*$ for all $f \in \mathcal{C}(R^n)$, where $f^*$ denotes the Fenchel conjugate of $f$, defined as $f^*(u) = \sup_{x \in R^n} \{ \langle u, x \rangle - f(x) \}$ for all $u \in R^n$.

We mention that order preserving isomorphisms acting on sets with specific structures had been studied for some time, in some cases in connection with applications to Physics (see, e.g., [14], [15], [16], [17]), but Artstein-Avidan and Milman’s work [4] was the first to deal with this issue in connection with the space of convex functions.

The results from [4] were the starting point of several interesting developments, consisting mainly of replacing $\mathcal{C}(X)$ by other sets. Along this line, we mention for instance the characterizations of fully order reversing mappings acting on:

i) the class of $s$-concave functions (see [3]), 
ii) the family of nonnegative convex functions vanishing at 0 (see [6]), 
iii) closed and convex cones, with the order given by the set inclusion, and the polarity mapping playing the role of the Fenchel conjugation (see [18]), 
iv) closed and convex sets (see [10], [19]), 
v) ellipsoids (see [8]).

Additional recent related results can be found also in [1], [5], [7] and [13].

All these papers deal exclusively with the finite dimensional case. In [21], it is proved that the composition of two order reversing bijections acting on the class of convex functions defined on certain locally convex topological vector spaces is the identity if and only in the same happens with their restrictions to the subset of affine functions, but no characterization of the kind established in [4] is given. To our knowledge, the extension of the above described results in [4] to infinite dimensional spaces remains as an open issue. Addressing it is the main goal of this paper. More precisely, we will generalize the analysis of order preserving and order reversing operators developed...

### Page 3

selected by Milman and Artstein-Avidan to the setting of arbitrary Banach spaces. As a corollary, we also obtain a characterization of order preserving involutions (see Corollary 6).

We mention now the main differences between [4] and our work.

When dealing, as we do, with arbitrary Banach spaces, the analysis becomes substantially more involved, demanding different tools. This situation occurs at several points in the analysis. For instance, a key element of the proof consists of establishing the fact that the restriction of the operators of interest to the space of affine functions is itself affine (see Propositions 5 and 7 for a precise statement). In the infinite dimensional case, we need to show that such restriction is also continuous (cf. Propositions 8 and 9), which is trivial in a finite dimensional setting.

We mention now another difference between our approach and the one in [4]. As Milman and Artstein-Avidan rightly point out in [4], one can establish the characterization for the order preserving case and obtain the one for the order reversing case as an easy corollary, or make the full analysis for order reversing operators and then get the result for order preserving ones as a corollary. In [4] they chose the second option, while we follow in this paper the first one. As a consequence, excepting for some basic results, (e.g. Proposition 1), or the use of some classical tools (e.g. Lemma 1), which appear in [4] as well as in our work, both prooflines are quite different.

We also obtain, as a by-product of our analysis, some results which are possibly interesting on their own, like two characterizations of affine functions in general Banach spaces (see Proposition 2 and Lemma 2), which are fully unrelated to the contents of [4].

2 The order preserving case

We start by introducing some quite standard notation. Let $X$ be a real Banach space of dimension at least 2. We denote by $X^*$ and $X^{**}$ its topological dual and bidual, respectively. We define as $\mathscr{C}(X)$ the set of lower semicontinuous proper convex functions $f : X \to \mathbb{R} \cup \{+\infty\}$, where the lower semicontinuity is understood to hold with respect to the strong (or norm) topology in $X$. For $f \in \mathscr{C}(X)$, $f^* : X^* \to \mathbb{R} \cup \{+\infty\}$ will denote its Fenchel conjugate, defined as $f^*(u) = \sup_{x \in X} \{\langle u, x \rangle - f(x) \}$ for all $u \in X^*$, where $\langle \cdot, \cdot \rangle $ stands for the duality coupling, i.e. $\langle u, x \rangle = u(x)$ for all $(u, x) \in X^* \times X$. As usual, $f^{**} : X^{**} \to \mathbb{R} \cup \{+\infty\}$ will indicate the biconjugate of $f$, i.e. $f^{**} = (f^*)^*$. Given a linear and continuous operator $C : X^* \to X^*$, $C^* : X^{**} \to X^{**}$ will denote the adjoint of $C$, defined by the equation $\langle C^* a, u \rangle = \langle a, Cu \rangle$ for all $(a, u) \in X^{**}\times X^*$. We consider the pointwise order in $\mathscr{C}(X)$, i.e. given $f,g \in \mathscr{C}(X)$, we write $f \leq g$ whenever $f(x) \leq g(x)$ for all $x \in X$. We denote as $\mathbb{R}_{++}$ the set of strictly positive real numbers.

The next definition introduces the family of operators on $\mathscr{C}(X)$ whose characterization is the main goal of this paper.

### Definition 1.
- An operator $T : \mathscr{C}(X) \to \mathscr{C}(X)$ is order preserving whenever $f \leq g$ implies $T(f) \leq T(g)$.
- An operator $T : \mathscr{C}(X) \to \mathscr{C}(X)$ is fully order preserving whenever $f \leq g$ iff $T(f) \leq T(g)$, and additionally $T$ is onto.

We define as $\mathscr{B}$ the family of fully order preserving operators $T : \mathscr{C}(X) \to \mathscr{C}(X)$.

We will prove that $T$ belongs to $\mathscr{B}$ if and only if there exist $c \in X$, $w \in X^*$, $\beta \in \mathbb{R}$, $\tau \in \mathbb{R}_{++}$ and a continuous automorphism $E$ of $X$ such that

\[
T(f)(x) = \tau f(Ex + c) + \langle w, x \rangle + \beta,
\]

for all $f \in \mathscr{C}(X)$ and all $x \in X$ (see Theorem 1 below).

We mention that for technical reasons, we work in this paper under the assumption that $\dim(X) \geq 2$. We do not make this fact explicit in our main results (Theorems 1 and 2) and Corollary 6, because for the case of $\dim(X) = 1$ (and indeed for any finite dimensional $X$), all of them have been established in [4].

We begin with an elementary property of the operators in $\mathscr{B}$.

**Proposition 1.** If $T$ belongs to $\mathscr{B}$ then,

i) $T$ is one-to-one,

ii) given a family $\{f_i\}_{i \in I} \subset \mathscr{C}(X)$ such that $\sup_{i \in I} f_i$ belongs to $\mathscr{C}(X)$ (i.e., the supremum is not identically equal to $+ \infty$), it holds that $T(\sup_{i \in I} f_i) = \sup_{i \in I} T(f_i)$.

**Proof.** i) If $T(f) = T(g)$ then $T(f) \leq T(g)$ and $T(g) \leq T(f)$, so that, since $T$ is fully order preserving, $f \leq g$ and $g \leq f$, implying that $f = g$.

ii) Let $\hat{f} = \sup_{i \in I} f_i$. Since $f_i \leq \hat{f}$ for all $i \in I$ and $T$ is order preserving, we get that $T(f_i) \leq T(\hat{f})$ for all $i \in I$, so that

\[
\sup_{i \in I} T(f_i) \leq T(\hat{f}) = T\left(\sup_{i \in I} f_i\right).
\]

In view of (1), $\sup_{i \in I} T(f_i)$ belongs to $\mathscr{C}(X)$. Since $T$ is onto, there exists $g \in \mathscr{C}(X)$ such that $\sup_{i \in I} T(f_i) = T(g)$. Observe that $T(f_i) \leq \sup_{i \in I} T(f_i) = T(g)$, so that, since $T$ is fully order preserving, $f_i \leq g$ for all $i \in I$, and hence $\hat{f} = \sup_{i \in I} f_i \leq g$. Since $T$ is order preserving,

\[
T\left(\sup_{i \in I} f_i\right) = T\left(\hat{f}\right) \leq T(g) = \sup_{i \in I} T(f_i).
\]

The result follows from (1) and (2).

□

### Page 5

### Page 6

Proof. i) Let $h = h_{u,\alpha}$. We will use Proposition 2 in order to establish affinity for $T(h)$. Take $h_{z,\delta}, h_{z',\delta'} \in A(T(h))$, i.e. $h_{z,\delta} \leq T(h), h_{z',\delta'} \leq T(h)$. Since $T$ is onto, there exist $g, g' \in \mathcal{E}(X)$ such that $h_{z,\delta} = T(g), h_{z',\delta'} = T(g')$, so that $T(g) \leq T(h), T(g') \leq T(h)$. Since $T$ is fully order preserving, we get $g \leq h, g' \leq h$. By Corollary 2, there exists $\eta, \eta' \in \mathbb{R}$ such that $g = h_{u,\eta}, g' = h_{u,\eta'}$. Assume, without loss of generality, that $\eta \leq \eta'$ so that $g \leq g'$, and hence, since $T$ is order preserving,

\[
h_{z,\delta} = T(g) \leq T(g') = h_{z',\delta'}. \tag{4}
\]

In view of Corollary 2, we get from (4) that $z = z'$. We have proved that there exists a unique $z \in X^*$ such that $h_{z,\delta} \in A(T(h))$ for some $\delta \in \mathbb{R}$, and hence $T(h)$ is affine by Proposition 2.

ii) Note that whenever $T \in \mathcal{B}$, its inverse, the operator $T^{-1}$, also belongs to $\mathcal{B}$. It follows that $T^{-1}$ also maps affine functions to affine functions, which establishes the result.

\[
\hat{R}(h_{u,\alpha}) = \begin{cases} h_{u,\alpha} & \text{if } u \in (-1, 1) \\ h_{-u,\alpha} & \text{otherwise}. \end{cases}
\]

It is immediate that \(\hat{R}\) is onto and fully order preserving; in fact it is an involution, meaning that \(\hat{R} \left( \hat{R}(h) \right) = h\) for all \(h \in \mathcal{A}\). Consider its extension \(R : \mathcal{C}(X) \to \mathcal{C}(X)\) as defined above, and take \(f_1(x) = 1/2|x|, f_2(x) = \max\{x, 0\}\). It is easy to check that \(R(f_1) = f_1 = (1/2)|x|, R(f_2) = |x|\), so that \(R(f_1) \leq R(f_2)\), but it is not true that \(f_1 \leq f_2\) (the inequality fails for \(x < 0\)). In fact any operator \(\hat{R} : \mathcal{A} \to \mathcal{A}\) of the form \(\hat{R}(h_{u,\alpha}) = h_{\psi(u), \varphi(\alpha)}\) where \(\psi : X^* \to X^*\) is a bijection and \(\varphi : \mathbb{R} \to \mathbb{R}\) is increasing and onto, turns out to be onto and fully order preserving in \(\mathcal{A}\), while operators \(\hat{T} : \mathcal{A} \to \mathcal{A}\) which are the restrictions of operators \(T\) in \(\mathcal{B}\) have a much more specific form.

We will prove next that \(\hat{T} : \mathcal{A} \to \mathcal{A}\) is indeed affine for all \(T \in \mathcal{B}\), with the following meaning: we identify \(\mathcal{A}\) with \(X^* \times \mathbb{R}\), associating \(h_{u,\alpha}\) to the pair \((u, \alpha)\), and hence we write \(\hat{T}(u, \alpha)\) instead of \(\hat{T}(h_{u,\alpha})\), and consider the action of \(\hat{T}\) as an operator acting on \(X^* \times \mathbb{R}\).

### Page 7

In view of Proposition 3, with this notation, for \(T \in \mathcal{B}\), \(\hat T\) maps \(X^* \times \mathbb{R}\) to \(X^* \times \mathbb{R}\), and so we will write
\begin{equation}
\hat T (u, \alpha) = (y(u, \alpha), \tau(u, \alpha)), \quad (5)
\end{equation}

with \(y : X^* \times \mathbb{R} \to X^*\), \(\tau : X^* \times \mathbb{R} \to \mathbb{R}\), i.e. \(y(u, \alpha)\) is the linear part of \(T(u, \alpha)\) and \(\tau(u, \alpha)\) is its additive constant, so that \(T(h_{u, \alpha}) (x) = \langle y(u, \alpha), x \rangle + \tau(u, \alpha)\).

We must prove that both \(y\) and \(\tau\) are affine. We start with \(y\), establishing some of its properties in the following two propositions.

\(\textbf{Proposition 4.}\) If \(T \in \mathcal{B}\) then \(y(\cdots)\), defined by (5), depends only upon its first argument.

\(\textbf{Proof.}\) Consider arbitrary pairs \((u, \alpha), (u, \delta) \in X^* \times \mathbb{R}\), and assume WLOG that \(\alpha \leq \delta\), so that \(h_{u, \alpha} \leq h_{u, \delta}\), and, since \(T\) is order preserving,
\begin{equation}
h_{y(u, \alpha), \tau(u, \alpha)} = T(h_{u, \alpha}) \leq T(h_{u, \delta}) = h_{y(u, \delta), \tau(u, \delta)}. \quad (6)
\end{equation}

It follows from (6) and Corollary 2 that \(y(u, \alpha) = y(u, \delta)\) and hence \(y\) does not depend upon its second argument. \(\square\)

In view of Proposition 4, we will write in the sequel \(y : X^* \to X^*\), with \(y(u) = y(u, \alpha)\) for an arbitrary \(\alpha \in \mathbb{R}\), and also
\begin{equation}
\hat T (u, \alpha) = (y(u), \tau(u, \alpha)). \quad (7)
\end{equation}
\(\textbf{Proposition 5.}\) Assume that \(T\) belongs to \(\mathcal{B}\). Then,
\begin{enumerate}
\item \(y : X^* \to X^*\), defined by (7), is one-to-one and onto,
\item both \(y(\cdot, x) : X^* \to \mathbb{R}\) and \(\langle y^{-1}(\cdot), x \rangle : X^* \to \mathbb{R}\) are quasiconvex for all \(x \in X\).
\end{enumerate}

\(\textbf{Proof.}\) i) Suppose that \(y(u_1) = y(u_2)\) for some \(u_1, u_2 \in X^*\). Assume WLOG that \(\tau(u_1, 0) \leq \tau(u_2, 0)\). Then \(T(h_{u_1, 0}) \leq T(h_{u_2, 0})\), implying, since \(T\) is fully order preserving, that \(h_{u_1, 0} \leq h_{u_2, 0}\), and hence \(u_1 = u_2\) by Corollary 2, so that \(y\) is one-to-one. Surjectivity of \(y\) follows from surjectivity of \(\hat T\), which is a consequence of Proposition 3(ii).

ii) Take \(u_1, u_2 \in X^*, s \in [0, 1]\). Note that
\begin{equation}
\begin{aligned}
h_{su_1 + (1-s)u_2, 0} &= sh_{u_1, 0} + (1-s)h_{u_2, 0} \leq \max \{ h_{u_1, 0}, h_{u_2, 0} \}.
\end{aligned}
\end{equation}
Hence,
\begin{equation}
T(h_{su_1+(1-s)u_2, 0}) \leq T(\max \{ h_{u_1,0}, h_{u_2,0} \}) = \max \{ T (h_{u_1,0}), T (h_{u_2,0}) \}, \qquad (8)
\end{equation}

### Page 10

Proposition 7. If $T \in \mathcal{B}$ then $\gamma : X^* \times \mathbb{R} \to \mathbb{R}$, defined by (7), is affine.

Proof. We start by proving that $\gamma$ is convex. Take $u_1, u_2 \in X^*$, $\alpha_1, \alpha_2 \in \mathbb{R}$ and $s \in [0, 1]$. We consider first the case in which $u_1 \neq u_2$. Define $\bar{u} = su_1 + (1 - s) u_2, \bar{\alpha} = s\alpha_1 + (1 - s) \alpha_2$. Observe that

Proof. We start by proving that $\gamma$ is convex. Take $u_1, u_2 \in X^*$, $\alpha_1, \alpha_2 \in \mathbb{R}$ and $s \in [0, 1]$. We consider first the case in which $u_1 \neq u_2$. Define $\bar{u} = su_1 + (1 - s) u_2, \bar{\alpha} = s\alpha_1 + (1 - s) \alpha_2$. Observe that
$$sh_{u_1, \alpha_1} + (1 - s) h_{u_2, \alpha_2} \leq \max \{h_{u_1, \alpha_1}, h_{u_2, \alpha_2}\}. \tag{13}$$
As in the proof of Proposition 5(ii), we get from (13) that $T (h_{\bar{u}, \bar{\alpha}}) \leq \max \{T (h_{u_1, \alpha_1}), T (h_{u_2, \alpha_2})\}$, so that, for all $x \in X$,
$$ \langle y(su_1 + (1 - s) u_2), x \rangle + \gamma (\bar{u}, \bar{\alpha}) = s \langle y(u_1), x \rangle + (1 - s) \langle y(u_2), x \rangle + \gamma (\bar{u}, \bar{\alpha}) \leq \max \{\langle y(u_1), x \rangle + \gamma (u_1, \alpha_1), \langle y(u_2), x \rangle + \gamma (u_2, \alpha_2)\}, \tag{14}$$
using Corollary 4 in the equality. Since $u_1 \neq u_2$, and $y$ is one-to-one by Proposition 5(i), we have that $y(u_1) \neq y(u_2)$, and hence there exists $\bar{x} \in X$ such that
$$ \langle y(u_1), \bar{x} \rangle + \gamma (u_1, \alpha_1) = \langle y(u_2), \bar{x} \rangle + \gamma (u_2, \alpha_2), $$
so that
$$ \langle y(u_1) - y(u_2), \bar{x} \rangle = \gamma (u_2, \alpha_2) - \gamma (u_1, \alpha_1). \tag{15}$$
Replacing (15) in (14), and noting that for $x = \bar{x}$ there is a tie between both arguments for the maximum in the rightmost expression of (14), we obtain, after some elementary algebra,
$$ \langle y(u_2), \bar{x} \rangle + s \gamma (u_2, \alpha_2) - s \gamma (u_1, \alpha_1) + \gamma (\bar{u}, \bar{\alpha}) \leq \langle y(u_2), \bar{x} \rangle + \gamma (u_2, \alpha_2). \tag{16}$$
Rearranging terms in (16) and using the definitions of $\bar{u}, \bar{\alpha}$, we get
$$ \gamma (s (u_1, \alpha_1) + (1 - s) (u_2, \alpha_2)) \leq s \gamma (u_1, \alpha_1) + (1 - s) \gamma (u_2, \alpha_2), \tag{17}$$
establishing joint convexity of $\gamma$ in its two arguments. In order to obtain affirmeness of $\gamma$, it suffices to prove that (17) holds indeed with equality. Suppose that, on the contrary, there exist $u_1, u_2 \in X^*$, $u_1 \neq u_2, \alpha_1, \alpha_2 \in \mathbb{R}$ and $\bar{s} \in [0, 1]$ such that (17) holds with strict inequality, and take $\theta \in \mathbb{R}$ such that
$$ \gamma (\bar{s} (u_1, \alpha_1) + (1 - \bar{s}) (u_2, \alpha_2)) < \theta < \bar{s} \gamma (u_1, \alpha_1) + (1 - \bar{s}) \gamma (u_2, \alpha_2). \tag{18}$$
Define now $\bar{\gamma} = \gamma (\bar{s} (u_1, \alpha_1) + (1 - \bar{s}) (u_2, \alpha_2)), \gamma_1 = \gamma (u_1, \alpha_1), \gamma_2 = \gamma (u_2, \alpha_2)$. Since $\gamma (\bar{u}, \cdot)$ is onto by Proposition 6, there exists $\eta \in \mathbb{R}$ such that $\theta = \gamma (\bar{u}, \eta)$, and hence it follows from (18) that
$$ h_{y (\bar{u}), \gamma} < h_{y (\bar{u}), \gamma (\bar{u}, \eta)} < h_{y (\bar{u}), \bar{s} \gamma_{1} + (1 - \bar{s}) \gamma_{2}} \leq \max \{h_{y (u_{1}), \gamma_{1}}, h_{y (u_{2}), \gamma_{2}}\} = \max \{\hat{T} (u_{1}, \alpha_{1}), \hat{T} (u_{2}, \alpha_{2})\} = T (\max \{h_{u_{1}, \alpha_{1}}, h_{u_{2}, \alpha_{2}}\}). \tag{19}$$

### Page 11

.Since \(h_{y(\bar{u}),\bar{\gamma}} = T(h_{\bar{u},\bar{\alpha}})\) and \(h_{y(\bar{u}),\gamma(\bar{u},\eta)} = T(h_{\bar{u},\eta})\), we can rewrite (19) as

\[
T(h_{\bar{u},\alpha}) < T(h_{\bar{u},\eta}) \leq T(\max\{h_{u_1,\alpha_1}, h_{u_2,\alpha_2}\}),
\]

which gives, since \(T\) is fully order preserving and one-to-one,

\[
h_{\bar{u},\alpha} < h_{\bar{u},\eta} \leq \max\{h_{u_1,\alpha_1}, h_{u_2,\alpha_2}\},
\]

and therefore, for all \(x \in X\),

\[
\langle \bar{u}, x \rangle + \bar{\alpha} < \langle \bar{u}, x \rangle + \eta \leq \max\{ \langle u_1, x \rangle + \alpha_1, \langle u_2, x \rangle + \alpha_2 \}.
\]

Since \(u_1 \neq u_2\), there exists \(\bar{x}\) such that \(\langle u_1 - u_2, \bar{x} \rangle = \alpha_2 - \alpha_1\), in which case

\[
\langle \bar{u}, \bar{x} \rangle + \bar{\alpha} = \langle u_1, \bar{x} \rangle + \alpha_1 = \langle u_2, \bar{x} \rangle + \alpha_2,
\]

which contradicts (20), and establishes that

\[
\gamma(s(u_1, \alpha_1) + (1 - s)(u_2, \alpha_2)) = s\gamma(u_1, \alpha_1) + (1 - s)\gamma(u_2, \alpha_2),
\]

completing the proof of affineness of \(\gamma\) when \(u_1 \neq u_2\). For the remaining case (i.e., \(u_1 = u_2\)), we must prove affineness of \(\gamma(u, \cdot)\) for a fixed \(u\). Take now \(u, v \in X^*\) with \(v \neq 0\), so that \(u \neq u + v\) and (21) holds with \(u_1 = u + v\), \(u_2 = u\). Note that \(\gamma(\cdot, \alpha)\) is affine for any fixed \(\alpha\), because for a fixed second argument (21) holds trivially when \(u_1 = u_2\). So, the restrictions of \(\gamma(\cdot, \bar{\alpha})\) and \(\gamma(\cdot, \alpha_1)\) to the subspace of \(X\) spanned by \(u\) and \(v\), being affine, are continuous, and taking limits with \(v \to 0\) in (21), we get affineness of \(\gamma(u, \cdot)\) for all \(u \in X^*\), completing the proof.

In the finite dimensional case, Proposition 7 would suffice for obtaining an explicit form of \(\hat{T}\), but in our setting we still have to prove continuity of \(\hat{T}\), which is not immediate from its affineness. For proving continuity, we will need the following elementary result.

Proposition 8. Take \(T \in \mathscr{B}\). If \(f \in \mathscr{C}(X)\) is finite everywhere, then \(T(f)\) is also finite everywhere.

Proof. Consider \(T \in \mathscr{B}\) and a finite everywhere \(f \in \mathscr{C}(X)\). Suppose that \(T(f)(x_0) = \infty\) for some \(x_0 \in X\) and define \(g_0 : X \to \mathbb{R} \cup \{\infty\}\) as

\[
g_0(x) = \begin{cases}
1 & \text{if } x = x_0 \\
\infty & \text{otherwise}.
\end{cases}
\]

Since \(T\) is onto, there exists \(f_0 \in \mathscr{C}(X)\) such that \(T(f_0) = g_0\). Since \(f\) is finite everywhere, \(\tilde{f} = \max\{f, f_0\}\) belongs to \(\mathscr{C}(X)\). Therefore, using Proposition 1(ii),

\[
T\left(\tilde{f}\right) = \max\{T(f), T(f_0)\} = \max\{T(f), g_0\},
\]

so that \(T\left(\tilde{f}\right)(x) = \infty\) for all \(x \in X\). Hence, we have that \(\tilde{f} \in \mathscr{C}(X)\) and \(T\left(\tilde{f}\right) \notin \mathscr{C}(X)\), in contradiction with our assumptions on \(T\).

\(\square\)

### Page 12

.Proposition 9. If \(T \in \mathcal{B}\) then both \(y : X^* \to X^*\) and \(\gamma : X^* \times \mathbb{R} \to \mathbb{R}\) are continuous.

Proof. First we prove that \(\gamma\) is continuous. Let \(g = \|\cdot \| \in \mathcal{E}(X)\) and define \(\bar{g} = T(g)\). Let \(B\) the the unit ball in \(X^*\), i.e. \(B = \{u \in X^* : \|\|u\| \leq 1\}\). Since \(\|x\| = \sup_{u \in B} \{h_u, 0\}(x) = \langle u, x \rangle\), we have

\[\bar{g}(x) = T(g)(x) = \sup_{u \in B} \{\langle T(h_u, 0) \rangle(x)\} = \sup_{u \in B} \{h_{\hat{T}(u, 0)}(x)\} = \sup_{u \in B} \{y(u, x) + \gamma(u, 0)\}.\]

Therefore,

\[\bar{g}(0) = \sup_{u \in B} \{\gamma(u, 0)\}.\]

Since \(\gamma : X^* \times \mathbb{R} \to \mathbb{R}\) is affine by Proposition 7, it can be written as \(\gamma(u, \alpha) = \bar{\gamma}(u) + \hat{\gamma}(\alpha) + \mu\) where \(\mu = \gamma(0, 0) \in \mathbb{R}\) and \(\hat{\gamma} : X^* \to \mathbb{R}, \hat{\gamma} : \mathbb{R} \to \mathbb{R}\) are linear functionals. Using this representation of \(\gamma\) and (23), we conclude that \(\sup_{u \in B} \{\hat{\gamma}(u)\} + \mu = \bar{g}(0) < \infty\), where the inequality follows from Proposition 8 and the definition of \(g\). Therefore

\[|\hat{\gamma}(u)| = \max\{\hat{\gamma}(u), \hat{\gamma}(-u)\} \leq \bar{g}(0) - \mu \in \mathbb{R},\]

for all \(u \in B\), and hence \(\hat{\gamma}\) is a bounded linear functional with \(\|\hat{\gamma}\| \leq \bar{g}(0) - \mu\), so that \(\gamma\) is continuous. Continuity of \(\hat{\gamma}\) follows trivially from its linearity and the fact that its domain is finite dimensional. Hence \(\gamma\) is continuous.

For proving continuity of \(y\), observe that \(y\) can be written as \(y(u) = Du + w\) where \(D : X^* \to X^*\) is a linear operator and \(w = y(0) \in X^*\). Combining this representation of \(y\) with (22) we have

\[\sup_{u \in B} \{\langle Du + w, x \rangle + \langle \hat{\gamma}(u), u \rangle + \mu\} = \bar{g}(x) < \infty\]

for all \(x \in X\), where the inequality follows from Proposition 8 and the definition of \(g\). Take \(u \in B\) and conclude from (24) that

\[\langle Du, x \rangle \leq \bar{g}(x) - \langle w, x \rangle - \hat{\gamma}(u) - \mu \leq \bar{g}(x) - \langle w, x \rangle + \|\hat{\gamma}\| \|u\| - \mu \leq \bar{g}(x) - \langle w, x \rangle + \|\hat{\gamma}\| - \mu.\]

Therefore

\[\sup_{u \in B} \{|\langle Du, x \rangle|\} = \sup_{u \in B} \{\langle Du, x \rangle\} < \infty\]

for all \(x \in X\). This means that the family of bounded linear operators \(\{Du \mid u \in B\}\) is pointwise bounded. Applying Banach-Steinhaus uniform boundedness principle (see, e.g., [11], p. 32), we get that this family is uniformly bounded, i.e., there exists \(\nu < \infty\) such that \(\|Du\| \leq \nu\) for all \(u \in B\). It follows that \(\|D\| \leq \nu\), and therefore \(D\) is bounded and linear, hence continuous. We conclude that the operator \(y\) defined as \(y(u) = Du + w\) is continuous, completing the proof.

Now we present a more explicit formula for the operator \(\hat{T}\).

Corollary 5. If \(T \in \mathcal{B}\), then there exist \(d \in X^{**}\), \(w \in X^*\), \(\beta \in \mathbb{R}\), \(\tau \in \mathbb{R}_{++}\) and a continuous automorphism \(D\) of \(X^*\) such that \(\hat{T}(u, \alpha) = (Du + w, \langle u, \rangle u) + \tau \alpha + \beta\) for all \(u \in X^*\) and all \(\alpha \in \mathbb{R}\).

### Page 13

Proposition 10. \[
(i) \text{ For } all f \in \mathcal{C}(X), f|X^* = f, \text{ where } f_X^* \text{ denotes the restriction of } f^{**} \text{ to } X \subset X^{**} .
\]

和 证明 All for all f \in \mathcal{C}(X) and all a \in X^{**}, f^{**}(a) = sup_{h_u, \alpha \in A(f)} (\langle a, u \rangle + \alpha).
]

Proof. A proof of item (i), sometimes called Fenchel-Moreau Theorem, can be found, for instance, in Theorem 1.11 of [11].

### Page 14

.meaning that

\[
T(f)(x) = \sup_{h_u, \alpha \in \mathcal{A}(f)} \{ \langle y(u), x \rangle + \gamma(u, \alpha) \} = \sup_{h_u, \alpha \in \mathcal{A}(f)} \{ \langle Du + w, x \rangle + \langle d, u \rangle + \gamma(u, \alpha) \} \]
(27)
using Corollary 5 in the last equality. Define now \( C \in \mathrm{Aut}(X^*) \), \( c \in X^{**} \) as \( C = \tau^{-1}D \), \( c = \tau^{-1}d \). Continuing from (27),

\[
T(f)(x) = \sup_{h_u, \alpha \in \mathcal{A}(f)} \{ \langle Du, x \rangle + \langle d, u \rangle + \gamma(u, \alpha) \} + \beta =

\[
T(f)(x) = \sup_{h_u, \alpha \in \mathcal{A}(f)} \{ \langle y(u), x \rangle + \gamma(u, \alpha) \} = \sup_{h_u, \alpha \in \mathcal{A}(f)} \{ \langle Du + w, x \rangle + \langle d, u \rangle + \gamma(u, \alpha) \} \]
(27)
using Corollary 5 in the last equality. Define now \( C \in \mathrm{Aut}(X^*) \), \( c \in X^{**} \) as \( C = \tau^{-1}D \), \( c = \tau^{-1}d \). Continuing from (27),

\[
T(f)(x) = \sup_{h_u, \alpha \in \mathcal{A}(f)} \{ \langle Du, x \rangle + \langle d, u \rangle + \gamma(u, \alpha) \} + \beta =
\]

\[\tau \left[ \sup_{h_u, \alpha \in \mathcal{A}(f)} \{ \langle C u, x \rangle + \langle c, u \rangle + \alpha \} + \langle w, x \rangle + \beta \right] = \tau \left[ \sup_{h_u, \alpha \in \mathcal{A}(f)} \{ \langle C^* x + c, u \rangle + \alpha \} \right] + \langle w, x \rangle + \beta. \]
(28)

Note that \( C^* \) is an operator in \( X^{**} \), so that the expression \( C^* x \) in (28) must be understood through the natural immersion of \( X \) in \( X^{**} \) (i.e., from now on we consider \( X \) as a subspace of \( X^{**} \)). Observe that, by Proposition 10(ii),

\[
\sup_{h_u, \alpha \in \mathcal{A}(f)} \{ \langle u, x \rangle + \alpha \} = f^{**}(a). \]
(29)

Replacing (29) in (28), with \( a = C^* x + c \in X^{**} \), we get

\[
T(f)(x) = \tau f^{**}(C^* x + c) + \langle w, x \rangle + \beta. \]
(30)

Define \( Y \subset X^{**} \) as \( Y = \{ C^* x + c : x \in X \} \). We claim that \( Y = X \). Assume first that there exists \( \tilde{x} \in X \setminus Y \), and define \( f_1 : X \rightarrow \mathbb{R} \cup \{+\infty\} \) as the indicator function of \( \{\tilde{x} \} \), i.e. \( f_1(x) = + \infty \) otherwise. It is easy to check that \( f_1^{**} : X^{**} \rightarrow \mathbb{R} \cup \{+\infty\} \) is still the indicator function of \( \{\tilde{x} \} \), seen now as a subset of \( X^{**} \). We look now at \( T(f_1) \). Since \( \tilde{x} \notin Y \), we have \( C^* x + c \neq \tilde{x} \) for all \( x \in X \), so that \( f_1^{**}(C^* x + c) = + \infty \) for all \( x \in X \). In view of (30), \( T(f_1)(x) = + \infty \) for all \( x \in X \), implying that \( T(f_1) \) is not proper, and so \( T(f_1) \notin \mathcal{C}(X) \), contradicting our assumptions on \( T \). We have shown that \( X \subset Y \). Suppose now that there exists \( \tilde{x} \in Y \setminus X \). Since \( \tilde{x} \) belongs to \( Y \), there exists \( x' \in X \) such that \( C^* x' + c = \tilde{x} \). Define \( f_2 : X \rightarrow \mathbb{R} \cup \{+\infty\} \) as the indicator function of \( \{x'\} \), i.e. \( f_2(x) = 0 \) if \( x = x' \), \( f_2(x) = +\infty \) otherwise. Since \( T \) is onto, there exists \( f \in \mathcal{C}(X) \) such that \( T(f) = f_2 \). Let \( \xi = -\tau^{-1}(\langle w, x' \rangle + \beta) \). In view of (30) we have \( f^{**}(C^* x + c) = \xi \) if \( x = x' \), \( f^{**}(C^* x + c) = +\infty \) otherwise. So, there exists a unique point in \( Y \), namely \( C^* x' + c = \tilde{x} \), where \( f^{**} \) takes a finite value, or equivalently, \( f^{**}(z) = +\infty \) for all \( z \in Y \setminus \{\tilde{x}\} \). Since we already know that \( X \subset Y \) and \( \tilde{x} \notin X \), so that \( X \subset Y \setminus \{\tilde{x}\} \), we conclude that \( f^{**}(z) = +\infty \) for all \( z \in X \), and hence, in view of Proposition 10(i), \( f(x) = +\infty \) for all \( x \in X \), so that \( f \) is not proper, implying that \( f \notin \mathcal{C}(X) \), a contradiction. We have completed the proof of the equality between \( X \) and \( Y \), establishing the claim. Now, since \( C^* x + c \in X \) for all \( x \in X \), as we have just proved, we apply Proposition 10(i), and rewrite (30) as:

\[
T(f)(x) = \tau f(C^* x + c) + \langle w, x \rangle + \beta. \]
(31)

### Page 15

Controller-based design for graph propagation in weighted networks 15 model it for nice derivatives of power expenditures. Since is Lipschitz with respect to the graphs \(x\) and \(v\), the J derivative belongs to \(L(G,r)\). This restriction eliminates a lot of possibilities (which is good; it means that controllability and observability of the graph \(x\) or, more generally, of all graphs in the subgraph \(\overline{\mathcal{C}}(\overline{x})\). In [13], the superstructure as a graph \(\overline{\mathcal{C}}(\overline{x})\) is both the under graph \(\overline{\mathcal{G}}(\overline{x})\) and its subgraph \(\mathcal{W}(\overline{x})\). Despite the inclusion of the whole subgraphs \(\overline{\mathcal{G}}(\overline{x})\) the controller architecture is purely subgraph based. Our approach allows both, controllability as well as

### Page 16

largest system of numbers which includes the integers but not the real or complex numbers. For example, the system of numbers 1, 2, 3, 4, 5, 6,... and the system of numbers -1, -2, -3, -4,... | 67.  This is the key concept of group theory.

Despite the wide range of applications and mathematical ingenuity involved in this topic, it is not often understood correctly, largely because of a lack of understanding of the basic ideas. This is especially true of group theory. The aim of these lecture notes is to fill that gap and to offer some intuitions drawn from group theory which may help students gain some small but sharp insights into the properties of abstract groups. These notes are written for a beginning abstract algebra class and as such they also begin with a few ideas from topology: what is a group? some motivation is given using constructions based on permutations, coverings of the real line etc. These ratings, which are characteristic of a course in abstract algebra using group theory, emerge naturally - and naturally in these lecture notes. Hence the notation introduced in these notes will be the same as in the study of groups in an introductory course in abstract algebra.

As such it should be possible for students to find the material in the notes fairly independent of the rest of the course content. The first two relations used in these notes bring important results together. The first states, most naturally, that if two elements of a group commute, then they commute with each other; the second, using equivalence classes, states that if two classes are equivalent, they must in fact both contain the identity element. The results in the section make use of these two facts. The third relation involves the quotient group of the equivalence classes of the group. It will be seen that quotient groups cannot in general be embedded into general groups.

Group theory relies heavily upon algebraic structure. We may, metaphorically, consider group theory to be the study of algebraic structures in a small number of ways: as algebraic theories in the sense of homomorphisms, morphisms and congruence relations (closeness); as linear algebraic structures with a linear structure; as algebraic structures which can be understood (isomorphic) to algebraic structures in two other mathematical theories (for example, rings and fields); and as algebraic structures related to algebraic structures in more than one mathematical theory. All of these approaches are useful, but we will adopt the algebraic theory of groups to prove the rest of the theorem in this document. Note that we will not define what is meant by a group.

### Page 19

Appendices Appendices. 1

[8]{Cohomology Schüler-Papp Conditions for Lattice Pentagon and Star in a TribuBioBioUnitary probability subgraph tensor computation} The toric connection lies at the center of Lie algebras in the theory of Lie groups and Lie algebras. 
As such it is important for developing various applications within Lie theory and Lie related topics. The toric algebra
was developed in 1981 by Liebscher and Schmidt in order to bring the connection of Lie algebra across the entire set of the toric algebra and to remove the metrical requirements from the algebraic point of view.