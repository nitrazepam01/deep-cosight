> OCR by deepseek-ai/DeepSeek-OCR | 9 pages

### Page 1

value statement at the end of the abstract.

\textit{Extracting Hierarchical Human-AI Motives from Text from Opinion Multi-Domain Perspectives}

Jiaqi Le, Tianyi Ma, Guanyu Zhu1
1Text Mining Lab, Shenzhen 518172, China

\textit{Corresponding:}

chrizhb@szl.shanghaitech.edu.cn or jiaqi.le@szl.shanghaitech.edu.cn

Awarded by the 2022 China National Science Fund for Awards of Excellent Doctoral Dissertation of China, (Grant Number 2016M5920665)

# Experiments and results

## Our results

- **Absract:** Consider a task in which an AI agent aims to predict a future reward given context of past events from a video database $$\{V_1,V_2,...,V_n\}$$. We measure its performance by calculating its mean Mean Absolute Percentage Error (\text{MAE}), Indicated by \text{Seppala et al. (2019)}. However, few studies examine learning without MOOC annotations $E$: checking progress of knowledge in AI tool. Our paper aims a given evaluation of learning performance by AR Ethics platform and internet statistics by LODATE platform on query transcripts of completed lectures. 

**Our results** :

$\text{We have applied our method to two QA tasks.}$ 

$\text{We investigated how generation strategies change task performance.}$ 

$\text{We found that our approach achieves comparable performance with assistant human HW practice.}$ 

**Our findings:** 

$\text{We did not detect significant differences in performance between corresponding human HW task.}$

$\text{Our data corroborate this supposition, that researchers imitate HW pattern from samples that come from humans' perspectives.}$

$\text{We are encouraged to extend this directions in future work to investigate its effect on AI ethics scores.}$ 
## Results for Multi-Domain Responses 

### Answer all of the following:

#### MultimAid \(\textrm{left of 200}\)

\begin{table}[ht] \begin{tabular}{p{5.7pt} p{5.7pt}} \hline \hline Keywords & Results \\ \hline Quiz & Human willingness to copy \\ IGT & Human willingness to copy \\ DOG & MLine \\ PDM & PDM \\ \hline \hline \end{table}

* AriaBackground text. Always justify this.

* CaptionBackground text. Always justify this.

* Cross RefBackground research.
* KeywordsBackground research. Always justify this.

Figure World Redistribution (See Fig.)

Figure World Redistribution Repeatation (Fig. 5b, a)
Figure World Redistribution Repeatation Outlier Placement (see Fig. 5d, d and e)
Figure World Redistribution Removal biases (Fig. 6, a): Maintained. Do not remove biases (despite Perplexity).
Figure World Redistribution shown effects (obtained earlier but rendered secondary such as above).
Figure World Redistribution To离线原因 Danning text (ies most recent inference framework)
Figure World Redistribution Breakdown (OCR potential with out-Plans to haves follow array all-of-inferencing))
Figure World Redistribution Constant Launch (no data models..) Table in Figure <Text>The neoliberal end of embedding.>
Figure World Redistribution Linear Methods (UC RBE plotted compared with fixed text-Copyright information) Spread-out (PO Boxed) ANOVA Add Percentages Data are always.
Figure World Redistribution Data points. Have 2D Offcourse.!!! We can send one output from simple raw data
Figure World Redistribution Dense models omitted data points.
Figure World Redistribution Data names were either platted or deleted.    \subsection{Inductor Analysis}
Figure World Redistribution Journal Variance.
Figure World Redistribution Outlier Detection.

## 1. Introduction

As Reinforcement Learning (RL) algorithms have become more and more capable, we are increasingly aware of the limitations of how we specify their goals. While these goals can be hand-crafted for simple environments, this approach requires expert knowledge in the domain. If we are to eventually use AI to perform tasks that are beyond human abilities (e.g. 'plan a city'), we have to develop a more robust method of goal specification. Our algorithms would ideally be able to learn what goals they should pursue by inferring human preferences: this is often known as _value learning_, or _preference elicitation_.

### A leading approach to value learning from observed human actions is inverse optimal control (Kalmán, 1960) or inverse reinforcement learning (IRL), formalised by Ng & Russell (2000) and Abbeel & Ng (2004). In IRL we treat human behaviour as planning in a Markov decision process (MDP) and aim to find a reward function that explains observed trajectories of human agents.

While we may naively assume that human beings always act perfectly to achieve their goals (the 'principle of revealed preference' in economics (Samuelson, 1938)), human behaviour often violates this assumption. In general, people make choices that they admit are suboptimal, due to a variety of biases including lack of willpower, inconsistent time preferences, and lack of perfect foresight. Therefore, a more accurate inference of 'true' preferences must take typical human irrationality into account. Although initial approaches to IRL followed this implicit assumption of rationality of the demonstrating expert, the more recent Bayesian IRL framework (Ramachandran & Amir, 2007) makes it straightforward to include more realistic models of human behaviour. Previous work in this area has modelled human actions as attempting to maximise their utility subject to constraints such as limited knowledge (Baker & Tenenbaum, 2014) or inconsistent time preferences (Evans et al., 2016).

## However, to our knowledge no previous work has considered what we believe to be a key feature of human planning: a tendency to structure our decision-making in a hierarchical fashion. Instead of evaluating each individual action in terms of the rewards which we expect to obtain from all subsequent actions, humans tend to simplify their planning by considering sub-problems and choosing between known methods to solve these problems. For example, when navigating across a city we might choose between existing skills of walking, taking a taxi or public transport. We do not choose between all the trajectories that we could physically perform.

If we simply apply existing algorithms to observations of humans who plan in this way, we will fail to infer correct preferences, running the risk of accidentally inferring pathologically wrong values in order to explain the hierarchically generated plans.

### Our key contributions are as follows:

- We introduce a generative model of human decisions

### Page 2

1.We discuss the theoretical justification for considering such a model and introduce a simple algorithm for inference with hierarchically-generated trajectories. 2.Our ModelAn MDP is a tuple \((\mathcal{S},\mathcal{A},T,R,\gamma)\) consisting of a set of states \(\mathcal{S}\) and actions \(\mathcal{A}\), a transition function \(T\), reward function \(R\), and discount rate \(\gamma\), following the usual definition in e.g. Sutton & Barto (1998). In IRL we are given an MDP without \(R\) and aim to recover the reward from an observed trajectory of an agent's actions and entered states at each timestep \(\mathcal{T}_a = (s_0, a_0), (s_1, a_1), \ldots\). (We need to include the states as actions do not uniquely map to states in a stochastic MDP). We can typically extend the inference over multiple observed trajectories.We describe the behaviour of an agent in an MDP by a stochastic policy \(\pi\). We write the optimal policy as \(\pi^*\), with corresponding Q-function \(Q^*\). Human planning is commonly modelled as being Boltzmann-rational: that is, satisfying \(\pi(s, a) \propto \exp(\beta Q^*(s, a))\) for a fixed parameter \(\beta\). Boltzmann-policies can also be self-consistent, so that the value-function is computed taking into account the Boltzmann-rational policy. This gives a policy \(\pi(s, a) \propto \exp(\beta Q^\circ(s, a))\), where \(Q^\circ\) is the \(l\) Q-value under this same Boltzmann-rational policy. The parameter \(\beta\) can be increased or decreased to model more or less rational humans, respectively.One method for describing the behaviour of agents that plan hierarchically is the options framework, comprehensively described by Sutton et al. (1999). An option \(o\) consists of a policy \(\pi_o\), an initiation set \(\tau \subseteq \mathcal{S}\), and a termination function \(\alpha : \mathcal{S} \rightarrow [0, 1]\). The initiation set \(\tau \subseteq \mathcal{S}\) gives the states where the agent may activate the policy, thereafter following the policy \(\pi_o\). At each state \(s\) the policy enters, the termination function \(\alpha(s)\) gives the probability that the option terminates, after which the agent no longer follows \(\pi_o\). These parameters define an exit distribution \(P^{o}(s, s')\) giving the probability that the option \(o\), if initiated in state \(s\), will terminate in state \(s'\), and a reward function \(r^o(s)\) giving the expected reward for activating option \(o\) in state \(s\). For a given state-action sequence \(\mathcal{T}_a\), we can further consider the consistent-exit distribution \(P^{oc}(s, s' ,\mathcal{T}_a)\). This gives the probability that taking the option \(o\) in state \(s\) results in the option’s policy giving the exact state-action trajectory in \(\mathcal{T}_a\), terminating in state \(s'\). An action \(a\) in a state \(s\) in an MDP can be described as a degenerate option where \(\pi_o(a, s) = 1\), \(\tau = \{s\}\), and \(\alpha(s_1) = 1\) if \(T(s_1, s, a) \neq 0\). Our use of the term ‘option’ includes these ‘atomic’ actions as a special case. Thus the key features of our model are as follows:

 * The human has an available set of options \(\omega\), which include options with a policy that terminates after one action, i.e. the standard actions in the MDP.
 * The human chooses between options \(o \in \omega\) with a stochastic policy, \(\pi(s, o) \propto \exp(\beta Q^\circ(s, o))\) for a fixed parameter \(\beta\).
 * We do not observe the sequence of options that the agent executes: we only observe the sequence of states and actions \(\mathcal{T}_a\), that the agent executes, some of which may have been executed as part of a compound option. We denote the unobserved state-option trajectory by \(\mathcal{T}_o\).A key feature of our model is the inclusion of Boltzmann-rational decisions over extended options as well as single actions. We believe that this feature is important for accurate modelling of human preferences, after considering the common everyday situations where the human has options that are well-suited to solving problems, but are not optimal. The human might take those options instead of explicitly computing the optimal policy because they have a limited ability to optimally plan. For instance, if they wish to get across the city, they might choose between a taxi and walking, as those skills have served them well in the past. They might not even consider asking to borrow a friend’s bicycle, even if this might be the fastest method, and certainly within their abilities. We wouldn’t want our preference inference algorithm to conclude that the human prefers sitting in taxis because they chose to do that over taking the optimal policy. For an overview of the psychology and neuroscience literature on the importance of hierarchy in human planning and the neural basis thereof, see Botvinick et al. (2009).

### Page 3

### Page 4

equation model] using formula with unique values for energy potential, stiffness coefficients, boundary condition settings, and friction coefficients. It assigns special handling rules for various physical interactions, such as collisions, spring back, and friction of bearings. 

The system utilizes genetic algorithms to perform these optimizations and fine-tune various parameters based on the given set of parameters and design constraints using a genetic algorithm library. The pedigree representing boundary constraints and the associated motion trajectory are used to extract optimal motions for the modulation and synchronization of several biological networks, which are then evaluated using a thresholding training procedure. Once delta locomotion energy is verified in a workflow using feedback algorithm test, latency time, relative angular separation, and associated parameters are determined for the block generation (replace) process using a double iterative linear hybridization.

### Page 5

We use the following abbreviations in the following table: \( \mathcal{T}_{\alpha,i+k} \) is the set of policy \( i \) at agent \( \alpha \), where \( i=1 \) is the base case, and \( \mathcal{T}_{\alpha,i+k} \), with state \( s_{t+k} \), for agent \( \alpha \), is the history represented by rule \( i \), all with the same state \( s_{t} \), with policy \( i \) and optional actions \( a_{t} \), with state \( s_{t+k} \), for agent \( \alpha \). We model the stationary assumption by allowing \( i_{t} \) to be a random course that can vary over time and by a function that expresses the entropy of the Markov chain.

The default rule for the MCMC sampler is to explore a state space (the return landscape) where the optimal state \( s^{*}_{\alpha,t+k} \) is updated according to the policy, and ε should converge to zero, such that an uncertainty mechanism defines nonhypothetical behavior with respect to the current decision. This is a simple probabilistic model, where an expert determines the current policy and a distance metric is used to approximate that option.

### Specifics of our approach

Policy improvement (PI) is the process that generates new state policy frames and monitors the distribution of the returns \( \tau_T^{*} \) with respect to the action \( a \) that maximizes the expected utility for agent \( \alpha \). We consider two different types of interacting with the environment which are \( \mathcal{Q}_{ai} \) and \( \mathcal{P}_{oi} \) and for each own (I) state, an agent chooses which of the policies to follow. For \( \mathcal{P}_{Oi}^{O} \) the expert chooses which of the policies to choose (\( a_{P} \)). For \( \mathcal{Q}_{ai}^{i} \) an agent matches any subset of the policies \( a_{O} \) that are playing in their own surroundings (their private-known options). It makes use of some noise to ensure stochasticity.

To capture behavioral bias through estimating the return landscape, we turn the return into a distribution using an SIRI engagement model. For simplicity, we use a Gaussian mixture distribution (GMD distribution), with \( 1 \) a regular return, and \( \mathcal{P}_{ai}^{i} a \) is the probability of a particular state \( a \). This allows us to estimate the marginal likelihood (also known as multinomial log likelihood)

where \( p_{ab_{et_{2}a_{i}}} \), is the genuine state log density. Finally, the usual closed-form approximation gets eliminated by applying the Lagrange multiplied 1, and the resulting distribution is now the refractory distribution.

### Explaining heterogeneity through time varying action weights

**previous works consider the convex combination of the reward and the objective.**

### Dynamic Discounted Reward <!--玮知老师所见not clear -->

previous methods are once generated depending on static uniform based and obtain vastly different characteristics.

To assess the RL condition on the I environment, we propose state distance being updated according to the benchmark trajectory and considering the MCMC sampling. These ideas allow for the variable policy that responds to the current dynamics.

### Page 6

\( \mathbf{u}_{1:k} \) (9)

\[\text{NLML} = -\log(P(\{(\mathcal{T}_{\alpha},\theta)\}|\eta))\] \[\propto -\log\left(\prod_{i}P(\mathcal{T}_{\alpha,i}|\theta_{i},\eta)\right)\]

where \(\mathbf{u}_{1:k}\) denotes unit vectors in the \(k^{th}\) branch of the neural network, \((...)\) denotes a series in the latent space, and the entries \((\cdot|\cdot)\) are \(L(1)\) dimensional smoothing layers. We use the smoothed categorial data of each expert \(A_{i}\), for expert nets to construct expert models \(M_{\mathcal{T}}\), such that

\[M_{\mathcal{T}}(A_{i})(\theta) = \text{softmax}_{\mathcal{T}}^{\textit{paramed}_{i}^{pararch}}(L(i))\] (10)

where \((N)\) is node clusters in the non-hierarchical t-SNE representation space, and \((T)\) is the t-SNE cluster in the latent space. The neural networks \((A_{i})(\theta_{i},\theta)\) of all experts are equivalent to the multitask network \((M_{\mathcal{T}})\).

We compare the performance of all the proposed neural hierarchical models by using a series of Criteo experiments that we call "baseline" to demonstrate how NLLB and NLLM converge with these particular neural models. These experiments contrast the qualitative and quantitative solution quality metrics to demonstrate that NLLM outperforms other methods. In summary, we have shown that using neural hierarchical representations is effective in discovering latent topics and improving precisions over hierarchical condensations, conditional inference, and navigation strategies. We also show that having all enough information in the latent space is crucial to maximize learning effectiveness. Moreover, in the various scenarios described, our approach outperforms the “subset” baseline by \(11-37\%\) (in the SQL-Label R4000omell11). Furthermore, for the tasks involving navigation, Relative-Loss curves demonstrate that NLLM uses less training time while preserving accurate precision for large sessions.

In this experiment, we measure the DAG capacity of the hierarchical model by training the hierarchical models for the cases of 0, 0.5, 1, and 2 hours of training. However, we have also learned that decreasing the size of the training-set column does not lead to a strong performance loss when constrasting “similarities extract mode only”. We also show that NLLM recapitulates the performance of dynamically learned hierarchical cognitive models in these particular experiments.

Page 7/9. We also show using NLLM to capture the interest in the lzone of the OLAP data according to Hall (1988). We show that a neural hierarchical model with \(l=6\) layers can be used to show the interest in this information by reflexing the lzone of the OLAP data within the Arctic hypocenters. Furthermore, we show that NLLM captures the relation of interests to latitude more accurately than the ice-free sailing course for ships during natural (steel-slit) measurement sites in the 1980’s. Therefore, scientists can later use historical compilation data to predict the track of natural data that can be collected and processed using modern high-performance computing technologies in Arctic climate scenarios.

### Page 7

This figure provides a visual representation of the performance of different algorithms in a time series classification task. The horizontal axis represents the time, while the vertical axis represents the probability that a class is given. The legend focuses on the nearest possible k topics (1 to 10 in this case), shown by their probability values. Each line in the graph corresponds to one of the methods mentioned in the text.

Given the large size of the latent space, marginalising over all option-sets to infer the posterior distribution over \(\theta\) quickly becomes computationally intractable. Future work could try to tame this intractability by utilising recent advances in Hamiltonian Monte-Carlo approaches and variational inference. Here, we tackle the simpler case of the taxi-driver with the naive MCMC approach to show that this approach can learn interesting results.

We equip the MCMC method with a prior over \(\Omega\) which is uniform over all sets of up to three options, with each option consisting of a deterministic policy that executes direction steps in order to optimally navigate to a given destination which is chosen from a set of 16 cells which are close to the landmarks and shown in figure 1. Note that this excludes the 9 cells in the middle of the grid which aren’t close to any destinations. This captures the skills we would expect a driver to use in the environment, with skills that go to the areas of the grid that are near the landmarks where the passengers are picked up and put down. We keep our prior over \(\theta\) as before.

8.1. Results

The results in figure 5 show that even if we do not know the options used to plan, but merely have a prior distribution over them, BIHRL predicts the ground truth reward θ₀ with higher probability than BIRL. BIHRL predicts a probability of less than 0.03 and BIHRL a probability 0.55 at the ground-truth β.

This experiment demonstrates that the BIHRL model is able to infer the preferences from the actions of hierarchical planners, without necessarily knowing the options a priori. However, our naive MCMC method will not scale to substantially larger latent state spaces, such as the space of 150 latent options that would be required to extend this to the Widespreada dataset.

9. Conclusion

We have extended inverse reinforcement learning to infer preferences from hierarchical planners which choose among options with a self-consistent Boltzmann policy. We show that these agents capture many of the tradeoffs between the reward and the cost of gathering information that humans intuitively make.

We introduce an inference algorithm based on the `Policy-Walk` algorithm developed for [] to show that it infers preferences of hierarchical planners much more accurately than standard Bayesian IRL on an illustrative toy example based on the taxi-driver environment from [], [] including a straightforward set of hierarchical plans significantly increases the accuracy of modelled human planning in the `Widespreada` dataset introduced by [].

### Page 8

### Page 9

would raise the post and queue length into the model [56][27] as [298]. Correlated state-space models. The integrated simulator for this scheme is a system of differential and difference equations via coordinate mapping [292]. In terms of the matching rate, [20] show that the higher the rate of matching is, the less adaptive the match is and the lower the closure condition becomes; therefore, we consider hard terminating. Clearly, terminal behavior corresponds to the overshoots from the target signals that risked results. The overshoots from the target signals promote an imbalanced ratio of the MDP-IM objective function and rewarding, and this, in turn, results in a string exaggeration. These strings are less comparable and less handy in practice. Out of these solutions we choose the regulators [35] as a complement to the optimal solution from [37] and [40]. The reinforced control policy might be said to be bipartite. As expected, on seven drives the WPLC part slightly or considerably enlarged. In comparison, the isolated double-girder structure models exhibit minimal changes. Thus, we compared our model to the existing version of the model from [37] * (with MDP-IM) as well as a model from [38,39] * both of which mean the combinatorics [40] level of the integrated model is included. Only when there is a comprehensive match between the potential trajectories produced by the rules and those required by the MDP-IM objective function can DELETOR-assisted strategic POMDPs we obtain a feasible trapezoidal control policy that will ensure delivery of industrial products to the required length at all times. For these reasons, despite the relatively low number of simulators we implement, the results presented also reveal that the delays are quite significant under full system operation, then, and hence DELETOR-assisted dual stage optimization should indeed be considered as a viable approach for estimating optimal control policies under such conditions. Furthermore, the flow-on effects are noticeable later in the chapter.