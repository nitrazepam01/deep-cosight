> OCR by deepseek-ai/DeepSeek-OCR | 22 pages

### Page 1

x^+ = g(x,u) (x,u) ∈ D (1)

\[
\begin{array}{ll}
\text{98 pages} & \text{Full TeX} \\
& \text{with figures and tables} \\
\end{array}
\]

### Page 2

The primary objective of this paper is to lay down the theoretical foundations of a MPC framework for hybrid dynamical system, with the meaning of “hybrid” as clarified above. In contrast to the aforementioned works, the present approach directly incorporates hybrid time domains and trajectory evolution into the prediction and constraint structure, allowing for general, continuous-time hybrid dynamics beyond piecewise-affine, impulsive, or switched-only systems. Although modern MPC developments, such as economic and dissipativity-based MPC, turnpike phenomena, and formulations without terminal ingredients, offer powerful alternatives in continuous settings, their extension to hybrid systems is nontrivial and remains an open research direction. This paper focuses on transferring and formalizing early MPC principles in the hybrid context, providing a foundation for future integration of these newer techniques.

B. Contributions

Although the concept of applying MPC to hybrid systems has existed in literature for several decades, an MPC framework for hybrid plants of the form (1) is not yet available. This paper provides a comprehensive presentation of hybrid MPC for such systems, and formally introduces the structural conditions and theoretical tools needed to ensure:

1) Appropriate structure and properties of the hybrid prediction horizon;
2) Forward invariance of the terminal constraint set and of the feasible set (Proposition 5.1 and Proposition 5.2);
3) That the value function serves as a Lyapunov function via bounds in terms of the terminal and stage costs (Lemma 5.3 and Lemma 5.5);
4) Positive definiteness of the value function (multiple results in Section V-D and Section V-E);
5) Asymptotic stability of a closed set via hybrid MPC, including special cases when jumps or flows are persistent (Theorem 6.2);
6) Two examples showing in detail how to apply the results.

The results are illustrated in two examples throughout the paper, one pertaining to control of a bouncing ball and another one about sample-and-hold control. The overall framework was first outlined in the conference paper [29], which informally presents an initial version of the framework presented here but does not include any mathematical result. The conference paper [30] presents a preliminary version of the basic assumptions. These assumptions are relaxed here; in particular, the set to asymptotically stabilize does no longer need to be compact, expanding the applicability of our results to a broader class of stabilization problems (e.g., consensus, tracking, and estimation), and a condition relating the terminal constraint set and the set to asymptotically stabilize is removed (see (O4) in [30]). A result revealing properties of the value function was stated without proof in [30, Lemma 5.4 and Lemma 5.5], but significantly improved here, under weaker assumptions (cf. Lemma 5.3 and Lemma 5.5) and allowing the stage costs and the terminal cost to be not be positive definite. The latter results can be found in Section V-D and Section V-E, which further extend the relaxations in the conference paper [31] – in fact, only a version of items 2 and 3 in Theorem 5.6 are considered therein, which are further relaxed in those sections. Note that [30] and [31] do not include i) any mathematical proofs, ii) the intermediate results required to establish the properties of the value function and asymptotic stability, and iii) the many illustrations via examples, given by two examples revisited throughout the paper multiple times.

C. Organization of the Paper

Section II introduces notation and basic concepts of hybrid systems. Section III presents an overview of hybrid MPC and its main ingredients: the cost functional, the prediction horizon, the constrained optimal control problem, and the recursive implementation of hybrid MPC. The assumptions required for hybrid MPC to be asymptotically stabilizing are presented in Section IV. Section V reveals the main properties of the constrained optimal control problem associated with hybrid MPC. Section V-D characterizes properties guaranteeing positive definiteness of the value function. Section VI formalizes the asymptotic stabilization properties of hybrid MPC. Section VII presents examples to which the main results are applied. Open problems and future work are discussed in Section VIII.

II. Background on Hybrid Control Systems

Throughout the paper, we use \(\mathbb{R}\) to represent real numbers and \(\mathbb{R}_{\geq 0}\) its nonnegative subset. The set of nonnegative integers is denoted \(\mathbb{N}\). The notation \(S_{1} \subset S_{2}\) indicates \(S_{1}\) is a subset of \(S_{2}\), not necessarily proper. The 2-norm is denoted \(|\cdot|\). The distance of a vector \(x \in \mathbb{R}^{n}\) to a nonempty set \(\mathcal{A} \subset \mathbb{R}^{n}\) is denoted \(|\textbf{x}|_{\mathcal{A}} \coloneqq \inf_{a \in \mathcal{A}} |\textbf{x} - a|\). We denote by \(\mathcal{A} + \delta\mathbb{B}\) the set of all \(x \in \mathbb{R}^{n}\) such that \(|\textbf{x} - a| \leq \delta\) for some \(a \in \mathcal{A}\). The interior and closure of a set \(S \subset \mathbb{R}^{n}\) are denoted int \(S\) and cl \(S\), respectively. We denote by \(\Pi : \mathbb{R}^{n} \times \mathbb{R}^{m} \to \mathbb{R}^{n}\) the standard projection onto \(\mathbb{R}^{n}\) such that \(\Pi(x, y) = x\). A strictly increasing continuous function \(\alpha : \mathbb{R}_{\geq 0} \to \mathbb{R}_{\geq 0}\) with \(\alpha(0) = 0\) is said to be a class-\(\mathcal{K}\) function. An unbounded class-\(\mathcal{K}\) function is said to be a class-\(\mathcal{K}_{\infty}\) function.

A. Hybrid Control Systems

This paper focuses on the control of hybrid plants given as \(\mathcal{H}\) in Section I, where \(x \in \mathbb{R}^{n}\) is the state and \(u \in \mathbb{R}^{m}\) is the control input. The data of the hybrid plant \(\mathcal{H}\) is given by

* •the flow set \(C \subset \mathbb{R}^{n} \times \mathbb{R}^{m}\);
* •the flow map \(f : C \to \mathbb{R}^{n}\) describes the continuous evolution of \(x\) when \((x, u)\) belongs to \(C\);

### Page 3

### Page 4

illustrated algorithm. The text in the first iteration in the cellular automaton with informed margin automata system (known as 192 games) is used to investigate the reasons of the reaching patterns in the automaton. Six different patterns are considered in the given examples given the communicating states depending on the shape of the automata (for example, for the considered automata, which are shown in Figures 2a-2f). For simplicity, I can consider _a concrete automaton, depending on which buffered edges have the value 1, represented as 1 nodes. 5 is for regular communication of the buffered edges; 2 for intermediate_ communication and 4 for _weak-barrier communication with direct direct communication._ for example, the first example of them is the system 192 games _Brotherhood_. In the first concrete examples (_Unensee положительно_) we do not consider the buffered edges, which means the set of protocol states (taken as reaching time points) is non-empty in this the automaton.

Enjoy. We check the states of switching time points, this is determining: time interval for solving the reaching patterns in different possible patterns of the automaton behaviour associated with 192 games. Using the time to the reaching time points for the 192 game patterns: where the probability of success is considered correctly accepted stationary asymptotic algorithm for solving the 192 game problem, and also the results obtained using an abstract algorithm with a minimum requirement as a MAX-ALTERNATIVE CONVERGENCE FUNCTION EQUATION (or basis). The solutions of the CA-type problems are not easy to get either from the sequentially combined graph part and XEM for the game. A simple step-by-step explanation of the algorithm detailed in the preliminary part of this paper. [Figure 1].

### Algorithm: Bromine avoidance is conforming to rules

**Step 1:** **Define the game state.** Two players deliver their all possible rules (see Figure 8). It means that each changer should choose rules from a union of all possible rules. All the cases of rules are used in the game by setting elements in a normal state by the chosen rules. So player 1, playing a rule, induces vector \[ w \]. For example, the first attacker with edges Á-É and Á-F, after the rules are illustrated by Figure 8, we can write another vector for the rules.

Learn more example with Algorithm: Bromine avoidance ([Figure 1]).

### Page 5

has unique state trajectories \(t\mapsto x(t)\); that is, for each \(x_{\circ}\), and each pair of inputs \(t\mapsto u_{1}(t)\) and \(t\mapsto u_{2}(t)\) with \(\operatorname{dom}u_{1}=\operatorname{dom}u_{2}\) such that \(u_{1}(t)=u_{2}(t)\) for almost all \(t\in\operatorname{dom}u_{1}\), and \((x_{\circ},u_{1}(0))\in C\) and \((x_{\circ},u_{2}(0))\in C\), the respective solution pairs \((x_{1},u_{1})\) and \((x_{2},u_{2})\) have identical state trajectories over their common domain of definition.

The next result can be obtained using similar steps as in the proof of [18, Proposition 2.11].

**Proposition 2.8:** The hybrid control system \(\mathcal{H}\) has unique state trajectories if and only if Assumption 2.7 holds.

**Proof:** Necessity is obvious. For sufficiency, consider two solution pairs \((x,u)\) and \((x^{\prime},u^{\prime})\) with the same initial condition and domain, where \(u\) and \(u^{\prime}\) are equivalent. Since jumps of \(x\) and \(x^{\prime}\) occur at the same ordinary time, uniqueness follows from uniqueness during flows (by Assumption 2.7) and uniqueness during jumps.

**Remark 2.9:** For hybrid dynamical systems with single-valued flow and jump maps and no inputs (i.e., closed hybrid systems), uniqueness of solutions is guaranteed when flows are not possible on the intersection of the flow and jump sets, and the constrained differential equation (8) has unique solutions [18, Proposition 2.11] and [19, Proposition 2.34]. In contrast, Assumption 2.7 states that uniqueness during flows is necessary and sufficient for uniqueness of state trajectories. This property is due to the fact that given any two solution pairs \((x,u)\) and \((x^{\prime},u)\), their jump times are determined by the domain of the input; hence, by Definition 2.3, \(\operatorname{dom}(x,u)=\operatorname{dom}(x^{\prime},u)=\operatorname{dom}u\).

**B. Hybrid Control Systems under Static State-Feedback**

For analysis purposes, we also study the hybrid closed-loop system arising from assigning the input \(u=(u_{c},u_{d})\) to static state-feedback laws \(\kappa_{C}:\mathbb{R}^{n}\rightarrow\mathbb{R}^{m}\) and \(\kappa_{D}:\mathbb{R}^{n}\rightarrow\mathbb{R}^{m}\) for the hybrid plant \(\mathcal{H}\) in (1). With this input partition, the resulting hybrid closed-loop system, denoted \(\mathcal{H}_{\kappa}\), is given by

\[\mathcal{H}_{\kappa}:\left\{\begin{array}{l} \dot{x}=f_{\kappa}(x):=f(x,\kappa_{C}(x))\quad x\in C_{\kappa} \\ x^{+}=g_{\kappa}(x):=g(x,\kappa_{D}(x))\quad x\in D_{\kappa}\end{array}\right.\] (9)

where

\[\begin{array}[]{l}C_{\kappa}:=\{x\in\mathbb{R}^{n}:(x,\kappa_{C} (x))\in C\},\\ D_{\kappa}:=\{x\in\mathbb{R}^{n}:(x,\kappa_{D}(x))\in D\}\end{array}\]

Similar to \(\mathcal{H}\) in (1), we define solutions to (9) over hybrid time domains via Definition 2.3, as follows: a hybrid arc-input pair \((x,u)\) is said to be a solution pair to (9) generated by the feedback \(\kappa\) if there exists a solution pair \((x,u)\) to \(\mathcal{H}\) that satisfies (5) with \(u(t,j)=\kappa_{C}(x(t,j))\) for all \(t\in\operatorname{int}I^{j}\) and (6) with \(u(t,j)=\kappa_{D}(x(t,j))\) for all \((t,j)\in\operatorname{dom}(x,u)\) such that \((t,j+1)\in\operatorname{dom}(x,u)\). At times, we say that the \(x\) component of such solution pair is a state trajectory of (9).

**III. Hybrid MPC: Key Elements and Algorithm**

In this section, we introduce the main elements required to formulate an MPC algorithm to asymptotically stabilize the hybrid plant \(\mathcal{H}\) in (1).

**A. Overview**

In simple words, the algorithm measures the current state of \(\mathcal{H}\) and finds an optimal solution pair to \(\mathcal{H}\) over a finite horizon in hybrid time. The optimization problem consists of minimizing a cost functional, subject to constraints on the terminal time and terminal state. The cost functional assesses the cost of the evolution of the state and of the possible inputs to be applied, both over intervals of flows and at time instances at which jumps occur. The cost functional imposes a cost on the terminal state, relative to a desired target set. This optimization is performed over a hybrid prediction horizon, and the terminal hybrid time is allowed to vary within a set rather than being restricted to a point, as done, for instance, in discrete-time MPC where the optimization involves a fixed prediction horizon. This optimization approach is reminiscent of free end-time optimal control. It is instrumental for MPC for hybrid systems as the hybrid time domain of an optimal solution pair is not known a priori. Each time the state is measured, the algorithm finds an optimal control hybrid input that is applied to \(\mathcal{H}\) until the next measurement, leading to a receding horizon implementation. The initial optimization occurs at the initial time \((t,j)=(0,0)\). We refer to this algorithm as hybrid MPC.

There are three main differences between conventional continuous-time/discrete-time MPC, in their general formulations, and hybrid MPC:

* Since hybrid control systems can have solution pairs from nearby initial conditions with drastically different hybrid time domains, the terminal time has to be allowed to vary within a set. This set is the hybrid prediction horizon introduced above.

* To account for the differences in the hybrid time domains of optimal control hybrid inputs, the optimization times are not assumed to be periodic or known a priori. Instead, with the initial optimization being performed at time \((0,0)\), each subsequent optimization hybrid time is selected online and the reoptimization occurs before the current hybrid control input is exhausted.

* Since hybrid plants typically involve timer states, logic variables, and memory states, as Example 2.6 illustrates, the problem that hybrid MPC has to solve may be a set stabilization problem rather than a set-point stabilization problem. In fact, the stabilization problem associated with Example 2.6 consists of asymptotic stabilizing the state \(z\) to zero, \(\eta\) to some compact set, and \(\tau_{s}\) to the range \([0,T_{s}]\).

### Page 6

Fig. 1: Hybrid prediction horizon for the case of \(J = 1\), \(t_{0} = 2\), \(t_{1} = 1\), and \(t_{2} = 0\).

As highlighted in Example 2.5, the notion of a prediction horizon given by a singleton \(\{(T,J)\}\) can be overly restrictive and prevent a reasonable formulation of MPC for hybrid systems in the form (1). To maximize feasibility of the OCP, an appropriate selection of the prediction horizon \(\mathcal{T}\) should ensure that it intersects with unbounded

Approximate sufficiency for a dwell-time and standard form prediction horizon computation

To ensure a stable prediction horizon, certain inequalities are applied. First, there exists strictly increasing and decreasing functions \(q:\mathbb{R}_{+}\times\mathbb{R}^{+}\rightarrow\mathbb{R}\) and \(p:\mathbb{R}_{+}\times\mathbb{R}^{+}\rightarrow\mathbb{R}\) such that the function \(q(T,J)\) is a strict observation function for a standard form prediction horizon [Thibon, 2014]. This transformation has yet to be defined, but behaviourally it behaves like \(\{0,1\}\) for \(T>0\) and \(\{0,1\}\) for \(T<0\). Further, \(0 \leq q(T,J)/P \geq 0\) implies that the prediction horizon intersects with an unbounded polynomial.

Similarly, the integral inequality \(u \geq q(T,J)\widetilde{d}\widetilde{g}\) has no second order monomial terms, which allows the prediction function on the right-hand side of the equation of the process \(H\) to be simplified identically to \(u \geq \widetilde{d}\max_{q_{1},N\in\mathbb{N}}[l(p/Q_{1}-c_{1})\cdot T+q_{1}+(\sum q_{1}^{J}N_{F}+\frac{1}{p_{1}})^{\frac{1}{J-b}}]\), where \(q_{1}\) and parameters \(q_{2},N_{2}\in\mathbb{Q}\). Neglecting all terms of type \(q_{1}\) in the overbar function will have the same effect as \(L(p/Q)\) being a strictly increasing function. Thus, the initial condition given by a singleton is assumed, and the additional inequality follows that

\[f(t,S)\geq b+1/(p/Q)\] (11)

where \(p\in\mathbb{Q}\), \(a_{i}\geq q_{1}\) for \(\epsilon_{i}=\delta p_{i}\), i.e., all variables \(\epsilon_{i}\) are bounded and can, thus, be optimized using a once-off procedure.

The third result is the dependency of all conditions on the number of inputs. More exactly, (2) represents a dwell-time that is a solution to the polynomial (5) if there exists a solution. Therefore, the analysis required to compute a positive definite approximation (6) is as follows:

For each \(i\in\mathbb{N}\) and \(j\in\mathbb{N}_{1}\) the Hessian matrix \(H^{(m)}_{ij}\) with scalar \(m\) is given by the following characteristic polynomial:

### Page 9

equation in Fig. 2. Hybrid time domains associated with the hybrid MPC algorithm for the case of $N = 4$ and $N_c = 2$. The hybrid time instances $(T_1, J_1), (T_2, J_2)$ and $(T_3, J_3)$ at which Problem 3.5 is solved are circled.  

Fig. 3. Hybrid time domain of the optimal trajectory associated to the hybrid time domains in Figure 2.  

can be relaxed, as detailed in Section VI.  

Remark 3.8 (About Discretization): Problem 3.5 is formulated for the hybrid plant $\mathcal{H}$ in (1). Note that the flows of $\mathcal{H}$ are not explicitly discretized in the implementation given in Algorithm 1. Such a discretization can be made explicit by choosing a discretization method for $\hat{x} = f(x, u)$ and defining solutions to the resulting system on discretized hybrid time domains—indeed, they are defined over hybrid time domains. Discretization for hybrid systems for the purposed of hybrid MPC are presented in [34], along with a method to solve Problem 3.5 with discretized flows; see also [35].  

Remark 3.9 (Computing a Solution to Problem 3.5): Computation of an optimal solution to Problem 3.5 is a nontrivial task. In certain cases, Problem 3.5 can be solved by converting it into a finite- dimensional nonlinear program. The development of general numerical methods to solve Problem 3.5 is the current object of research, and can take the form of the maximum principle—see [36] and the references therein. The approaches in [34] and [35] solve Problem 3.5 with discretized flows. The algorithms therein use adaptations of off- the- shelf optimization solvers for mixed- integer nonlinear programs, with potentially large computational times. Note that [37] provide conditions under which the optimal cost can be continuously approximated numerically.  

## IV. Main Assumptions  

We list the basic assumptions imposed on Problem 3.5 to ensure feasibility and certify asymptotic stability of a given closed set $\mathcal{A} \subset X$ of interest. The required assumptions resemble those typically assumed in continuous-time and discrete-time MPC, when generalized to the set stabilization problem.6 Combined with the hybrid prediction horizon defined in Definition 3.2, the forthcoming assumptions guarantee most of the properties required to establish asymptotic stability, with the exception of positive definiteness of the value function, which is studied in depth in the forthcoming Sections V- D and V- E, as it can be achieved in multiple ways.  

The following assumptions assert basic positive definiteness properties and bounds on the cost functions. These properties are used in establishing positive definiteness of the value function and asymptotic stability of the closed- loop system resulting from using hybrid MPC.  

Assumption 4.1: Given a hybrid plant $\mathcal{H} = (C, f, D, g)$, a closed set $\mathcal{A} \subset \mathbb{R}^n$, flow cost $L_C$, and jump cost $L_D$,  

( W1) There exists a class- $K_{\infty}$ function $\alpha_C$ such that $L_C(x, u) \geq \alpha_C(|x|_\mathcal{A})$ for each $(x, u) \in C$ ( W2) There exists a class- $K_{\infty}$ function $\alpha_D$ such that $L_D(x, u) \geq \alpha_D(|x|_\mathcal{A})$ for each $(x, u) \in D$  

Assumption 4.2: Given a closed set $\mathcal{A} \subset \mathbb{R}^n$, a terminal constraint set $X$, and a terminal cost function $V$, there exists $\varepsilon > 0$ and a class- $K$ function $\alpha$ such that $V(x) \leq \alpha(|x|_{\mathcal{A}})$ for each $x \in X \cap (\mathcal{A} + \varepsilon \mathbb{B})$  

The key stabilizing ingredient of our hybrid MPC algorithm is given by the following familiar CLF- like assumption requiring the existence of a (static) state- feedback pair $\kappa=(\kappa_C,\kappa_D)$. Below,forward pre- invariance of $X$ means that every maximal closed- loop solution with initial state in $X$ is complete and stays in $X$ [38, Definition 3.1], and forward completeness of $X$ means that for every point $X$ there exists a complete solution. A weaker notion of invariance is weak forward invariance, which is used in the next section and simply requires that from each point in $X$, there exists a maximal and complete solution that stays in $X$.  

Assumption 4.3: Given a hybrid plant $\mathcal{H} = (C, f, D, g)$, a closed set $\mathcal{A} \subset \mathbb{R}^n$, flow cost $L_C$, jump cost $L_D$, terminal constraint set $X$, and terminal cost $V$, there exists a state- feedback law $\kappa = (\kappa_C,\kappa_D)$ such that the terminal constraint set $X$ is forward pre- invariant and forward complete for the hybrid system $\mathcal{H}$ in (9). Moreover, the terminal cost $V$ is differentiable on an open set containing. $\mathrm{cl}(X \cap C_\kappa)$, and  

\[\begin{array}{rl} & {\langle \nabla V(x),f_{\kappa}(x)\rangle \leq -L_{C}(x,\kappa_{C}(x))\quad \forall x\in X\cap C_{\kappa},}\\ & {V(g_{\kappa}(x)) - V(x)\leq -L_{D}(x,\kappa_{D}(x))\quad \forall x\in X\cap D_{\kappa}} \end{array} \quad (15)\]

\begin{array}{ll}\text {S }=\left(\frac{J}{J}\sum_{s_{j}}^{s_{j}+1}L_{C}(x(t,j),\kappa_{C}(x(t,j)))\right)\\ +\left(-\sum_{j=0}^{J-1}L_{D}(x(s_{j+1},j),\kappa_{D}(x(s_{j+1},j)))\right)+V(x(T ,J))\end{array}
\]
parts with the function has been 'Public works'
X to be
process of
positive stand body, ns'
optimization, the Renal Administration, issues instead
friendly.

### Page 11

base.Since \(X\) is forward pre-invariant for \(H_{\kappa}\), \(x\) remains in \(X\) and, by (15),
\[
\mathcal{I}(x, u) \le - \left( \sum_{j=0}^{J} \int_{s_j}^{s_{j+1}} \frac{dV}{dt}(x(t, j)) dt \right. \\
+ \left. \sum_{j=0}^{J-1} (V(x(s_{j+1}, j+1)) - V(x(s_{j+1}, j))) + V(x(T, J)) - (V(x(T, J)) - V(x(0, 0))) + V(x(T, J)) \right. \\
= V(x(0, 0)) = V(x_o)
\]
Item 2 follows from the definition of \(\mathcal{I}^*\) in (14) since the value of \(\mathcal{I}^*\) is the result of minimizing the cost functional over all such feasible solution pairs. Item 1 follows from the fact that the set \(X\) is forward complete and that the cost obtained by any optimal solution cannot be larger than the cost for the solution generated using \(\kappa\).

The following continuity property of \(\mathcal{I}^*\) is a direct consequence of item 1 in Lemma 5.3.

Proposition 5.4: Under Assumption 4.3, for each \(\epsilon > 0\), there exists \(\delta > 0\) such that \(\mathcal{I}^*(x) \le \epsilon\) for each \(x \in \mathcal{X}\) satisfying \(\left| \mathcal{I}^{\ast}(\mathcal{I}^{\ast}) \right| \le \epsilon\).

clearing Properties of the Value Function

Next, we show that \(\mathcal{I}^*\) is upper bounded by a nonincreasing function along optimal trajectories, which decreases during flows (respectively, at jumps) if \(L_C\) (respectively, \(L_D\)) satisfies the lower bound in (W1) (respectively, in (W2)). The proof follows the steps used in the standard MPC literature, see, e.g., [40, Section 3.4] and [7, Lemma 5.4].

\[
\mathcal{I}^*(x(t,J)) = \sum_{i=0}^{j} \int_{s_i}^{s_{i+1}} L_C(x(s,i), u(s,i)) ds \\
- \sum_{i=0}^{j-1} L_D(x(s_{i+1}, i), u(s_{i+1}, i)) \\
= V(x(T, J)) - V(x(0, 0)) + V(x(T, J))  - (V(x(T, J)) - V(x(0, 0))) + V(x(T, J)) \\
V(x(T, J)) - V(x(0, 0)) = V(x_o)
\]

Proof. Given the optimal solution \((x, u)\), we recall the feasible solution pair derived in the proof of Lemma 5.2 that starts from \(x(t,j)\). Call this solution pair \((z, v)\), and note that after time \((T - t, J - j) \in \text{dom}(z, v)\), \((z, v)\) corresponds to a solution generated by the feedback \(\kappa\), say \((x', u')\), where \((T, J)\) is the terminal time of \((x, u)\). Then, observe that \(\mathcal{I}^*(x(t,j)) \le \mathcal{I}(z, v)\) as \((z, v)\) is not optimal. The following holds:
\[
\mathcal{I}^*(x(t,j)) \le \mathcal{I}(z, v) = \mathcal{I}(z, u) \\
= \sum_{i=0}^{j} \int_{s_i}^{s_{i+1}} L_C(x(s,i), u(s,i)) ds \\
- \sum_{i=0}^{j-1} L_D(x(s_{i+1}, i), u(s_{i+1}, i)) \\
- V(x(T, j)) + \mathcal{I}(x', u')
\]
The bound \(\mathcal{I}(z, v)\) follows from the fact that \((z, v)\) starts from \(x(t,j)\) and that \((z, v)\) is not optimal. The equality captures the following fact: the cost of \((x, u)\), which is up to \((T, J)\), plus the cost of \((x', u')\) is equal to the cost of \((x, u)\) up to \((t, j)\) plus the cost of \((z, v)\). By solving for the cost of \((z, v)\) from this relationship we obtain the equality above. Finally, since \(x(t, j) = x'(0, 0)\), the term \(- V(x(t, j)) + \mathcal{I}(x', u') \) is nonpositive by Lemma 5.3, leading to the desired result as \(\mathcal{I}(x, u) = \mathcal{I}^*(x(0, 0))\) since \((x, u)\) is optimal.

\[\text{The Solution-Dependent Conditions for Positive Definiteness of the Value Function}\]
To ensure that the value function is positive definite and bounded from below by a positive definite function, we start by assuming the existence of a positive definite function \(\alpha\) such that an appropriate combination of the following conditions holds for every optimal solution pair \((x, u)\):
1. there exist \((t, j), (t', J') \in \text{dom}(x, u)\) with \(t + j \le t' + j'\) such that \(t' - t + j' - j \ge \alpha(|x(0, 0)|_{\mathcal{A}} )\) and
\[
|x(s,i)|_{\mathcal{A}} \ge \alpha(|x(0, 0)|_{\mathcal{A}})
\]
2. for all \((s, i) \in \text{dom}(x, u)\) satisfying \(t + j \le s + i \le t' + j'\);
3. there exist \((t, j), (t', J') \in \text{dom}(x, u)\) with \(t + j \le t' + j'\) such that \(t' - t \ge \alpha(|x(0, 0)|_{\mathcal{A}} )\) and
\[
|x(s,i)|_{\mathcal{A}} \ge \alpha(|x(0, 0)|_{\mathcal{A}})
\]
4. for all \((s, i) \in \text{dom}(x, u)\) satisfying \(t + j \le s + i \le t' + j'\);
5. \(|x(t, j)|_{\mathcal{A}} \ge \alpha(|x(0, 0)|_{\mathcal{A}} )\) for some \((t, j) \in \text{dom}(x, u)\) such that \((t, j + 1) \in \text{dom}(x, u)\);
6. \(|x(T, J)|_{\mathcal{A}} \ge \alpha(|x(0, 0)|_{\mathcal{A}} )\), where \((T, J)\) is the terminal time of \((x, u)\);
7. there exist an open neighborhood \(\mathcal{U}\) of \(\mathcal{A}\) and a continuous function \(\sigma : \mathbb{R} \ge 0 \to \mathbb{R} \ge 0\) such that
\[
|f(x, u)| \le \sigma(|x|_{\mathcal{A}}) \quad \text{for all } (x, u) \in C \text{ such that } x \in \mathcal{U}.
\]
Section V-E provides sufficient conditions for these properties. Note that P2 implies P1), and that P3) implies P1).
Theorem 5.6: **(Positive Definiteness of the Value Function):** Given a hybrid plant \(\mathcal{H} = (C, f, D, g)\) as in (1), the data \((T, L_C, L_D, V, X)\) defining Problem 3.5, and a closed set \(\mathcal{A} \subset \mathbb{R}^n\), if there exists a positive definite function \(\alpha : \mathbb{R} \ge 0 \to \mathbb{R} \ge 0\) such that one of the following conditions holds:
1. Every optimal solution pair \((x, u)\) satisfies P1), and
\[
L_C : C \to \mathbb{R} \ge 0 \text{ and } L_D : D \to \mathbb{R} \ge 0 \text{ are positive definite with respect to } \mathcal{A};
\]

### Page 12

Sample Pseudo-code for Pattern-Generation Algorithm (Program 1)

```
1: Ensure that there exists a positive definite function \(\alpha^* : \mathbb{R} \to \mathbb{R}_0\) such that the weight function \(V(x)\) is positive definite with respect to \(\alpha^*\) and \(L\).

2: Generate an optimal solution pair \((x, u)\) satisfying P1 and P2 by finding the minimum distance from a point \((t, j, e)\) to a set of points \((t' - t)\), \((t' - j)^2\), and \((t' - j)^3\).

3: Find the optimal solution pair \((x', u)\) for inequality (3) by fitting the middle point \(L_C(x_s, x_s)\) of the interval \((0, l)\) to the middle point of the interval \((L_C \cdot x_s, L_C \cdot x_s)\).

4: Extend the position to a vector \((x(u), u)\) where \(L_C \cdot x(u) \gg l\) and \(L_C \cdot x(u) > l\).

5: Find a positive bounded \(L_3 \leq L_C \cdot x(u) \leq L_{C + 1}\) from the minimum solution \((x(u), u)\) with respect to \(L\).

6: Find a positive bounded \(L_3 \leq L_C \cdot x(u) \leq L_{C + 1}\) from the maximum solution \((x(u), u)\) with respect to \(L\).

7: Set \(x(u)\) as the middle point of the interval \((L_C \cdot x(s_1 + 1), L_C \cdot x(s_2 + 1))\).

8: Find the optimal solution pair \((x', u)\), satisfying P4 and P5.

9: Output the optimal solution pair \((x(t), u)\) for objective function (6).
```

When all the above conditions are fulfilled, the following algorithm will be called to generate an optimal solution pair \((x', u)\) for objective function (6).

### Page 13

### Extended Algebra for Kirchhoff-Law Operators

The application of the Extended Algebra for Kirchhoff-Law operators to the discrete T–TáW map is illustrated in Fig. 4 and 5, respectively. The diagrams consists on a set of equations for two variables \( x_i \) and \( t_i \) determined for all bubbly surface \( f \).

$$\begin{align*}
   x_i &= \cfrac{1}{4} \cfrac{N}{2} \left( t_i + t_i^2 + x_{N-1} \right) \\
   t_i &= \cfrac{1}{2} \norm x_i - \cfrac{1}{2}
\end{align*}$$

The equations are solved by non-linear iterations. For every surface \( t_i \), the iteration is ended when the difference between successive iterates under our rtp end predictability limit is less than the prediction limit. The final primary and secondary solution form in the table of Figure 4. \(\Lambda\) will be the combination of the primary ranges \( \Lambda_{prod} \) and \( \Lambda_{recom} \) which is included by an additional, illustrative case for the secondary component.

For switching \( j = 1 \), at the first time slot \( \tau \), we have as solutions:

$$x_{\text{solution}} = \frac{n+x_{\text{even}}}{n+x_{\text{even}}^2},  \quad t_{\text{secondary}} = 0$$
By placing the Futures \( F_{\text{secondary}} \), we obtain

$$F_{\text{secondary}} = \frac{2t_{\text{secondary}}}{n+x_{\text{even}}^2}$$

The values of \( F_{\text{secondary}} \) are found in Table 2. However, tables were able to given the extra primary densities and traditionally corresponding position dynamics for switch reconversions.

Exodus \( \tilde{A} \) could not always be used in renormalization of data vectors \( z_{\text{revisited}},  \tilde{s}_{\text{fixed}}\): While living the switched input, inasmuch as the map endures a set of it-hits to which is observed that the value \( \pi_i \) is finishing. Starting from the vector forms of the remaining set as set of \( \Lambda_{\text{recalculated}} \) of \( F_{\text{secondary}} = \tilde{A} \), and similarly on 0 and \( n^{x}_{\text{primary *}} \). For example, we can reproduce data A from Equation 6 for a spelling condensing, rebuilding of the destination set from Eq.1. From Table 1 we have that

$$A = \begin{pmatrix}
  1 \\
  1 
\end{pmatrix}$$

Rewriting circuit 2.3 and 1.1 by the transformed \( F_{\text{secondary}} \), are following as shifted set.

Next is the discretization assign of the ICCM state set for the same \( n \) number of disadvantages of \( \frac{1}{N+1} \) from input \( x_{\text{sub}}[x_{\text{sub}}] \).

$$\tilde{A} = \begin{pmatrix}
  0 \\
  0
\end{pmatrix}$$

Since the a finite exhaust noise of \( x \) [in the fixedranet, \( \overline{\tilde{A}}_{\text{recalculated}}  \] is the product of LAC come up ).

As a way on overcoming the dimensional discrepancy above, the interior element of a cell transversal-resulimenic vessel with \( \tilde{\eta}_{\text{secondary}} \) One cell can rewrite.

Given the reconfiguration results of a resolution functional concant \( \tilde{\epsilon} \), its degree of determination \( \tilde{d} \) shows would be includes the vertex residues \( \tilde{x}_{\text{exterior}} \) for its i-th position vector \( x_{\text{exteriors}} \) and outside.

mplies ${\cal J} (x, u) \geq \widetilde\alpha_D (r/2) $. Thus, it follows that
\[

Before stating solution-independent sufficient conditions for P1)-P4), we revisit the sample-and-hold control example.

Example 5.8 (Sample-and-Hold Control (revisited)): Consider the digital control system in Example 2.6 and suppose that the function \(\tilde{f}\) is linear, i.e., \(\tilde{f}(z, \eta) = Az + B\eta\) for some matrices \(A\) and \(B\). Let \(\cal A = \{0\} \times \{0\} \times [0, T_s]\). Along the first period of flow, a solution pair \((x, u)\) is given by \(\tau_s(t,0) = t + \tau_s(0,0)\) and
\[
\left[
\begin{array}{c}
    z(t,0) \\
    \eta(t,0)
\end{array}
\right] = \exp(\widetilde{A} t)
\left[
\begin{array}{c}
    z(0,0) \\
    \eta(0,0)
\end{array}
\right], \quad \widetilde{A} :=
\left[
\begin{array}{cc}
    A & B \\
    0 & 0
\end{array}
\right]
\]
Note that the function \(t \mapsto \exp(\widetilde{A} t)\) is continuous and \(\exp(\widetilde{A} t)\) is nonsingular for each \(t \geq 0\). Moreover, regardless

© The IET 2021.

### Page 14

Lemma 5.10: Suppose that item 1 in Proposition 5.9 holds. Then, there exists a class-\(K\) function \(\alpha\) such that every solution pair \((x, u)\) to \(\mathcal{H}\) is such that, for each \(T\) satisfying \((T, 0) \in \text{dom}(x, u)\), \(|x(t, 0)|_A \geq \alpha(|x(0, 0)|_A)\) for all \((t, 0) \in \text{dom}(x, u) \cap ([0, T] \times \{0\})\).

Lemma 5.11: Suppose that item 2 in Proposition 5.9 holds. Then, there exists a class-\(K\) function \(\alpha\) such that every solution pair \((x, u)\) to \(\mathcal{H}\) satisfies \(|x(t, 0)|_A \geq \alpha(|x(0, 0)|_A)\) for all \(t \in [0, \alpha(|x(0, 0)|_A)]\) such that \((t, 0) \in \text{dom}(x, u)\).

We are now ready to present the proof of Proposition 5.9.

Proof: If either item 1 or item 2 holds, an application of Lemma 5.10 or Lemma 5.11 implies that there exists a class-\(K\) function \(\alpha'\) such that every solution pair \((x, u)\) to \(\mathcal{H}\) satisfies the following: for each \(T\) such that \((T, 0) \in \text{dom}(x, u)\), we have \(|x(t, 0)|_A \geq \alpha'(|x(0, 0)|_A)\) for all \(t \in [0, \min\{T, \alpha'(|x(0, 0)|_A)\}]\) such that \((t, 0) \in \text{dom}(x, u)\). Take a feasible solution pair \((x, u)\). We have the following cases:

I) If the solution jumps at \((t_1, 0)\) for some \(t_1 \leq \alpha'(|x(0, 0)|_A)\), then \((t_1, 1) \in \text{dom}(x, u)\) and P3) holds with \(\alpha = \alpha'\).

II) Otherwise, either \((\alpha'(|x(0, 0)|_A), 0) \in \text{dom}(x, u)\), in which case the solution evolves continuously beyond \(x(t_1, 0)\) and satisfies P2) with \(j = j' = 0\), or

III) it terminates at time \((T, 0)\) with \(T \leq \alpha'(|x(0, 0)|_A)\) and, hence, satisfies P4) with \(J = 0\), both with \(\alpha = \alpha'\).

In the case of the time horizon \(\mathcal{T}\) being bounded away from the origin considered in item a).i), P1) holds with \(\alpha = \alpha'\) since each feasible solution pair \((x, u)\) is such that there exists \((t', j') \in \text{dom}(x, u)\) such that \(t' + j' \geq c > 0\), which, in turn, implies that there exists \((t, j) \in \text{dom}(x, u)\) such that \(t + j \leq t' + j'\) and \(t' - t + j - j' \geq \alpha'(|x(0, 0)|_A)\).

Finally, P2) or P4) hold under the conditions in b). Without loss of generality, suppose that \(\sigma\) in (25) is such that there exists \(c > 0\) such that \(\sigma(r) \geq c\) for all \(r > 0\). With \(\varepsilon\) coming from (25), let

\[
\varphi(r) := \max_{r' \in [r/2, r]} \sigma(r') \quad \forall \varepsilon \in (0, \varepsilon] \tag{27}
\]

which satisfies \(\varphi(r) \geq c\) for all \(r \in (0, \varepsilon]\). Continuity of \(\varphi\) follows via Berge’s maximum theorem [41, Theorem 1.4.16] by showing upper and lower semicontinuity of the set-valued map mapping each \(r \in [0, \varepsilon]\) to the compact interval \([r/2, r]\). Indeed, lower semicontinuity of the mapping can be verified directly, while upper semicontinuity follows from the fact that the mapping is outer semicontinuous (since its graph is closed relative to \((0, \varepsilon] \times \mathbb{R}\) [18, Lemma 5.10]) and locally bounded, due to [18, Lemma 5.15]. Let \(\tau(0) := 0\) and

\[
\tau(r) := \begin{cases} x/r, & \text{if } r \in (0, \varepsilon] \\ r/t_1, & \text{if } r \in (\varepsilon, \infty) \end{cases}
\]

and similarly, for every \(r \geq 0\), let \(\rho(r) := \min\{r, \varepsilon\}\). Note that \(\tau\) is continuous, positive definite, upper bounded by \(\frac{\varepsilon}{2\varphi(\varepsilon)}\), and lower bounded by zero. Hence, there exists a continuous function \(\tilde{\tau} : \mathbb{R}_{\geq 0} \to \mathbb{R}_{\geq 0}\) that is positive definite, increasing, and such that \(\tilde{\tau}(r) \leq \tau(r)\) for all \(r \geq 0\). It follows that, for any continuous solution pair \((x, u)\) to \(\mathcal{H}\) with \(|x(0, 0)|_A > 0\), \(t < \tilde{\tau}(\rho(|x(0, 0)|_A))\) satisfying \((t, 0) \in \text{dom}(x, u)\) implies \(|x(t, 0)|_A \geq \frac{\rho(|x(0, 0)|_A)}{2}\). To show this implication, first note that, by definition of \(\tilde{\tau}\), \(\tilde{\tau}(\rho(|x(0, 0)|_A))\) is upper bounded by \(\tau(\rho(|x(0, 0)|_A))\) and that, since \(\rho\) is upper bounded by \(\varepsilon\), \(\tilde{\tau}(\rho(|x(0, 0)|_A)) \varphi(\rho(|x(0, 0)|_A)) \leq \frac{\rho(|x(0, 0)|_A)}{\varepsilon}\). Using (25) and the construction of \(\varphi\) above, we have \(|x(t, 0)|_A \geq |x(0, 0)|_A - \varphi(|x(0, 0)|_A) t\) for all \((t, 0) \in \text{dom}(x, u)\) such that \(0 < |x(t, 0)|_A \leq \varepsilon\). Then, the lower bound on \(|x(t, 0)|_A\) can be obtained by evaluating the latter inequality at \(t = \tilde{\tau}(\rho(|x(0, 0)|_A))\), leading to \(|x(t, 0)|_A \geq \frac{\rho(|x(0, 0)|_A)}{2}\). In other words, \(\tilde{\tau} \circ \rho(r)\) is a lower bound on the ordinary time to reach the \((r/2)\)-disc around \(\mathcal{A}\) from the \(r\)-disc around \(\mathcal{A}\) via flows, for each \(r \geq 0\). Finally, for every \(r \geq 0\), let \(\hat{\alpha}_D(r) := \hat{\alpha}_D(\rho(r)/2)\), and note that \(\hat{\alpha}_D\) and \(\rho\) are continuous, nondecreasing, and positive definite.

Let \((x, u)\) be an optimal solution pair and \(\{t_j\}_{j=0}^{J+1}\) be a sequence such that \(\text{dom}(x, u) = \bigcup_{j=0}^{J} ([t_j, t_{j+1}] \times \{j\})\), with \(|x(0, 0)|_A > 0\). First, suppose \(\hat{\alpha}_D(r) \geq r\) for all \(r \geq 0\). Then, following the same arguments as before, one can deduce that for any \(t < \tilde{\tau}(\rho(|x(0, 0)|_A))\) and \(j \in \mathbb{N}\) satisfying \((t, j) \in \text{dom}(x, u)\), we have \(|x(t, j)|_A \geq \frac{\rho(|x(0, 0)|_A)}{2}\), due to the fact that the distance of \(x\) to \(\mathcal{A}\) does not decrease during jumps, in light of (26). Therefore, either there exists \((s, i) \in \text{dom}(x, u)\) such that \(s = \tilde{\tau}(\rho(|x(0, 0)|_A))\) and every \((t, j) \in \text{dom} x\) with \(t + j \leq s + i\) satisfies \(|x(t, j)|_A \geq \frac{\rho(|x(0, 0)|_A)}{2}\), or letting \(T := t_{J+1}\), \(x(T, J) \geq \frac{\rho(|x(0, 0)|_A)}{2}\). In other words, P2) or P4) holds \(\alpha = \alpha_0\), where

\[
\alpha_0(r) := \min \left\{ \tilde{\tau}(\rho(r)), \frac{\rho(r)}{2} \right\} \quad \forall r \geq 0 \tag{28}
\]

Next, suppose that there exists \(J_{\max} \in \mathbb{N}\) such that for every \((T, J) \in \mathcal{T}\), \(J \leq J_{\max}\), but \(\hat{\alpha}_D(r) \geq r\) does not necessarily hold for all \(r \geq 0\). Let \(T := t_{J+1}\). If there exists \(j \in \{0, 1, \ldots, J\}\) such that \(t_{j+1} - t_j \geq \tilde{\tau}(\rho(|x(t, j)|_A))\), let \(i\) be the smallest such \(j\), otherwise, let \(i := J+1\). Then,

\[
|x(t_j, j)|_A \geq \hat{\alpha}_D(|x(t_j, j-1)|_A) \geq \hat{\alpha}_D\left(\rho(t_{j-1}, j-1)\Big|_{A}\right) = \hat{\alpha}_D(|x(t_{j-1}, j-1)|_A)
\]

for all \(j \in \{1, 2, \ldots, \min\{i, J\}\}\), and consequently,

\[
|x(t_j, j)|_A \geq \hat{\alpha}_D^j(|x(0, 0)|_A) \quad \forall j \in \{0, 1, \ldots, \min\{i, J\}\} \tag{29}
\]

where \(\hat{\alpha}_D^j\) denotes the \(j\)-times composition of \(\hat{\alpha}_D\), e.g., \(\hat{\alpha}_D^0\) is the identity function and \(\hat{\alpha}_D^2 = \hat{\alpha}_D \circ \hat{\alpha}_D\). In addition, if \(i \leq J\), since \(\rho\) is nondecreasing, by (29),

\[
|x(t, i)|_A \geq \frac{\rho(|x(t, i)|_A)}{2} \geq \frac{\rho(\hat{\alpha}_D^j(|x(0, 0)|_A))}{2}
\]

for all \(t \in [t_i, t_i+\tilde{\tau}(\rho(|x(t_i, i)|_A))]\), which, as \(\tilde{\tau}\) is increasing, implies that, for all \(t \in [t_i, t_i+\tilde{\tau}(\rho(\hat{\alpha}_D(|x(0, 0)|_A)))]\),

\[
|x(t, i)|_A \geq \frac{\rho(\hat{\alpha}_D^j(|x(0, 0)|_A))}{2} \tag{30}
\]

### Page 16

Lehrwerte für die Integrierbarkeit (zwischen Verträgen) sind im Abkürzung Z[M] angegeben.  

From those points—see Remark 4.4. While not emphasized in Definition 6.1, note that in general, from any initial condition \(x_{\circ} \in \mathcal{X}\) , there might be infinitely many solution pairs generated by the hybrid MPC algorithm. This is for two reasons. First, the minimizers of the cost functional \(\mathcal{I}\) might be nonunique; that is, given \(x_{\circ} \in \mathcal{X}\) , there may be multiple solution pairs achieving the infimum in (14). Second, assuming that the recursive feasibility property stated in Proposition 5.2 holds, the optimal control input \(u_{i}\) computed at optimization time \((T_{i}, J_{i})\) — see Section III-E— can be updated at any (hybrid) time before the domain of \(u_{i}\) ends, that is, at any \((T_{i} + t, J_{i} + j)\) , as long as \((t, j) \in \mathrm{dom} u_{i}\) .  

Theorem 6.2: Given a hybrid plant \(\mathcal{H} = (C,\mathcal{F},D,g)\) as in (1), the data \((\mathcal{T}, L_{C}, L_{D}, V, X)\) defining Problem 3.5 with \(\mathcal{T}\) having \(t_{0} > 0\) and \(J \geq 1\) , and a closed set \(\mathcal{A} \subset \mathbb{R}^{n}\) , suppose that Assumptions 2.7, 4.1, 4.2, and 4.3 hold. Furthermore, suppose that there exists a class-\(\mathcal{K}\) function \(\alpha^{*} : \mathbb{R}_{\geq 0} \to \mathbb{R}_{\geq 0}\) such that the value function \(\mathcal{I}^{*}\) in (14) satisfies\(^{(11)}^{(19)}\) for each  \(x_{\circ} \in \mathcal{X}\) and that an optimal solution pair \((x,u) \in \mathcal{S}_{\mathcal{H}}(x_{\circ})\) to Problem 3.5 exists.\(^{(12)}\) Then the hybrid MPC algorithm renders the set \(\mathcal{A}\) asymptotically stable for the hybrid system \(\mathcal{H}\) if any of the following statements are true: Each solution pair generated by the hybrid MPC algorithm has persistent jumps and \(L_{D}\) satisfies (W2); The condition Lagrange multiplier is given below.(W3):(A1) \(L_{C}\) and \(L_{D}\) satisfy (W1) and (W2), respectively;  

(A5) Each solution pair generated by the hybrid MPC algorithm has persistent flows and \(L_{C}\) satisfies (W1).

Proof: The proof proceeds as follows. First, a complete optimal solution with initial condition in a (small enough) neighborhood of the set \(\mathcal{A}\) is constructed by induction. Using properties of \(\mathcal{I}\) established earlier, we show that \(\mathcal{I}\) is a Lyapunov function certifying Lyapunov stability of \(\mathcal{A}\) . Finally, attractivity of \(\mathcal{A}\) is shown using the assumptions in the different items in the statement.  

Using Assumption 2.7, Proposition 2.8 implies that, for any given input, \(\mathcal{H}\) has unique state trajectories. From Proposition 5.1, we have that \(\mathcal{X} \subset \mathcal{X}\) . Then, pick \(\delta ^{\prime} > 0\) and note that existence of optimal solutions from \(\mathcal{X}\) implies that, for every \(\xi \in \Pi (C \cup D) \cap \mathcal{X}\) satisfying \(\left| \xi \right|_{\mathcal{A}} \leq \delta ^{\prime}\) , there exists an optimal solution \((x_{0}, u_{0})\) starting from \(\xi\) . Now, the structure of the hybrid prediction horizon in Definition 3.2 implies that there exists \(\epsilon > 0\) such that \(\widehat{T} + \widehat{J} \geq c\) for any \((\widehat{T}, \widehat{J}) \in \mathcal{T}\) . Hence, the terminal time \((T_{0}, J_{0})\) of \((x_{0}, u_{0})\) satisfies \(T_{0} + J_{0} \geq c\) . In addition, due to the recursive feasibility property stated in Proposition 5.2 and existence of optimal solutions, there exists an optimal solution pair \((x_{1}, u_{1})\) starting from \(x_{0}(T_{0}, J_{0})\) whose terminal time \((T_{1}, J_{1})\) satisfies \(T_{1} + J_{1} \geq c\) . Then, following Definition 3.7, the concatenation of \((x_{0}, u_{0})\) and \((x_{1}, u_{1})\) form a solution pair. Specifically, \((t, j) \mapsto (x^{\prime}(t, j), u^{\prime}(t, j))  

defined as follows is a solution pair to \(\mathcal{H}\) : for every \((t, j)\in \mathrm{dom}(x_{0}, u_{0})\) such that \(t + j< T + J\)  

\[(x^{\prime}(t,j),u^{\prime}(t,j)) = (x_{0}(t,j),u_{0}(t,j)) \quad (14)\]  

and for every \((t, j) \in \mathbb{R}_{\geq 0} \times \mathbb{N}\) such that \((t - T_{0}, j - J_{0}) \in \mathrm{dom}(x_{1}, u_{1})\) and \((t - T_{0}) + (j - J_{0}) < T_{1} + J_{1}\)  

\[(x^{\prime}(t,j),u^{\prime}(t,j)) = (x_{1}(t - T_{0}, j - J_{0}), u_{1}(t - T_{0}, j - J_{0})).\]  

Setting \((x^{\prime}(T_{0} + T_{1}, J_{0} + J_{1}), u^{\prime}(T_{0} + T_{1}, J_{0} + J_{1})) = (x_{1}(T_{1}, J_{1}), u_{1}(T_{1}, J_{1}))\) , the terminal time \((T^{\prime}, J^{\prime})\) of \((x^{\prime}, u^{\prime})\) is such that \(T^{\prime} + J^{\prime} \geq 2c\) . Similarly, \((x^{\prime}, u^{\prime})\) can be concatenated with an optimal pair \((x_{2}, u_{2})\) from \((x_{1}(T_{1}, J_{1}))\) with terminal \((T_{2}, J_{2})\) satisfying \(T_{2} + J_{2} \geq c\) . By induction, the resulting solution pair starts from \(\xi\) and constitutes a complete optimal solution generated by the hybrid MPC algorithm.  

Next, we establish that the value function \(\mathcal{I}^{*}\) is a Lyapunov function. Since \(\mathcal{X} \subset \mathcal{X}\) from Proposition 5.1, by Assumption 4.2, there exists a class- \(\mathcal{K}\) function, denoted \(\alpha_{2}\) , such that \(\mathcal{I}^{*}(\widehat{x}) \leq \alpha_{2}(\left|x\right|_{A})\) for each \(\widehat{x} \in \mathcal{X}\) satisfying \(\left| \widehat{x} \right|_{\widehat{A}} \leq \eta\) , for some \(\eta > 0\) . By assumption, there exists a class- \(\mathcal{K}\) function \(\alpha^{*}\) such that \(\mathcal{I}^{*}(\widehat{x}) \geq \alpha^{*}(\left|x\right|_{A})\) for each \(\widehat{x} \in \mathcal{X}\) such that an optimal solution \(x\) with respect to \(\mathcal{A}\) (or ℐ) is positive definite with respect to 𝒜. Moreover, by Lemma 5.5, for each solution pair \((x, u)\) generated by hybrid MPC, \(\mathcal{I}^{*}(x(t, j)) \leq \mathcal{I}^{*}(x(0, 0))\) for each \((t, j) \in \mathrm{dom}(x, u)\) . Then, given \(\varepsilon > 0\) there exists \(\delta \in (0, \min\{\delta^{\prime}, \eta, \alpha^{* - 1} \circ \alpha_{2}(\varepsilon)\}]\) such that, for each \(\xi \in \Pi (C \cup D)\) satisfying \(|\xi|_{A} \leq \delta\) , each (complete) optimal solution pair \((x, u)\) constructed as above satisfies  

\[ \mathcal{I}^{*}(x(t,j)) \leq \mathcal{I}^{*}((0,0)) - \sum_{j=0}^{j} \int_{s_{1}}^{s_{i+1}} L_{C}(x(s, i), u(s, i)) ds \]  

\[-\sum_{i=0}^{j-1} L_{D}(x(s_{i+1}, i), u(s_{i+1}, i))\]  

\[\leq \mathcal{I}^{*}((0,0)) - \sum_{j=0}^{j} \int_{s_{i}}^{s_{i+1}} \tilde{\alpha}_{C}(|x(s, i)|_{\mathcal{A}})) ds\]  

\[\left.- \sum_{i=0}^{j-1} \tilde{\alpha}_{D}(|x(s_{i+1}, i)|_{\mathcal{A}}) \quad (34)\]  

where \(\{s_{i}\}_{i = 0}^{j + 1}\) is the sequence of generalized jump times of the truncation of \((x,u)\) up to time \((t,j)\) see (7). Next, we show attractivity when items (AS1)- (AS3) hold, one at a time.

### Page 17

vertical line of logic shown in Figure. First, suppose that item (AS1) holds. Then, (34) holds with \(\tilde{\alpha}_{C} = \alpha_{C}\) and \(\tilde{\alpha}_{D} = \alpha_{D}\) for the class-K<∞ functions \(\alpha_{C}\) and \(\alpha_{D}\) given in item (AS1), via (W1) and (W2). Let
\[ c := \lim_{t+j \to \infty, (t,j) \in \text{dom}(x,u)} \mathcal{F}^{*}(x(t,j)) \]
\[ (35) \]

as \(\mathcal{F}^{*}(x_{0}) \geq \alpha^{*}(|x_{0}|_{A})\) for all \(x_{0} \in \mathcal{X}\), since \(\alpha^{*}\) is a class-K<∞ function, \(c = 0\) implies \(\lim_{t+j \to \infty, (t,j) \in \text{dom}(x,u)} |x(t,j)|_{A} = 0\). Assuming the opposite, we have \(\mathcal{F}^{*}(x(t,j)) \geq c > 0\) for all \((t,j) \in \text{dom}(x,u)\). By continuity of \(\mathcal{F}^{*}\) on \(\mathcal{A} \cap \mathcal{X}\) obtained from Proposition 5.4, and the fact that \(\mathcal{F}^{*}(\mathcal{A} \cap \mathcal{X}) = 0\) since \(\mathcal{F}^{*}\) is positive definite relative to \(\mathcal{A}\) by assumption, this means that there exists \(\bar{\epsilon} > 0\) so \(|x(t,j)|_{A} \geq \bar{\epsilon}\) for all \((t,j) \in \text{dom}(x,u)\). Let \(\{(T_{k},J_{k})\}_{k=0}^{\infty}\) be the sequence of optimization times of \((x,u)\). By Lemma 5.5, for each \(k \in \mathbb{N}\),
\[

Figs. 4-6 below show the trajectories of the MTC in the vertical plane of the simulation environment following the same goal pose for each suspension system.

Fig. 4: Position and velocity trajectories of the bouncing ball controlled by hybrid MPC projected onto ordinary time $t$.

4.2. Suspension System: 4P-Wheel-MTC
Figs. 7-9 show the simulation results for the response of the 4-pole MTC to the smooth goal pose for each suspension system.

Fig. 5: Evolution of the total energy of the bouncing ball controlled by hybrid MPC projected onto ordinary time $t$.

Example 4.1: Initial task
Starting in state 0 at time 0, at $t=0$, we start our jump to the initial $x(0)=x_0=0$.

Consider the helicopter’s trajectory over the first $5$ seconds of its jump as a flight map $X(t).$
$X: t \rightarrow x(t)$ $X: t \rightarrow \lim X(t)$

Fig. 4 compares four systems (2P-Wheel-MTC (in red dotted box), 4P-Wheel-MTC (in red solid box), 2P-Wheel-MTC (in blue dotted box), and 4P-Wheel-MTC (in blue solid box)) with the orbiter’s landing point (in red solid dot) as a reference.

Conceptually, let G(t) denote the support system time at which the MTC lands.

4.3. Experiment Results

Fig. 6 shows the results of the example. At $t=4\,\mathrm{s}$ the MTC religates the landing and the control system appears to stabilize the eight revolutions achieved by all cases. In this example, we restore $k=1$ at $t=12.5$ s, as depicted in the [Figure 7](#).

Finally, we discuss the possible trajectory errors – the deviation between the rigid body and the compliant MTC – that occur during transitions to ongoing plant grasps. In Fig. 9, for simplicity, only the sequence of bounding trajectories from left to right is shown, because the relative positions of the MTC’s and the oscillation frequencies are identical. Note also that in this paper, we always assume that each trajectory is followed by the cyclic stiffening and damping imposed by the controller and the bounds on the motion.

Acknowledgements
The authors enthusiastically thank the reviewers for their incisive feedback, and the hard work of YH and CYK for their thoughtful suggestions on the paper’s development. They are further grateful to the Aerospace Test Validation Facility (ATVF) support staff, as well as Jet Propulsion Laboratory for undertaking the research to make this paper possible. They are also especially thankful to Bevan Henton, Shannon Miller, and Jessica Nally for preparing the experiments.

References
[1] R. Abbott, Y. Biswas, and D. Wagstaff, “Spacecraft Attitude Control and Control of Multi-Mass Manipulators: A Survey,” Acta Astronautica, vol. 108, pp. 177–196, January 2015.
[2] H. Kollar, B. Markov, and L. Coupé, “FLUGATTAGE: a platform for low-cost, ﬂight-quality, continuous robots.” The International Journal of Robotics Research, vol. 35, no. 11, pp. 1307–1325, 2016.
[3] R. A. Fogg, V. J. Cleworth, and J. L. Shapiro, “The halting problem,” IEEE Control Systems Magazine, vol. 7, no. 6, pp. 3–31, 1987.
[4] A. Sondermeyer, R. Allan, and J. Kremers, “Stabilizing postlanding phases of the falcon 9 flight nimble,” in AIAA Guidance, Navigation, and Control Conference, Bourne, MA, 2006.
[5] J. Berthier, T. Veron, and X. Sciuttoaja, “Control design for failure: Human cognition,” Control Engineering Practice, vol. 14, pp. 965–977, 2006.
[6] Z. Guo, J. Xiao, and J. Huang, “Solid envelope tracking of a hybrid air vehicle,” in AIAA Guidance, Navigation, and Control Conference, Rome, Italy, 2008.
[7] A. Veriansen, “Design and control of damping consuming fixed mode switching,” in 22nd IMACS Symposium on Discrete Event Systems, June 2002, pp. 85–90.
[8] L. Ermakova and V. Glukhov, “Upper bound for the number of fixed modes of switched linear discrete-time systems,” in 2014 American Control Conference, Phoenix, AZ, 2014, pp. 2853–2858.
[9] D. Bulls and D. P. Babiloni, “On the number of leading eigenvalues in a singular time-varying semidef-mixed MIMO linear time-invariant QFs,” Math. Nachr., vol. 288, pp. 3343–3358, December 2015.
[10] M. E. Farina, D. Babiloni, L. Ermakova, and V. Glukhov, “Robust synthesis for general matched nozzles in chimera mode,” Δstab.-INT, vol. 50, no. 12, pp. 1629–1652, 2016.
[11] X. C. Ding, X. J. Li, C. C. Cai, W. Cheng, P. M. Fornaro, and C. H. Jia, “Microrobot swarming based on maneuvering of parametrized bobsled tracks with defined velocity and angular sliding,” Int. J. Adv. Robot. Syst., vol. 14, no. 1, pp. 1–10, 2017.
[12] X. Lv, X. Chen, Z. Dang, Z. Lu, Z. Yu, and Z. Yin, “Topology optimization for a moving manipulator based on solid boids and its application to on-demand reconfiguration and collision avoidance,” Mech. Mach. Theory, vol. 138, p. 102569, 2020.
[13] Y. Xianyu and R. Sha, “Nonlinear obstacle avoidance control for floating quad-rotor RBO using attitude error quantizers,” in Int. Symp. Aerosp. Robot., Chicago, IL, USA, Sep. 2019, pp. 141-147.
[14] D. van Niekerk, B. Wang, and A. Zasa, “Endpoint space technique for learning’,” IEEE Acces. Society for Control Systems, in Techn. Workshop Signal Processing Advances, Phoenix, AZ, June 2015.
[15] Y. Pineda, I. Robinson, B. von Eggers, B. Christian, and F. Pradal, “Stickypedestrians: a decentralized networking approach to autonomous transportation,” in IEEE American Control Conf. America, Washington, DC, 2018.
[16] D. Moresi, D. Panoiu, and P.-A. Absil, “Safe bicycle randon,” Internat. Jour. of Robotics Res., vol. 26, no. 1, pp. 1-20, 2007.
[17] I. Robinson, V. Limelette, S. Declercq, D. Moresi, and P.-A. Absil, “Robosloth: Effective on-line safe robot learning,” in IEEE Int. Conf. on Robotics and Automation, Rio de Janeiro, Brazil, May 2016.
[18] L. Rothwell, G. Q., 6th ed., “Motion Planning Using Hybrid Systems,” Enginering Society of America Encyclopaedia, B001, Fabrik Campus of Nighthawk Engineering Inc. 2007.
[19] D. Yang, A. Rudrappa, V. F. Molina Iribarne, and J. Cheng, “Hybrid control with linear and bilinear convex sets in interval field,” Automatica, vol. 38, no. 2, pp. 361–367, 2002.
[20] V. L. Onufriev and A. V. Tikhonov, “Algorithmic machinery of optimization in vector spaces,” Math. Mech., vol. 42, no. 3, pp. 195-228, 1986.
[21] H. Barth, “Mathematical aspects of voting theory in graphicሣвес,” Stockholm universitetsshop OVERGEDOM, 1961.
[82] R.H.G. von, E. Turmaine, RUTH G. von, and POHLEND, H., “P-proverbals of visualization,” in IEEE SA MANS Conf. on ICMC-MM., Stockholm, Sweden, Sept. 2020.
[83] U. Winter, R. Geiger, Z. Yan, and S. Crnkovic, “Digital platforms for drone traffic modelling and the estimation of impact probabilities,” in International Conference on Engineering in Developing Countries 2018, 2018.
[84] T. Ren, A. Detracher, H. Mergl, and J. Schmidhuber, “An overview and comparison of differentiable programming,” arXiv preprint, 2020.
[85] L. B. Cook, R. J. Moore, C. E. Dahlke, R. Sharf, C. Thaler, C. Lee, J. Espinoza, C. A. Abajian, R. J. Johnson, R. Brooker, A. Einhorn, C. Wener, and J. Ard, “A comparison of planners,” Artiﬁcial Intelligence inc., vol. 103, no. 7, pp. 1637–1663, 1998.
[86] K. Bshouty, G. Hofmann, M. Garaffolo, and D. Meyers, “Comparison of five matlab solvers for gradient algorithms,” in 2016 American Control Conf. AFRICON, Sun-dome, 2016.
[87] D. Panoiu and P.-A. Absil, “Toward non-linear optimization for attitude tracking control,” in Conference on Garp r 15, Dec. 2011, pp. 1321-1326.
[88] R. de la Casimera, B. Johnstone, R. J. S. Vieira, J. K. Shewchuk, R. Guibas, and J. Vecche, “Optimal feedback control of a nonholonomic mobile robot,” The international Journal of Robotics Research, vol. 31, no. 14, pp. 1698–1725, 2012.
[89] M. Gal and D. Milpasa, “Boosted constrained Bures distance for sparse activities: Application to a nonlinear control of quadrotors,” Bio-Inspired-Prognal and Applications, vol. 2015, 1-5 2015.
[90] Y. Sun, H. Zheng, Y.H. Tan and S. Y. Cao, “Mixed integer programming for tensor 2 soft tmdp,” in Motion Planning for Robotics, vol. 7839, Springer-Verlag Berlin, 2013.
[91] T. Quizon, R. Di Leonardo, and A. B. Rosa, “SWAP movement planning under obstacle reconfigura-tion using the mixed-integer linear time-programming technique,” in Int. Conf. on Robotics and Automation, Sophia Antipolis, France, June 2020.
[92] W. Zhou and S. Virte, “Visual planning in robot motion by solving mixed-integer linear programs,” Robotica, vol. 21, no. 3, pp. 341–351, 2003.
[93] E. Tamayo, T. Riaño, and L. Ferrián, “Mixed bifeature Sparse Inverse Algorithm,” Int. J. of For.-Eng. Optimization, vol. 10, no. 5, pp. 483–500, 2010.
[94] J.-P. Hueber, M. Schmid, and U. Wormsberger, “Visual motion planning under limits of obstacle data”, in Intelligent Robots and Computer Vision, Springer, Berlin, 2002, pp. 784–792.
[95] A. Reise and D. Kim, “On the computational complexity of planning under dynamical and spatial obstacles,” Appl. Optim. VII, pp. L1-L8, 1992.

Math. Nachr., 282
Jun., 2021, No. 21, pp. 184-209. 1. Proposal Systems Without Human Assistance via Swarm Algorithms

Fig. 13:

### Page 20

Solution

The solution of the given problem involves finding the roots of the function \(L_C(x, u) = \begin{bmatrix} a_{\text{hot}}(z - z_{\text{max}}) + a_{\text{on}}q & \text{if } z \leq z_{\text{max}} \\ 0 & \text{if } z \in (z_{\text{min}}, z_{\text{max}}) \end{bmatrix}\) and \(\text{D}_{C}(x, u) = \begin{bmatrix} a_{\text{cold}}(z_{\text{min}} - z) & \text{if } z \leq z_{\text{min}} \\ a_{\text{on}} & \text{if } z \in (z_{\text{min}}, z_{\text{max}}) \end{bmatrix}\). The function \(L_C(x, u)\) is defined as the sum of four functions, each of which has a specific range of values. A gradient descent algorithm is used to find the roots and the coefficients of the functions. The algorithm is as follows:

1. \(a_{\text{hot}}(z - z_{\text{max}}) + a_{\text{on}}q \leq 1\) for all \(z \in (z_{\text{min}}, z_{\text{max}})\)

2. \(a_{\text{on}} \leq 1\) for all \(z \in (z_{\text{min}}, z_{\text{max}})\)

3. \(a_{\text{cold}}(z_{\text{min}} - z) \leq 1\) for all \(z \in (z_{\text{min}}, z_{\text{max}})\)

4. \(a_{\text{on}} \leq 1\) for all \(z \in (z_{\text{min}}, z_{\text{max}})\)

The coefficients \(a_{\text{hot}}, a_{\text{on}}, a_{\text{cold}}, a_{\text{on}}\) are updated iteratively using the gradient descent algorithm.

The main control action \(V\) is given by

\[V(x) = \begin{cases} (z - z_{\text{max}})^2 & \text{if } z \leq z_{\text{max}} \\ 0 & \text{if } z \in (z_{\text{min}}, z_{\text{max}}) \\ (z - z_{\text{min}})^2 & \text{if } z \leq z_{\text{min}} \end{cases}\]

The main control output is given by \(V(x)\) for all \(x\) in the region specified by the constraints \(\mathcal{U}\). The main control action \(V\) is given by

\[V(x) = \begin{cases} z \in \mathcal{U} & \text{if } z \notin \mathcal{X} \\ z = x + b & \text{if } z \in \mathcal{X} \setminus \mathcal{U} \\ -c_{\text{on}}(z - z_{\text{max}}) - c_{\text{on}} & \text{if } z \in \mathcal{X} \setminus \mathcal{U} \\ -c_{\text{on}} & \text{if } z \in \mathcal{X} \setminus \mathcal{U} \end{cases}\]

where \(c_{\text{on}}\) is the coefficient of the cooling function and \(c_{\text{nond}}\) is its threshold. The main control output is given by

\[V(x) = -c_{\text{on}}(z - z_{\text{max}}) - c_{\text{on}}\]

The main control output is given by

\[V(x,u) = \begin{cases} (z - z_{\text{min}})^2 & \text{if } z > z_{\text{max}} \\ 0 & \text{if } z \in (z_{\text{min}}, z_{\text{max}}) \\ (z - z_{\text{max}})^2 & \text{if } z > z_{\text{max}} \end{cases}\]

The main control output is given by

\[V(x,u) = \begin{cases} (z - z_{\text{max}})^2 & \text{if } z \leq z_{\text{max}} \\ 0 & \text{if } z \in (z_{\text{min}}, z_{\text{max}}) \\ (z - z_{\text{max}})^2 & \text{if } z > z_{\text{max}} \end{cases}\]

The main control output is given by

\[V(x,u) = \begin{cases} z \in \mathcal{U} & \text{if } z \notin \mathcal{X} \\ z = x + b & \text{if } z \in \mathcal{X} \setminus \mathcal{U} \\ -c_{\text{on}}(z - z_{\text{max}}) - c_{\text{on}} & \text{if } z \in \mathcal{X} \setminus \mathcal{U} \\ -c_{\text{on}} & \text{if } z \in \mathcal{X} \setminus \mathcal{U} \end{cases}\]

where the terms \((1+q)\) and \((2-q)\) penalize being at the "wrong" mode. Picking the feedback \(\kappa_C\) as \(\kappa_C(x) = 0\) if \(x \in \widetilde{C}\), and one otherwise, and the feedback \(\kappa_D\) as \(\kappa_D(x) = 1\) if \(x \in \widetilde{D}\), and zero otherwise, we obtain the following properties for the variation of \(V\) along flows and jumps of the hybrid closed-loop system \(\mathcal{H}_{\kappa}\) in (9): for every \(x \in X \cap \widetilde{C} = \widetilde{C}\) we have that \(\langle \nabla V(x), f(x, \kappa_C(x)) \rangle\) is given by

\[\begin{cases} -(z_{\text{max}} - z_0)(z - z_{\text{max}}) & \text{if } z \geq z_{\text{max}} \\ 0 & \text{if } z \in (z_{\text{min}}, z_{\text{max}}) \\ -(z_0 + z_{\Delta} - z_{\text{min}})(z_{\text{min}} - z) & \text{if } z \leq z_{\text{min}} \end{cases}\]

and, for every \(x \in X \cap \widetilde{D} = \widetilde{D}\), \(V(g(x, \kappa_D(x))) - V(x)\) is equal to

\[\begin{cases} -\frac{(z - z_{\text{max}})^2}{2} & \text{if } z \geq z_{\text{max}} \\ \frac{0}{2} & \text{if } z \in (z_{\text{min}}, z_{\text{max}}) \\ -\frac{(z - z_{\text{min}})^2}{2} & \text{if } z \leq z_{\text{min}} \end{cases}\]

It follows that, with the choices of \(L_C\) and \(L_D\) above, Assumption 4.3 holds and that \(X\) is forward pre-invariant for \(\mathcal{H}_{\kappa}\). From item P5) in Theorem 5.6, there exists a class-\(\mathcal{K}\) function \(\alpha^*: \mathbb{R}_{\geq 0} \rightarrow \mathbb{R}_{\geq 0}\) such that the value function \(\mathcal{J}^*\) in (14) satisfies (19) for each \(x_o \in \mathcal{X}\). Finally, from [37, Section 6.1], we have that an optimal solution pair \((x, u) \in \widehat{\mathcal{S}}_{\mathcal{H}}(x_o)\) exists from each such \(x_o\).

**VII. Conclusion**

This paper formalizes an MPC framework for hybrid dynamical systems. The formulation allows to quantify the cost and predict trajectories over hybrid time domains. Conditions guaranteeing recursive feasibility, positive definiteness of the value function and its decrease along solutions, leading to asymptotic stability of a closed set are proposed. The conditions involve the data of the system and the OCP, leading to checkable design conditions. Future work will focus on the hybrid MPC design to satisfy other dynamical properties of interest, such as safety, temporal logic, and robustness. An add-on to our Hybrid Equations Toolbox (HyEQ) for Matlab/Simulink [42] to compute numerical solutions of Problem 3.5 is currently being developed. Employing the results in [43] to relax the conditions for positive definiteness of the value function as suggested by an anonymous reviewer is also part of future work.

**References**

[1] J. Richalet, "Algorithmic control of industrial processes," Proc. of the 4th IFAC Sympo. on Identification and System Parameter Estimation, pp. 1119-1167, 1976.

[2] C. R. Cutler and B. L. Ramaker, "Dynamic matrix control – a computer control algorithm," in Joint Aut. Cont. Conf., no. 17, 1980, p. 72.

[3] M. Morari and J. H. Lee, "Model predictive control: past, present and future," Comp. & Chem. Eng., vol. 23, no. 4-5, pp. 667-682, 1999.

[4] S. J. Qin and T. A. Badgwell, "A survey of industrial model predictive control technology," Control Engineering Practice, vol. 11, no. 7, pp. 733-764, 2003.

[5] J. B. Rawlings and D. Q. Mayne, Model predictive control: Theory and design. Nob Hill Pub., 2009.

[6] F. Borrelli, A. Bemporad, and M. Morari, Predictive control for linear and hybrid systems. Cambridge University Press, 2017.

[7] L. Grüne and J. Pannek, Nonlinear model predictive control, 2nd ed. Springer, 2017.

[8] D. Q. Mayne and H. Michalska, "Receding horizon control of nonlinear systems," IEEE Trans. Aut. Cont., vol. 35, no. 7, pp. 814-824, 1990.

### Page 21

[10] D. Q. Mayne, "'Model predictive control: Recent developments and future promise,'" Automatica, vol. 50, no. 12, pp. 2967–2986, 2014.

[11] E. Camacho, D. Ramirez, D. Limon, D. Muñoz de la Peña, and T. Alamo, "'Model predictive control techniques for hybrid systems,'" Annual Reviews in Control, vol. 34, no. 1, pp. 21–31, 2010.

[12] A. Bemporad and M. Morari, "'Control of systems integrating logic, dynamics, and constraints,'" Automatica, vol. 35, no. 3, pp. 407–427, 1999.

[13] A. Bemporad, M. Morari, V. Dua, and E. N. Pistikopoulos, "'The explicit linear quadratic regulator for constrained systems,'" Automatica, vol. 38, no. 1, pp. 3–20, 2002.

[14] A. Bemporad and C. Filippi, "'Suboptimal explicit receding horizon control with guaranteed stability,'" Automatica, vol. 42, no. 3, pp. 457–462, 2006.

[15] R. G. Sanfelice, 'Hybrid Model Predictive Control, edition 1 ed.' Basel: Birkhäuser, 09/2018 2018, pp. 199–226.

[16] A. van der Schaft and H. Schumacher, 'An Introduction to Hybrid Dynamical Systems', LNCS, Springer, 2000.

[17] J.-P. Aubin, J. Lygeros, M. Quincampoix, S. S. Sastry, and N. Seube, "'Impulse differential inclusions: a viability approach to hybrid systems," IEEE Transactions on Automatic Control, vol. 47, no. 1, pp. 2–20, 2002.

[18] R. Goebel, R. G. Sanfelice, and A. R. Teel, 'Hybrid Dynamical Systems: Modeling, Stability, and Robustness,' Princeton University Press, 2012.

[19] R. G. Sanfelice, Hybrid Feedback Control.' New Jersey: Princeton University Press, 2021.

[20] P. Mhaskar, N. H. El-Farra, and P. D. Christofides, "'Robust predictive control of switched systems: Satisfying uncertain schedules subject to state and control constraints," International Journal of Adaptive Control and Signal Processing, vol. 22, no. 2, pp. 161–179, 2008.

[21] P. Sopasakis, P. Patrinos, H. Sarimveis, and A. Bemporad, "'Model predictive control for linear impulsive systems," IEEE Transactions on Automatic Control, vol. 60, no. 8, pp. 2277–2282, Aug 2015.

[22] F. L. Pereira, F. A. C. C. Fontes, A. P. Aguiar, and J. B. de Sousa, An Optimization-Based Framework for Impulsive Control Systems.' Cham: Springer International Publishing, 2015, pp. 277–300.

[23] M. A. Müller, P. Martius, and F. Allgöwer, "'Model predictive control of switched nonlinear systems under average dwell-time," Journal of Process Control, vol. 22, no. 9, pp. 1702–1710, 2012.

[24] P. Colaneri and R. Scattolini, "'Robust model predictive control of discrete-time switched systems," IFAC Proceedings Volumes, vol. 40, no. 14, pp. 208–212, 2007.

[25] R. Findeisen, "Nonlinear model predictive control: a sampled-data feedback perspective," Ph.D. dissertation, Stuttgart Univ., 2006.

[26] L. Magni and R. Scattolini, "'Model predictive control of continuous-time nonlinear systems with piecewise constant control," IEEE Transactions on Automatic Control, vol. 49, no. 6, pp. 900–906, June 2004.

[27] P. Tabuada, 'Event-triggered real-time scheduling of stabilizing control tasks," IEEE Transactions on Automatic Control, vol. 52, no. 9, pp. 1680–1685, 2007.

[28] P. Mhaskar, N. H. El-Farra, and P. D. Christofides, "'Predictive control of switched nonlinear systems with scheduled mode transitions," IEEE Trans. Aut. Cont., vol. 50, no. 11, pp. 1670–1680, 2005.

[29] B. Altin, P. Ojagh, and R. G. Sanfelice, "A model predictive control framework for hybrid systems," in Proceedings of the 6th IFAC Conference on Nonlinear Model Predictive Control (NMPC), vol. 51, August 2018, pp. 128–133.

[30] B. Altin and R. G. Sanfelice, "Asymptotically stabilizing model predictive control for hybrid dynamical systems," in Proceedings of the American Control Conference, July 2019, pp. 3630–3635.

[31] B. Altin and R. G. Sanfelice, "'Model predictive control for hybrid dynamical systems: Sufficient conditions for asymptotic stability with persistent flows or jumps," in Proc. Amer. Cont. Conf., July 2020, pp. 1791–1796.

### Page 22