> OCR by deepseek-ai/DeepSeek-OCR | 29 pages

### Page 1

value can have a substantial impact, because they 1 Data Engineering Research Group: Institut Gamborg, FOR University of Goteborg, Sweden {msberg,se,hovemeyer,loetke}@for.vtt.fi http://www.ivg.tu-groningen.nl/

Jonckheere-Terpstra test for nonclassical error versus sensitivity relationship of quantum spin network controllers with applications

Monkheere T, S. Schirmer, F. Langeb

November 27, 2007

# Abstract

Summary: Selective information transfer in spin ring networks by energy landscape shaping control has the property that the error 1 − prob, where prob is the transfer success probability, and the sensitivity of the error to spin coupling uncertainties are statistically increasing across a family of controllers of increasing error. The need for a statistical Hypothesis Testing of a concordant trend is made necessary by the noise behavior of the sensitivity versus the error as a consequence of the optimization of the controllers in a challenging error landscape. Here, we examine the concordant trend between the error and another measure of performance—the logarithmic sensitivity—used in robust control to formulate a well known fundamental limitation. Contrary to error versus sensitivity, the error versus logarithmic sensitivity trend is less obvious, because of the amplification of the noise due to the logarithmic normalization. This results in the Kendall τ test for rank correlation between the error and the log sensitivity to be somewhat pessimistic with marginal significance level. Here it is shown that the Jonckheere-Terpstra test, because it tests the Alternative Hypothesis of an ordering of the medians of some groups of log sensitivity data, alleviates this statistical problem. This identifies cases of concordant trend between the error and the logarithmic sensitivity, a highly anti-classical features that goes against the well known sensitivity versus complementary sensitivity limitation. 1) Introduction 1

### Page 2

Figure 1: Classical tracking error $e_{track}$ in the classical control architecture

where $r(s)$ is an extraneous reference and $e_{track}(s)$ is the tracking error, as shown in Fig. 1. $T(s)$ appears in the logarithmic sensitivity of $S(s)$ as $S^{-1}(dS) = (dL)L^{-1}T$. The conflict is obvious from $S + T = I$. The SDoF limitation can be overcome by a 2-Degree of Freedom (2DoF) configuration, as already pointed out by Horowitz [12, Chap. 6] and recently made explicit in [3, 45].

1.2 The quantum state overlap control problem

This paper investigates whether this fundamental limitation survives in the quantum world, more precisely, in spin-$\frac{1}{2}$ networks where fabrication uncertainties at nano-scale make it hard to ensure precise coupling strengths between the spins. The answer is definitely negative for the class of quantum control problems where the objective is to achieve maximum overlap between the controlled wave function $|\Psi_D(t_f)\rangle$ at some final time $t_f$ and some reference or target wave function $|\Psi_{\text{target}}\rangle$. In the preceding, $D$ denotes the controller. The controller belongs to a class of quantum control systems where the controller sole authority is to modify, in a physically meaningful manner, the parameters of the Hamiltonian [8]. Even though the basic concepts developed here remain valid for the whole class of such controllers, here, however, we will more specifically consider controllers that induce energy level shifts [5, 15, 16, 17, 18, 24, 33]. More specifically in this paper, $D = \text{diag}(D_1, D_2, ..., D_M)$ is a diagonal matrix of bias fields added to the Hamiltonian in some subspace. The concept is illustrated in Fig. 2. In practice, we could envisage, e.g., electron spins in quantum dots whose energy levels can be controlled by voltages applied to surface gates [27].

By “maximum overlap,” or “maximum fidelity,” we mean

$$\text{max}_{D, t_f} |\langle \Psi_{\text{target}} | \Psi_D(t_f) \rangle| \leq 1,$$  (1)

where $|\cdot|$ denotes the inner product in the complex Hilbert space. The upper bound on the fidelity is easily understood from the Cauchy–Schwartz inequality and the unit norm of the wave function. It turns out that this maximum overlap does not trickle down to the standard tracking problem

$$\text{min}_{D, t_f} \|\Psi_{\text{target}} - \Psi_D(t_f)\|,$$  (2)

### Page 3

Figure 2: Illustration of the excitation transfer problem. Excitation (spin "up") originally on spin #1 has to be transferred to spin #4. This is accomplished by bias fields \(\{D_m\}_{m=1}^12\), despite uncertainties on the couplings \(J_{m,m+1}\). (Not all bias fields are shown for clarity of the picture.) The initial (target) condition on the wave function \(\Psi\) means that the INput (OUTput) excitation is on spin #1 (spin #4).

### Problem Setup and Recursive Solution
but to a projective version of the tracking error [33]:
$$
\min_{D,t_f} \...
... - e^{j\phi} \Psi_D(t_f)\|,
$$
where \(\phi\) is the global phase factor—a purely quantum mechanical concept. The classical-quantum discrepancy can now be understood as follows: Under some circumstances, the projective tracking error may be very small, yet the classical tracking error may be so large as to allow for small logarithmic sensibility.
In the same way as the conventional tracking error leads to a sensitivity matrix \(S(s)\), the projective tracking error leads to an unconventional sensitivity matrix [33]. The problem is that this sensitivity matrix does not easily lend itself to an analytical formulation of the limitations on achievable performance. Besides, the challenging landscape in which the optimization (1) has to be conducted relative to both \(D\) and \(t_f\) leads to ripples in the sensitivity versus error plot, calling for a statistical approach to determine concordance or discordance between the two figures of merit.

### Statistical approach to classical-quantum discrepancy
We consider spin-rings as prototype quantum systems to test our hypothesis of anti-classicality. As shown in Fig. 2, a \(M\)-spin ring is an assembly of \(M\) spins arranged along a ring with near neighbor coupling \(J_{m,m+1}\) of the XX or Heisenberg type. We chose spin rings because they are prototypes of quantum routers [18] that have to transfer a single excitation (spin "up") from one spin to another spin. To achieve this objective, time-invariant but spatially distributed bias fields \(D_1, D_2, ..., D_M\) are deployed. Such controller fits in the class of controllers mentioned earlier, as the \(D_m\)'s appear on the diagonal of the Hamiltonian. With this data, which includes the controller, the

### Page 4

value of a physical system increases with time. The total amount of energy of such a system as it transforms from one state to another increases with time. The increase can be qualitatively described by the so-called discharge equation:

\[
W_{\text{discharge}} = W + \int_0^T dt \, \frac{dQ}{dt} .
\]

This equation combines the laws of thermodynamics and quantum mechanics. It is derived from considering the idea of conservation of energy of the system during the transformation, which requires that the total amount of energy in a system be constant at any point in time if no work is done on the system. The non-interacting atoms are assumed to be described by non-interacting quantum mechanics

### Page 5

To generate the random seed, digits are repeatedly added to a normal distributed variable \(u_{\alpha}\). The mean of this uniformly distributed variable is chosen to be \(1/(2\log M)\), and the variance to be \(1/(16\log^{2}M)\). With this definition, a transformation of the form \[r_{ij} = X_{i+j-1}+X_{2j-1}\] results in a random number \(A\) distributed according to the Inverse Gaussian integral. This random number \(A\) that the IEEE Sylvester standard is used to compute the final quantum state expectation value.

To evaluate the single-photon correlations, the quantum model discussed in section . 3 is chosen, and two multiplicatively coherent pulses generated from a coherent source arranged as a standard Bell basis are input into the two-photon hybrid source. The register is kept on the down-up sequence, and the quadratic distortions induced are applied to several of the registers in the sequence. These third resonators measure the down system; the two resonators below are used to measure the up-to-down transitions.

As this component removes \(|\alpha\rangle\), the analysis should be repeated for multiple copies, so that \(\alpha\) is a multi-dimensional vector. The probability density function of a six-dimensional coherent quantum state is \[|\Psi\rangle = \frac{1}{\sqrt{6(0.751)}^3} \begin{pmatrix} 0.674  -0.10 \\ -10.94 +2.90i \\ 0.72  +3.90i \\ i -9.96 \\ 11.68 -14.40 \\ -4.50  +4.20i \end{pmatrix}.\] The mean takes on the values 1.91, 4.37, 1.70, 2.00, 0.70, and 1.50, and the variance 2.30, 0, 2.92, 3.00, 0, 0, and 0.65 for the outputs of the twelve register if the random surface is rotated about the \(z\) axis by an angle 0, \(\pi/6\), \(3\pi/6\), 0, \(\pi\), etc. In all cases, the state is equal to \(R(0)R(1)R(2)|\Psi\rangle\) where \(R(t)\) is the жExitaries exponential cumulants. The entanglement between the system and the process, in terms of the largest entries of \(\mathcal{T},N(t)\). The quantum totality of these correlators necessary to determine only two Gaussian moments of the state can, in principle, be determined from single-photon correlation measurements if every correlated pair can be established.

The method employed can be applied to a variety of continuous variable \(|\Psi\rangle\), as long as the diagonal element of the state has been expressed as a function of the independent variables. The dipoles associated with states of the number energies of a higher-order \(P\)-order harmonic state and off-diagonal elements corresponding to the other dependent variables will have characteristics for which exact functions can not be derived. However, a series of experiments will enable one to assign approximate values to these dipoles and other higher-order dependent variables.

The experiment, performed by level meaning [13, 4], not only energetically determine the levels of the first \(2P\)-order states of the model, it allows the effective evaluation of the asymptotic quantum state expectation values for all spin degrees of freedom, as well as the photon-signal correlation.

a higher degree of entanglement in this model could be exploited with the latest electro-optic group packaging [6, 7] for the generation of a photon.

The mean of a diagonal observable in a black hole mirror will obtain the typical value of these eigenvalues of its quantum state.

The low temperature effect of quantum computing, the sampling of the infrared state, to the well known work [12, 10].

The technique applied, by means of comparisons [4, 2, 13, 4] can now deserve to be used for precise determinations of multiphoton coherent states deeper in the infrared.

The detailed statistical methods can be used to derive not only the characteristics of thermal equilibrium states at low temperature, but also to evaluate the standard deviations of the

### Page 6

Attractive force about one feature. As the equation explained, when we plotted the distribution of inter-battery forces, a histogram was obtained. The histogram showed many features, including most notably:
- The peak of the distribution is low at a value of 0.1, indicating that most batteries have very low forces applied to them.
- The peak is slightly higher at a value of 0.3, indicating that there is a small percentage of batteries that have forces applied to them that are relatively high.
- There is a small peak at a value of 1, indicating that there is a small percentage of batteries that have forces applied to them that are very high.

### Page 7

transition rate; the quantum correlation threshold.

### Page 8

Page 9/29

It thus appears that the quantum transport problem of maximizing \[|\langle \mathrm{OUT}|\Psi(t_f) \rangle|\]
or its “windowed” version \[{\frac{1}{{\delta t}}\int_{t_f - \delta t/2}^{t_f + \delta t/2} |\langle \mathrm{OUT}|\Psi(t) \rangle|dt }\]
is equivalent to minimizing some “tracking error” with the discrepancy that it is not required that the difference between the current state and the target state be small in the ordinary sense, but small in the sense of \[\min_{\varphi}\||\mathrm{OUT}]- e^{i\varphi}\langle \Psi(t_f)\rangle|.\] 
The latter is related to the Fubini-Study metric [10, p. 31] on the complex projective space \[C\mathbb{P}^{M-1}\].
We will refer to the left-hand side of Eq. (6) as the projective tracking error.

**3.1 Classical-quantum controller structure discrepancies**
The hidden feedback

\[
u(t) = -j D |\Psi(t)>

We will refer to the left-hand side of Eq. (6) as the projective tracking error.

**3.1 Classical-quantum controller structure discrepancies**
The hidden feedback

\[
u(t) = -j D |\Psi(t)>
\]

formulation of the “open-loop” controlled Schrödinger equation (4) where \[D\] is a diagonal matrix of spatially distributed biases makes the controller linear, as opposed to bilinear [9, 28]. Conceptually \[D\] could still be time-varying, but here we focus on a time-invariant design. As already said, in the latter, case the controller is linear time-invariant, but in the nonclassical sense of [28]. Yet another departure from classical control is that the controller is selective, that is, \[D\] depends on both \[|\mathrm{IN} \rangle\] and \[|\mathrm{OUT} \rangle\]. \[|\mathrm{IN} \rangle\] is the initial condition and, more importantly, \[|\mathrm{OUT} \rangle\] is to be interpreted as the reference. The controller is not driven by the tracking error, but depends on both the current state and the target state; from this point of view, the controller is of the 2DoF configuration.

Note that, because of the symmetry of the ring, \[D(|\mathrm{IN} \rangle,|\mathrm{OUT} \rangle)\] depends only on the distance between \[|\mathrm{IN} \rangle\] and \[|\mathrm{OUT} \rangle\].
Last but not least, the unitary evolution has the property that the controller is not asymptotically stable. Indeed, let \[D\] be a controller that achieves \[||\mathrm{OUT} \rangle - e^{-j(H+ D)t} |\mathrm{IN} \rangle || \leq \ \epsilon\]. Take an initial state \[|\mathrm{IN} \rangle\] nearby \[|\mathrm{IN} \rangle\], that is, \[||\mathrm{IN} \rangle - |\mathrm{IN} \rangle | = \eta\]. Using the unitary property of the evolution and the triangle inequality, we derive

\[\begin{align*}
\eta &= ||e^{-j(H+D)}t (|\mathrm{IN} \rangle - |\mathrm{IN} \rangle ) ||
&= ||(e^{-j(H+D)t}| \mathrm{IN} \rangle - |\mathrm{OUT} \rangle) + (|\mathrm{OUT} \rangle- e^{-j(H+D)t}| \mathrm{IN} \rangle ) ||
\leq \ \epsilon + ||\mathrm{OUT} \rangle - e^{-j(H+D)t} | \mathrm{IN} \rangle ||, |
\end{align*}\]

which yields

\[
||\mathrm{OUT} ) - e^{-j(H+D)t}| \mathrm{IN} \rangle || \geq \ \eta - \epsilon.
\]

Thus, for an infinitesimally accurate controller \[(\epsilon \downarrow 0)\], the perturbed state will remain away from the target \[|\mathrm{OUT} \rangle\]. The latter has the consequence that the controller is not a classical asymptotically stabilizing controller; it is only Lyapunov stable. The latter property means that the state remains localized and won’t diffuse. This means that in some cases [5] our controller achieves Anderson localization [2, 13, 23].

With these significant departures from classicality, one wonders whether the fundamental error versus log sensitivity limitation is still in force. The problem is that the phase factor appearing in the quantum tracking error does not lead to a classical sensitivity function. In [33], a sensitivity matrix 
\[\mathcal{S}(s)\]
was defined via the Laplace

8

### Page 9

Figure 3: Projective error \(|{\rm OUT}\rangle-e^{j\phi(t)}|\Psi(t)\rangle\). Note that, contrary to the classical case of Fig. 1, the sensitivity matrix \({\cal S}\) is first defined, from which the fictitious loop function \({\cal L}\) is defined. The loop matrix is initialized with \(|{\rm IN}\rangle\). The dotted paths are nonclassical and indicate that the gain \(D\) depends on both \(|{\rm IN}\rangle\) and \(|{\rm OUT}\rangle\).

transform \({\hat{\cal L}}\) of the projective tracking error as

\[ {\hat{\cal L}}(|{\rm OUT}\rangle 1(t)-e^{j\phi(t)}\Psi(t))=\mathscr{S}(s)|{\rm OUT}\rangle \]

for \(\phi(t)\) achieving the minimum of \(\|{\rm OUT}\rangle-e^{j\phi(t)}\Psi(t)\|\). This sensitivity operator takes the form

\[
\mathscr{S}(s)=\frac{1}{s}I-\bar{\mathscr{L}}\left[e^{i\phi(t)}\right]*(sI+j(H+D))^{-1}P
\]

where \(P\) is a permutation such that \(P|{\rm OUT}\rangle=|{\rm IN}\rangle\) and \(*\) denotes the complex domain convolution [33]. A clear relationship between \(\mathscr{S}(s)\) and its sensitivity to parameters \(J\) (a generic notation for \(J_{k,k+1}\)) in \(H\) cannot be expected. For this reason, we propose a statistical approach based on a great many numerical optimization experiments.

Note that, here, we define a sensitivity matrix without proceeding from a loop matrix as done classically. However, a fictitious loop matrix can be defined as

\[
\mathscr{L}=\mathscr{S}^{-1}(I-\mathscr{S})
\]

and plugged in the feedback diagram of Fig. 3. Clearly, the conventional architecture is recovered, but for a very special loop matrix that embodies the projectivization of the error.

From (8), it is clear that the sensitivity of the sensitivity \(\mathscr{S}\) relative to \(J\) amounts to sensitivity of error as defined by (6). From the classical control viewpoint, the log-sensitivity is

\[
\frac{\operatorname{der}}{\mathrm{d}J} \frac{1}{\operatorname{err}} = -\frac{1}{4} \frac{1}{\sqrt{\mathrm{prob}}} \frac{\mathrm{d}\mathrm{prob}}{\mathrm{d}J} \frac{1}{1-\sqrt{\mathrm{prob}}}
\]

The right-hand side is easily derived from the definition of error taken from (6). If \(\mathrm{prob}\approx 1\), as the data base [25] retains only those controllers with an error not exceeding 0.1, then the above can be approximated as

\[
\frac{\operatorname{der}}{\mathrm{d}J} \frac{1}{\operatorname{err}} \approx -\frac{1}{4} \frac{\mathrm{d}\mathrm{prob}}{\mathrm{d}J} \frac{1}{(1-\sqrt{\mathrm{prob}})} \frac{2}{(1+\sqrt{\mathrm{prob}})} = \frac{1}{2} \frac{\mathrm{d}\mathrm{prob}}{\mathrm{d}J} \frac{1}{1-\mathrm{prob}}
\]

### Page 10

4 Methods—Type I error

4.1 Overview

Here, as a first step towards an understanding of the error versus sensitivity issue, we proceed numerically by comparing the error $1-| \langle \text{OUT}|\Psi(t)|\rangle|^{2}$ and its (logarithmic) sensitivity to modeling uncertainties in $H$ across a variety of controllers with error not exceeding 0.1 (see [25] for the data). Precisely, we considered $all$ rings from $M=3$ to $M=20$ spins together with $all$ transfers between any two spins. However, by symmetry, we can restrict ourselves to $|\text{IN}\rangle = |1\rangle$. The study therefore amounts to a total of 108 case-studies, where a case-study is defined by a number of spins $M$ and an $|\text{OUT}\rangle$ spin. For every $M \in [2, 3, \ldots, 19, 20]$ and every ($\text{IN} = 1$, $|\text{OUT}| \le \lfloor M/2 \rfloor$) pair, controllers $D$ were computed by numerical optimization runs of $1 -(|\langle \text{OUT}| e^{-i(H+D)t_f}|\text{IN}\rangle|^2$ relative to $D, t_f$, either at the precise time $t_f$ or over a window around $t_f$, and controllers were ordered by increasing error, as explained in [24, 33] and as illustrated in Fig. 4. Given a case-study $(M,|\text{OUT}\rangle)$ out of a total number of 108 case-studies, the number $N$ of time-windowed optimization runs, or controllers, were between 114 and 1998, with an average of 939 controllers.

Note that we do not have a sampling of the set of $all$ controllers. The set of controllers is the subset of those locally optimal controllers computed by the search algorithm and achieving an error not exceeding 0.1.

The major difficulty is that the challenging error landscape and the potential for the solution to be trapped in some local minimums make the (absolute and logarithmic) sensitivity versus error plots quite noisy, as shown by Fig. 4, where controllers are ordered by increasing error. Despite this noisy behavior, the graph of Fig. 4 suggests a positive correlation between the sensitivity and the error (for this particular example). This observation is consistent with classical control; it is indeed easily seen that $dS = -S(dL)S$, meaning that if the error vanishes ( $S=0)$ so does the sensitivity ($dS =

### Page 12

.Now let’s apply these concepts and equations to complete the solution.

**4.3. JONCKHEERE-TERPSTRA TEST**

Consider an independent variable, here the error \( x = 1 - \text{prob where prob is the transfer success probability, and a dependent variable, here the logarithmic sensitivity of the probability relative to coupling errors } y = \left| \frac{\text{dprob}}{\text{d}J} \frac{1}{1 - \text{prob}} \right|\), where \( J \) is the near-neighbor spin coupling strength. (Note that \( 1 - \text{prob} \) is not the same error as err, but this does not matter as the Jonckheere-Terpstra test is nonparametric, that is, it does not depend on the values but on the ranking of such values.) We want to show that \( y(x) \) is statistically an increasing function (“positive correlation” between \( x \) and \( y \)). The range of values of \( x = 1 - \text{prob} \) is decomposed in a certain number of groups such that the independent variable increases along the groups. To be formal, consider a partitioning of the values of the independent variable

\[
\{ x_n \}_{n=1}^{N} = X_1 \sqcup X_2 \sqcup \cdots \sqcup X_I
\]

such that \( \forall x_k \in X_i, \forall x_{\ell} \in X_j \) with \( i < j \), we have \( x_k \leq x_{\ell} \) with at least one strict inequality. With this grouping of the values of the independent variable, we construct a grouping of the corresponding values of the dependent variable:

\[
\{ y_n \}_{n=1}^{N} = Y_1 \sqcup Y_2 \sqcup \cdots \sqcup Y_I, \quad Y_i := y(X_i).
\]

In each group of dependent variables, we compute the median of the population:

\[
\tilde{Y}_1, \, \tilde{Y}_2, \, \ldots, \, \tilde{Y}_I.
\]

In the Jonckheere-Terpstra test [14, 39], the Null Hypothesis is

\[
H_0 : \tilde{Y}_1 = \tilde{Y}_2 = \cdots = \tilde{Y}_I
\]

and the Alternative Hypothesis is

\[
H_A : \tilde{Y}_1 \leq \tilde{Y}_2 \leq \cdots \leq \tilde{Y}_I, \quad \text{with at least a strict inequality}.
\]

The Jonckheere-Terpstra is a test for the Alternative Hypothesis. It is robust and avoids the noise in the log sensitivity because it argues on the medians. (The difficult part, though, is how to group the values of \( 1 - \text{prob} \).)

The statistic is derived from a counting of the number of cases favorable to the increasing property of \( y \) relative to \( x \) (the number of concordant pairs in the Kendall tau language). Precisely, we start with the Mann-Whitney \( U \)-statistic associated with the pair \( (i, j) \) of groups:

\[
U_{ij} = \sum_{k=1}^{N_i} \sum_{\ell=1}^{N_j} \Phi(Y_j(\ell) - Y_i(k)), \quad i < j,
\]

where

Preexpensive ddess appear as, at measured differently in water before enter the Vascusity at the bead from another root sing the gradi ed by the blater. It is easi sour the essence misaty that the Five Grade May have feider funce on if the permissibility of the Box.

## 4.5 Combining test statistics from independent experiments
Let j\m;j j----o denote an z statistics (heg free with r t l a curres, ties i mill s hid perad minde l d l l cla -t parad bring l f Lity OUT)... (For Jonckheer L etP rs ta, nue would include the number of lins I in n experient data.)

### Page 15
\left(\frac{W}{2h_{\mathrm{D}} \left(2 h_{\mathrm{D}}\right)}\right), \quad h_{\mathrm{D}}=\frac{10^{5} h}{f_{\mathrm{out}}^{4}(t)}\right), \end{align*} **Algorithm 1**: **Energy-Stochastic (SS) Optimal Control**

**Here,**
\[d(t)=\left(\frac{Z_{\mathrm{D}}\left(x(t)\right)}{Z_{\mathrm{A}}\left(x\left(t^{+}\right)\right)}\right) \left(1-\frac{Z_{c}}{Z_{\mathrm{A}}}\left(x(t)\right)\right) \mathcal{\widehat{S}}(t)\]
\[W(t)=\left(\frac{r}{Z_{\mathrm{A}} \left(x(t)\right)}\right) \left[ \frac{h_{\mathrm{A}}}{2 m_{i}} X_{1}^{i}\left(x(t)\right)+\frac{h_{\mathrm{D}}}{2 m_{i}} \right.\] \[\left. X_{2}^{i}\left(x(t)\right)+\frac{c}{2 m_{i} h_{\mathrm{D}}}\right] \quad h_{\mathrm{A}}=\frac{\left(g c b_{I}^{i}\right)}{2 m_{i}} \quad h_{\mathrm{A}}=\frac{\left(\mu_{\mathrm{in}}^{A}\left(X_{1}^{i}\right)\right)}{2 m_{i}} \quad h_{\mathrm{A}}=\frac{\left(m_{i} r}{Z_{\mathrm{A}}\left(x(t)\right)}\right) \frac{m_{i} h_{\mathrm{D}}}{R\left(X_{1}^{i}\right)}\]

* **Lemma 4.5**: **Energy-Stochastic (SS) Optimal Control** 
\[\hspace{-0.7cm}\stackrel{(d(t) \text{ Step } N \text{ Operations}}^{N}){\text{Assuming} \Delta t} \mathcal{\hat{S}}^{\prime}\left(t-\Delta t\right) =\frac{\left(d^{\prime}\left(t-\Delta t\right)\right)^{4}}{d\left(t-\Delta t\right)}\]

* **Lemma 4.6**: **Energy-Stochastic (SS) Optimal Control** 
\[\hspace{0.8cm} \leftarrow{\downarrow}\text{Update }\mu_{\mathrm{in}}^{N}\text{, Compute, Edgeloss and Update }d(t)\] \[d(t)= \left(1-\frac{\left(B_{\mathrm{recovered sc}}(d(t))\right)^{4}}{B_{\mathrm{upt}}^{\times} d\left(t)\right)^{4}\right)\]

* **Lemma 4.7**: **Energy-Stochastic Optimal Control** **Following** **preasimulations run through algorithm 1, the dynamics are expressed as:** \[\hspace{1.4cm}+ 1 \text{th} \times\text{ NT State. }, \hspace{0.6cm}\mathcal{T}=^{-1} \text{Noisy,Anticipated Optimization Norm. } \hspace{0.4cm}(\text{may require the estimation of a matrix outside of this paper.}). \]

* **Implication of Lemma 4.7: Assuming \(R(\bigoplus)}{\mathcal{T}}\text{ is identical variable}\)**

* **Reference to Lemma 4.4**: **Algorithm 1**: **Energy-Stochastic (SS) Optimal Control**
\[\mathcal{\hat{S}}_{c}^{\prime}=\frac{\left(b_{T}\left(d(t)\right)^{4}\right)}{ \left(B_{\text{umped }}(d(t))\right)^{4}}\]

* **Lemma 4.8**: **Energy-Stochastic (SS) Optimal Control** **Following** **preasimulations run through algorithm 1, the dynamics are expressed as:** \[\hspace{1.4cm}+ 1 \text{ Nahss. }+ \bigoplus^{1 4}\arXiv lab size operation. node \(d(t)\) \text{will receive the NASL } \bigoplus\left(B_{\text{ACT}c}(d(t))\right)^{4}\) party }

* **Lemma 4.9**: **Sample Average Approximation Calculated (SA)** **Following **preasimulations run through algorithm 1, the dynamics are expressed as:** \[\hspace{0.6cm}\mathcal{T}=.$Al ok size $in operation. node $d(t)$ \text{will receive the NASL } \bigoplus\left(B_{\text{ACT}c}(d(t))\right)$ partition and solution }

* **Lemma 4.10**: **Sample Average Approximation Calculated (SA)** **Following **preasimulations run through algorithm 1, this becomes:** \[\hspace{nd monolayer of computation.}d_{i}(t)=T_{i}f_{d}\left(X^{-j}\right),\]

* **References to Lemmas 4.11- and 4.12:** **For Example 1** **Lemma 4.11**: **Energy Stochastic (SS) Optimal Control**  \[ \ \in \mathcal{S}_{o}^{*}\left(d(t)\right)=\frac{\left(u_{r,d}\left(t\right)\right)^{4}}{u,0,0} \] **Lemma 4.12**: **Energy Stochastic (SS) Optimal Control**  \[ R_{a}\left(u_{r}d\left(t\right)\right) \]

* **References to Lemmas 4.13- and 4.14:** **For Example 1** **Lemma 4.13**: **Energy Stochastic (SS) Approximate Controller: \(s_{e}\left(i\right)=d_{i}\left(t\right)=a_{i}\left(t\right)\)** \[ s_{e}\left(i\right)=d_{i}\left(t\right)=\] **Lemma 4.13**: **Which follows from the argument of Lemma 4.4:** **Energy Stochastic (SS) Optimal Control**  \[ \mathcal{T}=^{-1}\text{Noisy,Anticipated Optimization Norm. } \] **References to Lemmas 4.15-4.16:** **For Example 1** **Lemma 4.14**: **Energy Stochastic (SS) Optimal Control**  \[ \frac{\left(B_{\text{reduced sc}}(d(t))^{4}\right)}{e_{reduced sc}_{\text{opt}}(u_{\text{out}}\left(d\left(t\right)\right),d^{\prime}\left(t\right))**3} \]

* **References to Lemmas 4.17-:** **For Example 1** **Lemma 4.17**: **Energy Stochastic (SS) Optimal Control**  \[\mathcal{T}=^{4}\Sigma\marked{\text{A},\text{O}}  \] **References to Lemmas 4.18-:** **For Example 1** **Lemma 4.18**: **Energy Stochastic (SS) Optimal Control** \[ \frac{\left(u_{r,d}(t)^{4}\right)}{B_{\text{opt}}^{\times},d^{\prime}(t)^{4}} \]

* **References to Lemmas 4.19-:** **For Example 1** **Lemma 4.19**: **Energy Stochastic (SS) Optimal Control**  \[ s_{e}\left(d^{\prime}(t)\right)=d_{i}\left(t\right)=a_{i}\left(t\right) \] **Lemma 4.19**: **Which follows from the argument of Lemma 4.4:** **Energy Stochastic (SS) Optimal Control**
\[\hspace{0.6cm} u^{*}\left(x\left(t^{+}\right)\right)\sin\left(\frac{\pi}{N_{x}}\right), \quad b_{I}=\frac{w_{\text{SCHI}}}{\Delta t}\left(X_{-1}^{i}\right)^{5}+ \Delta t r\left(\Delta Z\right)\]

**Supplement to section 4.2 in REF:**
**Backward Notations** **Lagrange’s Identity**

### Page 16
n_i >= C });
Figure 5: Consistency of empirical cumulative distributions of observations divided in 10 groups of log-sensitivity data. Left: 11-ring with localization at \(\lvert 1\rangle\); right: 5-ring with \(\lvert 1\rangle \rightarrow \lvert 2\rangle\) transfer. The data before grouping is shown in Fig. 6 and Fig. 7, resp.
all mixed across all problems, with some rerun if a machine went down, etc. Each individual task selected an independent initial bias diagonal \(D\)-controller and initial time according to a uniform distribution. So, the initial values were iid (using Matlab’s pseudo-random number generator) and as already argued this should lead to random \(\log)\) sensitivity results.
6.1.2 Empirical cumulative distribution
Computation of the empirical cumulative distributions of the \(\{ y_n - \bar{y}_i \}_{n=1}^{N_i}\) data inside every bin after removal of the bin mean \(\bar{y}_i = (1/N_i) \sum_{n=1}^{N_i} y_n\) reveal that they are fairly consistent. Sample results are shown in Fig. 5, where the partition of the data is uniform in \((N/I)\)-observation groups, except for the last group. In general, the empirical cumulative distributions can be made closer by more careful grouping.
6.2 Statistical analysis of error versus log-sensitivity relation
Here we consider all case-studies of rings with \(M=3\) to \(M=20\) spins, with transport \(\lvert IN\rangle = \lvert 1\rangle \rightarrow \lvert OUT\rangle\), with \(\lvert OUT\rangle\) ranging from \(\lvert 1\rangle\) (Anderson localization) to \(\lceil {M \over 2} \rceil\). This totals to an amount of 108 cases. By symmetry, this covers all cases of transfer of excitation from any spin to any other spin in networks of \(M=3, 4, \ldots, 20\) spins.

### Page 17

ergic.The table entry \(\bar p \quad \bar n ^\text{T} \quad \text{max JP} \quad s_{\text{JT}} \quad \text{min p} \quad \bar p \quad \text{max p} \quad s_p\) is the result for Monte Carlo simulation based on 10,000 realizations given by equation (25). The columns are labeled with the Avrami exponent \(\alpha\) required in the formula (25) and the PV of \(\bar p\), \(\bar n ^\text{T}\), and \(s_{\text{JP}}\) for the \(\text{Monte Carlo}\) method, respectively.

The \(\text{Monte Carlo}\) method provides an alternative approximation based on Equation (25), specifically \(a = 150\), \(b = -(0.895)^{-1}\), \(c = 2.9 \times 10^{-5}\), and \(\tilde{p} = 0.000264 \times 10^{-4}\). This method requires numerically inverting \(a\left(1 - \text{e}^{-c n^8 / \alpha}\right)\), which becomes difficult for large values of \(\alpha\) with increasing \(\alpha\). Therefore, the AVIRMOX option is given by least squares and uses the option from Gencalex with the lines item A1 = 150, A2 = -0.895, A3 = 2.9 x 10^-5, A4 = 0.000264 x 10^-4 with the option command.

The columns are labeled with the maximum predictive error and the corresponding \(s_{p}\) values, as well as the Jákovce-Kantaroglou (J&K) model behavior for a completely miscible two-ﬂuid system composed of a Praseodymium isotope of hybridization up atom 59 as a filler.

There are two standard deviations, 1 \(\sigma\), based on computer results to be used as outlier values. The standard deviation-based 1 \(\sigma\) value is 10 percent less than the Above calculations in Table 1 for the Avrami exponent \(\alpha = 0.05\).

Reference 1 A standard deviation is an estimate of a population's standard deviation based on a set of sampled data. It’s the typical distance between each observation and the mean.

An example of the above-87 section in Fig. (C): (Table 11-1 El and 11-2 All). , 'p

### Page 18

wind/ state definitions, and non-time-varying temperatures packed in bowls since a 1-ring doesn’t fit the case of such a (i.e. highest) duration. To measure its sensitivity, our framework allows us to plot the values during and immediately after any specific transfer across 1000 seconds of one trend model and predict its directionality afterwards - for example, to the right of and to the left of a sensor line at a given point-in-time location [15,19].

### sensitivity metrics

#### family of models
We propose an exponential weighting function of stimuli that allows us to sample the total number of stimuli between the 10th and 80th signal percentile [Chapter 7.4.3 of [12]], scaled from 1. For other waves, such as ambient temperature, we adapt this definition from [Chapter 5.4 of [17]] into an appropriate context for the EEG. Conceptually, our family of models increase the maximum amplitude of a presentation by reducing its middle and lower peaks.

#### responsive context
In the restful lower noise noisy background reels, we also suggest using varying responsiveness ([Figure 6 caption  Figure 6: Sensitivity, logarithmic sensitivity, and error plotted on a logarithmic scale (for better comparison) versus index $n$ of controller (2000 of them) for Anderson localization around spin 1 in an 11-ring. All 3 figures of merit are concordant, indicating that Anderson localization is anti-classical.]). Intensity of stimulus, as a function of gain, is subject to variability, such as attenuations due to the wrapping of the substrate efferents (at its proximal site) shown in Figure 6a showing a V1 touch-screen dramatic backlight of ambient light in the background when using a physical screen; this can be modeled and perhaps counterbalanced with optogenetic responses to weaker input [Chapter 5.2 of [19].).

#### localizability vs non-localizability
As our selection of algorithms reflect the inference on non-Markovian dynamics [Chapter 6], it cannot always be made clear that a preferred direction is being searched, as some records may still be found towards the end rather than in the beginning of the window. The search may not allow the ability for data to follow the time in the presence of interference, varying ambient noise and other sources of input energy. Locality is easier to follow in the presence of higher noise, such as the UPPAAL3 [Chapter 5.5 and [4]] approaches. The best estimator for non-localizability and non-continuous information is typically a measure of distance with higher values further from the window being preferred. With the upper extreme in the kernel spatial domain in the log database, the non-bin and not-combinatorial case is not present as it is shown in Figure 5c.

#### optimality
When the selection is clear, we have suggested using a linear weighting function over the window to measure any particular trade-offs and a constant-logarithmic decision function by OPTIMISTIC in [Chapter 3 of [20,21]. This choice of log function allows the interpolation to predict both low and high spiking rates to be incorrect - errors are induced when this choice of weights is applied on the basis of specific episodes. For such reasons, we present a smooth weight fit to relative high confidence decisions.

Figure 6: Sensitivity, logarithmic sensitivity, and error plotted on a logarithmic scale

(default)

### Page 19

### Page 20

}}\right|.\) for each iteration \(k=1,2,3\) . Then conclude that the power is not visibly affected by those quantities.

<table><tr><td>I</td><td>Kendall tau</td><td></td><td>Z</td><td>p</td><td>Null Hypothesis</td></tr><tr><td>3</td><td>0.0575</td><td>1.4875</td><td>0.0684</td><td>&quot;accept&quot;</td><td></td></tr><tr><td>10</td><td>0.0575</td><td>1.5144</td><td>0.065</td><td>&quot;accept&quot;</td><td></td></tr><tr><td>100</td><td>0.0575</td><td>1.6696</td><td>0.0475</td><td>&quot;reject&quot;</td><td></td></tr></table>

### Page 21

Figure 9: Sensitivity, log-sensitivity, and error plotted on a logarithmic scale versus index of controller (290 of them) in a 15-ring under \(|1\rangle\rightarrow|6\rangle\) transport. While the sensitivity and the error are concordant, the log-sensitivity shows no trend—even possibly a slightly decreasing trend—versus \(n\) indicating rather classical behavior.

Table 5: Details of the 15-ring \(|1\rangle\rightarrow|6\rangle\) transport experiment on Jonckheere-Terpstra test of Null Hypothesis of no trend between error and logarithmic sensitivity

| 
\(I\) 
| 
Kendall tau 
| 
\( |Z| \) 
| 
\(p\) 
| 
Null Hypothesis
3 0.0285 0.1789 0.4290 “accept”
10 0.0285 0.7549 0.2252 “accept”
100 0.0285 0.2052 0.4200 “accept”
8 Discussion: Dependency of error versus log-sensitivity relation on \((|IN\rangle, |OUT\rangle)\)

In the previous study, the data incorporated all cases, up to symmetry, of \(|IN\rangle\rightarrow|OUT\rangle\) transfers, for all \(M\) ranging from 3 to 20, with an overall positive concordant trend between error and log sensitivity. Here we examine how much the classical/anti-classical behavior depends on the relative position of the \(|IN\rangle\) and \(|OUT\rangle\) spins. The overall Jonckheere-Terpstra \(|Z|\)-data with \(|IN\rangle=|1\rangle\) of Section 6.2 is divided into three \(|OUT\rangle\)-groups (\(I=3\)) that roughly correspond to a decomposition of the right-half of the ring into 3 equally-sized sectors:

* \(Y_{1}\): JT-data of \((120^{\circ}<\text{angle}(|1\rangle, |OUT\rangle)<180^{\circ}+\epsilon),\)
* \(Y_{2}\): JT-data of \((60^{\circ}<\text{angle}(|1\rangle, |OUT\rangle)<120^{\circ}),\)
* \(Y_{3}\): JT-data of \((0^{\circ}<\text{angle}(|1\rangle, |OUT\rangle)<60^{\circ}),\)

as illustrated in Fig. 11. If \(M\) is not divisible by 3, we arrange the \(M\)-spin data such that \(|Y_{3}|\geq |Y_{2}|\geq |Y_{1}|\) with at least a strict inequality. The more specific grouping of

### Page 25

estimate the potential impact on the Try创新创业训练计划教材. Variable | Missing Anchor Element.XItem | Dimensionless Y Bar2 | Estimated | Standard Error |95% Confidence Interval |Mean | Missing Elements Data |Missing | R-Rank Correlation Coefficients |Missing |Missing Categorical Variables |Missing |Missing What if i do not know the Thrush’s Origin and Sizes, because disrupting the tree will Cause amnesia to the player. TPCharacter

### Page 26

\[\cdot\prod_{x\in\text{clos.}}(\frac{{p}_{x}-{q}_{x}}{{|x|}^{1/{}_{tx}}})^{-2}} |\text{clos.}(x)|}^{\frac{({p}_{x}-{q}_{x})}}{{|x|}^{1/{}_{tx}}}|, |p_{x}|,|q_{x}|\begin{cases}&\makebox[105.275pt][c]{$x\in y$}\\ &\makebox[105.275pt][c]{$x\in\text{nonempty set}$}\end{cases},|\dfrac{p_{y}-q_{y}}{k}|, \end{cases}\] (99)

Here, the summation is taken in the constant \(b\).

To see how the average behaves, it suffices to consider the properties of the count vector

\[\begin{split} v(a_{j},d) & =\begin{cases}0&\text{$\half N_{<}1$ \\ d+\sum_{i=1}^{N}w_{N}(x_{i})}&\text{otherwise, where $w_{1}=-\infty$, $w_{N}(a_{N+1})=\ldots w_{N}(N-1)+1$,}\end{cases}, \\ v(a_{j})&=\dfrac{N-1}{b}\sum_{i=2}^{N}j_{i}[w_{i}-w_{i-1}],\end{split}\] (100)

The individual \(w_{i}\) directly arises from condition E also. Then, its average function (99) is

\[E=v(a) =\sum_{i=1}^{N_{<}1}v(a_{i})\] (101)

The count-vector of any set \(S\) may be approximated with the help of sample vector \(\textbf{c}=\{n_{1},n_{2},\ldots,n_{M}\}\) according to the weight formula E if

\[E=\frac{1}{h}\sum_{i=1}^{N_{<}1}\frac{N_{i}}{n_{i}} |n_{i}-\text{sample vector},\ n_{i}\in S|.\] (102)

If the sample vector does not coincide with a real coordinate of the space, then its relation to the weight formula, which then appears in other professional papers for the metric space \((S,A,T)\), \(\sum\nolimits_{i=1}^{N}\frac{N_{i}}{n_{i}}=\text{const}\), is more general (the causes for it remain the same).

The average value \(\bar{v}(a)\) possesses even the "time-stamp" \(n\) in bin \(i\). Let \(\pi_{i}:\{1,\keralsdots n_{i}\}\to\{1,\keralsdots n_{i}\}\) be the permutation of the set of \(N_{i}\) labels such that \(x_{\pi_{i}(n)}\) is nondecreasing. The \(y_{\pi_{i}(n)}\) sequence may have an overall nondecreasing trend in case the classical limitation is violated, but it is not uniformly nondecreasing. We show randomness in the latter sequence. We define \(r_{n}\) to be the rank of the observation \(y_{\pi_{i}(n)}\) in a nondecreasing reordering of the data \(\{y_{n}\}\). Inside the bin \(i\in I\), the rank von Neumann ratio is

\[\text{RVN}_{n}=\dfrac{\sum_{n=1}^{N_{i}-1}(r_{n}-r_{n+1})^{2}}{\sum_{n=1}^{N_{i}}(r_{n}-\bar{r}_{n})^{2}},\quad\left(\bar{r}_{i}=\dfrac{1}{N_{i}}\sum_{n=1} ^{N_{i}}r_{n}\right).\]

The statistic of the RVN is approximately \(\beta\)-distributed, from which the left-tailed critical values of \(\text{RVN}_{\alpha}\) are cataloged in [, Table 2]. Naturally, in case the classical limitation is challenged, some correlation should be expected as the data \(\{y_{n}\}\) is on the average increasing. For this reason, before running the von Neumann test, the "trend" should be removed.

For example, consider a 7-ring with target spin 3, and 15 groups for a total of 1515 observations. From [, Table 2] of the thresholds of the \(\beta\)-function statistic, and observing that each group contains about 100 observations, any value \(\geq 1.67\) would indicate randomness with 95% confidence. The data \(\{y_{n}\}_{n=1}^{N_{i}}\) is detrended with the

\[\text{detrend function of Matlab, which removes the best overall linear fit of the trend from the data in bin $i$. Repeating this for all bins, the rank von Neumann ratio test for all 15 bins yields}\]

\[\begin{split}\text{RVN}&=\\ & 2.1908\ 1.8130\ 2.1426\ 1.7964\ 1.7150\ 2.0761\ 1.3343\ 1.9043\ 1.7925\ 1.2026\\ &\quad 1.8994\ 2.0175\ 1.9932\ 1.8010\ 1.8657\end{split}\]

Observe that 13 out of 15 bins are beyond the threshold of 1.67, reinforcing our claim of randomness in the data.

References

[1] H. Abdi. The Kendall rank correlation coefficient. In N. Salkind, editor, Encyclopedia of measurements and Statistics. Sage, Thousand Oaks, CA, 2007.

[2] P. W. Anderson. Absence of diffusion in certain random lattices. Physical Review, 109(5):1492–1505, March 1958.

[3] M. Araki and H. Taguchi. Two-degree-of-freedom PID controllers. International Journal of Control, Automation, and Systems, 1(4), December 2003.

中心] & Fen
Results Page presented 0
Above figure this : top Centre as
Average denitivity as a (Coupon); & P] Mpress Center Columns;
abstract
bottom round trip; Circumduct Rose Text
2005 ex fluid AI
symbol is 
Pra^{-[1]}
\begin{tabular}[h]{C}{0}\\ AR
Fleming(F)
\end{tabular}
\sic
(misplete.
1. Lig
\begin{table}[h]8\\\{ & \table
\begin{tabular}{|r|c l c l l}
  \hline \textbf{Table 1: Title} & & \textbf{Date} &  & &  \\
  \underline{h} & & 0 \\
**What is the topic of the paper?** & & \(<\frac{\forall}{15}> \laad m}{\chi}_{\text{d}}|| \frac{\uppi_{2}}{\mathcal{F}}[6]
\hline
**What is the \underline{paper0}/style in the ApJ*, \linebreak and the equation in such a sense?** & **Abdalati et al. \begin{tabular}[c]{|c|}
\end{tabular},\begin{tabular}[c]{|c|}
\end{tabular}**
%\end{table}
**From the results of other audiences in this computational studies, I can say.. -Waps than other details.  **<équation=**m\}
**Calculating the superiority/indeficiency of the results of the final materials to版本。 -When I low among others, I am  
**Calculating the paper is derived like this. - Where from here would one see the rows and what does it do relativ to the users. 
Calculator, etc. - where there is a series of errors to glean when one internalizes in: 
1) How much reduction can one get from the results of the stock?
Taking M b, B, and (Tom, 2011)
 & {\M} {\qquad}\;\; & \bar p\in {\Oo} & \\\end{table}
**[Fra partial sum)](#)?
8
8
OFin approx. False
**[ where** :
L'=N(element)
OLE
[ 
**one #6 Variable 
\[parse.parse\] , output #6: final insight in any even fraction that have class of as 1 lels of chelist is par lels of and tails of id exponential sources ex merit
The matter is #3 and range not 
 . alpha-F 
 Block.
**prime count One with.
**C bin of rencovering equations #6: C) (note) **t
There in a of the is -> 
 alpha m_s
**(when I lever from another writers?
**#6 Average 
\centered we f_Sum one eff by use sum
Le 1 
cost
8
\begin{tabular}[c]{@{}@{}@{}}
-  
$\text{}\ . No\text{}\ # charge+in.
\end{tabular}
\end{table}